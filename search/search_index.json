{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"OpenFactCheck","text":"<p>Python framework for fact-checking with LLM pipelines.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install openfactcheck[openai,anthropic]\n</code></pre>"},{"location":"#quickstart","title":"Quickstart","text":"<pre><code>from openfactcheck.chat import ChatClient, OpenAIConfig, UserMessage\n\nclient = ChatClient(config=OpenAIConfig(model=\"gpt-4o\"))\nresponse = client.completion([UserMessage(content=\"Hello!\")])\nprint(response.message.content)\n</code></pre>"},{"location":"#reference","title":"Reference","text":"<ul> <li>Chat config \u2014 provider-specific configuration for model calls.</li> <li>API auth \u2014 token verifiers for the REST API.</li> </ul>"},{"location":"reference/","title":"Reference","text":"<p>Generated API reference for OpenFactCheck's public modules.</p> <ul> <li>OpenFactCheck \u2014 the top-level entry point that configures a run and executes it.</li> <li>Configuration \u2014 the settings that drive a run: model, pipeline, runtime, and Serper key.</li> <li>REST API \u2014 the HTTP API, rendered from the live OpenAPI spec.</li> <li>Types \u2014 the core fact-checking data vocabulary: claims, evidence, verdicts, results.</li> <li>Messages \u2014 chat message types shared across the layers.</li> <li>Chat \u2014 LLM client, provider configuration, and backend selection.</li> <li>Prompts \u2014 prompt templates and the markdown codec.</li> <li>Graph \u2014 the typed orchestration layer for composing components.</li> <li>Components \u2014 the category contracts and their implementations.</li> <li>Integrations \u2014 typed clients for external services such as web search.</li> <li>API \u2014 Python building blocks behind the REST API: auth, models, repositories.</li> </ul>"},{"location":"reference/config/","title":"Configuration","text":"<p>Configuration for fact-checking runs.</p> <p><code>OpenFactCheckConfig</code> is the layered settings object that drives a run; each stage's model is written as a plain <code>\"provider/model\"</code> string in a <code>ModelSpec</code> and resolved to a provider configuration on demand.</p>"},{"location":"reference/config/#settings","title":"Settings","text":""},{"location":"reference/config/#openfactcheck.config.base.OpenFactCheckConfig","title":"OpenFactCheckConfig","text":"<p>               Bases: <code>BaseSettings</code></p> <p>OpenFactCheck configuration.</p> <p>Reads from several sources automatically in priority order::</p> <pre><code># Just works: env vars + .env + config files + defaults\nconfig = OpenFactCheckConfig()\n\n# Programmatic overrides beat everything\nconfig = OpenFactCheckConfig(model=\"anthropic/claude-sonnet-4-6\")\n</code></pre> <p>Supported config files (looked up in the working directory):</p> <ul> <li><code>openfactcheck.json</code></li> <li><code>openfactcheck.yaml</code> / <code>openfactcheck.yml</code></li> </ul> <p>Environment variables use the <code>OPENFACTCHECK_</code> prefix:</p> <pre><code>OPENFACTCHECK_MODEL=anthropic/claude-sonnet-4-6\nOPENFACTCHECK_VERBOSITY=debug\nOPENFACTCHECK_PIPELINE=factool\nSERPER_API_KEY=...\n</code></pre> <p>The <code>pipeline</code> names a prebuilt pipeline, resolved against the registry when the run is built. Build a custom pipeline in code and pass it as <code>OpenFactCheck(graph=...)</code>.</p>"},{"location":"reference/config/#openfactcheck.config.base.OpenFactCheckConfig.model","title":"model  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>model: str | None = None\n</code></pre> <p>Global default model as <code>\"provider/model\"</code>. Unset lets each component use its own default.</p>"},{"location":"reference/config/#openfactcheck.config.base.OpenFactCheckConfig.verbosity","title":"verbosity  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>verbosity: Literal[\n    \"debug\", \"info\", \"warning\", \"error\", \"critical\"\n] = \"warning\"\n</code></pre> <p>Log level for a run.</p>"},{"location":"reference/config/#openfactcheck.config.base.OpenFactCheckConfig.serper_api_key","title":"serper_api_key  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>serper_api_key: SecretStr = Field(\n    default=SecretStr(\"\"),\n    validation_alias=AliasChoices(\n        \"SERPER_API_KEY\", \"OPENFACTCHECK_SERPER_API_KEY\"\n    ),\n)\n</code></pre> <p>Global Serper API key for web retrieval.</p>"},{"location":"reference/config/#openfactcheck.config.base.OpenFactCheckConfig.runtime","title":"runtime  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>runtime: RuntimeSpec = RuntimeSpec()\n</code></pre> <p>Global runtime defaults applied to the pipeline's model calls.</p>"},{"location":"reference/config/#openfactcheck.config.base.OpenFactCheckConfig.pipeline","title":"pipeline  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>pipeline: PipelineName | None = None\n</code></pre> <p>The prebuilt pipeline to run, by name. Build a custom pipeline in code and pass <code>OpenFactCheck(graph=...)</code>.</p>"},{"location":"reference/config/#openfactcheck.config.base.OpenFactCheckConfig.settings_customise_sources","title":"settings_customise_sources  <code>classmethod</code>","text":"<pre><code>settings_customise_sources(\n    settings_cls: type[BaseSettings],\n    init_settings: PydanticBaseSettingsSource,\n    env_settings: PydanticBaseSettingsSource,\n    dotenv_settings: PydanticBaseSettingsSource,\n    file_secret_settings: PydanticBaseSettingsSource,\n) -&gt; tuple[PydanticBaseSettingsSource, ...]\n</code></pre> <p>Priority: constructor &gt; env vars &gt; .env &gt; json/yaml config &gt; defaults.</p> Source code in <code>src/openfactcheck/config/base.py</code> <pre><code>@classmethod\ndef settings_customise_sources(\n    cls,\n    settings_cls: type[BaseSettings],\n    init_settings: PydanticBaseSettingsSource,\n    env_settings: PydanticBaseSettingsSource,\n    dotenv_settings: PydanticBaseSettingsSource,\n    file_secret_settings: PydanticBaseSettingsSource,  # noqa: ARG003 - required by pydantic-settings override.\n) -&gt; tuple[PydanticBaseSettingsSource, ...]:\n    \"\"\"Priority: constructor &gt; env vars &gt; .env &gt; json/yaml config &gt; defaults.\"\"\"\n    return (\n        init_settings,\n        env_settings,\n        dotenv_settings,\n        JsonConfigSettingsSource(settings_cls),\n        YamlConfigSettingsSource(settings_cls),\n    )\n</code></pre>"},{"location":"reference/config/#openfactcheck.config.base.PipelineName","title":"PipelineName","text":"<pre><code>PipelineName = str\n</code></pre> <p>The name of a registered prebuilt pipeline, validated against the registry when the run is built.</p>"},{"location":"reference/config/#specs","title":"Specs","text":""},{"location":"reference/config/#openfactcheck.config.models.ModelSpec","title":"ModelSpec","text":"<p>               Bases: <code>BaseModel</code></p> <p>A model selection and its sampling parameters, in <code>\"provider/model\"</code> form.</p> <p>Provider-specific parameters are validated against the resolved provider when the spec is turned into a chat configuration, so a parameter the provider does not accept (for example <code>top_k</code> on an OpenAI model) is rejected then.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str | None = None\n</code></pre> <p>Model name as <code>\"provider/model\"</code> (for example <code>\"anthropic/claude-sonnet-4-6\"</code>).</p> <p>When unset, the surrounding configuration supplies a fallback.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.temperature","title":"temperature  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>temperature: float | None = None\n</code></pre> <p>How random the model's output is. Lower is more focused, higher more varied.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.max_output_tokens","title":"max_output_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_output_tokens: int | None = None\n</code></pre> <p>Cap on the tokens the model may generate in its response.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.top_p","title":"top_p  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>top_p: float | None = None\n</code></pre> <p>Nucleus sampling cutoff.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>seed: int | None = None\n</code></pre> <p>Request reproducible sampling, for OpenAI-compatible providers.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.frequency_penalty","title":"frequency_penalty  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frequency_penalty: float | None = None\n</code></pre> <p>Penalize tokens by how often they have appeared, for OpenAI-compatible providers.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.presence_penalty","title":"presence_penalty  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>presence_penalty: float | None = None\n</code></pre> <p>Penalize tokens that have appeared at all, for OpenAI-compatible providers.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.reasoning_effort","title":"reasoning_effort  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>reasoning_effort: (\n    Literal[\"low\", \"medium\", \"high\"] | None\n) = None\n</code></pre> <p>Reasoning budget hint, for OpenAI-compatible providers.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.top_k","title":"top_k  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>top_k: int | None = None\n</code></pre> <p>Sample from the top-k tokens at each step, for Anthropic.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.thinking","title":"thinking  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>thinking: bool | None = None\n</code></pre> <p>Enable Anthropic extended thinking. Requires <code>thinking_budget_tokens</code>.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.thinking_budget_tokens","title":"thinking_budget_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>thinking_budget_tokens: int | None = None\n</code></pre> <p>Tokens Anthropic may spend on the reasoning trace when thinking is on.</p>"},{"location":"reference/config/#openfactcheck.config.models.ModelSpec.to_model_config","title":"to_model_config","text":"<pre><code>to_model_config(\n    *, fallback_name: str | None = None\n) -&gt; ModelConfig\n</code></pre> <p>Build the provider configuration for this spec.</p> <p>The name is taken from <code>name</code>, falling back to <code>fallback_name</code>; its first segment is the provider when it is a known one (openai, anthropic, openrouter), and the remainder is the model id. The set sampling parameters are then validated against the resolved provider.</p> PARAMETER DESCRIPTION <code>fallback_name</code> <p>Model name to use when <code>name</code> is unset.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/config/models.py</code> <pre><code>def to_model_config(self, *, fallback_name: str | None = None) -&gt; ModelConfig:\n    \"\"\"Build the provider configuration for this spec.\n\n    The name is taken from [`name`][ModelSpec.name], falling back to ``fallback_name``; its first segment\n    is the provider when it is a known one (openai, anthropic, openrouter), and the remainder is the model\n    id. The set sampling parameters are then validated against the resolved provider.\n\n    Args:\n        fallback_name: Model name to use when [`name`][ModelSpec.name] is unset.\n\n    Returns:\n        The provider configuration for the resolved model name and the set sampling parameters.\n\n    Raises:\n        ConfigError: If no name is set or supplied, or a parameter does not belong to the resolved\n            provider or fails its constraints. The message names the model, the provider, and each\n            offending parameter.\n    \"\"\"\n    resolved = (self.name or fallback_name or \"\").strip()\n    if not resolved:\n        raise ConfigError(\"no model name; set the spec's name or pass a fallback.\")\n    match resolved.partition(\"/\"):\n        case (\"openai\" | \"anthropic\" | \"openrouter\") as provider, \"/\", model if model:\n            base = {\"provider\": provider, \"model\": model}\n        case _:\n            base = {\"provider\": \"openai\", \"model\": resolved}\n    params = {key: value for key, value in self.model_dump(exclude={\"name\"}).items() if value is not None}\n    try:\n        return _MODEL_CONFIG_ADAPTER.validate_python({**base, **params})\n    except ValidationError as exc:\n        provider, model = base[\"provider\"], base[\"model\"]\n        issues: list[str] = []\n        for error in exc.errors():\n            # The discriminated union prefixes each path with the provider tag; drop it, it is already named.\n            loc = error[\"loc\"][1:] if error[\"loc\"][:1] == (provider,) else error[\"loc\"]\n            field = \".\".join(str(part) for part in loc) or \"value\"\n            issues.append(f\"{field}: {error['msg']}\")\n        raise ConfigError(\n            f\"invalid configuration for model '{model}' (provider '{provider}'): {'; '.join(issues)}.\"\n        ) from exc\n</code></pre>"},{"location":"reference/config/#openfactcheck.config.runtime.RuntimeSpec","title":"RuntimeSpec","text":"<p>               Bases: <code>BaseModel</code></p> <p>Execution settings for a component's model calls.</p> <p>An unset field inherits the chat layer's runtime default.</p>"},{"location":"reference/config/#openfactcheck.config.runtime.RuntimeSpec.timeout","title":"timeout  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>timeout: float | None = None\n</code></pre> <p>Per-request timeout in seconds.</p>"},{"location":"reference/config/#openfactcheck.config.runtime.RuntimeSpec.max_retries","title":"max_retries  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_retries: int | None = None\n</code></pre> <p>Automatic retries on transient transport failures.</p>"},{"location":"reference/config/#openfactcheck.config.runtime.RuntimeSpec.max_parse_retries","title":"max_parse_retries  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_parse_retries: int | None = None\n</code></pre> <p>Reprompts allowed when a structured-output reply fails validation.</p>"},{"location":"reference/config/#openfactcheck.config.runtime.RuntimeSpec.merged_over","title":"merged_over","text":"<pre><code>merged_over(base: RuntimeSpec) -&gt; RuntimeSpec\n</code></pre> <p>Return a spec where this spec's set fields override <code>base</code>.</p> PARAMETER DESCRIPTION <code>base</code> <p>The lower-priority spec to fall back to per field.</p> <p> TYPE: <code>RuntimeSpec</code> </p> Source code in <code>src/openfactcheck/config/runtime.py</code> <pre><code>def merged_over(self, base: \"RuntimeSpec\") -&gt; \"RuntimeSpec\":\n    \"\"\"Return a spec where this spec's set fields override ``base``.\n\n    Args:\n        base: The lower-priority spec to fall back to per field.\n\n    Returns:\n        A spec combining both, preferring this spec's non-``None`` fields.\n    \"\"\"\n    merged = base.model_dump()\n    merged.update({key: value for key, value in self.model_dump().items() if value is not None})\n    return RuntimeSpec(**merged)\n</code></pre>"},{"location":"reference/config/#openfactcheck.config.runtime.RuntimeSpec.to_runtime_config","title":"to_runtime_config","text":"<pre><code>to_runtime_config() -&gt; RuntimeConfig\n</code></pre> <p>Build the chat runtime configuration, letting unset fields take their defaults.</p> Source code in <code>src/openfactcheck/config/runtime.py</code> <pre><code>def to_runtime_config(self) -&gt; RuntimeConfig:\n    \"\"\"Build the chat runtime configuration, letting unset fields take their defaults.\n\n    Returns:\n        The runtime configuration for this spec.\n    \"\"\"\n    return RuntimeConfig(**{key: value for key, value in self.model_dump().items() if value is not None})\n</code></pre>"},{"location":"reference/config/#errors","title":"Errors","text":""},{"location":"reference/config/#openfactcheck.config.errors.ConfigError","title":"ConfigError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for every configuration error.</p> <p>Raised when a configuration value cannot be resolved into a valid run, for example a model name whose provider does not accept one of its sampling parameters. The message names what was wrong; the underlying cause, when there is one, is chained.</p>"},{"location":"reference/openfactcheck/","title":"<code>OpenFactCheck</code> class","text":"<p>The top-level fact-checking entry point.</p> <p><code>OpenFactCheck</code> runs a fact-checking graph: either a prebuilt pipeline named in configuration, or a graph built and passed directly. A directly-supplied graph overrides the configured pipeline. The synchronous methods wrap their asynchronous peers; <code>stream</code> / <code>astream</code> surface progress events as the run proceeds. A run returns the graph's raw output: a list of verdicts for a linear pipeline, a research result, or whatever a directly-supplied graph produces.</p>"},{"location":"reference/openfactcheck/#openfactcheck._core.OpenFactCheck","title":"OpenFactCheck","text":"<pre><code>OpenFactCheck(\n    config: OpenFactCheckConfig | None = None,\n    *,\n    graph: _RunGraph | None = None,\n)\n</code></pre> <p>Run a fact-checking pipeline from configuration, or a graph built directly.</p> <p>Pass a <code>graph</code> to run it directly, or leave it unset and let the configuration's <code>pipeline</code> name a prebuilt pipeline; a <code>graph</code> overrides the configured pipeline. Then <code>run</code> it (or <code>arun</code> from inside an event loop), or stream its progress with <code>stream</code> / <code>astream</code>.</p> Example <pre><code>from openfactcheck import OpenFactCheck\n\nofc = OpenFactCheck()  # loads openfactcheck.yaml or the environment; runs config.pipeline\nverdicts = ofc.run(\"The Eiffel Tower opened in 1889.\")\n</code></pre> <p>Configure the run.</p> PARAMETER DESCRIPTION <code>config</code> <p>The run configuration. Loaded from the environment and config files when omitted.</p> <p> TYPE: <code>OpenFactCheckConfig | None</code> DEFAULT: <code>None</code> </p> <code>graph</code> <p>A graph to run directly. Overrides the configured pipeline when given.</p> <p> TYPE: <code>_RunGraph | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/_core.py</code> <pre><code>def __init__(self, config: OpenFactCheckConfig | None = None, *, graph: _RunGraph | None = None) -&gt; None:\n    \"\"\"Configure the run.\n\n    Args:\n        config: The run configuration. Loaded from the environment and config files when omitted.\n        graph: A graph to run directly. Overrides the configured pipeline when given.\n\n    Raises:\n        ValueError: If no graph is given and the configuration names no pipeline.\n    \"\"\"\n    self._config = config if config is not None else OpenFactCheckConfig()\n    self._graph = graph\n    self._pipeline: Pipeline | None = None\n    self._mode: _Mode = self._resolve_mode()\n</code></pre>"},{"location":"reference/openfactcheck/#openfactcheck._core.OpenFactCheck.arun","title":"arun  <code>async</code>","text":"<pre><code>arun(content: str | Input) -&gt; Any\n</code></pre> <p>Run the configured graph over <code>content</code> and return its raw output.</p> PARAMETER DESCRIPTION <code>content</code> <p>The text to check, as a string or an <code>Input</code>.</p> <p> TYPE: <code>str | Input</code> </p> Source code in <code>src/openfactcheck/_core.py</code> <pre><code>async def arun(self, content: str | Input) -&gt; Any:  # noqa: ANN401 - the output type is the graph's, not OpenFactCheck's.\n    \"\"\"Run the configured graph over ``content`` and return its raw output.\n\n    Args:\n        content: The text to check, as a string or an [`Input`][openfactcheck.components.types.Input].\n\n    Returns:\n        The graph's output: a list of verdicts for a linear pipeline, or the graph's own output otherwise.\n    \"\"\"\n    inp = content if isinstance(content, Input) else Input(content=content)\n    async with AsyncExitStack() as stack:\n        return await self._build_graph(stack).arun(inp, state=None, deps=None)\n</code></pre>"},{"location":"reference/openfactcheck/#openfactcheck._core.OpenFactCheck.run","title":"run","text":"<pre><code>run(content: str | Input) -&gt; Any\n</code></pre> <p>Run the configured graph over <code>content</code> and return its raw output.</p> <p>The synchronous counterpart of <code>arun</code>; call <code>arun</code> from inside a running event loop.</p> PARAMETER DESCRIPTION <code>content</code> <p>The text to check, as a string or an <code>Input</code>.</p> <p> TYPE: <code>str | Input</code> </p> Source code in <code>src/openfactcheck/_core.py</code> <pre><code>def run(self, content: str | Input) -&gt; Any:  # noqa: ANN401 - the output type is the graph's, not OpenFactCheck's.\n    \"\"\"Run the configured graph over ``content`` and return its raw output.\n\n    The synchronous counterpart of [`arun`][OpenFactCheck.arun]; call ``arun`` from inside a running event\n    loop.\n\n    Args:\n        content: The text to check, as a string or an [`Input`][openfactcheck.components.types.Input].\n\n    Returns:\n        The graph's output.\n    \"\"\"\n    return asyncio.run(self.arun(content))\n</code></pre>"},{"location":"reference/openfactcheck/#openfactcheck._core.OpenFactCheck.astream","title":"astream  <code>async</code>","text":"<pre><code>astream(content: str | Input) -&gt; AsyncIterator[GraphEvent]\n</code></pre> <p>Run the fact-check and yield progress events as they happen.</p> <p>Yields a start and finish event per node, with node-level data events in between, then a final run-finished event.</p> PARAMETER DESCRIPTION <code>content</code> <p>The text to check, as a string or an <code>Input</code>.</p> <p> TYPE: <code>str | Input</code> </p> YIELDS DESCRIPTION <code>AsyncIterator[GraphEvent]</code> <p>Each <code>GraphEvent</code> in turn.</p> Source code in <code>src/openfactcheck/_core.py</code> <pre><code>async def astream(self, content: str | Input) -&gt; AsyncIterator[GraphEvent]:\n    \"\"\"Run the fact-check and yield progress events as they happen.\n\n    Yields a start and finish event per node, with node-level data events in between, then a final\n    run-finished event.\n\n    Args:\n        content: The text to check, as a string or an [`Input`][openfactcheck.components.types.Input].\n\n    Yields:\n        Each [`GraphEvent`][openfactcheck.graph.GraphEvent] in turn.\n    \"\"\"\n    inp = content if isinstance(content, Input) else Input(content=content)\n    async with AsyncExitStack() as stack:\n        graph = self._build_graph(stack)\n        async for event in graph.astream(inp, state=None, deps=None, options=RunOptions(stream_node_data=True)):\n            yield event\n</code></pre>"},{"location":"reference/openfactcheck/#openfactcheck._core.OpenFactCheck.stream","title":"stream","text":"<pre><code>stream(content: str | Input) -&gt; Iterator[GraphEvent]\n</code></pre> <p>Run the fact-check and return its progress events.</p> <p>The synchronous counterpart of <code>astream</code>; it runs to completion and returns the events. Call <code>astream</code> for events as they arrive.</p> PARAMETER DESCRIPTION <code>content</code> <p>The text to check, as a string or an <code>Input</code>.</p> <p> TYPE: <code>str | Input</code> </p> Source code in <code>src/openfactcheck/_core.py</code> <pre><code>def stream(self, content: str | Input) -&gt; Iterator[GraphEvent]:\n    \"\"\"Run the fact-check and return its progress events.\n\n    The synchronous counterpart of [`astream`][OpenFactCheck.astream]; it runs to completion and returns\n    the events. Call ``astream`` for events as they arrive.\n\n    Args:\n        content: The text to check, as a string or an [`Input`][openfactcheck.components.types.Input].\n\n    Returns:\n        An iterator over the run's [`GraphEvent`][openfactcheck.graph.GraphEvent] values.\n    \"\"\"\n\n    async def collect() -&gt; list[GraphEvent]:\n        return [event async for event in self.astream(content)]\n\n    return iter(asyncio.run(collect()))\n</code></pre>"},{"location":"reference/types/","title":"Types","text":"<p>Core data types for fact-checking.</p> <p>The vocabulary that flows between components, the per-claim record the pipeline collects, and the assembled run result. Each category contract is defined in terms of these types, so any implementation of one category produces what the next can take.</p>"},{"location":"reference/types/#openfactcheck.components.types.Input","title":"Input","text":"<p>               Bases: <code>BaseModel</code></p> <p>Content to be fact-checked.</p>"},{"location":"reference/types/#openfactcheck.components.types.Input.content","title":"content  <code>instance-attribute</code>","text":"<pre><code>content: str\n</code></pre> <p>The text to be fact-checked.</p>"},{"location":"reference/types/#openfactcheck.components.types.Claim","title":"Claim","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single factual claim extracted from the input.</p>"},{"location":"reference/types/#openfactcheck.components.types.Claim.text","title":"text  <code>instance-attribute</code>","text":"<pre><code>text: str\n</code></pre> <p>The factual statement being checked.</p>"},{"location":"reference/types/#openfactcheck.components.types.Query","title":"Query","text":"<p>               Bases: <code>BaseModel</code></p> <p>Search queries generated for verifying a claim.</p>"},{"location":"reference/types/#openfactcheck.components.types.Query.claim","title":"claim  <code>instance-attribute</code>","text":"<pre><code>claim: Claim\n</code></pre> <p>The claim the queries seek evidence for.</p>"},{"location":"reference/types/#openfactcheck.components.types.Query.questions","title":"questions  <code>instance-attribute</code>","text":"<pre><code>questions: list[str]\n</code></pre> <p>Search questions derived from the claim.</p>"},{"location":"reference/types/#openfactcheck.components.types.SourceMetadata","title":"SourceMetadata","text":"<p>               Bases: <code>BaseModel</code></p> <p>Base metadata for a source. Subclass for specific source types.</p>"},{"location":"reference/types/#openfactcheck.components.types.WebMetadata","title":"WebMetadata","text":"<p>               Bases: <code>SourceMetadata</code></p> <p>Metadata for a web source.</p>"},{"location":"reference/types/#openfactcheck.components.types.WebMetadata.url","title":"url  <code>instance-attribute</code>","text":"<pre><code>url: str\n</code></pre> <p>Address of the web page.</p>"},{"location":"reference/types/#openfactcheck.components.types.WebMetadata.title","title":"title  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>title: str | None = None\n</code></pre> <p>Title of the web page, or <code>None</code> when the source provides none.</p>"},{"location":"reference/types/#openfactcheck.components.types.Source","title":"Source","text":"<p>               Bases: <code>BaseModel</code></p> <p>A piece of evidence from any source type.</p>"},{"location":"reference/types/#openfactcheck.components.types.Source.content","title":"content  <code>instance-attribute</code>","text":"<pre><code>content: str\n</code></pre> <p>The source's textual content.</p>"},{"location":"reference/types/#openfactcheck.components.types.Source.metadata","title":"metadata  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>metadata: SourceMetadata = SourceMetadata()\n</code></pre> <p>Structured metadata describing where the source came from.</p>"},{"location":"reference/types/#openfactcheck.components.types.Evidence","title":"Evidence","text":"<p>               Bases: <code>BaseModel</code></p> <p>Evidence collected for a single claim.</p>"},{"location":"reference/types/#openfactcheck.components.types.Evidence.claim","title":"claim  <code>instance-attribute</code>","text":"<pre><code>claim: Claim\n</code></pre> <p>The claim this evidence bears on.</p>"},{"location":"reference/types/#openfactcheck.components.types.Evidence.sources","title":"sources  <code>instance-attribute</code>","text":"<pre><code>sources: list[Source]\n</code></pre> <p>Sources gathered for the claim; empty when nothing was found.</p>"},{"location":"reference/types/#openfactcheck.components.types.Verdict","title":"Verdict","text":"<p>               Bases: <code>BaseModel</code></p> <p>Verification result for a single claim: the judgment and the evidence behind it.</p>"},{"location":"reference/types/#openfactcheck.components.types.Verdict.claim","title":"claim  <code>instance-attribute</code>","text":"<pre><code>claim: Claim\n</code></pre> <p>The claim being judged.</p>"},{"location":"reference/types/#openfactcheck.components.types.Verdict.evidence","title":"evidence  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>evidence: Evidence | None = None\n</code></pre> <p>Evidence weighed in reaching the verdict, or <code>None</code> when the claim was judged without retrieval.</p>"},{"location":"reference/types/#openfactcheck.components.types.Verdict.label","title":"label  <code>instance-attribute</code>","text":"<pre><code>label: Literal[\n    \"supported\", \"refuted\", \"not_enough_evidence\"\n]\n</code></pre> <p>Whether the evidence supports, refutes, or is insufficient for the claim.</p>"},{"location":"reference/types/#openfactcheck.components.types.Verdict.confidence","title":"confidence  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>confidence: float | None = None\n</code></pre> <p>How strongly the evidence backs the assigned label, or <code>None</code> when the verifier reports no confidence.</p>"},{"location":"reference/types/#openfactcheck.components.types.Verdict.reasoning","title":"reasoning  <code>instance-attribute</code>","text":"<pre><code>reasoning: str\n</code></pre> <p>Explanation for the assigned label.</p>"},{"location":"reference/types/#openfactcheck.components.types.Verdict.error","title":"error  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>error: str | None = None\n</code></pre> <p>The factual error found in the claim, or <code>None</code> when the claim is accurate or the verifier finds no error.</p>"},{"location":"reference/types/#openfactcheck.components.types.Verdict.correction","title":"correction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>correction: str | None = None\n</code></pre> <p>A corrected version of the claim, or <code>None</code> when there is nothing to correct.</p>"},{"location":"reference/types/#openfactcheck.components.types.ReportSummary","title":"ReportSummary","text":"<p>               Bases: <code>BaseModel</code></p> <p>Counts of a report's verdicts and an overall read of its factuality.</p>"},{"location":"reference/types/#openfactcheck.components.types.ReportSummary.supported","title":"supported  <code>instance-attribute</code>","text":"<pre><code>supported: int\n</code></pre> <p>How many claims the evidence supported.</p>"},{"location":"reference/types/#openfactcheck.components.types.ReportSummary.refuted","title":"refuted  <code>instance-attribute</code>","text":"<pre><code>refuted: int\n</code></pre> <p>How many claims the evidence refuted.</p>"},{"location":"reference/types/#openfactcheck.components.types.ReportSummary.not_enough_evidence","title":"not_enough_evidence  <code>instance-attribute</code>","text":"<pre><code>not_enough_evidence: int\n</code></pre> <p>How many claims lacked sufficient evidence to judge.</p>"},{"location":"reference/types/#openfactcheck.components.types.ReportSummary.total","title":"total  <code>instance-attribute</code>","text":"<pre><code>total: int\n</code></pre> <p>How many claims were judged in total.</p>"},{"location":"reference/types/#openfactcheck.components.types.ReportSummary.factual","title":"factual  <code>instance-attribute</code>","text":"<pre><code>factual: bool | None\n</code></pre> <p>Whether the input reads as factual overall.</p> <p><code>True</code> when every claim was supported, <code>False</code> when any claim was refuted, and <code>None</code> when no refutation was found but some claim lacked enough evidence (or there were no claims), so no overall judgment can be drawn.</p>"},{"location":"reference/types/#openfactcheck.components.types.Report","title":"Report","text":"<p>               Bases: <code>BaseModel</code></p> <p>The complete result of fact-checking an input.</p>"},{"location":"reference/types/#openfactcheck.components.types.Report.input","title":"input  <code>instance-attribute</code>","text":"<pre><code>input: Input\n</code></pre> <p>The original input that was checked.</p>"},{"location":"reference/types/#openfactcheck.components.types.Report.verdicts","title":"verdicts  <code>instance-attribute</code>","text":"<pre><code>verdicts: list[Verdict]\n</code></pre> <p>The verdict for each extracted claim, each carrying its claim and the evidence behind it.</p>"},{"location":"reference/types/#openfactcheck.components.types.Report.revision","title":"revision  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>revision: str | None = None\n</code></pre> <p>The input revised to correct its factual errors, or <code>None</code> when no revision was produced.</p>"},{"location":"reference/types/#openfactcheck.components.types.Report.attribution","title":"attribution  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attribution: list[Source] | None = None\n</code></pre> <p>Sources cited as the attribution report for the result, or <code>None</code> when the pipeline produces none.</p>"},{"location":"reference/types/#openfactcheck.components.types.Report.summary","title":"summary  <code>property</code>","text":"<pre><code>summary: ReportSummary\n</code></pre> <p>Counts of the verdicts by label, with an overall factuality read.</p>"},{"location":"reference/api/","title":"API","text":"<p>REST API infrastructure.</p> <ul> <li>Auth \u2014 token verifiers (Cognito, Dev bypass) and the verifier protocol.</li> <li>Models \u2014 Pydantic domain models.</li> <li>Repositories \u2014 data access layer shared by API handlers.</li> <li>Errors \u2014 structured error hierarchy returned by API handlers.</li> </ul>"},{"location":"reference/api/errors/","title":"Errors","text":"<p>Exception hierarchy for the REST API. Middleware catches any subclass of <code>AppError</code> and converts it to a JSON response with a matching HTTP status and machine-readable error code.</p> <p>API error types and machine-readable error codes.</p>"},{"location":"reference/api/errors/#openfactcheck.api.errors.ErrorCode","title":"ErrorCode","text":"<p>               Bases: <code>StrEnum</code></p> <p>Machine-readable error codes returned in JSON error responses.</p>"},{"location":"reference/api/errors/#openfactcheck.api.errors.AppError","title":"AppError","text":"<pre><code>AppError(detail: str, code: ErrorCode, status: int = 500)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Base exception for all API errors.</p> <p>Caught by middleware and converted to a JSON response:</p> <pre><code>{\n  \"detail\": \"...\",\n  \"code\": \"MACHINE_CODE\",\n  \"status\": 500\n}\n</code></pre> <p>Create an error with the given detail, machine code, and HTTP status.</p> PARAMETER DESCRIPTION <code>detail</code> <p>Human-readable error message.</p> <p> TYPE: <code>str</code> </p> <code>code</code> <p>Machine-readable error code for clients.</p> <p> TYPE: <code>ErrorCode</code> </p> <code>status</code> <p>HTTP status code.</p> <p> TYPE: <code>int</code> DEFAULT: <code>500</code> </p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str, code: ErrorCode, status: int = 500) -&gt; None:\n    \"\"\"Create an error with the given detail, machine code, and HTTP status.\n\n    Args:\n        detail: Human-readable error message.\n        code: Machine-readable error code for clients.\n        status: HTTP status code.\n    \"\"\"\n    super().__init__(detail)\n    self.detail = detail\n    self.code = code\n    self.status = status\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.NotFoundError","title":"NotFoundError","text":"<pre><code>NotFoundError(detail: str = 'Resource not found')\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when a requested resource does not exist (HTTP 404).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Resource not found\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.NOT_FOUND, status=404)\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.AlreadyExistsError","title":"AlreadyExistsError","text":"<pre><code>AlreadyExistsError(detail: str = 'Resource already exists')\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when attempting to create a resource that already exists (HTTP 409).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Resource already exists\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.ALREADY_EXISTS, status=409)\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.ProjectLimitError","title":"ProjectLimitError","text":"<pre><code>ProjectLimitError(detail: str = 'Project limit reached')\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when a user has reached their maximum project count (HTTP 422).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Project limit reached\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.PROJECT_LIMIT_REACHED, status=422)\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.WorkspaceLimitError","title":"WorkspaceLimitError","text":"<pre><code>WorkspaceLimitError(\n    detail: str = \"Workspace limit reached\",\n)\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when a project has reached its maximum workspace count (HTTP 422).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Workspace limit reached\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.WORKSPACE_LIMIT_REACHED, status=422)\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.SecretLimitError","title":"SecretLimitError","text":"<pre><code>SecretLimitError(detail: str = 'Secret limit reached')\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when a user has reached their maximum secret count (HTTP 422).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Secret limit reached\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.SECRET_LIMIT_REACHED, status=422)\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.AuthError","title":"AuthError","text":"<pre><code>AuthError(detail: str = 'Authentication required')\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when authentication fails or is missing (HTTP 401).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Authentication required\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.UNAUTHORIZED, status=401)\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.ForbiddenError","title":"ForbiddenError","text":"<pre><code>ForbiddenError(detail: str = 'Forbidden')\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when the user lacks permission for the requested action (HTTP 403).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Forbidden\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.FORBIDDEN, status=403)\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.ExecutionTimeoutError","title":"ExecutionTimeoutError","text":"<pre><code>ExecutionTimeoutError(detail: str = 'Execution timed out')\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when pipeline execution exceeds the allowed timeout (HTTP 408).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Execution timed out\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.EXECUTION_TIMEOUT, status=408)\n</code></pre>"},{"location":"reference/api/errors/#openfactcheck.api.errors.ExecutionError","title":"ExecutionError","text":"<pre><code>ExecutionError(detail: str = 'Execution failed')\n</code></pre> <p>               Bases: <code>AppError</code></p> <p>Raised when pipeline execution fails (HTTP 500).</p> <p>Create the error, optionally with a custom detail message.</p> Source code in <code>src/openfactcheck/api/errors.py</code> <pre><code>def __init__(self, detail: str = \"Execution failed\") -&gt; None:\n    \"\"\"Create the error, optionally with a custom detail message.\"\"\"\n    super().__init__(detail, ErrorCode.EXECUTION_ERROR, status=500)\n</code></pre>"},{"location":"reference/api/auth/","title":"Auth","text":"<p>Token verifiers for the REST API. Pluggable implementations of the same verifier protocol; swap <code>CognitoVerifier</code> for <code>DevVerifier</code> in local development or tests.</p> <ul> <li><code>TokenVerifier</code> protocol \u2014 interface all verifiers implement.</li> <li><code>CognitoVerifier</code> class \u2014 verifies Cognito ID tokens against a user pool's JWKS.</li> <li><code>DevVerifier</code> class \u2014 bypass verifier that returns the same user on every call.</li> </ul>"},{"location":"reference/api/auth/cognito/","title":"<code>CognitoVerifier</code> class","text":"<p>Cognito JWT verification against a user pool's JWKS.</p>"},{"location":"reference/api/auth/cognito/#openfactcheck.api.auth.cognito.CognitoVerifier","title":"CognitoVerifier","text":"<pre><code>CognitoVerifier(\n    region: str,\n    user_pool_id: str,\n    client_id: str,\n    *,\n    jwk_client: PyJWKClient | None = None,\n)\n</code></pre> <p>Verify Cognito ID tokens against a user pool's JWKS.</p> <p>Validates the RS256 signature, expiration, issuer, audience, and the <code>token_use=id</code> claim. Returns the authenticated user or raises <code>AuthError</code> on any failure.</p> <p>Build a verifier bound to a specific Cognito user pool.</p> PARAMETER DESCRIPTION <code>region</code> <p>AWS region of the user pool, e.g. <code>\"us-east-1\"</code>.</p> <p> TYPE: <code>str</code> </p> <code>user_pool_id</code> <p>Cognito user pool identifier.</p> <p> TYPE: <code>str</code> </p> <code>client_id</code> <p>App client identifier that tokens must be issued to.</p> <p> TYPE: <code>str</code> </p> <code>jwk_client</code> <p>JWK client to use. Typically omitted so the verifier creates its own from the pool's JWKS endpoint; inject a custom one in tests.</p> <p> TYPE: <code>PyJWKClient | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/api/auth/cognito.py</code> <pre><code>def __init__(\n    self,\n    region: str,\n    user_pool_id: str,\n    client_id: str,\n    *,\n    jwk_client: PyJWKClient | None = None,\n) -&gt; None:\n    \"\"\"Build a verifier bound to a specific Cognito user pool.\n\n    Args:\n        region: AWS region of the user pool, e.g. ``\"us-east-1\"``.\n        user_pool_id: Cognito user pool identifier.\n        client_id: App client identifier that tokens must be issued to.\n        jwk_client: JWK client to use. Typically omitted so the verifier\n            creates its own from the pool's JWKS endpoint; inject a\n            custom one in tests.\n    \"\"\"\n    self._issuer = f\"https://cognito-idp.{region}.amazonaws.com/{user_pool_id}\"\n    self._client_id = client_id\n    self._jwk_client = jwk_client or PyJWKClient(f\"{self._issuer}/.well-known/jwks.json\")\n</code></pre>"},{"location":"reference/api/auth/cognito/#openfactcheck.api.auth.cognito.CognitoVerifier.verify","title":"verify","text":"<pre><code>verify(token: str) -&gt; AuthUser\n</code></pre> <p>Decode and validate a Cognito ID token.</p> PARAMETER DESCRIPTION <code>token</code> <p>The raw JWT string as received from the client.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/api/auth/cognito.py</code> <pre><code>def verify(self, token: str) -&gt; AuthUser:\n    \"\"\"Decode and validate a Cognito ID token.\n\n    Args:\n        token: The raw JWT string as received from the client.\n\n    Returns:\n        The authenticated user, populated from the token's ``sub``,\n        ``email``, and ``name`` claims.\n\n    Raises:\n        AuthError: If the token is invalid, expired, not an ID token,\n            or missing required claims.\n    \"\"\"\n    try:\n        signing_key = self._jwk_client.get_signing_key_from_jwt(token)\n        payload: dict[str, object] = jwt.decode(\n            token,\n            signing_key.key,\n            algorithms=[\"RS256\"],\n            issuer=self._issuer,\n            audience=self._client_id,\n            options={\"require\": [\"exp\", \"iss\", \"aud\", \"sub\", \"token_use\"]},\n        )\n    except jwt.InvalidTokenError as e:\n        raise AuthError(\"Invalid or expired token\") from e\n\n    if payload.get(\"token_use\") != \"id\":\n        raise AuthError(\"Token is not an ID token\")\n\n    sub = payload.get(\"sub\")\n    email = payload.get(\"email\")\n    if not isinstance(sub, str) or not isinstance(email, str):\n        raise AuthError(\"Token missing required claims\")\n\n    name = payload.get(\"name\", \"\")\n    if not isinstance(name, str):\n        name = \"\"\n\n    return AuthUser(sub=sub, email=email, name=name)\n</code></pre>"},{"location":"reference/api/auth/dev/","title":"<code>DevVerifier</code> class","text":"<p>Dev bypass verifier that returns the same user on every call, without validating the token.</p>"},{"location":"reference/api/auth/dev/#openfactcheck.api.auth.dev.DEV_USER","title":"DEV_USER  <code>module-attribute</code>","text":"<pre><code>DEV_USER = AuthUser(\n    sub=\"dev-user-00000000\",\n    email=\"dev@localhost\",\n    name=\"Dev User\",\n)\n</code></pre> <p>User returned by <code>DevVerifier</code> on every call.</p>"},{"location":"reference/api/auth/dev/#openfactcheck.api.auth.dev.DevVerifier","title":"DevVerifier","text":"<p>Bypass verifier for local development.</p> <p>Returns <code>DEV_USER</code> on every call, without validating the token.</p> Warning <p>Never use in production.</p>"},{"location":"reference/api/auth/dev/#openfactcheck.api.auth.dev.DevVerifier.verify","title":"verify","text":"<pre><code>verify(token: str) -&gt; AuthUser\n</code></pre> <p>Return the dev user regardless of the token value.</p> PARAMETER DESCRIPTION <code>token</code> <p>Ignored; present only to match the verifier protocol.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/api/auth/dev.py</code> <pre><code>def verify(self, token: str) -&gt; AuthUser:\n    \"\"\"Return the dev user regardless of the token value.\n\n    Args:\n        token: Ignored; present only to match the verifier protocol.\n\n    Returns:\n        The [`DEV_USER`][DEV_USER] constant on every call.\n    \"\"\"\n    return DEV_USER\n</code></pre>"},{"location":"reference/api/auth/protocol/","title":"<code>TokenVerifier</code> protocol","text":"<p>Every verifier implementation satisfies the same contract:</p> <pre><code>flowchart TD\n    P([TokenVerifier])\n    P -.-&gt; C[CognitoVerifier]\n    P -.-&gt; D[DevVerifier]</code></pre> <ul> <li><code>CognitoVerifier</code> \u2014 production. Validates JWTs against a user pool's JWKS.</li> <li><code>DevVerifier</code> \u2014 local development. Returns the same user on every call, no validation.</li> </ul> <p>Swap one for the other without changing any call site.</p> <p>Token verifier protocol for bearer-token auth implementations.</p>"},{"location":"reference/api/auth/protocol/#openfactcheck.api.auth.protocols.TokenVerifier","title":"TokenVerifier","text":"<p>               Bases: <code>Protocol</code></p> <p>Interface for verifying an Authorization bearer token.</p> <p>Implementations validate a bearer token and return the authenticated user, or raise if the token is invalid.</p>"},{"location":"reference/api/auth/protocol/#openfactcheck.api.auth.protocols.TokenVerifier.verify","title":"verify","text":"<pre><code>verify(token: str) -&gt; AuthUser\n</code></pre> <p>Verify a bearer token and return the authenticated user.</p> PARAMETER DESCRIPTION <code>token</code> <p>The raw token string from the Authorization header.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/api/auth/protocols.py</code> <pre><code>def verify(self, token: str) -&gt; AuthUser:\n    \"\"\"Verify a bearer token and return the authenticated user.\n\n    Args:\n        token: The raw token string from the Authorization header.\n\n    Returns:\n        The authenticated user.\n\n    Raises:\n        AuthError: If the token is invalid, expired, or missing required claims.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/models/","title":"Models","text":"<p>Pydantic domain models used by the REST API. Each module covers a concept (project, workspace, user) along with its create and update payloads.</p> <ul> <li>Project \u2014 projects and their mutation payloads.</li> <li>Workspace \u2014 workspaces, pipeline run state, and settings.</li> <li>User \u2014 authenticated user extracted from a JWT.</li> </ul>"},{"location":"reference/api/models/project/","title":"Project","text":"<p>A user's fact-checking project. The persisted model plus its create and update payloads.</p>"},{"location":"reference/api/models/project/#data-models","title":"Data model/s","text":""},{"location":"reference/api/models/project/#openfactcheck.api.models.project.Project","title":"Project","text":"<p>               Bases: <code>BaseModel</code></p> <p>A user's fact-checking project.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.Project.id","title":"id  <code>instance-attribute</code>","text":"<pre><code>id: str\n</code></pre> <p>Opaque unique identifier for the project.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.Project.user_id","title":"user_id  <code>instance-attribute</code>","text":"<pre><code>user_id: str\n</code></pre> <p>Identifier of the user who owns the project.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.Project.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>Human-readable project name.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.Project.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str = ''\n</code></pre> <p>Freeform project description.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.Project.created_at","title":"created_at  <code>instance-attribute</code>","text":"<pre><code>created_at: datetime\n</code></pre> <p>Timestamp the project was created.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.Project.updated_at","title":"updated_at  <code>instance-attribute</code>","text":"<pre><code>updated_at: datetime\n</code></pre> <p>Timestamp of the most recent modification.</p>"},{"location":"reference/api/models/project/#mutations","title":"Mutations","text":"<p>Payloads for creating and updating a project.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.ProjectCreate","title":"ProjectCreate","text":"<p>               Bases: <code>BaseModel</code></p> <p>Fields required to create a new project.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.ProjectCreate.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str = Field(min_length=1, max_length=255)\n</code></pre> <p>Human-readable project name, 1 to 255 characters.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.ProjectCreate.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str = Field(default='', max_length=10000)\n</code></pre> <p>Freeform project description, up to 10000 characters.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.ProjectUpdate","title":"ProjectUpdate","text":"<p>               Bases: <code>BaseModel</code></p> <p>Fields that can be updated on a project. All optional.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.ProjectUpdate.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str | None = Field(\n    default=None, min_length=1, max_length=255\n)\n</code></pre> <p>New project name, 1 to 255 characters. <code>None</code> leaves the existing name unchanged.</p>"},{"location":"reference/api/models/project/#openfactcheck.api.models.project.ProjectUpdate.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str | None = Field(\n    default=None, max_length=10000\n)\n</code></pre> <p>New description, up to 10000 characters. <code>None</code> leaves the existing description unchanged.</p>"},{"location":"reference/api/models/user/","title":"User","text":"<p>Authenticated user extracted from a JWT.</p> <p>Authenticated user model derived from JWT claims.</p>"},{"location":"reference/api/models/user/#openfactcheck.api.models.user.AuthUser","title":"AuthUser  <code>dataclass</code>","text":"<pre><code>AuthUser(sub: str, email: str, name: str = '')\n</code></pre> <p>Current authenticated user, extracted from a Cognito JWT.</p>"},{"location":"reference/api/models/user/#openfactcheck.api.models.user.AuthUser.sub","title":"sub  <code>instance-attribute</code>","text":"<pre><code>sub: str\n</code></pre> <p>Cognito user pool subject identifier.</p>"},{"location":"reference/api/models/user/#openfactcheck.api.models.user.AuthUser.email","title":"email  <code>instance-attribute</code>","text":"<pre><code>email: str\n</code></pre> <p>User's verified email address.</p>"},{"location":"reference/api/models/user/#openfactcheck.api.models.user.AuthUser.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str = ''\n</code></pre> <p>User's display name. May be empty when the identity provider doesn't supply one.</p>"},{"location":"reference/api/models/workspace/","title":"Workspace","text":"<p>A pipeline configuration scoped to a project. Each workspace carries settings, a content blob (the pipeline itself), and the latest run state.</p>"},{"location":"reference/api/models/workspace/#data-models","title":"Data model/s","text":""},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace","title":"Workspace","text":"<p>               Bases: <code>BaseModel</code></p> <p>A workspace within a project, containing a single pipeline configuration.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.id","title":"id  <code>instance-attribute</code>","text":"<pre><code>id: str\n</code></pre> <p>Opaque unique identifier for the workspace.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.user_id","title":"user_id  <code>instance-attribute</code>","text":"<pre><code>user_id: str\n</code></pre> <p>Identifier of the user who owns the workspace.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.project_id","title":"project_id  <code>instance-attribute</code>","text":"<pre><code>project_id: str\n</code></pre> <p>Identifier of the project the workspace belongs to.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>Human-readable workspace name.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str = ''\n</code></pre> <p>Freeform workspace description.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.locked","title":"locked  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>locked: bool = False\n</code></pre> <p>When <code>True</code>, the workspace is read-only.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.sort_order","title":"sort_order  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>sort_order: int = 0\n</code></pre> <p>Ordering hint for display; lower values appear first.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.settings","title":"settings  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>settings: WorkspaceSettings = Field(\n    default_factory=WorkspaceSettings,\n    validation_alias=AliasChoices(\n        \"settings\", \"settings_json\"\n    ),\n)\n</code></pre> <p>Workspace-level configuration.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.content","title":"content  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>content: JSONObject | None = Field(\n    default=None,\n    validation_alias=AliasChoices(\n        \"content\", \"content_json\"\n    ),\n)\n</code></pre> <p>Pipeline configuration as a JSON object. <code>None</code> for an empty workspace.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.run","title":"run  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>run: WorkspaceRun | None = Field(\n    default=None,\n    validation_alias=AliasChoices(\"run\", \"run_json\"),\n)\n</code></pre> <p>Latest pipeline run state, or <code>None</code> when the workspace has never been run.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.created_at","title":"created_at  <code>instance-attribute</code>","text":"<pre><code>created_at: datetime\n</code></pre> <p>Timestamp the workspace was created.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.Workspace.updated_at","title":"updated_at  <code>instance-attribute</code>","text":"<pre><code>updated_at: datetime\n</code></pre> <p>Timestamp of the most recent modification.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRun","title":"WorkspaceRun","text":"<p>               Bases: <code>BaseModel</code></p> <p>Latest pipeline run state for a workspace.</p> Note <p>Valid state combinations:</p> <ul> <li><code>running</code>: <code>started_at</code> is set; <code>completed_at</code> and <code>error</code> are not.</li> <li><code>completed</code>: <code>completed_at</code> is set; <code>error</code> is not.</li> <li><code>failed</code>: <code>completed_at</code> and <code>error</code> are both set.</li> </ul>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRun.status","title":"status  <code>instance-attribute</code>","text":"<pre><code>status: WorkspaceRunStatus\n</code></pre> <p>Current lifecycle state of the run.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRun.output","title":"output  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>output: str = ''\n</code></pre> <p>Captured text output from the pipeline execution.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRun.error","title":"error  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>error: str = ''\n</code></pre> <p>Error message. Populated when <code>status</code> is <code>failed</code>.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRun.started_at","title":"started_at  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>started_at: datetime | None = None\n</code></pre> <p>Timestamp the run started. <code>None</code> before any execution.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRun.completed_at","title":"completed_at  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>completed_at: datetime | None = None\n</code></pre> <p>Timestamp the run finished. <code>None</code> while the run is still in progress.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRunStatus","title":"WorkspaceRunStatus","text":"<p>               Bases: <code>StrEnum</code></p> <p>Lifecycle states for a pipeline run.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRunStatus.RUNNING","title":"RUNNING  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RUNNING = 'running'\n</code></pre> <p>Pipeline is currently executing.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRunStatus.COMPLETED","title":"COMPLETED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>COMPLETED = 'completed'\n</code></pre> <p>Pipeline finished successfully.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceRunStatus.FAILED","title":"FAILED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FAILED = 'failed'\n</code></pre> <p>Pipeline finished with an error.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceSettings","title":"WorkspaceSettings","text":"<p>               Bases: <code>BaseModel</code></p> <p>Optional per-workspace configuration.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceSettings.verbose_mode","title":"verbose_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>verbose_mode: bool = False\n</code></pre> <p>Emit verbose execution logs during pipeline runs.</p>"},{"location":"reference/api/models/workspace/#mutations","title":"Mutations","text":"<p>Payloads for creating and updating a workspace.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceCreate","title":"WorkspaceCreate","text":"<p>               Bases: <code>BaseModel</code></p> <p>Fields required to create a new workspace.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceCreate.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str = Field(min_length=1, max_length=255)\n</code></pre> <p>Human-readable workspace name, 1 to 255 characters.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceCreate.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str = Field(default='', max_length=10000)\n</code></pre> <p>Freeform workspace description, up to 10000 characters.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceUpdate","title":"WorkspaceUpdate","text":"<p>               Bases: <code>BaseModel</code></p> <p>Fields that can be updated on a workspace. All optional.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceUpdate.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str | None = Field(\n    default=None, min_length=1, max_length=255\n)\n</code></pre> <p>New workspace name, 1 to 255 characters. <code>None</code> leaves the existing name unchanged.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceUpdate.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str | None = Field(\n    default=None, max_length=10000\n)\n</code></pre> <p>New description, up to 10000 characters. <code>None</code> leaves the existing description unchanged.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceUpdate.locked","title":"locked  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>locked: bool | None = None\n</code></pre> <p>New lock state. <code>None</code> leaves the existing value unchanged.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceUpdate.settings","title":"settings  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>settings: WorkspaceSettings | None = None\n</code></pre> <p>Replacement settings. <code>None</code> leaves the existing settings unchanged.</p>"},{"location":"reference/api/models/workspace/#openfactcheck.api.models.workspace.WorkspaceUpdate.content","title":"content  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>content: JSONObject | None = None\n</code></pre> <p>Replacement pipeline configuration. <code>None</code> leaves the existing content unchanged.</p>"},{"location":"reference/api/repositories/","title":"Repositories","text":"<p>Data access layer for the REST API. Concrete implementations are backed by DynamoDB in production and swappable for tests.</p> <ul> <li>Protocols \u2014 structural typing contracts every concrete repository satisfies.</li> <li>Constants \u2014 shared limits and ID helpers used by every implementation.</li> <li>DynamoDB \u2014 DynamoDB-backed project and workspace repositories.</li> <li>SQLite \u2014 SQLite-backed project and workspace repositories.</li> </ul>"},{"location":"reference/api/repositories/constants/","title":"Constants","text":"<p>Shared limits and ID helpers used by every repository implementation.</p> <p>Shared constants and utilities for repository implementations.</p>"},{"location":"reference/api/repositories/constants/#openfactcheck.api.repositories.constants.MAX_PROJECTS_PER_USER","title":"MAX_PROJECTS_PER_USER  <code>module-attribute</code>","text":"<pre><code>MAX_PROJECTS_PER_USER = 50\n</code></pre> <p>Upper limit on the number of projects a single user may own.</p>"},{"location":"reference/api/repositories/constants/#openfactcheck.api.repositories.constants.MAX_WORKSPACES_PER_PROJECT","title":"MAX_WORKSPACES_PER_PROJECT  <code>module-attribute</code>","text":"<pre><code>MAX_WORKSPACES_PER_PROJECT = 5\n</code></pre> <p>Upper limit on the number of workspaces a single project may contain.</p>"},{"location":"reference/api/repositories/constants/#openfactcheck.api.repositories.constants.MAX_SECRETS_PER_USER","title":"MAX_SECRETS_PER_USER  <code>module-attribute</code>","text":"<pre><code>MAX_SECRETS_PER_USER = 20\n</code></pre> <p>Upper limit on the number of secrets a single user may store.</p>"},{"location":"reference/api/repositories/constants/#openfactcheck.api.repositories.constants.MAX_CONTENT_BYTES","title":"MAX_CONTENT_BYTES  <code>module-attribute</code>","text":"<pre><code>MAX_CONTENT_BYTES = 350000\n</code></pre> <p>Upper limit on the serialized size of a workspace's content blob.</p>"},{"location":"reference/api/repositories/constants/#openfactcheck.api.repositories.constants.MAX_PIPELINE_BYTES","title":"MAX_PIPELINE_BYTES  <code>module-attribute</code>","text":"<pre><code>MAX_PIPELINE_BYTES = 500000\n</code></pre> <p>Upper limit on the serialized size of a compiled pipeline.</p>"},{"location":"reference/api/repositories/constants/#openfactcheck.api.repositories.constants.generate_id","title":"generate_id","text":"<pre><code>generate_id() -&gt; str\n</code></pre> <p>Generate a URL-safe 12-character identifier.</p> Source code in <code>src/openfactcheck/api/repositories/constants.py</code> <pre><code>def generate_id() -&gt; str:\n    \"\"\"Generate a URL-safe 12-character identifier.\"\"\"\n    return token_urlsafe(9)  # 9 bytes \u2192 12 base64 chars\n</code></pre>"},{"location":"reference/api/repositories/protocols/","title":"Protocols","text":"<p>Structural typing contracts for the repository layer. Every concrete repository (DynamoDB and SQLite) satisfies these protocols.</p>"},{"location":"reference/api/repositories/protocols/#projects","title":"Projects","text":""},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.ProjectRepository","title":"ProjectRepository","text":"<p>               Bases: <code>Protocol</code></p> <p>Data access interface for projects.</p>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.ProjectRepository.list_by_user","title":"list_by_user  <code>async</code>","text":"<pre><code>list_by_user(\n    user_id: str, limit: int = 50, offset: int = 0\n) -&gt; list[Project]\n</code></pre> <p>List the user's projects, ordered by creation time (oldest first).</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def list_by_user(self, user_id: str, limit: int = 50, offset: int = 0) -&gt; list[Project]:\n    \"\"\"List the user's projects, ordered by creation time (oldest first).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.ProjectRepository.get","title":"get  <code>async</code>","text":"<pre><code>get(user_id: str, project_id: str) -&gt; Project | None\n</code></pre> <p>Return the project, or <code>None</code> if no project with that ID exists for the user.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def get(self, user_id: str, project_id: str) -&gt; Project | None:\n    \"\"\"Return the project, or ``None`` if no project with that ID exists for the user.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.ProjectRepository.create","title":"create  <code>async</code>","text":"<pre><code>create(user_id: str, data: ProjectCreate) -&gt; Project | None\n</code></pre> <p>Create a new project for the user.</p> <p>Returns <code>None</code> if the user has already reached <code>MAX_PROJECTS_PER_USER</code>.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def create(self, user_id: str, data: ProjectCreate) -&gt; Project | None:\n    \"\"\"Create a new project for the user.\n\n    Returns ``None`` if the user has already reached\n    [`MAX_PROJECTS_PER_USER`][openfactcheck.api.repositories.constants.MAX_PROJECTS_PER_USER].\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.ProjectRepository.update","title":"update  <code>async</code>","text":"<pre><code>update(\n    user_id: str, project_id: str, data: ProjectUpdate\n) -&gt; Project | None\n</code></pre> <p>Apply a partial update and return the updated project, or <code>None</code> if it doesn't exist.</p> <p>An update with no fields set returns the project unchanged.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def update(self, user_id: str, project_id: str, data: ProjectUpdate) -&gt; Project | None:\n    \"\"\"Apply a partial update and return the updated project, or ``None`` if it doesn't exist.\n\n    An update with no fields set returns the project unchanged.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.ProjectRepository.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(user_id: str, project_id: str) -&gt; bool\n</code></pre> <p>Delete the project and cascade-delete its workspaces and runs.</p> <p>Returns <code>False</code> if the project doesn't exist.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def delete(self, user_id: str, project_id: str) -&gt; bool:\n    \"\"\"Delete the project and cascade-delete its workspaces and runs.\n\n    Returns ``False`` if the project doesn't exist.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#workspaces","title":"Workspaces","text":""},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository","title":"WorkspaceRepository","text":"<p>               Bases: <code>Protocol</code></p> <p>Data access interface for workspaces.</p>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository.list_by_project","title":"list_by_project  <code>async</code>","text":"<pre><code>list_by_project(\n    user_id: str, project_id: str\n) -&gt; list[Workspace]\n</code></pre> <p>List the project's workspaces, ordered by <code>sort_order</code> (display order).</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def list_by_project(self, user_id: str, project_id: str) -&gt; list[Workspace]:\n    \"\"\"List the project's workspaces, ordered by ``sort_order`` (display order).\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; Workspace | None\n</code></pre> <p>Return the workspace, or <code>None</code> if no workspace with that ID exists in the project.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def get(self, user_id: str, project_id: str, workspace_id: str) -&gt; Workspace | None:\n    \"\"\"Return the workspace, or ``None`` if no workspace with that ID exists in the project.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository.create","title":"create  <code>async</code>","text":"<pre><code>create(\n    user_id: str, project_id: str, data: WorkspaceCreate\n) -&gt; Workspace | None\n</code></pre> <p>Create a new workspace at the end of the project's sort order.</p> <p>Returns <code>None</code> if the project has already reached <code>MAX_WORKSPACES_PER_PROJECT</code>.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def create(self, user_id: str, project_id: str, data: WorkspaceCreate) -&gt; Workspace | None:\n    \"\"\"Create a new workspace at the end of the project's sort order.\n\n    Returns ``None`` if the project has already reached\n    [`MAX_WORKSPACES_PER_PROJECT`][openfactcheck.api.repositories.constants.MAX_WORKSPACES_PER_PROJECT].\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository.update","title":"update  <code>async</code>","text":"<pre><code>update(\n    user_id: str,\n    project_id: str,\n    workspace_id: str,\n    data: WorkspaceUpdate,\n) -&gt; Workspace | None\n</code></pre> <p>Apply a partial update and return the updated workspace, or <code>None</code> if it doesn't exist.</p> <p>An update with no fields set returns the workspace unchanged.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def update(\n    self,\n    user_id: str,\n    project_id: str,\n    workspace_id: str,\n    data: WorkspaceUpdate,\n) -&gt; Workspace | None:\n    \"\"\"Apply a partial update and return the updated workspace, or ``None`` if it doesn't exist.\n\n    An update with no fields set returns the workspace unchanged.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; bool\n</code></pre> <p>Delete the workspace. Returns <code>False</code> if it doesn't exist.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def delete(self, user_id: str, project_id: str, workspace_id: str) -&gt; bool:\n    \"\"\"Delete the workspace. Returns ``False`` if it doesn't exist.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository.duplicate","title":"duplicate  <code>async</code>","text":"<pre><code>duplicate(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; Workspace | None\n</code></pre> <p>Copy the workspace and append <code>(copy)</code> to its name.</p> <p>Returns <code>None</code> if the source workspace doesn't exist, or if the project has already reached <code>MAX_WORKSPACES_PER_PROJECT</code>.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def duplicate(self, user_id: str, project_id: str, workspace_id: str) -&gt; Workspace | None:\n    \"\"\"Copy the workspace and append ``(copy)`` to its name.\n\n    Returns ``None`` if the source workspace doesn't exist, or if the project has already reached\n    [`MAX_WORKSPACES_PER_PROJECT`][openfactcheck.api.repositories.constants.MAX_WORKSPACES_PER_PROJECT].\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository.reorder","title":"reorder  <code>async</code>","text":"<pre><code>reorder(\n    user_id: str, project_id: str, ordered_ids: list[str]\n) -&gt; None\n</code></pre> <p>Reassign <code>sort_order</code> for the listed workspaces in the given sequence.</p> <p>Each ID in <code>ordered_ids</code> is numbered <code>1..N</code>. IDs not in the list keep their current <code>sort_order</code>.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def reorder(self, user_id: str, project_id: str, ordered_ids: list[str]) -&gt; None:\n    \"\"\"Reassign ``sort_order`` for the listed workspaces in the given sequence.\n\n    Each ID in ``ordered_ids`` is numbered ``1..N``. IDs not in the list keep\n    their current ``sort_order``.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/protocols/#openfactcheck.api.repositories.protocols.WorkspaceRepository.set_run","title":"set_run  <code>async</code>","text":"<pre><code>set_run(\n    user_id: str,\n    project_id: str,\n    workspace_id: str,\n    run: WorkspaceRun,\n) -&gt; None\n</code></pre> <p>Replace the workspace's latest run state with the given run.</p> Source code in <code>src/openfactcheck/api/repositories/protocols.py</code> <pre><code>async def set_run(self, user_id: str, project_id: str, workspace_id: str, run: WorkspaceRun) -&gt; None:\n    \"\"\"Replace the workspace's latest run state with the given run.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/","title":"DynamoDB","text":"<p>Concrete DynamoDB-backed implementations of the API repositories. Each repository inherits from a shared single-table base and exposes the same async CRUD surface.</p> <ul> <li>Base \u2014 <code>BaseDynamoRepository</code>.</li> <li>Projects \u2014 <code>DynamoProjectRepository</code>.</li> <li>Workspaces \u2014 <code>DynamoWorkspaceRepository</code>.</li> </ul>"},{"location":"reference/api/repositories/dynamodb/base/","title":"<code>BaseDynamoRepository</code> class","text":"<p>Generic DynamoDB repository base for single-table entities.</p>"},{"location":"reference/api/repositories/dynamodb/base/#openfactcheck.api.repositories.dynamodb.base.BaseDynamoRepository","title":"BaseDynamoRepository","text":"<pre><code>BaseDynamoRepository(\n    table_name: str, region_name: str = \"us-east-1\"\n)\n</code></pre> <p>Generic single-table DynamoDB operations.</p> <p>Entity repos extend this and add type-specific methods.</p> <p>Build a repository bound to a specific DynamoDB table.</p> PARAMETER DESCRIPTION <code>table_name</code> <p>Name of the DynamoDB table.</p> <p> TYPE: <code>str</code> </p> <code>region_name</code> <p>AWS region where the table lives.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'us-east-1'</code> </p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/base.py</code> <pre><code>def __init__(self, table_name: str, region_name: str = \"us-east-1\") -&gt; None:\n    \"\"\"Build a repository bound to a specific DynamoDB table.\n\n    Args:\n        table_name: Name of the DynamoDB table.\n        region_name: AWS region where the table lives.\n    \"\"\"\n    self._table = get_table(table_name, region_name)\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/projects/","title":"<code>DynamoProjectRepository</code> class","text":"<p>DynamoDB-backed project repository.</p>"},{"location":"reference/api/repositories/dynamodb/projects/#openfactcheck.api.repositories.dynamodb.projects.DynamoProjectRepository","title":"DynamoProjectRepository","text":"<pre><code>DynamoProjectRepository(\n    table_name: str, region_name: str = \"us-east-1\"\n)\n</code></pre> <p>               Bases: <code>BaseDynamoRepository</code></p> <p>DynamoDB-backed repository for project CRUD.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/base.py</code> <pre><code>def __init__(self, table_name: str, region_name: str = \"us-east-1\") -&gt; None:\n    \"\"\"Build a repository bound to a specific DynamoDB table.\n\n    Args:\n        table_name: Name of the DynamoDB table.\n        region_name: AWS region where the table lives.\n    \"\"\"\n    self._table = get_table(table_name, region_name)\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/projects/#openfactcheck.api.repositories.dynamodb.projects.DynamoProjectRepository.list_by_user","title":"list_by_user  <code>async</code>","text":"<pre><code>list_by_user(\n    user_id: str, limit: int = 50, offset: int = 0\n) -&gt; list[Project]\n</code></pre> <p>List the user's projects, ordered by creation time (oldest first).</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/projects.py</code> <pre><code>async def list_by_user(self, user_id: str, limit: int = 50, offset: int = 0) -&gt; list[Project]:\n    \"\"\"List the user's projects, ordered by creation time (oldest first).\"\"\"\n    items = await self._query_by_pk(project_pk(user_id), sk_prefix=\"PROJECT#\")\n    projects = sorted((Project.model_validate(item) for item in items), key=lambda p: p.created_at)\n    return projects[offset : offset + limit]\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/projects/#openfactcheck.api.repositories.dynamodb.projects.DynamoProjectRepository.get","title":"get  <code>async</code>","text":"<pre><code>get(user_id: str, project_id: str) -&gt; Project | None\n</code></pre> <p>Return the project, or <code>None</code> if no project with that ID exists for the user.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/projects.py</code> <pre><code>async def get(self, user_id: str, project_id: str) -&gt; Project | None:\n    \"\"\"Return the project, or ``None`` if no project with that ID exists for the user.\"\"\"\n    item = await self._get(project_pk(user_id), project_sk(project_id))\n    return Project.model_validate(item) if item else None\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/projects/#openfactcheck.api.repositories.dynamodb.projects.DynamoProjectRepository.create","title":"create  <code>async</code>","text":"<pre><code>create(user_id: str, data: ProjectCreate) -&gt; Project | None\n</code></pre> <p>Create a new project for the user.</p> <p>Returns <code>None</code> if the user has already reached <code>MAX_PROJECTS_PER_USER</code>.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/projects.py</code> <pre><code>async def create(self, user_id: str, data: ProjectCreate) -&gt; Project | None:\n    \"\"\"Create a new project for the user.\n\n    Returns ``None`` if the user has already reached [`MAX_PROJECTS_PER_USER`][MAX_PROJECTS_PER_USER].\n    \"\"\"\n    items = await self._query_by_pk(project_pk(user_id), sk_prefix=\"PROJECT#\")\n    if len(items) &gt;= MAX_PROJECTS_PER_USER:\n        return None\n\n    now = datetime.now(UTC)\n    pid = generate_id()\n    item: DynamoItem = {\n        \"PK\": project_pk(user_id),\n        \"SK\": project_sk(pid),\n        \"id\": pid,\n        \"userId\": user_id,\n        \"name\": data.name,\n        \"description\": data.description,\n        \"createdAt\": now.isoformat(),\n        \"updatedAt\": now.isoformat(),\n    }\n    await self._put(item)\n    return Project.model_validate(item)\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/projects/#openfactcheck.api.repositories.dynamodb.projects.DynamoProjectRepository.update","title":"update  <code>async</code>","text":"<pre><code>update(\n    user_id: str, project_id: str, data: ProjectUpdate\n) -&gt; Project | None\n</code></pre> <p>Apply a partial update and return the updated project, or <code>None</code> if it doesn't exist.</p> <p>An update with no fields set returns the project unchanged.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/projects.py</code> <pre><code>async def update(self, user_id: str, project_id: str, data: ProjectUpdate) -&gt; Project | None:\n    \"\"\"Apply a partial update and return the updated project, or ``None`` if it doesn't exist.\n\n    An update with no fields set returns the project unchanged.\n    \"\"\"\n    values = data.model_dump(exclude_none=True)\n    if not values:\n        return await self.get(user_id, project_id)\n\n    attrs = await self._update(project_pk(user_id), project_sk(project_id), values)\n    return Project.model_validate(attrs) if attrs else None\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/projects/#openfactcheck.api.repositories.dynamodb.projects.DynamoProjectRepository.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(user_id: str, project_id: str) -&gt; bool\n</code></pre> <p>Delete the project and cascade-delete its workspaces and runs.</p> <p>Returns <code>False</code> if the project doesn't exist.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/projects.py</code> <pre><code>async def delete(self, user_id: str, project_id: str) -&gt; bool:\n    \"\"\"Delete the project and cascade-delete its workspaces and runs.\n\n    Returns ``False`` if the project doesn't exist.\n    \"\"\"\n    deleted = await self._delete(project_pk(user_id), project_sk(project_id))\n    if not deleted:\n        return False\n\n    # Cascade: delete every child item (workspaces and runs) under this project.\n    children = await self._query_by_pk(workspace_pk(user_id, project_id), projection=\"PK, SK\")\n    if children:\n        await self._batch_delete(children)\n\n    return True\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/","title":"<code>DynamoWorkspaceRepository</code> class","text":"<p>DynamoDB-backed workspace repository.</p>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository","title":"DynamoWorkspaceRepository","text":"<pre><code>DynamoWorkspaceRepository(\n    table_name: str, region_name: str = \"us-east-1\"\n)\n</code></pre> <p>               Bases: <code>BaseDynamoRepository</code></p> <p>DynamoDB-backed repository for workspace CRUD, duplication, and reordering.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/base.py</code> <pre><code>def __init__(self, table_name: str, region_name: str = \"us-east-1\") -&gt; None:\n    \"\"\"Build a repository bound to a specific DynamoDB table.\n\n    Args:\n        table_name: Name of the DynamoDB table.\n        region_name: AWS region where the table lives.\n    \"\"\"\n    self._table = get_table(table_name, region_name)\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository.list_by_project","title":"list_by_project  <code>async</code>","text":"<pre><code>list_by_project(\n    user_id: str, project_id: str\n) -&gt; list[Workspace]\n</code></pre> <p>List the project's workspaces, ordered by <code>sort_order</code> (display order).</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/workspaces.py</code> <pre><code>async def list_by_project(self, user_id: str, project_id: str) -&gt; list[Workspace]:\n    \"\"\"List the project's workspaces, ordered by ``sort_order`` (display order).\"\"\"\n    items = await self._list_items(user_id, project_id)\n    workspaces = [Workspace.model_validate(item) for item in items]\n    return sorted(workspaces, key=lambda w: w.sort_order)\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; Workspace | None\n</code></pre> <p>Return the workspace, or <code>None</code> if no workspace with that ID exists in the project.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/workspaces.py</code> <pre><code>async def get(self, user_id: str, project_id: str, workspace_id: str) -&gt; Workspace | None:\n    \"\"\"Return the workspace, or ``None`` if no workspace with that ID exists in the project.\"\"\"\n    item = await self._get(workspace_pk(user_id, project_id), workspace_sk(workspace_id))\n    return Workspace.model_validate(item) if item else None\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository.create","title":"create  <code>async</code>","text":"<pre><code>create(\n    user_id: str, project_id: str, data: WorkspaceCreate\n) -&gt; Workspace | None\n</code></pre> <p>Create a new workspace at the end of the project's sort order.</p> <p>Returns <code>None</code> if the project has already reached <code>MAX_WORKSPACES_PER_PROJECT</code>.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/workspaces.py</code> <pre><code>async def create(self, user_id: str, project_id: str, data: WorkspaceCreate) -&gt; Workspace | None:\n    \"\"\"Create a new workspace at the end of the project's sort order.\n\n    Returns ``None`` if the project has already reached [`MAX_WORKSPACES_PER_PROJECT`][MAX_WORKSPACES_PER_PROJECT].\n    \"\"\"\n    items = await self._list_items(user_id, project_id)\n    if len(items) &gt;= MAX_WORKSPACES_PER_PROJECT:\n        return None\n\n    max_order = max((int(i.get(\"sortOrder\", 0)) for i in items), default=0)\n\n    now = datetime.now(UTC)\n    wid = generate_id()\n    item: DynamoItem = {\n        \"PK\": workspace_pk(user_id, project_id),\n        \"SK\": workspace_sk(wid),\n        \"id\": wid,\n        \"userId\": user_id,\n        \"projectId\": project_id,\n        \"name\": data.name,\n        \"description\": data.description,\n        \"locked\": False,\n        \"sortOrder\": max_order + 1,\n        \"settings\": {},\n        \"createdAt\": now.isoformat(),\n        \"updatedAt\": now.isoformat(),\n    }\n    await self._put(item)\n    return Workspace.model_validate(item)\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository.update","title":"update  <code>async</code>","text":"<pre><code>update(\n    user_id: str,\n    project_id: str,\n    workspace_id: str,\n    data: WorkspaceUpdate,\n) -&gt; Workspace | None\n</code></pre> <p>Apply a partial update and return the updated workspace, or <code>None</code> if it doesn't exist.</p> <p>An update with no fields set returns the workspace unchanged.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/workspaces.py</code> <pre><code>async def update(self, user_id: str, project_id: str, workspace_id: str, data: WorkspaceUpdate) -&gt; Workspace | None:\n    \"\"\"Apply a partial update and return the updated workspace, or ``None`` if it doesn't exist.\n\n    An update with no fields set returns the workspace unchanged.\n    \"\"\"\n    values: DynamoItem = {}\n    if data.name is not None:\n        values[\"name\"] = data.name\n    if data.description is not None:\n        values[\"description\"] = data.description\n    if data.locked is not None:\n        values[\"locked\"] = data.locked\n    if data.settings is not None:\n        values[\"settings\"] = data.settings.model_dump()\n    if data.content is not None:\n        values[\"content\"] = data.content\n\n    if not values:\n        return await self.get(user_id, project_id, workspace_id)\n\n    attrs = await self._update(workspace_pk(user_id, project_id), workspace_sk(workspace_id), values)\n    return Workspace.model_validate(attrs) if attrs else None\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; bool\n</code></pre> <p>Delete the workspace. Returns <code>False</code> if it doesn't exist.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/workspaces.py</code> <pre><code>async def delete(self, user_id: str, project_id: str, workspace_id: str) -&gt; bool:\n    \"\"\"Delete the workspace. Returns ``False`` if it doesn't exist.\"\"\"\n    return await self._delete(workspace_pk(user_id, project_id), workspace_sk(workspace_id))\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository.duplicate","title":"duplicate  <code>async</code>","text":"<pre><code>duplicate(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; Workspace | None\n</code></pre> <p>Copy the workspace and append <code>(copy)</code> to its name.</p> <p>Returns <code>None</code> if the source workspace doesn't exist, or if the project has already reached <code>MAX_WORKSPACES_PER_PROJECT</code>.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/workspaces.py</code> <pre><code>async def duplicate(self, user_id: str, project_id: str, workspace_id: str) -&gt; Workspace | None:\n    \"\"\"Copy the workspace and append ``(copy)`` to its name.\n\n    Returns ``None`` if the source workspace doesn't exist, or if the project has already reached\n    [`MAX_WORKSPACES_PER_PROJECT`][MAX_WORKSPACES_PER_PROJECT].\n    \"\"\"\n    source = await self.get(user_id, project_id, workspace_id)\n    if source is None:\n        return None\n\n    items = await self._list_items(user_id, project_id)\n    if len(items) &gt;= MAX_WORKSPACES_PER_PROJECT:\n        return None\n\n    max_order = max((int(i.get(\"sortOrder\", 0)) for i in items), default=0)\n\n    now = datetime.now(UTC)\n    new_id = generate_id()\n    item: DynamoItem = {\n        \"PK\": workspace_pk(user_id, project_id),\n        \"SK\": workspace_sk(new_id),\n        \"id\": new_id,\n        \"userId\": user_id,\n        \"projectId\": project_id,\n        \"name\": f\"{source.name[:248]} (copy)\",\n        \"description\": source.description,\n        \"locked\": False,\n        \"sortOrder\": max_order + 1,\n        \"settings\": source.settings.model_dump(),\n        \"createdAt\": now.isoformat(),\n        \"updatedAt\": now.isoformat(),\n    }\n    if source.content:\n        item[\"content\"] = source.content\n    await self._put(item)\n    return Workspace.model_validate(item)\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository.reorder","title":"reorder  <code>async</code>","text":"<pre><code>reorder(\n    user_id: str, project_id: str, ordered_ids: list[str]\n) -&gt; None\n</code></pre> <p>Reassign <code>sort_order</code> for the listed workspaces in the given sequence.</p> <p>Each ID in <code>ordered_ids</code> is numbered <code>1..N</code>. IDs not in the list keep their current <code>sort_order</code>.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/workspaces.py</code> <pre><code>async def reorder(self, user_id: str, project_id: str, ordered_ids: list[str]) -&gt; None:\n    \"\"\"Reassign ``sort_order`` for the listed workspaces in the given sequence.\n\n    Each ID in ``ordered_ids`` is numbered ``1..N``. IDs not in the list keep\n    their current ``sort_order``.\n    \"\"\"\n    pk = workspace_pk(user_id, project_id)\n\n    def _reorder() -&gt; None:\n        now = datetime.now(UTC)\n        for index, ws_id in enumerate(ordered_ids, start=1):\n            self._table.update_item(\n                Key={\"PK\": pk, \"SK\": workspace_sk(ws_id)},\n                UpdateExpression=\"SET #sortOrder = :sortOrder, #updatedAt = :updatedAt\",\n                ExpressionAttributeNames={\n                    \"#sortOrder\": \"sortOrder\",\n                    \"#updatedAt\": \"updatedAt\",\n                },\n                ExpressionAttributeValues={\n                    \":sortOrder\": index,\n                    \":updatedAt\": now.isoformat(),\n                },\n                ConditionExpression=\"attribute_exists(PK)\",\n            )\n\n    await asyncio.to_thread(_reorder)\n</code></pre>"},{"location":"reference/api/repositories/dynamodb/workspaces/#openfactcheck.api.repositories.dynamodb.workspaces.DynamoWorkspaceRepository.set_run","title":"set_run  <code>async</code>","text":"<pre><code>set_run(\n    user_id: str,\n    project_id: str,\n    workspace_id: str,\n    run: WorkspaceRun,\n) -&gt; None\n</code></pre> <p>Replace the workspace's latest run state with the given run.</p> Source code in <code>src/openfactcheck/api/repositories/dynamodb/workspaces.py</code> <pre><code>async def set_run(self, user_id: str, project_id: str, workspace_id: str, run: WorkspaceRun) -&gt; None:\n    \"\"\"Replace the workspace's latest run state with the given run.\"\"\"\n    await self._update(\n        workspace_pk(user_id, project_id),\n        workspace_sk(workspace_id),\n        {\"run\": run.model_dump(mode=\"json\")},\n    )\n</code></pre>"},{"location":"reference/api/repositories/sqlite/","title":"SQLite","text":"<p>Concrete SQLite-backed implementations of the API repositories. Each repository is constructed from an async session factory and exposes the same async CRUD surface as the DynamoDB backend.</p> <ul> <li>Projects \u2014 <code>SqliteProjectRepository</code>.</li> <li>Workspaces \u2014 <code>SqliteWorkspaceRepository</code>.</li> </ul>"},{"location":"reference/api/repositories/sqlite/projects/","title":"<code>SqliteProjectRepository</code> class","text":"<p>SQLite-backed project repository.</p>"},{"location":"reference/api/repositories/sqlite/projects/#openfactcheck.api.repositories.sqlite.projects.SqliteProjectRepository","title":"SqliteProjectRepository","text":"<pre><code>SqliteProjectRepository(\n    session_factory: async_sessionmaker[AsyncSession],\n)\n</code></pre> <p>SQLite-backed repository for project CRUD.</p> <p>Build a repository that opens sessions from the given factory.</p> PARAMETER DESCRIPTION <code>session_factory</code> <p>Async session factory bound to a SQLite engine.</p> <p> TYPE: <code>async_sessionmaker[AsyncSession]</code> </p> Source code in <code>src/openfactcheck/api/repositories/sqlite/projects.py</code> <pre><code>def __init__(self, session_factory: async_sessionmaker[AsyncSession]) -&gt; None:\n    \"\"\"Build a repository that opens sessions from the given factory.\n\n    Args:\n        session_factory: Async session factory bound to a SQLite engine.\n    \"\"\"\n    self._session_factory = session_factory\n</code></pre>"},{"location":"reference/api/repositories/sqlite/projects/#openfactcheck.api.repositories.sqlite.projects.SqliteProjectRepository.list_by_user","title":"list_by_user  <code>async</code>","text":"<pre><code>list_by_user(\n    user_id: str, limit: int = 50, offset: int = 0\n) -&gt; list[Project]\n</code></pre> <p>List the user's projects, ordered by creation time (oldest first).</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/projects.py</code> <pre><code>async def list_by_user(self, user_id: str, limit: int = 50, offset: int = 0) -&gt; list[Project]:\n    \"\"\"List the user's projects, ordered by creation time (oldest first).\"\"\"\n    async with self._session_factory() as session:\n        result = await session.execute(\n            select(ProjectRow)\n            .where(ProjectRow.user_id == user_id)\n            .order_by(ProjectRow.created_at)\n            .limit(limit)\n            .offset(offset),\n        )\n        return [Project.model_validate(row_to_dict(row)) for row in result.scalars()]\n</code></pre>"},{"location":"reference/api/repositories/sqlite/projects/#openfactcheck.api.repositories.sqlite.projects.SqliteProjectRepository.get","title":"get  <code>async</code>","text":"<pre><code>get(user_id: str, project_id: str) -&gt; Project | None\n</code></pre> <p>Return the project, or <code>None</code> if no project with that ID exists for the user.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/projects.py</code> <pre><code>async def get(self, user_id: str, project_id: str) -&gt; Project | None:\n    \"\"\"Return the project, or ``None`` if no project with that ID exists for the user.\"\"\"\n    async with self._session_factory() as session:\n        result = await session.execute(\n            select(ProjectRow).where(ProjectRow.id == project_id, ProjectRow.user_id == user_id),\n        )\n        row = result.scalar_one_or_none()\n        return Project.model_validate(row_to_dict(row)) if row else None\n</code></pre>"},{"location":"reference/api/repositories/sqlite/projects/#openfactcheck.api.repositories.sqlite.projects.SqliteProjectRepository.create","title":"create  <code>async</code>","text":"<pre><code>create(user_id: str, data: ProjectCreate) -&gt; Project | None\n</code></pre> <p>Create a new project for the user.</p> <p>Returns <code>None</code> if the user has already reached <code>MAX_PROJECTS_PER_USER</code>.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/projects.py</code> <pre><code>async def create(self, user_id: str, data: ProjectCreate) -&gt; Project | None:\n    \"\"\"Create a new project for the user.\n\n    Returns ``None`` if the user has already reached [`MAX_PROJECTS_PER_USER`][MAX_PROJECTS_PER_USER].\n    \"\"\"\n    async with self._session_factory() as session:\n        count_result = await session.execute(\n            select(func.count()).select_from(ProjectRow).where(ProjectRow.user_id == user_id),\n        )\n        if count_result.scalar_one() &gt;= MAX_PROJECTS_PER_USER:\n            return None\n\n        now = datetime.now(UTC)\n        row = ProjectRow(\n            id=generate_id(),\n            user_id=user_id,\n            name=data.name,\n            description=data.description,\n            created_at=now,\n            updated_at=now,\n        )\n        session.add(row)\n        await session.commit()\n        return Project.model_validate(row_to_dict(row))\n</code></pre>"},{"location":"reference/api/repositories/sqlite/projects/#openfactcheck.api.repositories.sqlite.projects.SqliteProjectRepository.update","title":"update  <code>async</code>","text":"<pre><code>update(\n    user_id: str, project_id: str, data: ProjectUpdate\n) -&gt; Project | None\n</code></pre> <p>Apply a partial update and return the updated project, or <code>None</code> if it doesn't exist.</p> <p>An update with no fields set returns the project unchanged.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/projects.py</code> <pre><code>async def update(self, user_id: str, project_id: str, data: ProjectUpdate) -&gt; Project | None:\n    \"\"\"Apply a partial update and return the updated project, or ``None`` if it doesn't exist.\n\n    An update with no fields set returns the project unchanged.\n    \"\"\"\n    values = data.model_dump(exclude_none=True)\n    if not values:\n        return await self.get(user_id, project_id)\n\n    values[\"updated_at\"] = datetime.now(UTC)\n\n    async with self._session_factory() as session:\n        cursor = await session.execute(\n            update(ProjectRow).where(ProjectRow.id == project_id, ProjectRow.user_id == user_id).values(**values),\n        )\n        await session.commit()\n        if cursor.rowcount == 0:\n            return None\n\n    return await self.get(user_id, project_id)\n</code></pre>"},{"location":"reference/api/repositories/sqlite/projects/#openfactcheck.api.repositories.sqlite.projects.SqliteProjectRepository.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(user_id: str, project_id: str) -&gt; bool\n</code></pre> <p>Delete the project and cascade-delete its workspaces and runs.</p> <p>Returns <code>False</code> if the project doesn't exist.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/projects.py</code> <pre><code>async def delete(self, user_id: str, project_id: str) -&gt; bool:\n    \"\"\"Delete the project and cascade-delete its workspaces and runs.\n\n    Returns ``False`` if the project doesn't exist.\n    \"\"\"\n    async with self._session_factory() as session:\n        cursor = await session.execute(\n            delete(ProjectRow).where(ProjectRow.id == project_id, ProjectRow.user_id == user_id),\n        )\n        await session.commit()\n        return bool(cursor.rowcount)\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/","title":"<code>SqliteWorkspaceRepository</code> class","text":"<p>SQLite-backed workspace repository.</p>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository","title":"SqliteWorkspaceRepository","text":"<pre><code>SqliteWorkspaceRepository(\n    session_factory: async_sessionmaker[AsyncSession],\n)\n</code></pre> <p>SQLite-backed repository for workspace CRUD, duplication, and reordering.</p> <p>Build a repository that opens sessions from the given factory.</p> PARAMETER DESCRIPTION <code>session_factory</code> <p>Async session factory bound to a SQLite engine.</p> <p> TYPE: <code>async_sessionmaker[AsyncSession]</code> </p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>def __init__(self, session_factory: async_sessionmaker[AsyncSession]) -&gt; None:\n    \"\"\"Build a repository that opens sessions from the given factory.\n\n    Args:\n        session_factory: Async session factory bound to a SQLite engine.\n    \"\"\"\n    self._session_factory = session_factory\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository.list_by_project","title":"list_by_project  <code>async</code>","text":"<pre><code>list_by_project(\n    user_id: str, project_id: str\n) -&gt; list[Workspace]\n</code></pre> <p>List the project's workspaces, ordered by <code>sort_order</code> (display order).</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>async def list_by_project(self, user_id: str, project_id: str) -&gt; list[Workspace]:\n    \"\"\"List the project's workspaces, ordered by ``sort_order`` (display order).\"\"\"\n    async with self._session_factory() as session:\n        result = await session.execute(\n            select(WorkspaceRow)\n            .where(\n                WorkspaceRow.user_id == user_id,\n                WorkspaceRow.project_id == project_id,\n            )\n            .order_by(WorkspaceRow.sort_order),\n        )\n        return [Workspace.model_validate(row_to_dict(row)) for row in result.scalars()]\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository.get","title":"get  <code>async</code>","text":"<pre><code>get(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; Workspace | None\n</code></pre> <p>Return the workspace, or <code>None</code> if no workspace with that ID exists in the project.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>async def get(self, user_id: str, project_id: str, workspace_id: str) -&gt; Workspace | None:\n    \"\"\"Return the workspace, or ``None`` if no workspace with that ID exists in the project.\"\"\"\n    async with self._session_factory() as session:\n        result = await session.execute(\n            select(WorkspaceRow).where(\n                WorkspaceRow.id == workspace_id,\n                WorkspaceRow.user_id == user_id,\n                WorkspaceRow.project_id == project_id,\n            ),\n        )\n        row = result.scalar_one_or_none()\n        return Workspace.model_validate(row_to_dict(row)) if row else None\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository.create","title":"create  <code>async</code>","text":"<pre><code>create(\n    user_id: str, project_id: str, data: WorkspaceCreate\n) -&gt; Workspace | None\n</code></pre> <p>Create a new workspace at the end of the project's sort order.</p> <p>Returns <code>None</code> if the project has already reached <code>MAX_WORKSPACES_PER_PROJECT</code>.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>async def create(self, user_id: str, project_id: str, data: WorkspaceCreate) -&gt; Workspace | None:\n    \"\"\"Create a new workspace at the end of the project's sort order.\n\n    Returns ``None`` if the project has already reached\n    [`MAX_WORKSPACES_PER_PROJECT`][MAX_WORKSPACES_PER_PROJECT].\n    \"\"\"\n    async with self._session_factory() as session:\n        if await self._count(session, user_id, project_id) &gt;= MAX_WORKSPACES_PER_PROJECT:\n            return None\n\n        now = datetime.now(UTC)\n        row = WorkspaceRow(\n            id=generate_id(),\n            user_id=user_id,\n            project_id=project_id,\n            name=data.name,\n            description=data.description,\n            locked=False,\n            sort_order=await self._next_sort_order(session, user_id, project_id),\n            settings_json=\"{}\",\n            created_at=now,\n            updated_at=now,\n        )\n        session.add(row)\n        await session.commit()\n        return Workspace.model_validate(row_to_dict(row))\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository.update","title":"update  <code>async</code>","text":"<pre><code>update(\n    user_id: str,\n    project_id: str,\n    workspace_id: str,\n    data: WorkspaceUpdate,\n) -&gt; Workspace | None\n</code></pre> <p>Apply a partial update and return the updated workspace, or <code>None</code> if it doesn't exist.</p> <p>An update with no fields set returns the workspace unchanged.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>async def update(self, user_id: str, project_id: str, workspace_id: str, data: WorkspaceUpdate) -&gt; Workspace | None:\n    \"\"\"Apply a partial update and return the updated workspace, or ``None`` if it doesn't exist.\n\n    An update with no fields set returns the workspace unchanged.\n    \"\"\"\n    values: dict[str, object] = {}\n    if data.name is not None:\n        values[\"name\"] = data.name\n    if data.description is not None:\n        values[\"description\"] = data.description\n    if data.locked is not None:\n        values[\"locked\"] = data.locked\n    if data.settings is not None:\n        values[\"settings_json\"] = data.settings.model_dump_json()\n    if data.content is not None:\n        values[\"content_json\"] = json.dumps(data.content)\n\n    if not values:\n        return await self.get(user_id, project_id, workspace_id)\n\n    values[\"updated_at\"] = datetime.now(UTC)\n\n    async with self._session_factory() as session:\n        cursor = await session.execute(\n            update(WorkspaceRow)\n            .where(\n                WorkspaceRow.id == workspace_id,\n                WorkspaceRow.user_id == user_id,\n                WorkspaceRow.project_id == project_id,\n            )\n            .values(**values),\n        )\n        await session.commit()\n        if cursor.rowcount == 0:\n            return None\n\n    return await self.get(user_id, project_id, workspace_id)\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; bool\n</code></pre> <p>Delete the workspace. Returns <code>False</code> if it doesn't exist.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>async def delete(self, user_id: str, project_id: str, workspace_id: str) -&gt; bool:\n    \"\"\"Delete the workspace. Returns ``False`` if it doesn't exist.\"\"\"\n    async with self._session_factory() as session:\n        cursor = await session.execute(\n            delete(WorkspaceRow).where(\n                WorkspaceRow.id == workspace_id,\n                WorkspaceRow.user_id == user_id,\n                WorkspaceRow.project_id == project_id,\n            ),\n        )\n        await session.commit()\n        return bool(cursor.rowcount)\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository.duplicate","title":"duplicate  <code>async</code>","text":"<pre><code>duplicate(\n    user_id: str, project_id: str, workspace_id: str\n) -&gt; Workspace | None\n</code></pre> <p>Copy the workspace and append <code>(copy)</code> to its name.</p> <p>Returns <code>None</code> if the source workspace doesn't exist, or if the project has already reached <code>MAX_WORKSPACES_PER_PROJECT</code>.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>async def duplicate(self, user_id: str, project_id: str, workspace_id: str) -&gt; Workspace | None:\n    \"\"\"Copy the workspace and append ``(copy)`` to its name.\n\n    Returns ``None`` if the source workspace doesn't exist, or if the project has already reached\n    [`MAX_WORKSPACES_PER_PROJECT`][MAX_WORKSPACES_PER_PROJECT].\n    \"\"\"\n    source = await self.get(user_id, project_id, workspace_id)\n    if source is None:\n        return None\n\n    async with self._session_factory() as session:\n        if await self._count(session, user_id, project_id) &gt;= MAX_WORKSPACES_PER_PROJECT:\n            return None\n\n        now = datetime.now(UTC)\n        row = WorkspaceRow(\n            id=generate_id(),\n            user_id=user_id,\n            project_id=project_id,\n            name=f\"{source.name[:248]} (copy)\",\n            description=source.description,\n            locked=False,\n            sort_order=await self._next_sort_order(session, user_id, project_id),\n            settings_json=json.dumps(source.settings.model_dump()),\n            content_json=json.dumps(source.content) if source.content else \"{}\",\n            created_at=now,\n            updated_at=now,\n        )\n        session.add(row)\n        await session.commit()\n        return Workspace.model_validate(row_to_dict(row))\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository.reorder","title":"reorder  <code>async</code>","text":"<pre><code>reorder(\n    user_id: str, project_id: str, ordered_ids: list[str]\n) -&gt; None\n</code></pre> <p>Reassign <code>sort_order</code> for the listed workspaces in the given sequence.</p> <p>Each ID in <code>ordered_ids</code> is numbered <code>1..N</code>. IDs not in the list keep their current <code>sort_order</code>.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>async def reorder(self, user_id: str, project_id: str, ordered_ids: list[str]) -&gt; None:\n    \"\"\"Reassign ``sort_order`` for the listed workspaces in the given sequence.\n\n    Each ID in ``ordered_ids`` is numbered ``1..N``. IDs not in the list keep\n    their current ``sort_order``.\n    \"\"\"\n    async with self._session_factory() as session:\n        for index, ws_id in enumerate(ordered_ids, start=1):\n            await session.execute(\n                update(WorkspaceRow)\n                .where(\n                    WorkspaceRow.id == ws_id,\n                    WorkspaceRow.user_id == user_id,\n                    WorkspaceRow.project_id == project_id,\n                )\n                .values(sort_order=index, updated_at=datetime.now(UTC)),\n            )\n        await session.commit()\n</code></pre>"},{"location":"reference/api/repositories/sqlite/workspaces/#openfactcheck.api.repositories.sqlite.workspaces.SqliteWorkspaceRepository.set_run","title":"set_run  <code>async</code>","text":"<pre><code>set_run(\n    user_id: str,\n    project_id: str,\n    workspace_id: str,\n    run: WorkspaceRun,\n) -&gt; None\n</code></pre> <p>Replace the workspace's latest run state with the given run.</p> Source code in <code>src/openfactcheck/api/repositories/sqlite/workspaces.py</code> <pre><code>async def set_run(self, user_id: str, project_id: str, workspace_id: str, run: WorkspaceRun) -&gt; None:\n    \"\"\"Replace the workspace's latest run state with the given run.\"\"\"\n    async with self._session_factory() as session:\n        await session.execute(\n            update(WorkspaceRow)\n            .where(\n                WorkspaceRow.id == workspace_id,\n                WorkspaceRow.user_id == user_id,\n                WorkspaceRow.project_id == project_id,\n            )\n            .values(run_json=run.model_dump_json(), updated_at=datetime.now(UTC)),\n        )\n        await session.commit()\n</code></pre>"},{"location":"reference/chat/","title":"Chat","text":"<p>Public API for the chat layer.</p> <p>Build a <code>ChatClient</code> with a provider-specific configuration (for example <code>OpenAIConfig</code>), send it a list of <code>Message</code> values, and receive a <code>ChatResponse</code> or a stream of <code>StreamEvent</code> values. Failures raise <code>ChatModelError</code> subclasses.</p> <p>Import the chat API from <code>openfactcheck.chat</code> directly; submodule paths are not part of the public API. Message types live in the <code>openfactcheck.messages</code> package.</p> Example <pre><code>from openfactcheck.chat import ChatClient, OpenAIConfig\nfrom openfactcheck.messages import SystemMessage, UserMessage\n\nclient = ChatClient(config=OpenAIConfig(model=\"gpt-4o\"))\nresponse = client.completion(\n    [\n        SystemMessage(content=\"You are a fact-checker.\"),\n        UserMessage(content=\"Is the sky blue?\"),\n    ]\n)\nprint(response.message.content)\n</code></pre>"},{"location":"reference/chat/#pages","title":"Pages","text":"<ul> <li><code>ChatClient</code> class \u2014 the facade callers use to send messages and receive completions or streams.</li> <li>Requests \u2014 the bundled payload a backend receives (messages + config + runtime).</li> <li>Responses \u2014 complete responses, streaming events, and usage data.</li> <li>Config \u2014 provider-specific configuration and runtime settings.</li> <li>Errors \u2014 exception hierarchy raised by the chat client and backends.</li> <li>Providers \u2014 provider-level capability declaration and configuration validation.</li> <li>Backends \u2014 the replaceable execution boundary that talks to the underlying SDK.</li> </ul>"},{"location":"reference/chat/client/","title":"<code>ChatClient</code> class","text":"<p>Stateful facade for the chat layer.</p> <p><code>ChatClient</code> is the one type most callers touch. Construct it once with a <code>ModelConfig</code>, then use its four methods to send a list of <code>Message</code> values and receive either a single <code>ChatResponse</code> or a stream of <code>StreamEvent</code> values.</p> <p>Every method comes in sync and async variants:</p> <ul> <li><code>ChatClient.completion</code> and   <code>ChatClient.acompletion</code> for single-shot calls.</li> <li><code>ChatClient.stream</code> and   <code>ChatClient.astream</code> for streaming.</li> </ul> <p>All four raise <code>ChatModelError</code> subclasses on failure.</p> Example <pre><code>from openfactcheck.chat import ChatClient, OpenAIConfig\nfrom openfactcheck.messages import UserMessage\n\nclient = ChatClient(config=OpenAIConfig(model=\"gpt-4o\"))\nresponse = client.completion([UserMessage(content=\"Hello\")])\nprint(response.message.content)\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient","title":"ChatClient","text":"<pre><code>ChatClient(\n    config: ModelConfig,\n    runtime: RuntimeConfig | None = None,\n    backend: ChatBackend | None = None,\n)\n</code></pre> <p>Configured chat client. Create once, call many times.</p> <p>Offers sync and async variants of both single-shot (<code>completion</code> / <code>acompletion</code>) and streaming (<code>stream</code> / <code>astream</code>) calls.</p> <p>All four methods raise <code>ChatModelError</code> subclasses on failure, most commonly <code>AuthenticationError</code>, <code>RateLimitError</code>, <code>ProviderError</code>, or <code>UnsupportedFeatureError</code>.</p> <p>The client keeps one pooled connection open across calls. Close it with <code>close</code> (or a <code>with</code> block) for sync use, and <code>aclose</code> (or an <code>async with</code> block) for async use, to release the pool when done.</p> Note <p>Use one client within a single event loop. Sharing a client across separate <code>asyncio.run</code> calls reuses a pool bound to a loop that has since closed; run the work under one loop instead.</p> <p>Build and validate a chat client.</p> PARAMETER DESCRIPTION <code>config</code> <p>Model-specific configuration. Validated eagerly against the matching <code>BaseProvider</code>, so configuration errors surface here instead of on the first call.</p> <p> TYPE: <code>ModelConfig</code> </p> <code>runtime</code> <p>Execution-level settings such as timeout and retries. Defaults to an empty <code>RuntimeConfig</code>.</p> <p> TYPE: <code>RuntimeConfig | None</code> DEFAULT: <code>None</code> </p> <code>backend</code> <p>Backend implementation. Defaults to the direct-SDK backend for the configured provider (for example <code>OpenAIBackend</code>). Pass an explicit backend only to override that default.</p> <p> TYPE: <code>ChatBackend | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>def __init__(\n    self,\n    config: ModelConfig,\n    runtime: RuntimeConfig | None = None,\n    backend: ChatBackend | None = None,\n) -&gt; None:\n    \"\"\"Build and validate a chat client.\n\n    Args:\n        config: Model-specific configuration. Validated eagerly against\n            the matching [`BaseProvider`][BaseProvider], so configuration\n            errors surface here instead of on the first call.\n        runtime: Execution-level settings such as timeout and retries.\n            Defaults to an empty [`RuntimeConfig`][RuntimeConfig].\n        backend: Backend implementation. Defaults to the direct-SDK\n            backend for the configured provider (for example\n            [`OpenAIBackend`][OpenAIBackend]). Pass an explicit backend\n            only to override that default.\n\n    Raises:\n        ProviderNotFoundError: Provider name is not registered or its\n            SDK is not installed.\n        ProviderError: Configuration is rejected by the provider.\n    \"\"\"\n    self._config = config\n    self._runtime = runtime if runtime is not None else RuntimeConfig()\n    self._provider = get_provider(config.provider)\n    self._provider.validate_config(config)\n    self._backend: ChatBackend = backend if backend is not None else default_backend(config.provider)\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.completion","title":"completion","text":"<pre><code>completion(messages: list[Message]) -&gt; ChatResponse\n</code></pre> <p>Send messages and return a complete response.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>def completion(self, messages: list[Message]) -&gt; ChatResponse:\n    \"\"\"Send messages and return a complete response.\n\n    Args:\n        messages: The conversation to send.\n\n    Returns:\n        The model's reply along with token usage and finish reason.\n    \"\"\"\n    return self._backend.completion(self._build_request(messages))\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.acompletion","title":"acompletion  <code>async</code>","text":"<pre><code>acompletion(messages: list[Message]) -&gt; ChatResponse\n</code></pre> <p>Send messages and await a complete response.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>async def acompletion(self, messages: list[Message]) -&gt; ChatResponse:\n    \"\"\"Send messages and await a complete response.\n\n    Args:\n        messages: The conversation to send.\n\n    Returns:\n        The model's reply along with token usage and finish reason.\n    \"\"\"\n    return await self._backend.acompletion(self._build_request(messages))\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.stream","title":"stream","text":"<pre><code>stream(\n    messages: list[Message],\n    *,\n    response_model: type[BaseModel] | None = None,\n) -&gt; Iterator[StreamEvent]\n</code></pre> <p>Stream a response as typed events.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> <code>response_model</code> <p>When given, the reply is constrained to this model's JSON schema and the streamed text is that JSON, chunk by chunk. Omit it for free text. See <code>stream_as</code> to receive parsed objects instead of raw JSON.</p> <p> TYPE: <code>type[BaseModel] | None</code> DEFAULT: <code>None</code> </p> YIELDS DESCRIPTION <code>StreamEvent</code> <p>A <code>TextDelta</code> for each content chunk, then a final</p> <code>StreamEvent</code> <p><code>StreamEnd</code> carrying <code>finish_reason</code> and <code>usage</code>.</p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>def stream(\n    self, messages: list[Message], *, response_model: type[BaseModel] | None = None\n) -&gt; Iterator[StreamEvent]:\n    \"\"\"Stream a response as typed events.\n\n    Args:\n        messages: The conversation to send.\n        response_model: When given, the reply is constrained to this model's\n            JSON schema and the streamed text is that JSON, chunk by chunk.\n            Omit it for free text. See [`stream_as`][ChatClient.stream_as] to\n            receive parsed objects instead of raw JSON.\n\n    Yields:\n        A [`TextDelta`][TextDelta] for each content chunk, then a final\n        [`StreamEnd`][StreamEnd] carrying ``finish_reason`` and ``usage``.\n\n    Raises:\n        UnsupportedFeatureError: ``response_model`` is given but the provider\n            does not support structured output.\n    \"\"\"\n    yield from self._backend.stream(self._build_request(messages, self._format_for(response_model)))\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.astream","title":"astream  <code>async</code>","text":"<pre><code>astream(\n    messages: list[Message],\n    *,\n    response_model: type[BaseModel] | None = None,\n) -&gt; AsyncIterator[StreamEvent]\n</code></pre> <p>Stream a response as typed events over an async iterator.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> <code>response_model</code> <p>When given, the reply is constrained to this model's JSON schema and the streamed text is that JSON, chunk by chunk. Omit it for free text. See <code>astream_as</code> to receive parsed objects instead of raw JSON.</p> <p> TYPE: <code>type[BaseModel] | None</code> DEFAULT: <code>None</code> </p> YIELDS DESCRIPTION <code>AsyncIterator[StreamEvent]</code> <p>A <code>TextDelta</code> for each content chunk, then a final</p> <code>AsyncIterator[StreamEvent]</code> <p><code>StreamEnd</code> carrying <code>finish_reason</code> and <code>usage</code>.</p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>async def astream(\n    self, messages: list[Message], *, response_model: type[BaseModel] | None = None\n) -&gt; AsyncIterator[StreamEvent]:\n    \"\"\"Stream a response as typed events over an async iterator.\n\n    Args:\n        messages: The conversation to send.\n        response_model: When given, the reply is constrained to this model's\n            JSON schema and the streamed text is that JSON, chunk by chunk.\n            Omit it for free text. See [`astream_as`][ChatClient.astream_as] to\n            receive parsed objects instead of raw JSON.\n\n    Yields:\n        A [`TextDelta`][TextDelta] for each content chunk, then a final\n        [`StreamEnd`][StreamEnd] carrying ``finish_reason`` and ``usage``.\n\n    Raises:\n        UnsupportedFeatureError: ``response_model`` is given but the provider\n            does not support structured output.\n    \"\"\"\n    async for event in self._backend.astream(self._build_request(messages, self._format_for(response_model))):\n        yield event\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.stream_collect","title":"stream_collect","text":"<pre><code>stream_collect(\n    messages: list[Message],\n    *,\n    response_model: type[BaseModel] | None = None,\n    on_delta: Callable[[TextDelta], None] | None = None,\n) -&gt; str\n</code></pre> <p>Stream a response, observe each chunk, and return the assembled text.</p> <p>A convenience over <code>stream</code> for callers that want both the streaming side effect (a live display, a progress sink) and the final text in one call, instead of collecting chunks by hand.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> <code>response_model</code> <p>When given, constrains the reply to this model's JSON schema; the assembled text (and each chunk) is then that JSON.</p> <p> TYPE: <code>type[BaseModel] | None</code> DEFAULT: <code>None</code> </p> <code>on_delta</code> <p>Called with each <code>TextDelta</code> as it arrives, for example to forward tokens to a progress sink. The terminal <code>StreamEnd</code> is not passed.</p> <p> TYPE: <code>Callable[[TextDelta], None] | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>def stream_collect(\n    self,\n    messages: list[Message],\n    *,\n    response_model: type[BaseModel] | None = None,\n    on_delta: Callable[[TextDelta], None] | None = None,\n) -&gt; str:\n    \"\"\"Stream a response, observe each chunk, and return the assembled text.\n\n    A convenience over [`stream`][ChatClient.stream] for callers that want both\n    the streaming side effect (a live display, a progress sink) and the final\n    text in one call, instead of collecting chunks by hand.\n\n    Args:\n        messages: The conversation to send.\n        response_model: When given, constrains the reply to this model's JSON\n            schema; the assembled text (and each chunk) is then that JSON.\n        on_delta: Called with each [`TextDelta`][TextDelta] as it arrives, for\n            example to forward tokens to a progress sink. The terminal\n            [`StreamEnd`][StreamEnd] is not passed.\n\n    Returns:\n        The concatenated text of every chunk (the JSON when ``response_model``\n        is given).\n\n    Raises:\n        UnsupportedFeatureError: ``response_model`` is given but the provider\n            does not support structured output.\n    \"\"\"\n    parts: list[str] = []\n    for event in self.stream(messages, response_model=response_model):\n        if isinstance(event, TextDelta):\n            parts.append(event.content)\n            if on_delta is not None:\n                on_delta(event)\n    return \"\".join(parts)\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.astream_collect","title":"astream_collect  <code>async</code>","text":"<pre><code>astream_collect(\n    messages: list[Message],\n    *,\n    response_model: type[BaseModel] | None = None,\n    on_delta: Callable[[TextDelta], None] | None = None,\n) -&gt; str\n</code></pre> <p>Stream a response over an async iterator, observe each chunk, and return the assembled text.</p> <p>Async peer of <code>stream_collect</code>; same behavior.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> <code>response_model</code> <p>When given, constrains the reply to this model's JSON schema; the assembled text (and each chunk) is then that JSON.</p> <p> TYPE: <code>type[BaseModel] | None</code> DEFAULT: <code>None</code> </p> <code>on_delta</code> <p>Called with each <code>TextDelta</code> as it arrives, for example to forward tokens to a progress sink. The terminal <code>StreamEnd</code> is not passed.</p> <p> TYPE: <code>Callable[[TextDelta], None] | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>async def astream_collect(\n    self,\n    messages: list[Message],\n    *,\n    response_model: type[BaseModel] | None = None,\n    on_delta: Callable[[TextDelta], None] | None = None,\n) -&gt; str:\n    \"\"\"Stream a response over an async iterator, observe each chunk, and return the assembled text.\n\n    Async peer of [`stream_collect`][ChatClient.stream_collect]; same behavior.\n\n    Args:\n        messages: The conversation to send.\n        response_model: When given, constrains the reply to this model's JSON\n            schema; the assembled text (and each chunk) is then that JSON.\n        on_delta: Called with each [`TextDelta`][TextDelta] as it arrives, for\n            example to forward tokens to a progress sink. The terminal\n            [`StreamEnd`][StreamEnd] is not passed.\n\n    Returns:\n        The concatenated text of every chunk (the JSON when ``response_model``\n        is given).\n\n    Raises:\n        UnsupportedFeatureError: ``response_model`` is given but the provider\n            does not support structured output.\n    \"\"\"\n    parts: list[str] = []\n    async for event in self.astream(messages, response_model=response_model):\n        if isinstance(event, TextDelta):\n            parts.append(event.content)\n            if on_delta is not None:\n                on_delta(event)\n    return \"\".join(parts)\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.completion_as","title":"completion_as","text":"<pre><code>completion_as(\n    messages: list[Message], response_model: type[T]\n) -&gt; T\n</code></pre> <p>Send messages and return a validated instance of <code>response_model</code>.</p> <p>Asks the model to reply with JSON matching <code>response_model</code> and validates the reply into an instance. On a validation failure, the error is sent back to the model and the call retried up to <code>max_parse_retries</code> times before raising.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> <code>response_model</code> <p>The Pydantic model the reply must conform to.</p> <p> TYPE: <code>type[T]</code> </p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>def completion_as[T: BaseModel](self, messages: list[Message], response_model: type[T]) -&gt; T:\n    \"\"\"Send messages and return a validated instance of ``response_model``.\n\n    Asks the model to reply with JSON matching ``response_model`` and\n    validates the reply into an instance. On a validation failure, the\n    error is sent back to the model and the call retried up to\n    [`max_parse_retries`][RuntimeConfig.max_parse_retries] times before\n    raising.\n\n    Args:\n        messages: The conversation to send.\n        response_model: The Pydantic model the reply must conform to.\n\n    Returns:\n        A validated instance of ``response_model``.\n\n    Raises:\n        UnsupportedFeatureError: The provider does not support structured\n            output.\n        StructuredOutputError: The reply still failed validation after the\n            allowed retries.\n    \"\"\"\n    self._require_structured_output()\n    response_format = ResponseFormat(\n        name=response_model.__name__,\n        json_schema=response_model.model_json_schema(),\n    )\n    conversation = list(messages)\n    retries_left = self._runtime.max_parse_retries\n    while True:\n        response = self._backend.completion(self._build_request(conversation, response_format))\n        raw = response.message.content\n        try:\n            return response_model.model_validate_json(raw)\n        except ValidationError as exc:\n            if retries_left &lt;= 0:\n                raise StructuredOutputError(\n                    f\"reply did not match {response_model.__name__} after retries.\",\n                    raw=raw,\n                    validation_error=exc,\n                ) from exc\n            retries_left -= 1\n            reprompt = UserMessage(\n                content=(\n                    \"Your previous reply did not match the required schema. \"\n                    f\"Fix these errors and return only valid JSON:\\n{exc}\"\n                ),\n            )\n            conversation = [*conversation, response.message, reprompt]\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.acompletion_as","title":"acompletion_as  <code>async</code>","text":"<pre><code>acompletion_as(\n    messages: list[Message], response_model: type[T]\n) -&gt; T\n</code></pre> <p>Send messages and await a validated instance of <code>response_model</code>.</p> <p>Async peer of <code>completion_as</code>; same validation and retry behavior.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> <code>response_model</code> <p>The Pydantic model the reply must conform to.</p> <p> TYPE: <code>type[T]</code> </p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>async def acompletion_as[T: BaseModel](self, messages: list[Message], response_model: type[T]) -&gt; T:\n    \"\"\"Send messages and await a validated instance of ``response_model``.\n\n    Async peer of [`completion_as`][ChatClient.completion_as]; same\n    validation and retry behavior.\n\n    Args:\n        messages: The conversation to send.\n        response_model: The Pydantic model the reply must conform to.\n\n    Returns:\n        A validated instance of ``response_model``.\n\n    Raises:\n        UnsupportedFeatureError: The provider does not support structured\n            output.\n        StructuredOutputError: The reply still failed validation after the\n            allowed retries.\n    \"\"\"\n    self._require_structured_output()\n    response_format = ResponseFormat(\n        name=response_model.__name__,\n        json_schema=response_model.model_json_schema(),\n    )\n    conversation = list(messages)\n    retries_left = self._runtime.max_parse_retries\n    while True:\n        response = await self._backend.acompletion(self._build_request(conversation, response_format))\n        raw = response.message.content\n        try:\n            return response_model.model_validate_json(raw)\n        except ValidationError as exc:\n            if retries_left &lt;= 0:\n                raise StructuredOutputError(\n                    f\"reply did not match {response_model.__name__} after retries.\",\n                    raw=raw,\n                    validation_error=exc,\n                ) from exc\n            retries_left -= 1\n            reprompt = UserMessage(\n                content=(\n                    \"Your previous reply did not match the required schema. \"\n                    f\"Fix these errors and return only valid JSON:\\n{exc}\"\n                ),\n            )\n            conversation = [*conversation, response.message, reprompt]\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.stream_as","title":"stream_as","text":"<pre><code>stream_as(\n    messages: list[Message], response_model: type[T]\n) -&gt; Iterator[T]\n</code></pre> <p>Stream a structured reply as progressively complete instances.</p> <p>Asks the model to reply with JSON matching <code>response_model</code> and yields the object as it is built: each item carries the fields that have arrived so far, the rest left unset, and the final item is the complete, validated instance. Useful for showing a field (a verdict's reasoning, an answer) filling in live while still ending with a fully typed result.</p> <p>Unlike <code>completion_as</code>, a failed final validation is not retried; streaming cannot replay a partial reply.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> <code>response_model</code> <p>The Pydantic model the reply must conform to.</p> <p> TYPE: <code>type[T]</code> </p> YIELDS DESCRIPTION <code>T</code> <p>Progressively complete instances of <code>response_model</code>; the final one</p> <code>T</code> <p>is fully validated.</p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>def stream_as[T: BaseModel](self, messages: list[Message], response_model: type[T]) -&gt; Iterator[T]:\n    \"\"\"Stream a structured reply as progressively complete instances.\n\n    Asks the model to reply with JSON matching ``response_model`` and yields\n    the object as it is built: each item carries the fields that have arrived\n    so far, the rest left unset, and the final item is the complete, validated\n    instance. Useful for showing a field (a verdict's reasoning, an answer)\n    filling in live while still ending with a fully typed result.\n\n    Unlike [`completion_as`][ChatClient.completion_as], a failed final\n    validation is not retried; streaming cannot replay a partial reply.\n\n    Args:\n        messages: The conversation to send.\n        response_model: The Pydantic model the reply must conform to.\n\n    Yields:\n        Progressively complete instances of ``response_model``; the final one\n        is fully validated.\n\n    Raises:\n        UnsupportedFeatureError: The provider does not support structured\n            output.\n        StructuredOutputError: The complete reply failed validation.\n    \"\"\"\n    self._require_structured_output()\n    response_format = ResponseFormat(\n        name=response_model.__name__,\n        json_schema=response_model.model_json_schema(),\n    )\n    adapter = TypeAdapter(partial_model(response_model))\n    raw = \"\"\n    for event in self._backend.stream(self._build_request(messages, response_format)):\n        if not isinstance(event, TextDelta):\n            continue\n        raw += event.content\n        try:\n            partial = adapter.validate_json(raw, experimental_allow_partial=\"trailing-strings\")\n        except ValidationError:\n            continue\n        yield cast(\"T\", partial)\n    try:\n        yield response_model.model_validate_json(raw)\n    except ValidationError as exc:\n        raise StructuredOutputError(\n            f\"reply did not match {response_model.__name__}.\",\n            raw=raw,\n            validation_error=exc,\n        ) from exc\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.astream_as","title":"astream_as  <code>async</code>","text":"<pre><code>astream_as(\n    messages: list[Message], response_model: type[T]\n) -&gt; AsyncIterator[T]\n</code></pre> <p>Stream a structured reply over an async iterator as progressively complete instances.</p> <p>Async peer of <code>stream_as</code>; same progressive behavior and same no-retry contract.</p> PARAMETER DESCRIPTION <code>messages</code> <p>The conversation to send.</p> <p> TYPE: <code>list[Message]</code> </p> <code>response_model</code> <p>The Pydantic model the reply must conform to.</p> <p> TYPE: <code>type[T]</code> </p> YIELDS DESCRIPTION <code>AsyncIterator[T]</code> <p>Progressively complete instances of <code>response_model</code>; the final one</p> <code>AsyncIterator[T]</code> <p>is fully validated.</p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>async def astream_as[T: BaseModel](self, messages: list[Message], response_model: type[T]) -&gt; AsyncIterator[T]:\n    \"\"\"Stream a structured reply over an async iterator as progressively complete instances.\n\n    Async peer of [`stream_as`][ChatClient.stream_as]; same progressive\n    behavior and same no-retry contract.\n\n    Args:\n        messages: The conversation to send.\n        response_model: The Pydantic model the reply must conform to.\n\n    Yields:\n        Progressively complete instances of ``response_model``; the final one\n        is fully validated.\n\n    Raises:\n        UnsupportedFeatureError: The provider does not support structured\n            output.\n        StructuredOutputError: The complete reply failed validation.\n    \"\"\"\n    self._require_structured_output()\n    response_format = ResponseFormat(\n        name=response_model.__name__,\n        json_schema=response_model.model_json_schema(),\n    )\n    adapter = TypeAdapter(partial_model(response_model))\n    raw = \"\"\n    async for event in self._backend.astream(self._build_request(messages, response_format)):\n        if not isinstance(event, TextDelta):\n            continue\n        raw += event.content\n        try:\n            partial = adapter.validate_json(raw, experimental_allow_partial=\"trailing-strings\")\n        except ValidationError:\n            continue\n        yield cast(\"T\", partial)\n    try:\n        yield response_model.model_validate_json(raw)\n    except ValidationError as exc:\n        raise StructuredOutputError(\n            f\"reply did not match {response_model.__name__}.\",\n            raw=raw,\n            validation_error=exc,\n        ) from exc\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Release the pooled sync connection.</p> <p>Pairs with the sync call methods; safe to call more than once. Prefer a <code>with</code> block, which calls this on exit.</p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Release the pooled sync connection.\n\n    Pairs with the sync call methods; safe to call more than once. Prefer a\n    `with` block, which calls this on exit.\n    \"\"\"\n    self._backend.close()\n</code></pre>"},{"location":"reference/chat/client/#openfactcheck.chat.client.ChatClient.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Release the pooled async connection.</p> <p>Pairs with the async call methods; safe to call more than once. Prefer an <code>async with</code> block, which calls this on exit.</p> Source code in <code>src/openfactcheck/chat/client.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Release the pooled async connection.\n\n    Pairs with the async call methods; safe to call more than once. Prefer an\n    `async with` block, which calls this on exit.\n    \"\"\"\n    await self._backend.aclose()\n</code></pre>"},{"location":"reference/chat/config/","title":"Chat configuration","text":"<p>Model and runtime configuration for the chat layer.</p> <p>Build a provider-specific configuration (for example <code>OpenAIConfig</code>), optionally pair it with a <code>RuntimeConfig</code> for execution-level settings, and pass both to <code>ChatClient</code>.</p> <p><code>ModelConfig</code> is the union of every provider configuration; callers that must work with any provider accept a <code>ModelConfig</code> and branch on <code>config.provider</code>.</p> Example <pre><code>from openfactcheck.chat import ChatClient, OpenAIConfig\nfrom openfactcheck.messages import UserMessage\n\nclient = ChatClient(config=OpenAIConfig(model=\"gpt-4o\", temperature=0.2))\nresponse = client.completion([UserMessage(content=\"Hello\")])\n</code></pre>"},{"location":"reference/chat/config/#base","title":"Base","text":""},{"location":"reference/chat/config/#openfactcheck.chat.config.BaseModelConfig","title":"BaseModelConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Common parameters shared across every provider config.</p> <p>Not instantiated directly; use a provider-specific subclass.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.BaseModelConfig.model","title":"model  <code>instance-attribute</code>","text":"<pre><code>model: str\n</code></pre> <p>Identifier of the model to call, for example <code>\"gpt-4o\"</code>.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.BaseModelConfig.temperature","title":"temperature  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>temperature: float | None = None\n</code></pre> <p>How random the model's output is. Lower values are more focused, higher values more varied.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.BaseModelConfig.max_output_tokens","title":"max_output_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_output_tokens: int | None = Field(default=None, gt=0)\n</code></pre> <p>Cap on the number of tokens the model may generate in its response.</p> <p>Must be positive.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.BaseModelConfig.top_p","title":"top_p  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>top_p: float | None = Field(default=None, ge=0.0, le=1.0)\n</code></pre> <p>Nucleus sampling cutoff.</p> <p>Range <code>[0.0, 1.0]</code>.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenAICompatibleConfig","title":"OpenAICompatibleConfig","text":"<p>               Bases: <code>BaseModelConfig</code></p> <p>Sampling parameters shared by OpenAI-compatible provider configs.</p> <p>Not instantiated directly; a concrete subclass adds its own <code>provider</code> discriminator.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenAICompatibleConfig.temperature","title":"temperature  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>temperature: float | None = Field(\n    default=None, ge=0.0, le=2.0\n)\n</code></pre> <p>How random the model's output is. Lower is more focused, higher more varied.</p> <p>Range <code>[0.0, 2.0]</code>.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenAICompatibleConfig.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>seed: int | None = None\n</code></pre> <p>Request reproducible sampling. Determinism is best-effort, not guaranteed.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenAICompatibleConfig.frequency_penalty","title":"frequency_penalty  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frequency_penalty: float | None = Field(\n    default=None, ge=-2.0, le=2.0\n)\n</code></pre> <p>Penalize tokens by how often they have already appeared. Positive values reduce repetition.</p> <p>Range <code>[-2.0, 2.0]</code>.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenAICompatibleConfig.presence_penalty","title":"presence_penalty  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>presence_penalty: float | None = Field(\n    default=None, ge=-2.0, le=2.0\n)\n</code></pre> <p>Penalize tokens that have already appeared at all. Positive values encourage novelty.</p> <p>Range <code>[-2.0, 2.0]</code>.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenAICompatibleConfig.reasoning_effort","title":"reasoning_effort  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>reasoning_effort: (\n    Literal[\"low\", \"medium\", \"high\"] | None\n) = None\n</code></pre> <p>Reasoning budget hint. Ignored by non-reasoning models.</p>"},{"location":"reference/chat/config/#provider-configs","title":"Provider configs","text":""},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenAIConfig","title":"OpenAIConfig","text":"<p>               Bases: <code>OpenAICompatibleConfig</code></p> <p>Configuration for an OpenAI model.</p> Example <pre><code>config = OpenAIConfig(\n    model=\"gpt-4o\",\n    temperature=0.2,\n    max_output_tokens=500,\n)\n</code></pre>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenAIConfig.provider","title":"provider  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>provider: Literal['openai'] = 'openai'\n</code></pre> <p>Identifies this configuration as OpenAI's.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenRouterConfig","title":"OpenRouterConfig","text":"<p>               Bases: <code>OpenAICompatibleConfig</code></p> <p>Configuration for a model served through OpenRouter.</p> <p>OpenRouter exposes an OpenAI-compatible Chat Completions API. Address a model by its namespaced OpenRouter id, for example <code>\"openai/gpt-4o\"</code> or <code>\"anthropic/claude-sonnet-4\"</code>.</p> Example <pre><code>config = OpenRouterConfig(\n    model=\"anthropic/claude-sonnet-4\",\n    temperature=0.2,\n    max_output_tokens=500,\n)\n</code></pre>"},{"location":"reference/chat/config/#openfactcheck.chat.config.OpenRouterConfig.provider","title":"provider  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>provider: Literal['openrouter'] = 'openrouter'\n</code></pre> <p>Identifies this configuration as OpenRouter's.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.AnthropicConfig","title":"AnthropicConfig","text":"<p>               Bases: <code>BaseModelConfig</code></p> <p>Configuration for an Anthropic model.</p> Example <pre><code>config = AnthropicConfig(\n    model=\"claude-sonnet-4-6\",\n    temperature=0.4,\n    thinking=True,\n    thinking_budget_tokens=4000,\n)\n</code></pre>"},{"location":"reference/chat/config/#openfactcheck.chat.config.AnthropicConfig.provider","title":"provider  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>provider: Literal['anthropic'] = 'anthropic'\n</code></pre> <p>Identifies this configuration as Anthropic's.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.AnthropicConfig.temperature","title":"temperature  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>temperature: float | None = Field(\n    default=None, ge=0.0, le=1.0\n)\n</code></pre> <p>How random the model's output is. Lower is more focused, higher more varied.</p> <p>Range <code>[0.0, 1.0]</code>.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.AnthropicConfig.top_k","title":"top_k  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>top_k: int | None = Field(default=None, gt=0)\n</code></pre> <p>Sample from the top-k tokens at each step.</p> <p>Must be positive.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.AnthropicConfig.thinking","title":"thinking  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>thinking: bool = False\n</code></pre> <p>Enable extended thinking so the model can show its reasoning before answering.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.AnthropicConfig.thinking_budget_tokens","title":"thinking_budget_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>thinking_budget_tokens: int | None = Field(\n    default=None, gt=0\n)\n</code></pre> <p>How many tokens the model may spend on the reasoning trace.</p> <p>Only set when <code>thinking=True</code>. Must be positive.</p>"},{"location":"reference/chat/config/#union-type","title":"Union type","text":""},{"location":"reference/chat/config/#openfactcheck.chat.config.ModelConfig","title":"ModelConfig","text":"<pre><code>ModelConfig = Annotated[\n    OpenAIConfig | AnthropicConfig | OpenRouterConfig,\n    Discriminator(\"provider\"),\n]\n</code></pre> <p>Union of every provider configuration.</p> <p>Accept this type in code that must work with any provider; branch on <code>config.provider</code> to distinguish the concrete subclass.</p>"},{"location":"reference/chat/config/#runtime","title":"Runtime","text":""},{"location":"reference/chat/config/#openfactcheck.chat.config.RuntimeConfig","title":"RuntimeConfig","text":"<p>               Bases: <code>BaseModel</code></p> <p>Execution-level settings for a chat request.</p> Example <pre><code>runtime = RuntimeConfig(timeout=30.0, max_retries=5)\n</code></pre>"},{"location":"reference/chat/config/#openfactcheck.chat.config.RuntimeConfig.timeout","title":"timeout  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>timeout: float | None = Field(default=None, gt=0)\n</code></pre> <p>Per-request timeout in seconds.</p> <p>Must be positive. Unset uses the SDK default.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.RuntimeConfig.max_retries","title":"max_retries  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_retries: int = Field(default=2, ge=0)\n</code></pre> <p>Automatic retries on transient failures.</p> <p>Set to <code>0</code> to disable. Must be non-negative.</p>"},{"location":"reference/chat/config/#openfactcheck.chat.config.RuntimeConfig.max_parse_retries","title":"max_parse_retries  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_parse_retries: int = Field(default=0, ge=0)\n</code></pre> <p>Reprompts allowed when a structured-output reply fails validation.</p> <p>Applies only to <code>ChatClient.completion_as</code> and its async peer. <code>0</code> raises on the first validation failure; a positive value re-sends the validation error to the model up to that many times before raising. Distinct from <code>max_retries</code>, which retries transport failures. Must be non-negative.</p>"},{"location":"reference/chat/config/#provider-name","title":"Provider name","text":""},{"location":"reference/chat/config/#openfactcheck.chat.config.ProviderName","title":"ProviderName","text":"<pre><code>ProviderName = Literal['openai', 'anthropic', 'openrouter']\n</code></pre> <p>Identifier for a chat provider.</p>"},{"location":"reference/chat/errors/","title":"Errors","text":"<p>Error hierarchy for the chat layer.</p> <p>Every chat layer error derives from <code>ChatModelError</code>. Backend implementations convert SDK-specific exceptions into these types, so caller code handles a single error surface regardless of which provider is in use.</p> <p>Catch the specific subclass you can recover from, or catch <code>ChatModelError</code> to handle every chat-layer failure in one place.</p> Example <pre><code>from openfactcheck.chat import (\n    AuthenticationError,\n    ChatClient,\n    ChatModelError,\n    OpenAIConfig,\n    RateLimitError,\n)\nfrom openfactcheck.messages import UserMessage\n\nclient = ChatClient(config=OpenAIConfig(model=\"gpt-4o\"))\n\ntry:\n    response = client.completion([UserMessage(content=\"Hello\")])\nexcept AuthenticationError:\n    ...  # fix credentials\nexcept RateLimitError:\n    ...  # back off and retry\nexcept ChatModelError:\n    ...  # everything else\n</code></pre>"},{"location":"reference/chat/errors/#base","title":"Base","text":""},{"location":"reference/chat/errors/#openfactcheck.chat.errors.ChatModelError","title":"ChatModelError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for every chat layer error.</p> <p>Catch this to handle any failure from <code>ChatClient</code> without branching on the specific cause.</p>"},{"location":"reference/chat/errors/#specific-failures","title":"Specific failures","text":""},{"location":"reference/chat/errors/#openfactcheck.chat.errors.ProviderNotFoundError","title":"ProviderNotFoundError","text":"<p>               Bases: <code>ChatModelError</code></p> <p>The requested provider name is not registered or its SDK is not installed.</p> <p>Raised when <code>ChatClient</code> is constructed with a <code>ModelConfig</code> whose provider has no registered <code>BaseProvider</code>, or when a backend's provider SDK cannot be imported.</p>"},{"location":"reference/chat/errors/#openfactcheck.chat.errors.AuthenticationError","title":"AuthenticationError","text":"<p>               Bases: <code>ChatModelError</code></p> <p>Credentials are missing, invalid, or expired.</p> <p>Typically surfaces when the provider's API key environment variable is unset, or when the key has since been revoked. Consult the provider's documentation for the expected environment variable.</p>"},{"location":"reference/chat/errors/#openfactcheck.chat.errors.RateLimitError","title":"RateLimitError","text":"<p>               Bases: <code>ChatModelError</code></p> <p>The provider's rate limit was exceeded.</p> <p>Back off and retry. The provider often reports a retry-after window in the underlying response; consult the provider's documentation for the exact policy.</p>"},{"location":"reference/chat/errors/#openfactcheck.chat.errors.ProviderError","title":"ProviderError","text":"<p>               Bases: <code>ChatModelError</code></p> <p>Provider returned an error that doesn't map to a more specific type.</p> <p>A catch-all for unexpected status codes, malformed responses, and transport failures. Callers usually treat this as non-retriable without additional context.</p>"},{"location":"reference/chat/errors/#openfactcheck.chat.errors.UnsupportedFeatureError","title":"UnsupportedFeatureError","text":"<p>               Bases: <code>ChatModelError</code></p> <p>The provider does not support a requested feature.</p> <p>Raised when a <code>ModelConfig</code> or message payload requests behavior the provider can't satisfy, for example tool calls on a model that doesn't support them.</p>"},{"location":"reference/chat/errors/#openfactcheck.chat.errors.StructuredOutputError","title":"StructuredOutputError","text":"<pre><code>StructuredOutputError(\n    message: str,\n    *,\n    raw: str,\n    validation_error: ValidationError,\n)\n</code></pre> <p>               Bases: <code>ChatModelError</code></p> <p>A structured-output reply could not be validated against the requested model.</p> <p>Raised by <code>ChatClient.completion_as</code> and its async peer after the reply fails validation and any configured reprompts are exhausted. The raw reply text and the underlying validation error are attached for inspection.</p> <p>Record the raw reply text and the validation error that rejected it.</p> Source code in <code>src/openfactcheck/chat/errors.py</code> <pre><code>def __init__(self, message: str, *, raw: str, validation_error: ValidationError) -&gt; None:\n    \"\"\"Record the raw reply text and the validation error that rejected it.\"\"\"\n    self.raw = raw\n    self.validation_error = validation_error\n    super().__init__(message)\n</code></pre>"},{"location":"reference/chat/requests/","title":"Requests","text":"<p>Request types for the chat layer.</p> <p>A <code>ChatRequest</code> bundles the messages, model configuration, and runtime settings for a single chat call. End users rarely construct one directly; <code>ChatClient</code> builds a request per call. Request values surface when writing a custom <code>ChatBackend</code>.</p> Example <pre><code>from openfactcheck.chat import (\n    ChatRequest,\n    OpenAIConfig,\n    RuntimeConfig,\n)\nfrom openfactcheck.messages import UserMessage\n\nrequest = ChatRequest(\n    messages=[UserMessage(content=\"Hello\")],\n    config=OpenAIConfig(model=\"gpt-4o\"),\n    runtime=RuntimeConfig(timeout=30.0),\n)\n</code></pre>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ResponseFormat","title":"ResponseFormat","text":"<p>               Bases: <code>BaseModel</code></p> <p>Schema a structured-output reply must conform to.</p> <p>Built from a Pydantic model by <code>ChatClient.completion_as</code> and carried on the request so a backend can enforce it with the provider's native mechanism. The reply's content is then the JSON matching this schema.</p>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ResponseFormat.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>Schema name, used as the json_schema name or the forced tool name.</p>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ResponseFormat.json_schema","title":"json_schema  <code>instance-attribute</code>","text":"<pre><code>json_schema: dict[str, object]\n</code></pre> <p>The JSON Schema the reply must satisfy, from the model's <code>model_json_schema()</code>.</p>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ResponseFormat.strict","title":"strict  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>strict: bool = True\n</code></pre> <p>Whether to request strict schema enforcement where the provider supports it.</p>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ChatRequest","title":"ChatRequest","text":"<p>               Bases: <code>BaseModel</code></p> <p>A complete request for a single chat completion or stream.</p> Example <pre><code>request = ChatRequest(\n    messages=[UserMessage(content=\"Hello\")],\n    config=OpenAIConfig(model=\"gpt-4o\"),\n)\n</code></pre>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ChatRequest.messages","title":"messages  <code>instance-attribute</code>","text":"<pre><code>messages: list[Message]\n</code></pre> <p>Conversation history up to the point where the model should respond.</p>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ChatRequest.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config: ModelConfig\n</code></pre> <p>Model-specific settings: provider, model name, and sampling parameters.</p>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ChatRequest.runtime","title":"runtime  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>runtime: RuntimeConfig = Field(\n    default_factory=RuntimeConfig\n)\n</code></pre> <p>Execution-level settings such as timeout and retries.</p> <p>Defaults to an empty <code>RuntimeConfig</code>.</p>"},{"location":"reference/chat/requests/#openfactcheck.chat.requests.ChatRequest.response_format","title":"response_format  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>response_format: ResponseFormat | None = None\n</code></pre> <p>Optional structured-output schema.</p> <p>When set, the backend asks the model to return JSON matching the schema, and the reply's content is that JSON string. Unset for plain text replies.</p>"},{"location":"reference/chat/responses/","title":"Responses","text":"<p>Response types for the chat layer.</p> <p>Non-streaming calls (<code>ChatClient.completion</code> and <code>ChatClient.acompletion</code>) return a <code>ChatResponse</code>. Streaming calls (<code>ChatClient.stream</code> and <code>ChatClient.astream</code>) yield a sequence of <code>StreamEvent</code> values ending with a <code>StreamEnd</code> that carries the final <code>FinishReason</code> and token <code>Usage</code>.</p> Example <pre><code>from openfactcheck.chat import (\n    ChatClient,\n    OpenAIConfig,\n    StreamEnd,\n    TextDelta,\n)\nfrom openfactcheck.messages import UserMessage\n\nclient = ChatClient(config=OpenAIConfig(model=\"gpt-4o\"))\n\n# Non-streaming.\nresponse = client.completion([UserMessage(content=\"Hello\")])\nprint(response.message.content)\n\n# Streaming.\nfor event in client.stream([UserMessage(content=\"Tell me a joke\")]):\n    match event:\n        case TextDelta(content=chunk):\n            print(chunk, end=\"\")\n        case StreamEnd(finish_reason=reason):\n            print()\n            print(f\"done: {reason}\")\n</code></pre>"},{"location":"reference/chat/responses/#non-streaming","title":"Non-streaming","text":""},{"location":"reference/chat/responses/#openfactcheck.chat.responses.ChatResponse","title":"ChatResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>Complete response from a non-streaming model call.</p> Example <pre><code>response = client.completion([UserMessage(content=\"Hello\")])\nprint(response.message.content, response.usage.output_tokens)\n</code></pre>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.ChatResponse.message","title":"message  <code>instance-attribute</code>","text":"<pre><code>message: AssistantMessage\n</code></pre> <p>The model's reply.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.ChatResponse.model","title":"model  <code>instance-attribute</code>","text":"<pre><code>model: str\n</code></pre> <p>Identifier of the model that produced the response, e.g. <code>\"gpt-4o\"</code>.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.ChatResponse.provider","title":"provider  <code>instance-attribute</code>","text":"<pre><code>provider: ProviderName\n</code></pre> <p>Provider that served the response.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.ChatResponse.usage","title":"usage  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>usage: Usage | None = None\n</code></pre> <p>Token counts, or <code>None</code> when the provider didn't report usage.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.ChatResponse.finish_reason","title":"finish_reason  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>finish_reason: FinishReason | None = None\n</code></pre> <p>Why the model stopped, or <code>None</code> when the provider didn't report a reason.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.Usage","title":"Usage","text":"<p>               Bases: <code>BaseModel</code></p> <p>Token usage reported by a single model call.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.Usage.input_tokens","title":"input_tokens  <code>instance-attribute</code>","text":"<pre><code>input_tokens: int\n</code></pre> <p>Tokens consumed by the prompt.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.Usage.output_tokens","title":"output_tokens  <code>instance-attribute</code>","text":"<pre><code>output_tokens: int\n</code></pre> <p>Tokens generated in the response.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.FinishReason","title":"FinishReason","text":"<p>               Bases: <code>StrEnum</code></p> <p>Why the model stopped generating.</p> <p>Values correspond to stop reasons reported by every supported provider, normalized into a single enum.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.FinishReason.STOP","title":"STOP  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>STOP = 'stop'\n</code></pre> <p>Natural stop: the model decided the response was complete.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.FinishReason.TOOL_CALLS","title":"TOOL_CALLS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>TOOL_CALLS = 'tool_calls'\n</code></pre> <p>The model emitted tool calls and is waiting for their results before continuing.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.FinishReason.LENGTH","title":"LENGTH  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>LENGTH = 'length'\n</code></pre> <p>Response was truncated to honor <code>max_output_tokens</code> or the provider's maximum.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.FinishReason.CONTENT_FILTER","title":"CONTENT_FILTER  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>CONTENT_FILTER = 'content_filter'\n</code></pre> <p>Response was blocked or truncated by the provider's content policy.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.FinishReason.ERROR","title":"ERROR  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ERROR = 'error'\n</code></pre> <p>Generation aborted due to a provider or transport error.</p>"},{"location":"reference/chat/responses/#streaming-events","title":"Streaming events","text":""},{"location":"reference/chat/responses/#openfactcheck.chat.responses.TextDelta","title":"TextDelta","text":"<p>               Bases: <code>BaseModel</code></p> <p>A text fragment arriving during a streaming call.</p> <p>Concatenate every <code>TextDelta.content</code> in emission order to reconstruct the final message body.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.TextDelta.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: Literal['text'] = 'text'\n</code></pre> <p>Event tag identifying this as a text chunk.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.TextDelta.content","title":"content  <code>instance-attribute</code>","text":"<pre><code>content: str\n</code></pre> <p>Text fragment from the model.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.StreamEnd","title":"StreamEnd","text":"<p>               Bases: <code>BaseModel</code></p> <p>Terminal event for a stream. Carries completion metadata.</p> <p>Always emitted exactly once as the last event in the stream, even on non-<code>stop</code> terminations.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.StreamEnd.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: Literal['end'] = 'end'\n</code></pre> <p>Event tag identifying this as the stream terminator.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.StreamEnd.finish_reason","title":"finish_reason  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>finish_reason: FinishReason | None = None\n</code></pre> <p>Why the model stopped, or <code>None</code> when the provider didn't report a reason.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.StreamEnd.usage","title":"usage  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>usage: Usage | None = None\n</code></pre> <p>Token counts, or <code>None</code> when the provider didn't report usage.</p>"},{"location":"reference/chat/responses/#openfactcheck.chat.responses.StreamEvent","title":"StreamEvent  <code>module-attribute</code>","text":"<pre><code>StreamEvent = Annotated[\n    TextDelta | StreamEnd, Discriminator(\"type\")\n]\n</code></pre> <p>Events yielded by streaming calls.</p> <p><code>StreamEnd</code> is always the last event; every event before it is a <code>TextDelta</code>.</p>"},{"location":"reference/chat/backends/","title":"Backends","text":"<p>Default backend dispatch for the chat layer.</p> <p>Maps a provider name to its direct-SDK <code>ChatBackend</code>. <code>ChatClient</code> consults this mapping when a caller does not pass <code>backend=</code> explicitly.</p> <p>Callers who want a non-default backend pass the backend instance to <code>ChatClient</code> themselves.</p>"},{"location":"reference/chat/backends/#pages","title":"Pages","text":"<ul> <li><code>ChatBackend</code> protocol: the interface every backend satisfies.</li> <li><code>OpenAIBackend</code> class: direct-SDK backend for OpenAI.</li> <li><code>OpenRouterBackend</code> class: OpenRouter via the OpenAI-compatible API.</li> <li><code>AnthropicBackend</code> class: direct-SDK backend for Anthropic.</li> </ul>"},{"location":"reference/chat/backends/anthropic/","title":"<code>AnthropicBackend</code> class","text":"<p>Direct-SDK chat backend for Anthropic.</p> <p>Implements the <code>ChatBackend</code> protocol by calling the <code>anthropic</code> SDK directly, with no gateway or wrapper library in between. All <code>anthropic</code> imports are isolated to this subpackage so replacing or removing the SDK only changes files under <code>openfactcheck.chat.backends.anthropic</code>.</p> <p>Users rarely construct <code>AnthropicBackend</code> explicitly; <code>ChatClient</code> uses it automatically when <code>config.provider</code> is <code>\"anthropic\"</code>. Pass it by hand only when combining it with other non-default <code>ChatClient</code> settings.</p>"},{"location":"reference/chat/backends/anthropic/#openfactcheck.chat.backends.anthropic.backend.AnthropicBackend","title":"AnthropicBackend","text":"<pre><code>AnthropicBackend()\n</code></pre> <p>Direct-SDK chat backend for Anthropic.</p> <p>Satisfies the <code>ChatBackend</code> protocol. Accepts <code>AnthropicConfig</code> only; passing a config from another provider raises <code>UnsupportedFeatureError</code>. Anthropic's API rejects requests without an explicit output cap, so <code>max_output_tokens</code> must be set on the config.</p> <p>Build the backend with no client yet; clients are created on first use.</p> Source code in <code>src/openfactcheck/chat/backends/anthropic/backend.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Build the backend with no client yet; clients are created on first use.\"\"\"\n    self._sync_client: Anthropic | None = None\n    self._async_client: AsyncAnthropic | None = None\n</code></pre>"},{"location":"reference/chat/backends/anthropic/#openfactcheck.chat.backends.anthropic.backend.AnthropicBackend.completion","title":"completion","text":"<pre><code>completion(request: ChatRequest) -&gt; ChatResponse\n</code></pre> <p>Execute a single chat completion and return the full response.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> Source code in <code>src/openfactcheck/chat/backends/anthropic/backend.py</code> <pre><code>def completion(self, request: ChatRequest) -&gt; ChatResponse:\n    \"\"\"Execute a single chat completion and return the full response.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Returns:\n        The model's reply along with token usage and finish reason.\n\n    Raises:\n        ChatModelError: On any SDK or transport failure.\n    \"\"\"\n    kwargs, messages = self._prepare(request)\n    client = self._client(request)\n    try:\n        response = client.messages.create(messages=messages, **kwargs)\n    except Exception as exc:\n        raise map_error(exc) from exc\n    return to_chat_response(response, request.config.model, request.config.provider)\n</code></pre>"},{"location":"reference/chat/backends/anthropic/#openfactcheck.chat.backends.anthropic.backend.AnthropicBackend.acompletion","title":"acompletion  <code>async</code>","text":"<pre><code>acompletion(request: ChatRequest) -&gt; ChatResponse\n</code></pre> <p>Execute a single chat completion asynchronously and return the full response.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> Source code in <code>src/openfactcheck/chat/backends/anthropic/backend.py</code> <pre><code>async def acompletion(self, request: ChatRequest) -&gt; ChatResponse:\n    \"\"\"Execute a single chat completion asynchronously and return the full response.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Returns:\n        The model's reply along with token usage and finish reason.\n\n    Raises:\n        ChatModelError: On any SDK or transport failure.\n    \"\"\"\n    kwargs, messages = self._prepare(request)\n    client = self._aclient(request)\n    try:\n        response = await client.messages.create(messages=messages, **kwargs)\n    except Exception as exc:\n        raise map_error(exc) from exc\n    return to_chat_response(response, request.config.model, request.config.provider)\n</code></pre>"},{"location":"reference/chat/backends/anthropic/#openfactcheck.chat.backends.anthropic.backend.AnthropicBackend.stream","title":"stream","text":"<pre><code>stream(request: ChatRequest) -&gt; Iterator[StreamEvent]\n</code></pre> <p>Stream a chat completion as typed events.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> YIELDS DESCRIPTION <code>StreamEvent</code> <p>A <code>TextDelta</code> for each content chunk, then a</p> <code>StreamEvent</code> <p>final <code>StreamEnd</code> carrying <code>finish_reason</code> and</p> <code>StreamEvent</code> <p><code>usage</code>.</p> Source code in <code>src/openfactcheck/chat/backends/anthropic/backend.py</code> <pre><code>def stream(self, request: ChatRequest) -&gt; Iterator[StreamEvent]:\n    \"\"\"Stream a chat completion as typed events.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Yields:\n        A [`TextDelta`][TextDelta] for each content chunk, then a\n        final [`StreamEnd`][StreamEnd] carrying ``finish_reason`` and\n        ``usage``.\n\n    Raises:\n        ChatModelError: On any SDK or transport failure.\n    \"\"\"\n    kwargs, messages = self._prepare(request)\n    client = self._client(request)\n    try:\n        stream_iter = client.messages.create(messages=messages, stream=True, **kwargs)\n    except Exception as exc:\n        raise map_error(exc) from exc\n\n    input_tokens = 0\n    output_tokens = 0\n    stop_reason: str | None = None\n    try:\n        for event in stream_iter:\n            if event.type == \"message_start\":\n                input_tokens = event.message.usage.input_tokens\n            elif event.type == \"content_block_delta\":\n                if event.delta.type == \"text_delta\":\n                    yield TextDelta(content=event.delta.text)\n                elif event.delta.type == \"input_json_delta\":\n                    # Structured output uses a forced tool call; its input JSON streams here.\n                    yield TextDelta(content=event.delta.partial_json)\n            elif event.type == \"message_delta\":\n                output_tokens = event.usage.output_tokens\n                if event.delta.stop_reason is not None:\n                    stop_reason = event.delta.stop_reason\n    except Exception as exc:\n        raise map_error(exc) from exc\n\n    yield to_stream_end(stop_reason, input_tokens, output_tokens)\n</code></pre>"},{"location":"reference/chat/backends/anthropic/#openfactcheck.chat.backends.anthropic.backend.AnthropicBackend.astream","title":"astream  <code>async</code>","text":"<pre><code>astream(request: ChatRequest) -&gt; AsyncIterator[StreamEvent]\n</code></pre> <p>Stream a chat completion as typed events over an async iterator.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> YIELDS DESCRIPTION <code>AsyncIterator[StreamEvent]</code> <p>A <code>TextDelta</code> for each content chunk, then a</p> <code>AsyncIterator[StreamEvent]</code> <p>final <code>StreamEnd</code> carrying <code>finish_reason</code> and</p> <code>AsyncIterator[StreamEvent]</code> <p><code>usage</code>.</p> Source code in <code>src/openfactcheck/chat/backends/anthropic/backend.py</code> <pre><code>async def astream(self, request: ChatRequest) -&gt; AsyncIterator[StreamEvent]:\n    \"\"\"Stream a chat completion as typed events over an async iterator.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Yields:\n        A [`TextDelta`][TextDelta] for each content chunk, then a\n        final [`StreamEnd`][StreamEnd] carrying ``finish_reason`` and\n        ``usage``.\n\n    Raises:\n        ChatModelError: On any SDK or transport failure.\n    \"\"\"\n    kwargs, messages = self._prepare(request)\n    client = self._aclient(request)\n    try:\n        stream_iter = await client.messages.create(messages=messages, stream=True, **kwargs)\n    except Exception as exc:\n        raise map_error(exc) from exc\n\n    input_tokens = 0\n    output_tokens = 0\n    stop_reason: str | None = None\n    try:\n        async for event in stream_iter:\n            if event.type == \"message_start\":\n                input_tokens = event.message.usage.input_tokens\n            elif event.type == \"content_block_delta\":\n                if event.delta.type == \"text_delta\":\n                    yield TextDelta(content=event.delta.text)\n                elif event.delta.type == \"input_json_delta\":\n                    # Structured output uses a forced tool call; its input JSON streams here.\n                    yield TextDelta(content=event.delta.partial_json)\n            elif event.type == \"message_delta\":\n                output_tokens = event.usage.output_tokens\n                if event.delta.stop_reason is not None:\n                    stop_reason = event.delta.stop_reason\n    except Exception as exc:\n        raise map_error(exc) from exc\n\n    yield to_stream_end(stop_reason, input_tokens, output_tokens)\n</code></pre>"},{"location":"reference/chat/backends/anthropic/#openfactcheck.chat.backends.anthropic.backend.AnthropicBackend.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Release the sync SDK client and its connection pool, if one was built.</p> <p>Idempotent: the client is rebuilt on the next sync call.</p> Source code in <code>src/openfactcheck/chat/backends/anthropic/backend.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Release the sync SDK client and its connection pool, if one was built.\n\n    Idempotent: the client is rebuilt on the next sync call.\n    \"\"\"\n    if self._sync_client is not None:\n        self._sync_client.close()\n        self._sync_client = None\n</code></pre>"},{"location":"reference/chat/backends/anthropic/#openfactcheck.chat.backends.anthropic.backend.AnthropicBackend.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Release the async SDK client and its connection pool, if one was built.</p> <p>Idempotent: the client is rebuilt on the next async call.</p> Source code in <code>src/openfactcheck/chat/backends/anthropic/backend.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Release the async SDK client and its connection pool, if one was built.\n\n    Idempotent: the client is rebuilt on the next async call.\n    \"\"\"\n    if self._async_client is not None:\n        await self._async_client.close()\n        self._async_client = None\n</code></pre>"},{"location":"reference/chat/backends/base/","title":"<code>ChatBackend</code> protocol","text":"<p>Protocol for chat execution backends.</p> <p>A backend owns the actual SDK call for a given provider. <code>ChatBackend</code> is the replaceable execution boundary that <code>ChatClient</code> talks to; the rest of the chat layer depends only on the protocol, never on a concrete backend.</p> <p>Four call methods describe the request surface: sync and async variants of single-shot completion and streaming. Implementations accept a <code>ChatRequest</code> and produce a <code>ChatResponse</code> or a stream of <code>StreamEvent</code> values, mapping any SDK or transport error to a <code>ChatModelError</code> subclass.</p> <p>A backend reuses one underlying client per axis across calls, so it owns a connection pool. The lifecycle pair <code>close</code> and <code>aclose</code> release that pool; <code>ChatClient</code> forwards them through its own lifecycle methods and context managers.</p> <p>See <code>OpenAIBackend</code> for a reference implementation.</p>"},{"location":"reference/chat/backends/base/#openfactcheck.chat.backends.base.ChatBackend","title":"ChatBackend","text":"<p>               Bases: <code>Protocol</code></p> <p>Execution backend for a single chat provider.</p> <p>Implementations take a <code>ChatRequest</code>, issue the provider SDK call, and return a <code>ChatResponse</code> or a stream of <code>StreamEvent</code> values. Errors are mapped to <code>ChatModelError</code> subclasses so callers handle a single error surface regardless of the underlying SDK.</p>"},{"location":"reference/chat/backends/base/#openfactcheck.chat.backends.base.ChatBackend.completion","title":"completion","text":"<pre><code>completion(request: ChatRequest) -&gt; ChatResponse\n</code></pre> <p>Execute a single chat completion and return the full response.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> Source code in <code>src/openfactcheck/chat/backends/base.py</code> <pre><code>def completion(self, request: ChatRequest) -&gt; ChatResponse:\n    \"\"\"Execute a single chat completion and return the full response.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Returns:\n        The model's reply along with token usage and finish reason.\n\n    Raises:\n        ChatModelError: On any failure (authentication, rate limit,\n            provider error, unsupported feature, etc.).\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/chat/backends/base/#openfactcheck.chat.backends.base.ChatBackend.acompletion","title":"acompletion  <code>async</code>","text":"<pre><code>acompletion(request: ChatRequest) -&gt; ChatResponse\n</code></pre> <p>Execute a single chat completion asynchronously and return the full response.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> Source code in <code>src/openfactcheck/chat/backends/base.py</code> <pre><code>async def acompletion(self, request: ChatRequest) -&gt; ChatResponse:\n    \"\"\"Execute a single chat completion asynchronously and return the full response.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Returns:\n        The model's reply along with token usage and finish reason.\n\n    Raises:\n        ChatModelError: On any failure.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/chat/backends/base/#openfactcheck.chat.backends.base.ChatBackend.stream","title":"stream","text":"<pre><code>stream(request: ChatRequest) -&gt; Iterator[StreamEvent]\n</code></pre> <p>Stream a chat completion as typed events.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> YIELDS DESCRIPTION <code>StreamEvent</code> <p>A <code>TextDelta</code> for each content chunk, then a</p> <code>StreamEvent</code> <p>final <code>StreamEnd</code> carrying <code>finish_reason</code> and</p> <code>StreamEvent</code> <p><code>usage</code>.</p> Source code in <code>src/openfactcheck/chat/backends/base.py</code> <pre><code>def stream(self, request: ChatRequest) -&gt; Iterator[StreamEvent]:\n    \"\"\"Stream a chat completion as typed events.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Yields:\n        A [`TextDelta`][TextDelta] for each content chunk, then a\n        final [`StreamEnd`][StreamEnd] carrying ``finish_reason`` and\n        ``usage``.\n\n    Raises:\n        ChatModelError: On any failure.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/chat/backends/base/#openfactcheck.chat.backends.base.ChatBackend.astream","title":"astream","text":"<pre><code>astream(request: ChatRequest) -&gt; AsyncIterator[StreamEvent]\n</code></pre> <p>Stream a chat completion as typed events over an async iterator.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> YIELDS DESCRIPTION <code>AsyncIterator[StreamEvent]</code> <p>A <code>TextDelta</code> for each content chunk, then a</p> <code>AsyncIterator[StreamEvent]</code> <p>final <code>StreamEnd</code> carrying <code>finish_reason</code> and</p> <code>AsyncIterator[StreamEvent]</code> <p><code>usage</code>.</p> Source code in <code>src/openfactcheck/chat/backends/base.py</code> <pre><code>def astream(self, request: ChatRequest) -&gt; AsyncIterator[StreamEvent]:\n    \"\"\"Stream a chat completion as typed events over an async iterator.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Yields:\n        A [`TextDelta`][TextDelta] for each content chunk, then a\n        final [`StreamEnd`][StreamEnd] carrying ``finish_reason`` and\n        ``usage``.\n\n    Raises:\n        ChatModelError: On any failure.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/chat/backends/base/#openfactcheck.chat.backends.base.ChatBackend.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Release the sync client and its connection pool.</p> <p>Idempotent, and a no-op when no sync call has been made. Closes only the sync side; call <code>aclose</code> for the async side.</p> Source code in <code>src/openfactcheck/chat/backends/base.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Release the sync client and its connection pool.\n\n    Idempotent, and a no-op when no sync call has been made. Closes only\n    the sync side; call [`aclose`][ChatBackend.aclose] for the async side.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/chat/backends/base/#openfactcheck.chat.backends.base.ChatBackend.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Release the async client and its connection pool.</p> <p>Idempotent, and a no-op when no async call has been made. Closes only the async side; call <code>close</code> for the sync side.</p> Source code in <code>src/openfactcheck/chat/backends/base.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Release the async client and its connection pool.\n\n    Idempotent, and a no-op when no async call has been made. Closes only\n    the async side; call [`close`][ChatBackend.close] for the sync side.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/chat/backends/openai/","title":"<code>OpenAIBackend</code> class","text":"<p>Direct-SDK chat backend for OpenAI.</p> <p>Implements the <code>ChatBackend</code> protocol by calling the <code>openai</code> SDK directly, with no gateway or wrapper library in between. All <code>openai</code> imports are isolated to this subpackage so replacing or removing the SDK only changes files under <code>openfactcheck.chat.backends.openai</code>.</p> <p>Users rarely construct <code>OpenAIBackend</code> explicitly; <code>ChatClient</code> uses it automatically when <code>config.provider</code> is <code>\"openai\"</code>. Pass it by hand only when combining it with other non-default <code>ChatClient</code> settings.</p>"},{"location":"reference/chat/backends/openai/#openfactcheck.chat.backends.openai.backend.OpenAIBackend","title":"OpenAIBackend","text":"<pre><code>OpenAIBackend(\n    *,\n    base_url: str | None = None,\n    api_key: str | None = None,\n)\n</code></pre> <p>Direct-SDK chat backend for OpenAI.</p> <p>Satisfies the <code>ChatBackend</code> protocol. Accepts OpenAI-compatible provider configs; passing any other provider's config raises <code>UnsupportedFeatureError</code>. The base URL and API key are configurable so the same SDK can target any OpenAI-compatible endpoint.</p> <p>Build the backend, optionally targeting an OpenAI-compatible endpoint.</p> PARAMETER DESCRIPTION <code>base_url</code> <p>API base URL to call. Unset uses the standard OpenAI endpoint; set it to target a compatible endpoint such as OpenRouter.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>api_key</code> <p>API key to authenticate with. Unset reads the standard <code>OPENAI_API_KEY</code> environment variable.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/chat/backends/openai/backend.py</code> <pre><code>def __init__(self, *, base_url: str | None = None, api_key: str | None = None) -&gt; None:\n    \"\"\"Build the backend, optionally targeting an OpenAI-compatible endpoint.\n\n    Args:\n        base_url: API base URL to call. Unset uses the standard OpenAI\n            endpoint; set it to target a compatible endpoint such as\n            OpenRouter.\n        api_key: API key to authenticate with. Unset reads the standard\n            ``OPENAI_API_KEY`` environment variable.\n    \"\"\"\n    self._base_url = base_url\n    self._api_key = api_key\n    self._sync_client: OpenAI | None = None\n    self._async_client: AsyncOpenAI | None = None\n</code></pre>"},{"location":"reference/chat/backends/openai/#openfactcheck.chat.backends.openai.backend.OpenAIBackend.completion","title":"completion","text":"<pre><code>completion(request: ChatRequest) -&gt; ChatResponse\n</code></pre> <p>Execute a single chat completion and return the full response.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> Source code in <code>src/openfactcheck/chat/backends/openai/backend.py</code> <pre><code>def completion(self, request: ChatRequest) -&gt; ChatResponse:\n    \"\"\"Execute a single chat completion and return the full response.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Returns:\n        The model's reply along with token usage and finish reason.\n\n    Raises:\n        ChatModelError: On any SDK or transport failure.\n    \"\"\"\n    kwargs, messages = self._prepare(request)\n    client = self._client(request)\n    try:\n        response = client.chat.completions.create(messages=messages, **kwargs)\n    except Exception as exc:\n        raise map_error(exc) from exc\n    return to_chat_response(response, request.config.model, request.config.provider)\n</code></pre>"},{"location":"reference/chat/backends/openai/#openfactcheck.chat.backends.openai.backend.OpenAIBackend.acompletion","title":"acompletion  <code>async</code>","text":"<pre><code>acompletion(request: ChatRequest) -&gt; ChatResponse\n</code></pre> <p>Execute a single chat completion asynchronously and return the full response.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> Source code in <code>src/openfactcheck/chat/backends/openai/backend.py</code> <pre><code>async def acompletion(self, request: ChatRequest) -&gt; ChatResponse:\n    \"\"\"Execute a single chat completion asynchronously and return the full response.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Returns:\n        The model's reply along with token usage and finish reason.\n\n    Raises:\n        ChatModelError: On any SDK or transport failure.\n    \"\"\"\n    kwargs, messages = self._prepare(request)\n    client = self._aclient(request)\n    try:\n        response = await client.chat.completions.create(messages=messages, **kwargs)\n    except Exception as exc:\n        raise map_error(exc) from exc\n    return to_chat_response(response, request.config.model, request.config.provider)\n</code></pre>"},{"location":"reference/chat/backends/openai/#openfactcheck.chat.backends.openai.backend.OpenAIBackend.stream","title":"stream","text":"<pre><code>stream(request: ChatRequest) -&gt; Iterator[StreamEvent]\n</code></pre> <p>Stream a chat completion as typed events.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> YIELDS DESCRIPTION <code>StreamEvent</code> <p>A <code>TextDelta</code> for each content chunk, then a</p> <code>StreamEvent</code> <p>final <code>StreamEnd</code> carrying <code>finish_reason</code> and</p> <code>StreamEvent</code> <p><code>usage</code>.</p> Source code in <code>src/openfactcheck/chat/backends/openai/backend.py</code> <pre><code>def stream(self, request: ChatRequest) -&gt; Iterator[StreamEvent]:\n    \"\"\"Stream a chat completion as typed events.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Yields:\n        A [`TextDelta`][TextDelta] for each content chunk, then a\n        final [`StreamEnd`][StreamEnd] carrying ``finish_reason`` and\n        ``usage``.\n\n    Raises:\n        ChatModelError: On any SDK or transport failure.\n    \"\"\"\n    kwargs, messages = self._prepare(request)\n    client = self._client(request)\n    try:\n        stream_iter = client.chat.completions.create(\n            messages=messages,\n            stream=True,\n            stream_options={\"include_usage\": True},\n            **kwargs,\n        )\n    except Exception as exc:\n        raise map_error(exc) from exc\n\n    last_chunk: ChatCompletionChunk | None = None\n    accumulated_usage: object = None\n    try:\n        for chunk in stream_iter:\n            last_chunk = chunk\n            if chunk.usage is not None:\n                accumulated_usage = chunk.usage\n            if chunk.choices:\n                content = chunk.choices[0].delta.content\n                if content:\n                    yield TextDelta(content=content)\n    except Exception as exc:\n        raise map_error(exc) from exc\n\n    yield to_stream_end(last_chunk, accumulated_usage)\n</code></pre>"},{"location":"reference/chat/backends/openai/#openfactcheck.chat.backends.openai.backend.OpenAIBackend.astream","title":"astream  <code>async</code>","text":"<pre><code>astream(request: ChatRequest) -&gt; AsyncIterator[StreamEvent]\n</code></pre> <p>Stream a chat completion as typed events over an async iterator.</p> PARAMETER DESCRIPTION <code>request</code> <p>Messages, model configuration, and runtime settings bundled for the call.</p> <p> TYPE: <code>ChatRequest</code> </p> YIELDS DESCRIPTION <code>AsyncIterator[StreamEvent]</code> <p>A <code>TextDelta</code> for each content chunk, then a</p> <code>AsyncIterator[StreamEvent]</code> <p>final <code>StreamEnd</code> carrying <code>finish_reason</code> and</p> <code>AsyncIterator[StreamEvent]</code> <p><code>usage</code>.</p> Source code in <code>src/openfactcheck/chat/backends/openai/backend.py</code> <pre><code>async def astream(self, request: ChatRequest) -&gt; AsyncIterator[StreamEvent]:\n    \"\"\"Stream a chat completion as typed events over an async iterator.\n\n    Args:\n        request: Messages, model configuration, and runtime settings\n            bundled for the call.\n\n    Yields:\n        A [`TextDelta`][TextDelta] for each content chunk, then a\n        final [`StreamEnd`][StreamEnd] carrying ``finish_reason`` and\n        ``usage``.\n\n    Raises:\n        ChatModelError: On any SDK or transport failure.\n    \"\"\"\n    kwargs, messages = self._prepare(request)\n    client = self._aclient(request)\n    try:\n        stream_iter = await client.chat.completions.create(\n            messages=messages,\n            stream=True,\n            stream_options={\"include_usage\": True},\n            **kwargs,\n        )\n    except Exception as exc:\n        raise map_error(exc) from exc\n\n    last_chunk: ChatCompletionChunk | None = None\n    accumulated_usage: object = None\n    try:\n        async for chunk in stream_iter:\n            last_chunk = chunk\n            if chunk.usage is not None:\n                accumulated_usage = chunk.usage\n            if chunk.choices:\n                content = chunk.choices[0].delta.content\n                if content:\n                    yield TextDelta(content=content)\n    except Exception as exc:\n        raise map_error(exc) from exc\n\n    yield to_stream_end(last_chunk, accumulated_usage)\n</code></pre>"},{"location":"reference/chat/backends/openai/#openfactcheck.chat.backends.openai.backend.OpenAIBackend.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Release the sync SDK client and its connection pool, if one was built.</p> <p>Idempotent: the client is rebuilt on the next sync call.</p> Source code in <code>src/openfactcheck/chat/backends/openai/backend.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Release the sync SDK client and its connection pool, if one was built.\n\n    Idempotent: the client is rebuilt on the next sync call.\n    \"\"\"\n    if self._sync_client is not None:\n        self._sync_client.close()\n        self._sync_client = None\n</code></pre>"},{"location":"reference/chat/backends/openai/#openfactcheck.chat.backends.openai.backend.OpenAIBackend.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Release the async SDK client and its connection pool, if one was built.</p> <p>Idempotent: the client is rebuilt on the next async call.</p> Source code in <code>src/openfactcheck/chat/backends/openai/backend.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Release the async SDK client and its connection pool, if one was built.\n\n    Idempotent: the client is rebuilt on the next async call.\n    \"\"\"\n    if self._async_client is not None:\n        await self._async_client.close()\n        self._async_client = None\n</code></pre>"},{"location":"reference/chat/backends/openrouter/","title":"<code>OpenRouterBackend</code> class","text":"<p>Chat backend for OpenRouter.</p> <p>OpenRouter exposes an OpenAI-compatible Chat Completions API, so this backend serves it by reusing <code>OpenAIBackend</code> pointed at OpenRouter's endpoint with an OpenRouter API key. No new SDK import lives here: the <code>openai</code> SDK stays isolated to <code>openfactcheck.chat.backends.openai</code>.</p> <p>Users rarely construct <code>OpenRouterBackend</code> explicitly; <code>ChatClient</code> uses it automatically when <code>config.provider</code> is <code>\"openrouter\"</code>.</p>"},{"location":"reference/chat/backends/openrouter/#openfactcheck.chat.backends.openrouter.backend.OpenRouterBackend","title":"OpenRouterBackend","text":"<pre><code>OpenRouterBackend(\n    *,\n    api_key: str | None = None,\n    base_url: str = OPENROUTER_BASE_URL,\n)\n</code></pre> <p>               Bases: <code>OpenAIBackend</code></p> <p>Chat backend for OpenRouter.</p> <p>Serves OpenRouter's OpenAI-compatible Chat Completions API by reusing <code>OpenAIBackend</code> with OpenRouter's base URL and API key. Accepts <code>OpenRouterConfig</code>.</p> <p>Build an OpenRouter backend.</p> PARAMETER DESCRIPTION <code>api_key</code> <p>OpenRouter API key. Unset reads the <code>OPENROUTER_API_KEY</code> environment variable.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>base_url</code> <p>OpenRouter API base URL.</p> <p> TYPE: <code>str</code> DEFAULT: <code>OPENROUTER_BASE_URL</code> </p> Source code in <code>src/openfactcheck/chat/backends/openrouter/backend.py</code> <pre><code>def __init__(self, *, api_key: str | None = None, base_url: str = OPENROUTER_BASE_URL) -&gt; None:\n    \"\"\"Build an OpenRouter backend.\n\n    Args:\n        api_key: OpenRouter API key. Unset reads the ``OPENROUTER_API_KEY``\n            environment variable.\n        base_url: OpenRouter API base URL.\n\n    Raises:\n        AuthenticationError: If no key is passed and ``OPENROUTER_API_KEY``\n            is unset.\n    \"\"\"\n    key = api_key if api_key is not None else os.environ.get(\"OPENROUTER_API_KEY\")\n    if not key:\n        raise AuthenticationError(\"OpenRouter requires an API key; set OPENROUTER_API_KEY or pass api_key.\")\n    super().__init__(base_url=base_url, api_key=key)\n</code></pre>"},{"location":"reference/chat/providers/","title":"Providers","text":"<p>Provider lookup for the chat layer.</p> <p>Each supported chat provider has a concrete <code>BaseProvider</code> subclass. <code>get_provider</code> returns the matching provider for a given <code>config.provider</code> name.</p> <p>End users rarely call <code>get_provider</code> directly; <code>ChatClient</code> uses it internally to dispatch on <code>config.provider</code>.</p>"},{"location":"reference/chat/providers/#lookup","title":"Lookup","text":""},{"location":"reference/chat/providers/#openfactcheck.chat.providers.get_provider","title":"get_provider","text":"<pre><code>get_provider(name: str) -&gt; BaseProvider\n</code></pre> <p>Return the provider definition matching <code>name</code>.</p> PARAMETER DESCRIPTION <code>name</code> <p>Provider identifier from <code>config.provider</code>.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/chat/providers/__init__.py</code> <pre><code>def get_provider(name: str) -&gt; BaseProvider:\n    \"\"\"Return the provider definition matching ``name``.\n\n    Args:\n        name: Provider identifier from ``config.provider``.\n\n    Returns:\n        The provider definition for ``name``.\n\n    Raises:\n        ProviderNotFoundError: If ``name`` is not a known provider.\n    \"\"\"\n    provider = _PROVIDERS.get(name)\n    if provider is None:\n        supported = \", \".join(sorted(_PROVIDERS))\n        raise ProviderNotFoundError(f\"Unknown provider '{name}'. Supported: {supported}.\")\n    return provider\n</code></pre>"},{"location":"reference/chat/providers/#pages","title":"Pages","text":"<ul> <li><code>BaseProvider</code> class: the abstract base every provider extends.</li> <li><code>OpenAIProvider</code> class: concrete provider for OpenAI models.</li> <li><code>OpenRouterProvider</code> class: concrete provider for OpenRouter models.</li> <li><code>AnthropicProvider</code> class: concrete provider for Anthropic models.</li> </ul>"},{"location":"reference/chat/providers/anthropic/","title":"<code>AnthropicProvider</code> class","text":"<p>Anthropic provider definition.</p> <p>Declares the Anthropic provider's capability flags and validates that a <code>ModelConfig</code> passed to <code>ChatClient</code> is an <code>AnthropicConfig</code> with the provider-required fields set.</p>"},{"location":"reference/chat/providers/anthropic/#openfactcheck.chat.providers.anthropic.AnthropicProvider","title":"AnthropicProvider","text":"<p>               Bases: <code>BaseProvider</code></p> <p>Anthropic provider definition.</p> <p>Declares streaming, tool calling, and structured output as supported. Requires <code>max_output_tokens</code> to be set on the config, since Anthropic's API rejects requests without an explicit output cap.</p>"},{"location":"reference/chat/providers/anthropic/#openfactcheck.chat.providers.anthropic.AnthropicProvider.validate_config","title":"validate_config","text":"<pre><code>validate_config(config: ModelConfig) -&gt; None\n</code></pre> <p>Check that <code>config</code> is an <code>AnthropicConfig</code> with required fields.</p> PARAMETER DESCRIPTION <code>config</code> <p>Configuration to validate.</p> <p> TYPE: <code>ModelConfig</code> </p> Source code in <code>src/openfactcheck/chat/providers/anthropic.py</code> <pre><code>def validate_config(self, config: ModelConfig) -&gt; None:\n    \"\"\"Check that ``config`` is an [`AnthropicConfig`][AnthropicConfig] with required fields.\n\n    Args:\n        config: Configuration to validate.\n\n    Raises:\n        ProviderError: If ``config`` is not an\n            [`AnthropicConfig`][AnthropicConfig], or if\n            ``max_output_tokens`` is not set.\n    \"\"\"\n    if not isinstance(config, AnthropicConfig):\n        raise ProviderError(f\"Expected AnthropicConfig, got {type(config).__name__}.\")\n    if config.max_output_tokens is None:\n        raise ProviderError(\"Anthropic requires max_output_tokens; set it on AnthropicConfig.\")\n</code></pre>"},{"location":"reference/chat/providers/base/","title":"<code>BaseProvider</code> class","text":"<p>Protocol for chat provider definitions.</p> <p>A provider encapsulates provider-level concerns (capability declaration and per-provider configuration validation) so the rest of the chat layer can dispatch on <code>config.provider</code> without knowing the concrete type.</p> <p>Subclass <code>BaseProvider</code> to add support for a new provider, then register the subclass in <code>openfactcheck.chat.providers</code>.</p> Example <pre><code>from openfactcheck.chat.config import ModelConfig\nfrom openfactcheck.chat.errors import ProviderError\nfrom openfactcheck.chat.providers.base import BaseProvider, ProviderCapabilities\n\n\nclass FakeProvider(BaseProvider):\n    name = \"fake\"\n    capabilities = ProviderCapabilities(streaming=True)\n\n    def validate_config(self, config: ModelConfig) -&gt; None:\n        if not config.model.startswith(\"fake/\"):\n            raise ProviderError(f\"Unexpected model id: {config.model}\")\n</code></pre>"},{"location":"reference/chat/providers/base/#provider","title":"Provider","text":""},{"location":"reference/chat/providers/base/#openfactcheck.chat.providers.base.BaseProvider","title":"BaseProvider","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract provider definition.</p> <p>Concrete subclasses declare the provider's <code>name</code> and <code>capabilities</code>, and implement <code>validate_config</code> to check provider-specific configuration rules.</p>"},{"location":"reference/chat/providers/base/#openfactcheck.chat.providers.base.BaseProvider.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>Stable identifier matched against <code>config.provider</code> to dispatch to this provider.</p>"},{"location":"reference/chat/providers/base/#openfactcheck.chat.providers.base.BaseProvider.capabilities","title":"capabilities  <code>instance-attribute</code>","text":"<pre><code>capabilities: ProviderCapabilities\n</code></pre> <p>Capability flags for this provider.</p>"},{"location":"reference/chat/providers/base/#openfactcheck.chat.providers.base.BaseProvider.validate_config","title":"validate_config  <code>abstractmethod</code>","text":"<pre><code>validate_config(config: ModelConfig) -&gt; None\n</code></pre> <p>Raise if <code>config</code> is invalid for this provider.</p> <p>Implementations narrow the config type and check provider-specific semantic constraints, for example temperature ranges for reasoning models or required fields the provider enforces at the API level.</p> PARAMETER DESCRIPTION <code>config</code> <p>Provider configuration to validate.</p> <p> TYPE: <code>ModelConfig</code> </p> Source code in <code>src/openfactcheck/chat/providers/base.py</code> <pre><code>@abstractmethod\ndef validate_config(self, config: ModelConfig) -&gt; None:\n    \"\"\"Raise if ``config`` is invalid for this provider.\n\n    Implementations narrow the config type and check provider-specific\n    semantic constraints, for example temperature ranges for reasoning\n    models or required fields the provider enforces at the API level.\n\n    Args:\n        config: Provider configuration to validate.\n\n    Raises:\n        ProviderError: If the configuration is invalid for this\n            provider.\n    \"\"\"\n</code></pre>"},{"location":"reference/chat/providers/base/#capabilities","title":"Capabilities","text":""},{"location":"reference/chat/providers/base/#openfactcheck.chat.providers.base.ProviderCapabilities","title":"ProviderCapabilities","text":"<p>               Bases: <code>BaseModel</code></p> <p>Provider-level capability flags.</p> <p>Every flag defaults to <code>False</code>; each provider implementation must explicitly declare what it supports. Values describe provider defaults, not per-model guarantees.</p>"},{"location":"reference/chat/providers/base/#openfactcheck.chat.providers.base.ProviderCapabilities.streaming","title":"streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>streaming: bool = False\n</code></pre> <p>Whether the provider supports streaming responses.</p>"},{"location":"reference/chat/providers/base/#openfactcheck.chat.providers.base.ProviderCapabilities.tool_calling","title":"tool_calling  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tool_calling: bool = False\n</code></pre> <p>Whether the provider supports tool calls as part of a response.</p>"},{"location":"reference/chat/providers/base/#openfactcheck.chat.providers.base.ProviderCapabilities.structured_output","title":"structured_output  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>structured_output: bool = False\n</code></pre> <p>Whether the provider supports constrained (schema-bound) output.</p>"},{"location":"reference/chat/providers/openai/","title":"<code>OpenAIProvider</code> class","text":"<p>OpenAI provider definition.</p> <p>Declares the OpenAI provider's capability flags and validates that a <code>ModelConfig</code> passed to <code>ChatClient</code> is an <code>OpenAIConfig</code>.</p>"},{"location":"reference/chat/providers/openai/#openfactcheck.chat.providers.openai.OpenAIProvider","title":"OpenAIProvider","text":"<p>               Bases: <code>BaseProvider</code></p> <p>OpenAI provider definition.</p> <p>Declares streaming, tool calling, and structured output as supported.</p>"},{"location":"reference/chat/providers/openai/#openfactcheck.chat.providers.openai.OpenAIProvider.validate_config","title":"validate_config","text":"<pre><code>validate_config(config: ModelConfig) -&gt; None\n</code></pre> <p>Check that <code>config</code> is an <code>OpenAIConfig</code>.</p> PARAMETER DESCRIPTION <code>config</code> <p>Configuration to validate.</p> <p> TYPE: <code>ModelConfig</code> </p> Source code in <code>src/openfactcheck/chat/providers/openai.py</code> <pre><code>def validate_config(self, config: ModelConfig) -&gt; None:\n    \"\"\"Check that ``config`` is an [`OpenAIConfig`][OpenAIConfig].\n\n    Args:\n        config: Configuration to validate.\n\n    Raises:\n        ProviderError: If ``config`` is not an\n            [`OpenAIConfig`][OpenAIConfig].\n    \"\"\"\n    if not isinstance(config, OpenAIConfig):\n        raise ProviderError(f\"Expected OpenAIConfig, got {type(config).__name__}.\")\n</code></pre>"},{"location":"reference/chat/providers/openrouter/","title":"<code>OpenRouterProvider</code> class","text":"<p>OpenRouter provider definition.</p> <p>Declares the OpenRouter provider's capability flags and validates that a <code>ModelConfig</code> passed to <code>ChatClient</code> is an <code>OpenRouterConfig</code>.</p>"},{"location":"reference/chat/providers/openrouter/#openfactcheck.chat.providers.openrouter.OpenRouterProvider","title":"OpenRouterProvider","text":"<p>               Bases: <code>BaseProvider</code></p> <p>OpenRouter provider definition.</p> <p>Declares streaming, tool calling, and structured output as supported; actual per-model support depends on the upstream model OpenRouter routes to.</p>"},{"location":"reference/chat/providers/openrouter/#openfactcheck.chat.providers.openrouter.OpenRouterProvider.validate_config","title":"validate_config","text":"<pre><code>validate_config(config: ModelConfig) -&gt; None\n</code></pre> <p>Check that <code>config</code> is an <code>OpenRouterConfig</code>.</p> PARAMETER DESCRIPTION <code>config</code> <p>Configuration to validate.</p> <p> TYPE: <code>ModelConfig</code> </p> Source code in <code>src/openfactcheck/chat/providers/openrouter.py</code> <pre><code>def validate_config(self, config: ModelConfig) -&gt; None:\n    \"\"\"Check that ``config`` is an [`OpenRouterConfig`][OpenRouterConfig].\n\n    Args:\n        config: Configuration to validate.\n\n    Raises:\n        ProviderError: If ``config`` is not an\n            [`OpenRouterConfig`][OpenRouterConfig].\n    \"\"\"\n    if not isinstance(config, OpenRouterConfig):\n        raise ProviderError(f\"Expected OpenRouterConfig, got {type(config).__name__}.\")\n</code></pre>"},{"location":"reference/components/","title":"Components","text":"<p>Public API for the components layer.</p> <p>A component is a callable implementing one of the category contracts: <code>ClaimProcessor</code>, <code>QueryGenerator</code>, <code>Retriever</code>, and <code>Verifier</code> (plus the optional <code>Reviser</code>). Any implementation of a contract is interchangeable with any other. The contracts are defined in terms of the data types in this layer (<code>Claim</code>, <code>Evidence</code>, <code>Verdict</code>, and so on). Import both from <code>openfactcheck.components</code>; concrete implementations live in subpackages.</p>"},{"location":"reference/components/#pages","title":"Pages","text":"<ul> <li>Contracts \u2014 the category protocols every component implements.</li> <li>Provenance \u2014 shared metadata describing where a ported component comes from.</li> <li>Dummy \u2014 placeholder components that run a pipeline with no external dependencies.</li> <li>Factool \u2014 a port of the Factool knowledge-QA factuality pipeline.</li> <li>Nodes \u2014 components prebuilt as graph nodes, for composing pipelines on the graph.</li> </ul>"},{"location":"reference/components/dummy/","title":"Dummy components","text":"<p>Deterministic dummy components.</p> <p>A complete set of placeholder components: each satisfies a category contract but performs no real work. Wiring them all runs a pipeline end to end with no external dependencies, which makes them handy as placeholders and as test fixtures.</p>"},{"location":"reference/components/dummy/#openfactcheck.components.dummy.COMPONENTS","title":"COMPONENTS  <code>module-attribute</code>","text":"<pre><code>COMPONENTS: Mapping[str, Component] = {\n    \"claim_processor\": Component(\n        factory=DummyClaimProcessor, role=\"claim_processor\"\n    ),\n    \"query_generator\": Component(\n        factory=DummyQueryGenerator, role=\"query_generator\"\n    ),\n    \"retriever\": Component(\n        factory=DummyRetriever, role=\"retriever\"\n    ),\n    \"verifier\": Component(\n        factory=DummyVerifier, role=\"verifier\"\n    ),\n}\n</code></pre> <p>The dummy components, discovered through the <code>openfactcheck.components</code> entry point.</p>"},{"location":"reference/components/dummy/#openfactcheck.components.dummy.DummyClaimProcessor","title":"DummyClaimProcessor  <code>dataclass</code>","text":"<pre><code>DummyClaimProcessor()\n</code></pre> <p>Claim processor that performs no extraction.</p> <p>Wraps the whole input as a single claim, or yields nothing when the input is blank.</p>"},{"location":"reference/components/dummy/#openfactcheck.components.dummy.DummyQueryGenerator","title":"DummyQueryGenerator  <code>dataclass</code>","text":"<pre><code>DummyQueryGenerator()\n</code></pre> <p>Query generator that produces no search questions.</p> <p>Wraps the claim in a query with an empty question list, so a paired retriever has nothing to search for.</p>"},{"location":"reference/components/dummy/#openfactcheck.components.dummy.DummyRetriever","title":"DummyRetriever  <code>dataclass</code>","text":"<pre><code>DummyRetriever()\n</code></pre> <p>Retriever that fetches no evidence.</p> <p>Returns an empty evidence set for any query, which turns the pipeline into closed-book verification.</p>"},{"location":"reference/components/dummy/#openfactcheck.components.dummy.DummyVerifier","title":"DummyVerifier  <code>dataclass</code>","text":"<pre><code>DummyVerifier()\n</code></pre> <p>Verifier that reaches no conclusion.</p> <p>Returns a fixed inconclusive verdict for any claim, ignoring the evidence.</p>"},{"location":"reference/components/factool/","title":"Factool components","text":"<p>Factool knowledge-QA components.</p> <p>A port of the knowledge-based QA factuality pipeline from FacTool (Chern et al., 2023). The components mirror the paper's stages: extract atomic claims, generate skeptical search queries, retrieve web evidence, and verify each claim against that evidence.</p> <p>The LLM components run on an injected chat client; <code>PROVENANCE</code> records the recommended default model along with the source paper, repository, pinned commit, and license. Import from <code>openfactcheck.components.factool</code>.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.PROVENANCE","title":"PROVENANCE  <code>module-attribute</code>","text":"<pre><code>PROVENANCE = Provenance(\n    paper_title=\"FacTool: Factuality Detection in Generative AI - A Tool Augmented Framework for Multi-Task and Multi-Domain Scenarios\",\n    paper_url=\"https://arxiv.org/abs/2307.13528\",\n    citation=_CITATION,\n    repository_url=\"https://github.com/GAIR-NLP/factool\",\n    repository_commit=\"3f3914bc090b644be044b7e0005113c135d8b20f\",\n    license=\"Apache-2.0\",\n    default_model=\"gpt-4o-mini\",\n    paper_models=(\"gpt-3.5-turbo-0301\", \"gpt-4-0314\"),\n)\n</code></pre> <p>Provenance for the Factool knowledge-QA components.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.COMPONENTS","title":"COMPONENTS  <code>module-attribute</code>","text":"<pre><code>COMPONENTS: Mapping[str, Component] = {\n    \"claim_processor\": Component(\n        factory=FactoolClaimProcessor,\n        role=\"claim_processor\",\n        default_model=default_model,\n    ),\n    \"query_generator\": Component(\n        factory=FactoolQueryGenerator,\n        role=\"query_generator\",\n        default_model=default_model,\n    ),\n    \"retriever\": Component(\n        factory=FactoolRetriever, role=\"retriever\"\n    ),\n    \"verifier\": Component(\n        factory=FactoolVerifier,\n        role=\"verifier\",\n        default_model=default_model,\n    ),\n}\n</code></pre> <p>The Factool components, discovered through the <code>openfactcheck.components</code> entry point.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolClaimProcessor","title":"FactoolClaimProcessor  <code>dataclass</code>","text":"<pre><code>FactoolClaimProcessor(\n    client: ChatClient,\n    prompt: PromptTemplate = (\n        lambda: PromptTemplate.from_file(\n            _PROMPTS_DIR / \"claim_processor.md\"\n        )\n    )(),\n)\n</code></pre> <p>Extract atomic factual claims, following Factool's knowledge-QA method.</p> <p>Prompts the model to break the input into short, self-contained claims with their coreferences resolved.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolClaimProcessor.client","title":"client  <code>instance-attribute</code>","text":"<pre><code>client: ChatClient\n</code></pre> <p>The chat client used to call the model.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolClaimProcessor.prompt","title":"prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>prompt: PromptTemplate = field(\n    default_factory=lambda: from_file(\n        _PROMPTS_DIR / \"claim_processor.md\"\n    )\n)\n</code></pre> <p>The claim-extraction prompt. Defaults to Factool's; override to customise.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolClaimProcessorModel","title":"FactoolClaimProcessorModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>Factool's structured claim-extraction result.</p> <p>The claim processor maps this onto a list of <code>Claim</code>. It is also the value handed to a call's <code>on_partial</code> hook, the claim list growing as the model writes it.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolQueryGenerator","title":"FactoolQueryGenerator  <code>dataclass</code>","text":"<pre><code>FactoolQueryGenerator(\n    client: ChatClient,\n    prompt: PromptTemplate = (\n        lambda: PromptTemplate.from_file(\n            _PROMPTS_DIR / \"query_generator.md\"\n        )\n    )(),\n)\n</code></pre> <p>Generate skeptical search queries for a claim, following Factool's method.</p> <p>Prompts the model for concise search-engine queries aimed at verifying the claim.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolQueryGenerator.client","title":"client  <code>instance-attribute</code>","text":"<pre><code>client: ChatClient\n</code></pre> <p>The chat client used to call the model.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolQueryGenerator.prompt","title":"prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>prompt: PromptTemplate = field(\n    default_factory=lambda: from_file(\n        _PROMPTS_DIR / \"query_generator.md\"\n    )\n)\n</code></pre> <p>The query-generation prompt. Defaults to Factool's; override to customise.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolQueryGeneratorModel","title":"FactoolQueryGeneratorModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>Factool's structured query-generation result.</p> <p>The query generator maps this onto a <code>Query</code>. It is also the value handed to a call's <code>on_partial</code> hook, the query list growing as the model writes it.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolRetriever","title":"FactoolRetriever  <code>dataclass</code>","text":"<pre><code>FactoolRetriever(\n    serper: SerperClient, num_results: int = 10\n)\n</code></pre> <p>Retrieve evidence for a claim's queries via Serper web search.</p> <p>Searches every query attached to the claim and collects the answer box, knowledge graph, and organic snippets into one evidence set.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolRetriever.serper","title":"serper  <code>instance-attribute</code>","text":"<pre><code>serper: SerperClient\n</code></pre> <p>The Serper client used for web search.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolRetriever.num_results","title":"num_results  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_results: int = 10\n</code></pre> <p>Number of organic results to request for each query.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolVerifier","title":"FactoolVerifier  <code>dataclass</code>","text":"<pre><code>FactoolVerifier(\n    client: ChatClient,\n    prompt: PromptTemplate = (\n        lambda: PromptTemplate.from_file(\n            _PROMPTS_DIR / \"verifier.md\"\n        )\n    )(),\n)\n</code></pre> <p>Judge a claim against its evidence, following Factool's method.</p> <p>Factool reports factuality as a boolean and, when the claim is wrong, the error and a correction. The boolean maps to <code>supported</code> or <code>refuted</code>; Factool emits no confidence, so the verdict leaves it unset.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolVerifier.client","title":"client  <code>instance-attribute</code>","text":"<pre><code>client: ChatClient\n</code></pre> <p>The chat client used to call the model.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolVerifier.prompt","title":"prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>prompt: PromptTemplate = field(\n    default_factory=lambda: from_file(\n        _PROMPTS_DIR / \"verifier.md\"\n    )\n)\n</code></pre> <p>The verification prompt. Defaults to Factool's; override to customise.</p>"},{"location":"reference/components/factool/#openfactcheck.components.factool.FactoolVerifierModel","title":"FactoolVerifierModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>Factool's structured per-claim verification result.</p> <p>The verifier maps this onto a <code>Verdict</code>. It is also the value handed to a verifier call's <code>on_partial</code> hook, filling in field by field as the model writes it.</p>"},{"location":"reference/components/protocols/","title":"Contracts","text":"<p>Component category contracts for the fact-checking pipeline.</p> <p>Each component category is a <code>Protocol</code> defining the signature any implementation must match. Implementations live in sibling subpackages (<code>dummy</code>, <code>default</code>, and paper ports such as <code>factool</code>) and are interchangeable with any other implementation of the same category.</p>"},{"location":"reference/components/protocols/#openfactcheck.components.protocols.ClaimProcessor","title":"ClaimProcessor","text":"<p>               Bases: <code>Protocol</code></p> <p>Turn input text into atomic factual claims.</p> <p>One input may yield zero or more claims. Returning an empty list is valid (for example, when the input contains no checkable claims).</p>"},{"location":"reference/components/protocols/#openfactcheck.components.protocols.QueryGenerator","title":"QueryGenerator","text":"<p>               Bases: <code>Protocol</code></p> <p>Generate search queries for a claim.</p> <p>Produces the search questions a <code>Retriever</code> runs to gather evidence. One claim yields one <code>Query</code> holding any number of questions.</p>"},{"location":"reference/components/protocols/#openfactcheck.components.protocols.Retriever","title":"Retriever","text":"<p>               Bases: <code>Protocol</code></p> <p>Fetch evidence for a claim's queries.</p> <p>A <code>Query</code> carries both the claim and the search questions generated for it. Evidence is a set of sources bearing on the claim's truthfulness: web pages, documents, vector-store results. A retriever that finds nothing should return <code>Evidence(claim=query.claim, sources=[])</code>.</p>"},{"location":"reference/components/protocols/#openfactcheck.components.protocols.Verifier","title":"Verifier","text":"<p>               Bases: <code>Protocol</code></p> <p>Decide whether evidence supports, refutes, or is insufficient for a claim.</p>"},{"location":"reference/components/protocols/#openfactcheck.components.protocols.Reviser","title":"Reviser","text":"<p>               Bases: <code>Protocol</code></p> <p>Revise input text to correct the factual errors its verdicts found.</p> <p>An optional final stage: most pipelines stop at the <code>Verdict</code> for each claim. A reviser weaves the per-claim corrections back into the original text, producing a revised version that preserves the wording and style of what was checked.</p>"},{"location":"reference/components/provenance/","title":"Provenance","text":"<p>Provenance metadata shared across ported components.</p> <p>A paper port records where its components come from and what they are based on. The concrete value lives in the port's own package; this module defines the shared shape so every port reports the same fields, which a registry or docs guide can surface uniformly.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance","title":"Provenance  <code>dataclass</code>","text":"<pre><code>Provenance(\n    paper_title: str,\n    paper_url: str,\n    citation: str,\n    repository_url: str,\n    repository_commit: str,\n    license: str,\n    default_model: str,\n    paper_models: tuple[str, ...],\n)\n</code></pre> <p>Where a ported component comes from and what it is based on.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance.paper_title","title":"paper_title  <code>instance-attribute</code>","text":"<pre><code>paper_title: str\n</code></pre> <p>Title of the source paper.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance.paper_url","title":"paper_url  <code>instance-attribute</code>","text":"<pre><code>paper_url: str\n</code></pre> <p>URL of the source paper.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance.citation","title":"citation  <code>instance-attribute</code>","text":"<pre><code>citation: str\n</code></pre> <p>BibTeX entry for the source paper, so users can cite the original work.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance.repository_url","title":"repository_url  <code>instance-attribute</code>","text":"<pre><code>repository_url: str\n</code></pre> <p>URL of the authors' reference implementation.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance.repository_commit","title":"repository_commit  <code>instance-attribute</code>","text":"<pre><code>repository_commit: str\n</code></pre> <p>Commit of the reference implementation this port was read against.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance.license","title":"license  <code>instance-attribute</code>","text":"<pre><code>license: str\n</code></pre> <p>License of the reference implementation.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance.default_model","title":"default_model  <code>instance-attribute</code>","text":"<pre><code>default_model: str\n</code></pre> <p>Recommended default model for the components, close to the paper's setup.</p>"},{"location":"reference/components/provenance/#openfactcheck.components.provenance.Provenance.paper_models","title":"paper_models  <code>instance-attribute</code>","text":"<pre><code>paper_models: tuple[str, ...]\n</code></pre> <p>Exact model snapshots reported in the paper's experiments.</p>"},{"location":"reference/components/rarr/","title":"RARR components","text":"<p>RARR components.</p> <p>A port of RARR (Gao et al., 2023), a research-and-revise approach to attribution and factuality. The components mirror the paper's stages: take the input as one passage, generate comprehensive verification questions, retrieve one piece of evidence per question, then iteratively check the passage against each piece of evidence (the agreement gate) and edit it to agree when it disagrees (the editor). A separate selector picks the evidence that best covers the questions as an attribution report.</p> <p>The agreement gate and editor run a sequential loop in which the passage is revised in place, so each check sees the latest version; the pipeline expresses this as a graph cycle. The retriever, agreement gate, editor, and evidence selector are specific to RARR's shape and do not implement the shared component protocols.</p> <p>The LLM components run on an injected chat client; <code>PROVENANCE</code> records the recommended default model along with the source paper, repository, pinned commit, and license. Import from <code>openfactcheck.components.rarr</code>.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.PROVENANCE","title":"PROVENANCE  <code>module-attribute</code>","text":"<pre><code>PROVENANCE = Provenance(\n    paper_title=\"RARR: Researching and Revising What Language Models Say, Using Language Models\",\n    paper_url=\"https://aclanthology.org/2023.acl-long.910/\",\n    citation=_CITATION,\n    repository_url=\"https://github.com/anthonywchen/RARR\",\n    repository_commit=\"51a1a10fe5bada837a368f98cb55288ac5168c9e\",\n    license=\"N/A\",\n    default_model=\"gpt-4o-mini\",\n    paper_models=(\"text-davinci-003\", \"palm-540b\"),\n)\n</code></pre> <p>Provenance for the RARR components.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.QuestionedSource","title":"QuestionedSource","text":"<pre><code>QuestionedSource = tuple[str, Source]\n</code></pre> <p>A search question paired with the single piece of evidence retrieved for it.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRAgreementGate","title":"RARRAgreementGate  <code>dataclass</code>","text":"<pre><code>RARRAgreementGate(\n    client: ChatClient,\n    prompt: PromptTemplate = (\n        lambda: PromptTemplate.from_file(\n            _PROMPTS_DIR / \"agreement_gate.md\"\n        )\n    )(),\n)\n</code></pre> <p>Decide whether a passage agrees with a piece of evidence, following RARR's method.</p> <p>Given the passage so far, a question, and the evidence found for it, the gate reasons about the answer each implies and decides whether they agree, disagree, or whether the evidence is irrelevant. A <code>disagrees</code> verdict (mapped to <code>refuted</code>) opens the gate, signalling the passage should be edited.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRAgreementGate.client","title":"client  <code>instance-attribute</code>","text":"<pre><code>client: ChatClient\n</code></pre> <p>The chat client used to call the model.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRAgreementGate.prompt","title":"prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>prompt: PromptTemplate = field(\n    default_factory=lambda: from_file(\n        _PROMPTS_DIR / \"agreement_gate.md\"\n    )\n)\n</code></pre> <p>The agreement-gate prompt. Defaults to RARR's; override to customise.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRAgreementGateModel","title":"RARRAgreementGateModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>RARR's structured agreement-gate result.</p> <p>The gate maps this onto a <code>Verdict</code>, where <code>disagrees</code> opens the gate for editing. It is also the value handed to a call's <code>on_partial</code> hook, filling in as the model writes it.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRClaimProcessor","title":"RARRClaimProcessor  <code>dataclass</code>","text":"<pre><code>RARRClaimProcessor()\n</code></pre> <p>Treat the whole input as a single claim, following RARR's method.</p> <p>RARR researches and revises a passage as a whole rather than decomposing it into atomic claims, so this returns the input verbatim as one claim.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARREditor","title":"RARREditor  <code>dataclass</code>","text":"<pre><code>RARREditor(\n    client: ChatClient,\n    prompt: PromptTemplate = (\n        lambda: PromptTemplate.from_file(\n            _PROMPTS_DIR / \"editor.md\"\n        )\n    )(),\n    max_edit_distance: int = DEFAULT_MAX_EDIT_DISTANCE,\n    max_edit_ratio: float = DEFAULT_MAX_EDIT_RATIO,\n)\n</code></pre> <p>Edit a passage to agree with a piece of evidence, following RARR's method.</p> <p>Given the passage so far, a question, and disagreeing evidence, the editor rewrites the passage to agree with the evidence while changing as little as possible. To preserve the original wording, an edit is rejected (the passage is left unchanged) when it changes more than the allowed number of characters or fraction of the passage.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARREditor.client","title":"client  <code>instance-attribute</code>","text":"<pre><code>client: ChatClient\n</code></pre> <p>The chat client used to call the model.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARREditor.prompt","title":"prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>prompt: PromptTemplate = field(\n    default_factory=lambda: from_file(\n        _PROMPTS_DIR / \"editor.md\"\n    )\n)\n</code></pre> <p>The editing prompt. Defaults to RARR's; override to customise.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARREditor.max_edit_distance","title":"max_edit_distance  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_edit_distance: int = DEFAULT_MAX_EDIT_DISTANCE\n</code></pre> <p>Largest character edit distance an edit may have before it is rejected.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARREditor.max_edit_ratio","title":"max_edit_ratio  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_edit_ratio: float = DEFAULT_MAX_EDIT_RATIO\n</code></pre> <p>Largest edit distance, as a fraction of the passage length, an edit may have before it is rejected.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARREditorModel","title":"RARREditorModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>RARR's structured edit result.</p> <p>The editor returns the <code>fix</code> when it is within the size limits. It is also the value handed to a call's <code>on_partial</code> hook, filling in as the model writes it.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRConfigError","title":"RARRConfigError","text":"<p>               Bases: <code>RARRError</code></p> <p>An optional dependency or configuration the RARR components need is missing.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRError","title":"RARRError","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for errors raised by the RARR components.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARREvidenceSelector","title":"RARREvidenceSelector","text":"<pre><code>RARREvidenceSelector(\n    *,\n    max_selected: int = DEFAULT_MAX_SELECTED,\n    ranker_model: str = DEFAULT_RANKER_MODEL,\n)\n</code></pre> <p>Select the attribution report by maximizing question coverage, following RARR's method.</p> <p>Scores every source against every question with a cross-encoder, then picks the subset of at most <code>max_selected</code> sources that maximizes total coverage, where a question's coverage is the score of the best source for it. The cross-encoder is loaded once on first use and reused; it needs the <code>rarr</code> extra (<code>sentence-transformers</code>).</p> <p>Build an evidence selector.</p> PARAMETER DESCRIPTION <code>max_selected</code> <p>Largest number of sources to keep in the report.</p> <p> TYPE: <code>int</code> DEFAULT: <code>DEFAULT_MAX_SELECTED</code> </p> <code>ranker_model</code> <p>Cross-encoder model id used to score sources against questions.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_RANKER_MODEL</code> </p> Source code in <code>src/openfactcheck/components/rarr/evidence_selector.py</code> <pre><code>def __init__(self, *, max_selected: int = DEFAULT_MAX_SELECTED, ranker_model: str = DEFAULT_RANKER_MODEL) -&gt; None:\n    \"\"\"Build an evidence selector.\n\n    Args:\n        max_selected: Largest number of sources to keep in the report.\n        ranker_model: Cross-encoder model id used to score sources against questions.\n    \"\"\"\n    self._max_selected = max_selected\n    self._ranker_name = ranker_model\n    self._ranker: CrossEncoder | None = None\n</code></pre>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRQueryGenerator","title":"RARRQueryGenerator  <code>dataclass</code>","text":"<pre><code>RARRQueryGenerator(\n    client: ChatClient,\n    prompt: PromptTemplate = (\n        lambda: PromptTemplate.from_file(\n            _PROMPTS_DIR / \"query_generator.md\"\n        )\n    )(),\n    num_rounds: int = DEFAULT_NUM_ROUNDS,\n)\n</code></pre> <p>Generate comprehensive verification questions for a passage, following RARR's method.</p> <p>Prompts the model for the questions a reader would look up to check the passage. To widen coverage, it samples the model several times and takes the union of the questions, deduplicating while keeping first-seen order. Sampling benefits from a chat client with a non-zero temperature.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRQueryGenerator.client","title":"client  <code>instance-attribute</code>","text":"<pre><code>client: ChatClient\n</code></pre> <p>The chat client used to call the model.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRQueryGenerator.prompt","title":"prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>prompt: PromptTemplate = field(\n    default_factory=lambda: from_file(\n        _PROMPTS_DIR / \"query_generator.md\"\n    )\n)\n</code></pre> <p>The question-generation prompt. Defaults to RARR's; override to customise.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRQueryGenerator.num_rounds","title":"num_rounds  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_rounds: int = DEFAULT_NUM_ROUNDS\n</code></pre> <p>How many times to sample questions before taking their union.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRQueryGeneratorModel","title":"RARRQueryGeneratorModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>RARR's structured comprehensive-question-generation result.</p> <p>The query generator unions these across sampling rounds and maps them onto a <code>Query</code>. It is also the value handed to a call's <code>on_partial</code> hook, the question list growing as the model writes it.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRRetriever","title":"RARRRetriever  <code>dataclass</code>","text":"<pre><code>RARRRetriever(serper: SerperClient, num_results: int = 5)\n</code></pre> <p>Retrieve one piece of evidence per question via Serper web search, following RARR's method.</p> <p>Searches every question in one batch and keeps the single top result for each: the answer box or knowledge graph when present, otherwise the first organic snippet. Each kept result stays paired with the question that found it, since RARR checks the passage against that question during revision. Questions that turn up nothing are dropped.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRRetriever.serper","title":"serper  <code>instance-attribute</code>","text":"<pre><code>serper: SerperClient\n</code></pre> <p>The Serper client used for web search.</p>"},{"location":"reference/components/rarr/#openfactcheck.components.rarr.RARRRetriever.num_results","title":"num_results  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_results: int = 5\n</code></pre> <p>Number of organic results to request for each question.</p>"},{"location":"reference/components/nodes/","title":"Nodes","text":"<p>Prebuilt graph nodes that wire components onto the graph layer.</p> <p>A node factory wraps a component as a graph <code>Step</code>, so a pipeline is composed by wiring nodes with the graph API rather than hand-writing step functions. Each namespace builds its components as nodes (<code>nodes.factool</code>, <code>nodes.factcheckgpt</code>, <code>nodes.rarr</code>), and <code>nodes.dummy</code> wires the deterministic placeholders. A namespace is imported lazily on first access, so reaching one namespace's nodes never loads the others. Mix nodes from different namespaces in one graph.</p> Example <pre><code>from openfactcheck.components import nodes\nfrom openfactcheck.graph import GraphBuilder\n\ng = GraphBuilder(input_type=Input, output_type=list[Verdict])\nclaim_processor = nodes.factcheckgpt.claim_processor(g, chat)  # FactcheckGPT's claim processor\nquery_generator = nodes.rarr.query_generator(g, chat)  # RARR's question generation\nretriever = nodes.factool.retriever(g, serper)  # Factool's web retrieval\nverifier = nodes.factool.verifier(g, chat)  # Factool's verifier\ng.add(\n    g.edge_from(g.start_node).to(claim_processor),\n    g.edge_from(claim_processor).map().to(query_generator),\n    g.edge_from(query_generator).to(retriever),\n    g.edge_from(retriever).to(verifier),\n    g.edge_from(verifier).collect().to(g.end_node),\n)\n</code></pre>"},{"location":"reference/components/nodes/#pages","title":"Pages","text":"<ul> <li>Dummy \u2014 the deterministic placeholder components, prebuilt as nodes.</li> <li>Factool \u2014 the Factool components, prebuilt as nodes.</li> <li>FactcheckGPT \u2014 the FactcheckGPT components, prebuilt as nodes.</li> <li>RARR \u2014 RARR's nodes, including the retrieve-and-revise subgraph.</li> </ul>"},{"location":"reference/components/nodes/dummy/","title":"Dummy nodes","text":"<p>Dummy nodes: the deterministic placeholder components, prebuilt as graph nodes.</p> <p>Each factory builds a dummy component and lifts it onto a graph as a node, doing the small wiring a step needs: unwrapping the node's input and forwarding the streaming hook. The dummy components reach no real conclusions, so a graph wired from these nodes runs end to end with no model or network calls, which makes it a handy skeleton or test fixture.</p>"},{"location":"reference/components/nodes/dummy/#openfactcheck.components.nodes.dummy.claim_processor","title":"claim_processor","text":"<pre><code>claim_processor(\n    g: AnyGraphBuilder,\n    *,\n    node_id: str = \"dummy/claim_processor\",\n) -&gt; Step[Input, list[Claim], Any, Any]\n</code></pre> <p>Build the dummy claim processor and lift it onto <code>g</code> as a node: input text in, the whole text as one claim out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'dummy/claim_processor'</code> </p> Source code in <code>src/openfactcheck/components/nodes/dummy.py</code> <pre><code>def claim_processor(\n    g: AnyGraphBuilder,\n    *,\n    node_id: str = \"dummy/claim_processor\",\n) -&gt; Step[Input, list[Claim], Any, Any]:\n    \"\"\"Build the dummy claim processor and lift it onto ``g`` as a node: input text in, the whole text as one claim out.\n\n    Args:\n        g: The builder to register the node onto.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = DummyClaimProcessor()\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Input, Any, Any]) -&gt; list[Claim]:\n        return await component(ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/dummy/#openfactcheck.components.nodes.dummy.query_generator","title":"query_generator","text":"<pre><code>query_generator(\n    g: AnyGraphBuilder,\n    *,\n    node_id: str = \"dummy/query_generator\",\n) -&gt; Step[Claim, Query, Any, Any]\n</code></pre> <p>Build the dummy query generator and lift it onto <code>g</code> as a node: one claim in, a query with no questions out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'dummy/query_generator'</code> </p> Source code in <code>src/openfactcheck/components/nodes/dummy.py</code> <pre><code>def query_generator(\n    g: AnyGraphBuilder,\n    *,\n    node_id: str = \"dummy/query_generator\",\n) -&gt; Step[Claim, Query, Any, Any]:\n    \"\"\"Build the dummy query generator and lift it onto ``g`` as a node: one claim in, a query with no questions out.\n\n    Args:\n        g: The builder to register the node onto.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = DummyQueryGenerator()\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Claim, Any, Any]) -&gt; Query:\n        return await component(ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/dummy/#openfactcheck.components.nodes.dummy.retriever","title":"retriever","text":"<pre><code>retriever(\n    g: AnyGraphBuilder, *, node_id: str = \"dummy/retriever\"\n) -&gt; Step[Query, Evidence, Any, Any]\n</code></pre> <p>Build the dummy retriever and lift it onto <code>g</code> as a node: a query in, empty evidence out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'dummy/retriever'</code> </p> Source code in <code>src/openfactcheck/components/nodes/dummy.py</code> <pre><code>def retriever(\n    g: AnyGraphBuilder,\n    *,\n    node_id: str = \"dummy/retriever\",\n) -&gt; Step[Query, Evidence, Any, Any]:\n    \"\"\"Build the dummy retriever and lift it onto ``g`` as a node: a query in, empty evidence out.\n\n    Args:\n        g: The builder to register the node onto.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = DummyRetriever()\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Query, Any, Any]) -&gt; Evidence:\n        return await component(ctx.inputs)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/dummy/#openfactcheck.components.nodes.dummy.verifier","title":"verifier","text":"<pre><code>verifier(\n    g: AnyGraphBuilder, *, node_id: str = \"dummy/verifier\"\n) -&gt; Step[Evidence, Verdict, Any, Any]\n</code></pre> <p>Build the dummy verifier and lift it onto <code>g</code> as a node: evidence in, an inconclusive verdict out.</p> <p>The node's input is the <code>Evidence</code>, which carries the claim it bears on, so the verifier is called with that claim and evidence.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'dummy/verifier'</code> </p> Source code in <code>src/openfactcheck/components/nodes/dummy.py</code> <pre><code>def verifier(\n    g: AnyGraphBuilder,\n    *,\n    node_id: str = \"dummy/verifier\",\n) -&gt; Step[Evidence, Verdict, Any, Any]:\n    \"\"\"Build the dummy verifier and lift it onto ``g`` as a node: evidence in, an inconclusive verdict out.\n\n    The node's input is the [`Evidence`][Evidence], which carries the claim it bears on, so the verifier is\n    called with that claim and evidence.\n\n    Args:\n        g: The builder to register the node onto.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = DummyVerifier()\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Evidence, Any, Any]) -&gt; Verdict:\n        return await component(ctx.inputs.claim, ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factcheckgpt/","title":"FactcheckGPT nodes","text":"<p>FactcheckGPT nodes: the FactcheckGPT components, prebuilt as graph nodes.</p> <p>Each factory builds a FactcheckGPT component and lifts it onto a graph as a node, doing the small wiring a step needs: unwrapping the node's input and forwarding the streaming hook. The reviser is not offered as a node: it needs the original input alongside the verdicts, which the facade supplies, not the graph dataflow.</p>"},{"location":"reference/components/nodes/factcheckgpt/#openfactcheck.components.nodes.factcheckgpt.PROVENANCE","title":"PROVENANCE  <code>module-attribute</code>","text":"<pre><code>PROVENANCE = Provenance(\n    paper_title=\"Factcheck-Bench: Fine-Grained Evaluation Benchmark for Automatic Fact-Checkers\",\n    paper_url=\"https://aclanthology.org/2024.findings-emnlp.830/\",\n    citation=_CITATION,\n    repository_url=\"https://github.com/yuxiaw/Factcheck-GPT\",\n    repository_commit=\"a9e6a04a953ad880806529c504a679dd1ad06528\",\n    license=\"Apache-2.0\",\n    default_model=\"gpt-4o-mini\",\n    paper_models=(\"gpt-3.5-turbo-0613\",),\n)\n</code></pre> <p>Provenance for the FactcheckGPT components.</p>"},{"location":"reference/components/nodes/factcheckgpt/#openfactcheck.components.nodes.factcheckgpt.PIPELINE","title":"PIPELINE  <code>module-attribute</code>","text":"<pre><code>PIPELINE = Pipeline(build_graph, default_model)\n</code></pre> <p>The FactcheckGPT pipeline, discovered through the <code>openfactcheck.pipelines</code> entry point.</p>"},{"location":"reference/components/nodes/factcheckgpt/#openfactcheck.components.nodes.factcheckgpt.claim_processor","title":"claim_processor","text":"<pre><code>claim_processor(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factcheckgpt/claim_processor\",\n) -&gt; Step[Input, list[Claim], Any, Any]\n</code></pre> <p>Build FactcheckGPT's claim processor and lift it onto <code>g</code> as a node: input text in, atomic claims out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>chat</code> <p>Chat client backing the component.</p> <p> TYPE: <code>ChatClient</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'factcheckgpt/claim_processor'</code> </p> Source code in <code>src/openfactcheck/components/nodes/factcheckgpt.py</code> <pre><code>def claim_processor(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factcheckgpt/claim_processor\",\n) -&gt; Step[Input, list[Claim], Any, Any]:\n    \"\"\"Build FactcheckGPT's claim processor and lift it onto ``g`` as a node: input text in, atomic claims out.\n\n    Args:\n        g: The builder to register the node onto.\n        chat: Chat client backing the component.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = FactcheckGPTClaimProcessor(client=chat)\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Input, Any, Any]) -&gt; list[Claim]:\n        return await component(ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factcheckgpt/#openfactcheck.components.nodes.factcheckgpt.query_generator","title":"query_generator","text":"<pre><code>query_generator(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factcheckgpt/query_generator\",\n) -&gt; Step[Claim, Query, Any, Any]\n</code></pre> <p>Build FactcheckGPT's query generator and lift it onto <code>g</code> as a node: one claim in, its search query out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>chat</code> <p>Chat client backing the component.</p> <p> TYPE: <code>ChatClient</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'factcheckgpt/query_generator'</code> </p> Source code in <code>src/openfactcheck/components/nodes/factcheckgpt.py</code> <pre><code>def query_generator(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factcheckgpt/query_generator\",\n) -&gt; Step[Claim, Query, Any, Any]:\n    \"\"\"Build FactcheckGPT's query generator and lift it onto ``g`` as a node: one claim in, its search query out.\n\n    Args:\n        g: The builder to register the node onto.\n        chat: Chat client backing the component.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = FactcheckGPTQueryGenerator(client=chat)\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Claim, Any, Any]) -&gt; Query:\n        return await component(ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factcheckgpt/#openfactcheck.components.nodes.factcheckgpt.retriever","title":"retriever","text":"<pre><code>retriever(\n    g: AnyGraphBuilder,\n    serper: SerperClient | None = None,\n    *,\n    node_id: str = \"factcheckgpt/retriever\",\n) -&gt; Step[Query, Evidence, Any, Any]\n</code></pre> <p>Build FactcheckGPT's retriever and lift it onto <code>g</code> as a node: a query in, evidence out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>serper</code> <p>Web-search client for the retriever. Defaults to a client that reads its key from the environment.</p> <p> TYPE: <code>SerperClient | None</code> DEFAULT: <code>None</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'factcheckgpt/retriever'</code> </p> Source code in <code>src/openfactcheck/components/nodes/factcheckgpt.py</code> <pre><code>def retriever(\n    g: AnyGraphBuilder,\n    serper: SerperClient | None = None,\n    *,\n    node_id: str = \"factcheckgpt/retriever\",\n) -&gt; Step[Query, Evidence, Any, Any]:\n    \"\"\"Build FactcheckGPT's retriever and lift it onto ``g`` as a node: a query in, evidence out.\n\n    Args:\n        g: The builder to register the node onto.\n        serper: Web-search client for the retriever. Defaults to a client that reads its key from the\n            environment.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = FactcheckGPTRetriever(serper=serper if serper is not None else SerperClient())\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Query, Any, Any]) -&gt; Evidence:\n        return await component(ctx.inputs)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factcheckgpt/#openfactcheck.components.nodes.factcheckgpt.verifier","title":"verifier","text":"<pre><code>verifier(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factcheckgpt/verifier\",\n) -&gt; Step[Evidence, Verdict, Any, Any]\n</code></pre> <p>Build FactcheckGPT's verifier and lift it onto <code>g</code> as a node: evidence in, a verdict out.</p> <p>The node's input is the <code>Evidence</code>, which carries the claim it bears on, so the verifier is called with that claim and evidence.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>chat</code> <p>Chat client backing the component.</p> <p> TYPE: <code>ChatClient</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'factcheckgpt/verifier'</code> </p> Source code in <code>src/openfactcheck/components/nodes/factcheckgpt.py</code> <pre><code>def verifier(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factcheckgpt/verifier\",\n) -&gt; Step[Evidence, Verdict, Any, Any]:\n    \"\"\"Build FactcheckGPT's verifier and lift it onto ``g`` as a node: evidence in, a verdict out.\n\n    The node's input is the [`Evidence`][Evidence], which carries the claim it\n    bears on, so the verifier is called with that claim and evidence.\n\n    Args:\n        g: The builder to register the node onto.\n        chat: Chat client backing the component.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = FactcheckGPTVerifier(client=chat)\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Evidence, Any, Any]) -&gt; Verdict:\n        return await component(ctx.inputs.claim, ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factcheckgpt/#openfactcheck.components.nodes.factcheckgpt.build_graph","title":"build_graph","text":"<pre><code>build_graph(\n    *, chat: ChatClient, serper: SerperClient | None = None\n) -&gt; Graph[Input, list[Verdict], None, None]\n</code></pre> <p>Wire FactcheckGPT's pipeline into a runnable graph.</p> <p>Extracts claims, then for each claim in parallel generates a query, retrieves evidence, and verifies, collecting the per-claim verdicts at the end. The reviser is not part of the graph; the facade applies it.</p> PARAMETER DESCRIPTION <code>chat</code> <p>Chat client backing the claim processor, query generator, and verifier.</p> <p> TYPE: <code>ChatClient</code> </p> <code>serper</code> <p>Web-search client for the retriever. Defaults to a client that reads its key from the environment.</p> <p> TYPE: <code>SerperClient | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/components/nodes/factcheckgpt.py</code> <pre><code>def build_graph(*, chat: ChatClient, serper: SerperClient | None = None) -&gt; Graph[Input, list[Verdict], None, None]:\n    \"\"\"Wire FactcheckGPT's pipeline into a runnable graph.\n\n    Extracts claims, then for each claim in parallel generates a query, retrieves evidence, and verifies,\n    collecting the per-claim verdicts at the end. The reviser is not part of the graph; the facade applies it.\n\n    Args:\n        chat: Chat client backing the claim processor, query generator, and verifier.\n        serper: Web-search client for the retriever. Defaults to a client that reads its key from the\n            environment.\n\n    Returns:\n        A graph from input text to the list of per-claim verdicts.\n    \"\"\"\n    g: GraphBuilder[Input, list[Verdict], None, None] = GraphBuilder(\n        input_type=Input, output_type=list[Verdict], name=\"factcheckgpt\"\n    )\n    cp = claim_processor(g, chat)\n    qg = query_generator(g, chat)\n    rt = retriever(g, serper)\n    vf = verifier(g, chat)\n    g.add(\n        g.edge_from(g.start_node).to(cp),\n        g.edge_from(cp).map().to(qg),\n        g.edge_from(qg).to(rt),\n        g.edge_from(rt).to(vf),\n        g.edge_from(vf).collect().to(g.end_node),\n    )\n    return g.build()\n</code></pre>"},{"location":"reference/components/nodes/factool/","title":"Factool nodes","text":"<p>Factool nodes: the Factool knowledge-QA components, prebuilt as graph nodes.</p> <p>Each factory builds a Factool component and lifts it onto a graph as a node, doing the small wiring a step needs: unwrapping the node's input and forwarding the streaming hook. Wire the returned nodes with the graph API, and mix them with other papers' nodes freely.</p>"},{"location":"reference/components/nodes/factool/#openfactcheck.components.nodes.factool.PROVENANCE","title":"PROVENANCE  <code>module-attribute</code>","text":"<pre><code>PROVENANCE = Provenance(\n    paper_title=\"FacTool: Factuality Detection in Generative AI - A Tool Augmented Framework for Multi-Task and Multi-Domain Scenarios\",\n    paper_url=\"https://arxiv.org/abs/2307.13528\",\n    citation=_CITATION,\n    repository_url=\"https://github.com/GAIR-NLP/factool\",\n    repository_commit=\"3f3914bc090b644be044b7e0005113c135d8b20f\",\n    license=\"Apache-2.0\",\n    default_model=\"gpt-4o-mini\",\n    paper_models=(\"gpt-3.5-turbo-0301\", \"gpt-4-0314\"),\n)\n</code></pre> <p>Provenance for the Factool knowledge-QA components.</p>"},{"location":"reference/components/nodes/factool/#openfactcheck.components.nodes.factool.PIPELINE","title":"PIPELINE  <code>module-attribute</code>","text":"<pre><code>PIPELINE = Pipeline(build_graph, default_model)\n</code></pre> <p>The Factool pipeline, discovered through the <code>openfactcheck.pipelines</code> entry point.</p>"},{"location":"reference/components/nodes/factool/#openfactcheck.components.nodes.factool.claim_processor","title":"claim_processor","text":"<pre><code>claim_processor(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factool/claim_processor\",\n) -&gt; Step[Input, list[Claim], Any, Any]\n</code></pre> <p>Build Factool's claim processor and lift it onto <code>g</code> as a node: input text in, atomic claims out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>chat</code> <p>Chat client backing the component.</p> <p> TYPE: <code>ChatClient</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'factool/claim_processor'</code> </p> Source code in <code>src/openfactcheck/components/nodes/factool.py</code> <pre><code>def claim_processor(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factool/claim_processor\",\n) -&gt; Step[Input, list[Claim], Any, Any]:\n    \"\"\"Build Factool's claim processor and lift it onto ``g`` as a node: input text in, atomic claims out.\n\n    Args:\n        g: The builder to register the node onto.\n        chat: Chat client backing the component.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = FactoolClaimProcessor(client=chat)\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Input, Any, Any]) -&gt; list[Claim]:\n        return await component(ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factool/#openfactcheck.components.nodes.factool.query_generator","title":"query_generator","text":"<pre><code>query_generator(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factool/query_generator\",\n) -&gt; Step[Claim, Query, Any, Any]\n</code></pre> <p>Build Factool's query generator and lift it onto <code>g</code> as a node: one claim in, its search query out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>chat</code> <p>Chat client backing the component.</p> <p> TYPE: <code>ChatClient</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'factool/query_generator'</code> </p> Source code in <code>src/openfactcheck/components/nodes/factool.py</code> <pre><code>def query_generator(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factool/query_generator\",\n) -&gt; Step[Claim, Query, Any, Any]:\n    \"\"\"Build Factool's query generator and lift it onto ``g`` as a node: one claim in, its search query out.\n\n    Args:\n        g: The builder to register the node onto.\n        chat: Chat client backing the component.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = FactoolQueryGenerator(client=chat)\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Claim, Any, Any]) -&gt; Query:\n        return await component(ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factool/#openfactcheck.components.nodes.factool.retriever","title":"retriever","text":"<pre><code>retriever(\n    g: AnyGraphBuilder,\n    serper: SerperClient | None = None,\n    *,\n    node_id: str = \"factool/retriever\",\n) -&gt; Step[Query, Evidence, Any, Any]\n</code></pre> <p>Build Factool's retriever and lift it onto <code>g</code> as a node: a query in, evidence out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>serper</code> <p>Web-search client for the retriever. Defaults to a client that reads its key from the environment.</p> <p> TYPE: <code>SerperClient | None</code> DEFAULT: <code>None</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'factool/retriever'</code> </p> Source code in <code>src/openfactcheck/components/nodes/factool.py</code> <pre><code>def retriever(\n    g: AnyGraphBuilder,\n    serper: SerperClient | None = None,\n    *,\n    node_id: str = \"factool/retriever\",\n) -&gt; Step[Query, Evidence, Any, Any]:\n    \"\"\"Build Factool's retriever and lift it onto ``g`` as a node: a query in, evidence out.\n\n    Args:\n        g: The builder to register the node onto.\n        serper: Web-search client for the retriever. Defaults to a client that reads its key from the\n            environment.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = FactoolRetriever(serper=serper if serper is not None else SerperClient())\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Query, Any, Any]) -&gt; Evidence:\n        return await component(ctx.inputs)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factool/#openfactcheck.components.nodes.factool.verifier","title":"verifier","text":"<pre><code>verifier(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factool/verifier\",\n) -&gt; Step[Evidence, Verdict, Any, Any]\n</code></pre> <p>Build Factool's verifier and lift it onto <code>g</code> as a node: evidence in, a verdict out.</p> <p>The node's input is the <code>Evidence</code>, which carries the claim it bears on, so the verifier is called with that claim and evidence.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>chat</code> <p>Chat client backing the component.</p> <p> TYPE: <code>ChatClient</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'factool/verifier'</code> </p> Source code in <code>src/openfactcheck/components/nodes/factool.py</code> <pre><code>def verifier(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"factool/verifier\",\n) -&gt; Step[Evidence, Verdict, Any, Any]:\n    \"\"\"Build Factool's verifier and lift it onto ``g`` as a node: evidence in, a verdict out.\n\n    The node's input is the [`Evidence`][Evidence], which carries the claim it\n    bears on, so the verifier is called with that claim and evidence.\n\n    Args:\n        g: The builder to register the node onto.\n        chat: Chat client backing the component.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = FactoolVerifier(client=chat)\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Evidence, Any, Any]) -&gt; Verdict:\n        return await component(ctx.inputs.claim, ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/factool/#openfactcheck.components.nodes.factool.build_graph","title":"build_graph","text":"<pre><code>build_graph(\n    *, chat: ChatClient, serper: SerperClient | None = None\n) -&gt; Graph[Input, list[Verdict], None, None]\n</code></pre> <p>Wire Factool's knowledge-QA pipeline into a runnable graph.</p> <p>Extracts claims, then for each claim in parallel generates a query, retrieves evidence, and verifies, collecting the per-claim verdicts at the end.</p> PARAMETER DESCRIPTION <code>chat</code> <p>Chat client backing the claim processor, query generator, and verifier.</p> <p> TYPE: <code>ChatClient</code> </p> <code>serper</code> <p>Web-search client for the retriever. Defaults to a client that reads its key from the environment.</p> <p> TYPE: <code>SerperClient | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/components/nodes/factool.py</code> <pre><code>def build_graph(*, chat: ChatClient, serper: SerperClient | None = None) -&gt; Graph[Input, list[Verdict], None, None]:\n    \"\"\"Wire Factool's knowledge-QA pipeline into a runnable graph.\n\n    Extracts claims, then for each claim in parallel generates a query, retrieves evidence, and verifies,\n    collecting the per-claim verdicts at the end.\n\n    Args:\n        chat: Chat client backing the claim processor, query generator, and verifier.\n        serper: Web-search client for the retriever. Defaults to a client that reads its key from the\n            environment.\n\n    Returns:\n        A graph from input text to the list of per-claim verdicts.\n    \"\"\"\n    g: GraphBuilder[Input, list[Verdict], None, None] = GraphBuilder(\n        input_type=Input, output_type=list[Verdict], name=\"factool\"\n    )\n    cp = claim_processor(g, chat)\n    qg = query_generator(g, chat)\n    rt = retriever(g, serper)\n    vf = verifier(g, chat)\n    g.add(\n        g.edge_from(g.start_node).to(cp),\n        g.edge_from(cp).map().to(qg),\n        g.edge_from(qg).to(rt),\n        g.edge_from(rt).to(vf),\n        g.edge_from(vf).collect().to(g.end_node),\n    )\n    return g.build()\n</code></pre>"},{"location":"reference/components/nodes/rarr/","title":"RARR nodes","text":"<p>RARR nodes: the research-and-revise stages, prebuilt as graph nodes.</p> <p>RARR works on a whole passage rather than atomic claims, and its retrieve-check-edit loop is a cycle, so its nodes differ from the linear factories:</p> <ul> <li><code>claim_processor</code> emits a single claim (the whole   passage), so it is wired without a fan-out.</li> <li><code>query_generator</code> lifts RARR's query generator.</li> <li><code>retriever_verifier_loop</code> is a subgraph: it   retrieves evidence per question, then loops over each piece, checking the passage against it and editing   the passage to agree when it disagrees. It plugs into a graph as one node.</li> </ul>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.DEFAULT_MAX_REVISION_STEPS","title":"DEFAULT_MAX_REVISION_STEPS  <code>module-attribute</code>","text":"<pre><code>DEFAULT_MAX_REVISION_STEPS = 200\n</code></pre> <p>Safety cap on the revision loop. The loop normally stops once all evidence is processed.</p>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.PIPELINE","title":"PIPELINE  <code>module-attribute</code>","text":"<pre><code>PIPELINE = Pipeline(build_graph, default_model)\n</code></pre> <p>The RARR pipeline, discovered through the <code>openfactcheck.pipelines</code> entry point.</p>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.ResearchResult","title":"ResearchResult  <code>dataclass</code>","text":"<pre><code>ResearchResult(\n    passage: str,\n    pending: tuple[QuestionedSource, ...],\n    gates: tuple[Verdict, ...],\n)\n</code></pre> <p>The outcome of the retrieve-and-revise loop: the revised passage and the checks behind it.</p>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.ResearchResult.passage","title":"passage  <code>instance-attribute</code>","text":"<pre><code>passage: str\n</code></pre> <p>The passage after every disagreement found has been edited in.</p>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.ResearchResult.pending","title":"pending  <code>instance-attribute</code>","text":"<pre><code>pending: tuple[QuestionedSource, ...]\n</code></pre> <p>The <code>(question, evidence)</code> pairs still to check; empty once the loop has finished.</p>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.ResearchResult.gates","title":"gates  <code>instance-attribute</code>","text":"<pre><code>gates: tuple[Verdict, ...]\n</code></pre> <p>The agreement check recorded for each processed pair, in order.</p>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.claim_processor","title":"claim_processor","text":"<pre><code>claim_processor(\n    g: AnyGraphBuilder,\n    *,\n    node_id: str = \"rarr/claim_processor\",\n) -&gt; Step[Input, Claim, Any, Any]\n</code></pre> <p>Lift RARR's claim processor onto <code>g</code> as a node: input text in, one claim out.</p> <p>RARR treats the whole passage as a single claim, so this node emits one claim rather than a list, and is wired without a <code>map</code> fan-out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>node_id</code> <p>Identifier for the node.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'rarr/claim_processor'</code> </p> Source code in <code>src/openfactcheck/components/nodes/rarr.py</code> <pre><code>def claim_processor(\n    g: AnyGraphBuilder,\n    *,\n    node_id: str = \"rarr/claim_processor\",\n) -&gt; Step[Input, Claim, Any, Any]:\n    \"\"\"Lift RARR's claim processor onto ``g`` as a node: input text in, one claim out.\n\n    RARR treats the whole passage as a single claim, so this node emits one claim rather than a list, and is\n    wired without a ``map`` fan-out.\n\n    Args:\n        g: The builder to register the node onto.\n        node_id: Identifier for the node.\n\n    Returns:\n        The registered claim-processor node.\n    \"\"\"\n    component = RARRClaimProcessor()\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Input, Any, Any]) -&gt; Claim:\n        claims = await component(ctx.inputs)\n        return claims[0]\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.query_generator","title":"query_generator","text":"<pre><code>query_generator(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"rarr/query_generator\",\n) -&gt; Step[Claim, Query, Any, Any]\n</code></pre> <p>Build RARR's query generator and lift it onto <code>g</code> as a node: one claim in, its search query out.</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>chat</code> <p>Chat client backing the component.</p> <p> TYPE: <code>ChatClient</code> </p> <code>node_id</code> <p>Identifier for the node, used to wire edges and label events.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'rarr/query_generator'</code> </p> Source code in <code>src/openfactcheck/components/nodes/rarr.py</code> <pre><code>def query_generator(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    *,\n    node_id: str = \"rarr/query_generator\",\n) -&gt; Step[Claim, Query, Any, Any]:\n    \"\"\"Build RARR's query generator and lift it onto ``g`` as a node: one claim in, its search query out.\n\n    Args:\n        g: The builder to register the node onto.\n        chat: Chat client backing the component.\n        node_id: Identifier for the node, used to wire edges and label events.\n\n    Returns:\n        The registered node, ready to wire with [`edge_from`][openfactcheck.graph.GraphBuilder.edge_from].\n    \"\"\"\n    component = RARRQueryGenerator(client=chat)\n\n    @g.step_node(node_id=node_id)\n    async def step(ctx: StepContext[Claim, Any, Any]) -&gt; Query:\n        return await component(ctx.inputs, on_partial=ctx.emit if ctx.streaming else None)\n\n    return step\n</code></pre>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.retriever_verifier_loop","title":"retriever_verifier_loop","text":"<pre><code>retriever_verifier_loop(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    serper: SerperClient | None = None,\n    *,\n    node_id: str = \"rarr/retriever_verifier_loop\",\n    max_revision_steps: int = DEFAULT_MAX_REVISION_STEPS,\n) -&gt; Step[Query, ResearchResult, Any, Any]\n</code></pre> <p>Build RARR's retrieve-and-revise cycle as a subgraph and lift it onto <code>g</code> as one node.</p> <p>The subgraph retrieves one piece of evidence per question, then loops: it checks the current passage against the next <code>(question, evidence)</code> pair with the agreement gate and, on disagreement, edits the passage to agree before moving on, so each check sees the passage as edited so far. It returns the revised passage and the agreement check for each pair.</p> <p>The node's input passage is read from the query's claim, so feed it a query whose claim is the passage to revise (as <code>claim_processor</code> produces).</p> PARAMETER DESCRIPTION <code>g</code> <p>The builder to register the node onto.</p> <p> TYPE: <code>AnyGraphBuilder</code> </p> <code>chat</code> <p>Chat client backing the agreement gate and editor.</p> <p> TYPE: <code>ChatClient</code> </p> <code>serper</code> <p>Web-search client for retrieval. Defaults to a client that reads its key from the environment.</p> <p> TYPE: <code>SerperClient | None</code> DEFAULT: <code>None</code> </p> <code>node_id</code> <p>Identifier for the node.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'rarr/retriever_verifier_loop'</code> </p> <code>max_revision_steps</code> <p>Safety cap on revision-loop laps; the loop normally stops once all evidence is processed.</p> <p> TYPE: <code>int</code> DEFAULT: <code>DEFAULT_MAX_REVISION_STEPS</code> </p> Source code in <code>src/openfactcheck/components/nodes/rarr.py</code> <pre><code>def retriever_verifier_loop(\n    g: AnyGraphBuilder,\n    chat: ChatClient,\n    serper: SerperClient | None = None,\n    *,\n    node_id: str = \"rarr/retriever_verifier_loop\",\n    max_revision_steps: int = DEFAULT_MAX_REVISION_STEPS,\n) -&gt; Step[Query, ResearchResult, Any, Any]:\n    \"\"\"Build RARR's retrieve-and-revise cycle as a subgraph and lift it onto ``g`` as one node.\n\n    The subgraph retrieves one piece of evidence per question, then loops: it checks the current passage\n    against the next ``(question, evidence)`` pair with the agreement gate and, on disagreement, edits the\n    passage to agree before moving on, so each check sees the passage as edited so far. It returns the revised\n    passage and the agreement check for each pair.\n\n    The node's input passage is read from the query's claim, so feed it a query whose claim is the passage to\n    revise (as [`claim_processor`][claim_processor] produces).\n\n    Args:\n        g: The builder to register the node onto.\n        chat: Chat client backing the agreement gate and editor.\n        serper: Web-search client for retrieval. Defaults to a client that reads its key from the\n            environment.\n        node_id: Identifier for the node.\n        max_revision_steps: Safety cap on revision-loop laps; the loop normally stops once all evidence is\n            processed.\n\n    Returns:\n        The registered subgraph node.\n    \"\"\"\n    retrieve = RARRRetriever(serper=serper if serper is not None else SerperClient())\n    gate = RARRAgreementGate(client=chat)\n    editor = RARREditor(client=chat)\n\n    inner: GraphBuilder[Query, ResearchResult, Any, Any] = GraphBuilder(\n        input_type=Query, output_type=ResearchResult, name=\"rarr_retriever_verifier_loop\"\n    )\n\n    @inner.step_node\n    async def retriever_start(ctx: StepContext[Query, Any, Any]) -&gt; ResearchResult:\n        pairs = await retrieve(ctx.inputs)\n        return ResearchResult(passage=ctx.inputs.claim.text, pending=tuple(pairs), gates=())\n\n    @inner.step_node\n    async def reviser(ctx: StepContext[ResearchResult, Any, Any]) -&gt; ResearchResult:\n        result = ctx.inputs\n        question, source = result.pending[0]\n        verdict = await gate(result.passage, question, source, on_partial=ctx.emit if ctx.streaming else None)\n        passage = (\n            await editor(result.passage, question, source, on_partial=ctx.emit if ctx.streaming else None)\n            if verdict.label == \"refuted\"\n            else result.passage\n        )\n        return ResearchResult(passage=passage, pending=result.pending[1:], gates=(*result.gates, verdict))\n\n    more_pending = inner.decision_node(ResearchResult, node_id=\"more_pending\")\n    inner.add(\n        inner.edge_from(inner.start_node).to(retriever_start),\n        inner.edge_from(retriever_start).to(more_pending),\n        more_pending.when(lambda result: len(result.pending) &gt; 0, reviser, max_iterations=max_revision_steps),\n        more_pending.otherwise(inner.end_node),\n        inner.edge_from(reviser).to(more_pending),\n    )\n    return g.subgraph_node(inner.build(), node_id=node_id)\n</code></pre>"},{"location":"reference/components/nodes/rarr/#openfactcheck.components.nodes.rarr.build_graph","title":"build_graph","text":"<pre><code>build_graph(\n    *, chat: ChatClient, serper: SerperClient | None = None\n) -&gt; Graph[Input, ResearchResult, None, None]\n</code></pre> <p>Wire RARR's research-and-revise pipeline into a runnable graph.</p> <p>Treats the input as one passage, generates verification questions, then runs the retrieve-and-revise cycle, returning the revised passage and its agreement checks.</p> PARAMETER DESCRIPTION <code>chat</code> <p>Chat client backing the query generator, agreement gate, and editor.</p> <p> TYPE: <code>ChatClient</code> </p> <code>serper</code> <p>Web-search client for retrieval. Defaults to a client that reads its key from the environment.</p> <p> TYPE: <code>SerperClient | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/components/nodes/rarr.py</code> <pre><code>def build_graph(*, chat: ChatClient, serper: SerperClient | None = None) -&gt; Graph[Input, ResearchResult, None, None]:\n    \"\"\"Wire RARR's research-and-revise pipeline into a runnable graph.\n\n    Treats the input as one passage, generates verification questions, then runs the retrieve-and-revise\n    cycle, returning the revised passage and its agreement checks.\n\n    Args:\n        chat: Chat client backing the query generator, agreement gate, and editor.\n        serper: Web-search client for retrieval. Defaults to a client that reads its key from the\n            environment.\n\n    Returns:\n        A graph from input text to the research result.\n    \"\"\"\n    g: GraphBuilder[Input, ResearchResult, None, None] = GraphBuilder(\n        input_type=Input, output_type=ResearchResult, name=\"rarr\"\n    )\n    cp = claim_processor(g)\n    qg = query_generator(g, chat)\n    loop = retriever_verifier_loop(g, chat, serper)\n    g.add(\n        g.edge_from(g.start_node).to(cp),\n        g.edge_from(cp).to(qg),\n        g.edge_from(qg).to(loop),\n        g.edge_from(loop).to(g.end_node),\n    )\n    return g.build()\n</code></pre>"},{"location":"reference/graph/","title":"Graph","text":"<p>Public API for the graph layer.</p> <p>Build a graph of typed async nodes connected by edges, then run it to completion. Assemble the graph with a <code>GraphBuilder</code>, wire steps with edges, call <code>build</code>, and run the result with <code>Graph.run</code> or its async peer <code>Graph.arun</code>. Failures raise <code>GraphError</code> subclasses.</p> <p>Import everything from <code>openfactcheck.graph</code> directly; submodule paths are not part of the public API.</p> Example <pre><code>from openfactcheck.graph import GraphBuilder, StepContext\n\ng = GraphBuilder(input_type=str, output_type=dict)\n\n\n@g.step_node\nasync def extract(ctx: StepContext[str]) -&gt; list[str]:\n    return ctx.inputs.split()\n\n\n@g.step_node\nasync def count(ctx: StepContext[list[str]]) -&gt; dict:\n    return {\"n\": len(ctx.inputs)}\n\n\ng.add(\n    g.edge_from(g.start_node).to(extract),\n    g.edge_from(extract).to(count),\n    g.edge_from(count).to(g.end_node),\n)\nresult = g.build().run(\"a b c\", state=None, deps=None)\n</code></pre>"},{"location":"reference/graph/#pages","title":"Pages","text":"<ul> <li>Builder \u2014 assemble and validate a graph from steps and edges.</li> <li>Graph and execution \u2014 the runnable graph, run options, streaming, stepping, and resume.</li> <li>Steps \u2014 typed nodes, edges, and the per-invocation context.</li> <li>Joins and reducers \u2014 fan-in nodes and the reducers that combine branch outputs.</li> <li>Decisions \u2014 conditional branching to one of several downstream paths.</li> <li>Pause \u2014 suspend a run to await human input.</li> <li>Events \u2014 progress events emitted while a run executes.</li> <li>Forks \u2014 fork-stack identity for fanned-out tasks.</li> <li>Errors \u2014 the graph error hierarchy.</li> <li>Persistence \u2014 snapshot stores and resume.</li> <li>Mermaid \u2014 render a graph as a diagram.</li> </ul>"},{"location":"reference/graph/builder/","title":"Builder","text":"<p>Assemble and validate a typed graph from steps and edges.</p> <p><code>GraphBuilder</code> collects work-bearing <code>Step</code> nodes and the edges between them, then <code>build</code> validates the structure and returns a runnable <code>Graph</code>.</p> Example <pre><code>g = GraphBuilder(input_type=str, output_type=dict)\n\n\n@g.step_node\nasync def extract(ctx: StepContext[str]) -&gt; list[str]:\n    return ctx.inputs.split()\n\n\n@g.step_node\nasync def count(ctx: StepContext[list[str]]) -&gt; dict:\n    return {\"n\": len(ctx.inputs)}\n\n\ng.add(\n    g.edge_from(g.start_node).to(extract),\n    g.edge_from(extract).to(count),\n    g.edge_from(count).to(g.end_node),\n)\ngraph = g.build()\nresult = graph.run(\"a b c\", state=None, deps=None)\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.AnyGraphBuilder","title":"AnyGraphBuilder","text":"<pre><code>AnyGraphBuilder = GraphBuilder[Any, Any, Any, Any]\n</code></pre> <p>A graph builder with its type parameters left open.</p> <p>For helpers that register a node onto any builder without reading the run's state or dependencies; the registered node is still typed on the value it carries, so edges stay type-checked.</p>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.EdgePathBuilder","title":"EdgePathBuilder  <code>dataclass</code>","text":"<pre><code>EdgePathBuilder(\n    source_id: str,\n    kind: EdgeKind = EdgeKind.PLAIN,\n    join: _InlineJoinSpec | None = None,\n)\n</code></pre> <p>A partially-built edge, awaiting its destination.</p> <p>Returned by <code>GraphBuilder.edge_from</code>, carrying the output type of its source node so <code>to</code> only accepts a destination whose input type the source can feed. A mismatched destination is a type error, caught before the graph is built.</p>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.EdgePathBuilder.source_id","title":"source_id  <code>instance-attribute</code>","text":"<pre><code>source_id: str\n</code></pre> <p>Identifier of the node the edge will leave.</p>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.EdgePathBuilder.kind","title":"kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>kind: EdgeKind = PLAIN\n</code></pre> <p>Whether the edge delivers the whole output or fans an iterable per item.</p>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.EdgePathBuilder.join","title":"join  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>join: _InlineJoinSpec | None = None\n</code></pre> <p>A fan-in folded onto this edge by <code>collect</code> or <code>reduce</code>.</p>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.EdgePathBuilder.map","title":"map","text":"<pre><code>map() -&gt; EdgePathBuilder[ItemT, StateT, DepsT]\n</code></pre> <p>Fan the source's iterable output out to one parallel branch per item.</p> <p>Available only when the source node's output is iterable. The chosen destination then receives a single item, and a downstream <code>Join</code> collects the per-item results. Run concurrency is bounded by <code>Graph.arun</code>'s <code>concurrency</code> limit.</p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def map[ItemT](\n    self: EdgePathBuilder[Iterable[ItemT], StateT, DepsT],\n) -&gt; EdgePathBuilder[ItemT, StateT, DepsT]:\n    \"\"\"Fan the source's iterable output out to one parallel branch per item.\n\n    Available only when the source node's output is iterable. The chosen\n    destination then receives a single item, and a downstream\n    [`Join`][Join] collects the per-item results. Run concurrency is bounded\n    by [`Graph.arun`][Graph.arun]'s ``concurrency`` limit.\n\n    Returns:\n        A builder over the collection's item type whose\n        [`to`][EdgePathBuilder.to] names the per-item destination.\n    \"\"\"\n    return EdgePathBuilder(self.source_id, kind=EdgeKind.MAP)\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.EdgePathBuilder.collect","title":"collect","text":"<pre><code>collect() -&gt; EdgePathBuilder[\n    list[CollectItemT], StateT, DepsT\n]\n</code></pre> <p>Gather a fork's branch outputs into one ordered list, inline on the edge.</p> <p>The mirror of <code>map</code>: where <code>map</code> fans an iterable out to one branch per item, <code>collect</code> folds those branches back into a single list in source order and delivers it to the destination. It builds the fan-in join for you, so the destination receives <code>list[item]</code> with no separately declared <code>collect_node</code>; reach for that explicit form when a join gathers several distinct inbound edges.</p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def collect[CollectItemT](\n    self: EdgePathBuilder[CollectItemT, StateT, DepsT],\n) -&gt; EdgePathBuilder[list[CollectItemT], StateT, DepsT]:\n    \"\"\"Gather a fork's branch outputs into one ordered list, inline on the edge.\n\n    The mirror of [`map`][EdgePathBuilder.map]: where ``map`` fans an\n    iterable out to one branch per item, ``collect`` folds those branches\n    back into a single list in source order and delivers it to the\n    destination. It builds the fan-in join for you, so the destination\n    receives ``list[item]`` with no separately declared\n    [`collect_node`][GraphBuilder.collect_node]; reach for that explicit form\n    when a join gathers several distinct inbound edges.\n\n    Returns:\n        A builder over the collected list type whose\n        [`to`][EdgePathBuilder.to] names the destination that receives it.\n    \"\"\"\n    return EdgePathBuilder(\n        self.source_id,\n        join=_InlineJoinSpec(verb=\"collect\", reducer=reduce_list_append, initial_factory=list, ordered=True),\n    )\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.EdgePathBuilder.reduce","title":"reduce","text":"<pre><code>reduce(\n    reducer: Reducer[AccT, ReduceItemT]\n    | ContextReducer[StateT, DepsT, AccT, ReduceItemT],\n    initial_factory: Callable[[], AccT],\n) -&gt; EdgePathBuilder[AccT, StateT, DepsT]\n</code></pre> <p>Fold a fork's branch outputs with a reducer, inline on the edge.</p> <p>The general fan-in that <code>collect</code> specializes: each branch's value folds into a running accumulator, and the final accumulator is delivered to the destination. It builds the fan-in join for you; reach for <code>reduce_node</code> when a join gathers several distinct inbound edges.</p> PARAMETER DESCRIPTION <code>reducer</code> <p>The fold applied to each branch's value, taking the running accumulator and one value, optionally preceded by a <code>ReducerContext</code>.</p> <p> TYPE: <code>Reducer[AccT, ReduceItemT] | ContextReducer[StateT, DepsT, AccT, ReduceItemT]</code> </p> <code>initial_factory</code> <p>Builds the accumulator each time the join fires.</p> <p> TYPE: <code>Callable[[], AccT]</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def reduce[ReduceItemT, AccT](\n    self: EdgePathBuilder[ReduceItemT, StateT, DepsT],\n    reducer: Reducer[AccT, ReduceItemT] | ContextReducer[StateT, DepsT, AccT, ReduceItemT],\n    initial_factory: Callable[[], AccT],\n) -&gt; EdgePathBuilder[AccT, StateT, DepsT]:\n    \"\"\"Fold a fork's branch outputs with a reducer, inline on the edge.\n\n    The general fan-in that [`collect`][EdgePathBuilder.collect] specializes:\n    each branch's value folds into a running accumulator, and the final\n    accumulator is delivered to the destination. It builds the fan-in join\n    for you; reach for [`reduce_node`][GraphBuilder.reduce_node] when a join\n    gathers several distinct inbound edges.\n\n    Args:\n        reducer: The fold applied to each branch's value, taking the running\n            accumulator and one value, optionally preceded by a\n            [`ReducerContext`][ReducerContext].\n        initial_factory: Builds the accumulator each time the join fires.\n\n    Returns:\n        A builder over the accumulator type whose\n        [`to`][EdgePathBuilder.to] names the destination that receives it.\n    \"\"\"\n    return EdgePathBuilder(\n        self.source_id,\n        join=_InlineJoinSpec(verb=\"reduce\", reducer=reducer, initial_factory=initial_factory, ordered=False),\n    )\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.EdgePathBuilder.to","title":"to","text":"<pre><code>to(\n    dest: DestNode[OutputT, StateT, DepsT],\n) -&gt; Edge | _InlineJoinEdge\n</code></pre> <p>Complete the edge by naming its destination node.</p> PARAMETER DESCRIPTION <code>dest</code> <p>The node the edge enters; its input type must accept the source node's output type.</p> <p> TYPE: <code>DestNode[OutputT, StateT, DepsT]</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def to(self, dest: DestNode[OutputT, StateT, DepsT]) -&gt; Edge | _InlineJoinEdge:\n    \"\"\"Complete the edge by naming its destination node.\n\n    Args:\n        dest: The node the edge enters; its input type must accept the\n            source node's output type.\n\n    Returns:\n        The finished edge from the recorded source to ``dest``, or, when\n        [`collect`][EdgePathBuilder.collect] or [`reduce`][EdgePathBuilder.reduce]\n        folded a fan-in onto this edge, a token the builder expands into the\n        inline join and its two edges.\n    \"\"\"\n    if self.join is not None:\n        return _InlineJoinEdge(source_id=self.source_id, dest_id=dest.id, spec=self.join)\n    return Edge(source_id=self.source_id, dest_id=dest.id, kind=self.kind)\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder","title":"GraphBuilder","text":"<pre><code>GraphBuilder(\n    *,\n    input_type: type[InputT] | None = None,\n    output_type: type[OutputT] | None = None,\n    state_type: type[StateT] | None = None,\n    deps_type: type[DepsT] | None = None,\n    name: str = \"graph\",\n)\n</code></pre> <p>               Bases: <code>Generic[InputT, OutputT, StateT, DepsT]</code></p> <p>Collect steps and edges, then build a validated <code>Graph</code>.</p> <p>Construct it the named way, <code>GraphBuilder(input_type=..., output_type=...)</code>, or with positional type parameters, <code>GraphBuilder[Input, Output]</code>. The type parameters, in order, are:</p> <ol> <li><code>InputT</code>: the value the built graph accepts.</li> <li><code>OutputT</code>: the value the built graph returns.</li> <li><code>StateT</code>: run-scoped mutable state shared across every node. Optional;    defaults to <code>None</code> (no shared state).</li> <li><code>DepsT</code>: read-only dependencies injected into every node (clients,    configuration). Optional; defaults to <code>None</code> (no deps).</li> </ol> <p><code>StateT</code> and <code>DepsT</code> flow unchanged into every <code>StepContext</code>.</p> Example <pre><code># str in, a dict out, no shared state, a Deps bag of clients.\ng = GraphBuilder(input_type=str, output_type=dict[str, int], deps_type=Deps)\n# the positional form is equivalent: GraphBuilder[str, dict[str, int], None, Deps]\n</code></pre> <p>Start an empty builder.</p> <p>The four <code>*_type</code> arguments are optional and bind the builder's type parameters by name; <code>state_type</code> and <code>deps_type</code> default to <code>None</code>. They are recorded for introspection and are not required at run time.</p> PARAMETER DESCRIPTION <code>input_type</code> <p>The type the built graph accepts.</p> <p> TYPE: <code>type[InputT] | None</code> DEFAULT: <code>None</code> </p> <code>output_type</code> <p>The type the built graph returns.</p> <p> TYPE: <code>type[OutputT] | None</code> DEFAULT: <code>None</code> </p> <code>state_type</code> <p>The run-scoped state type, or <code>None</code> for no state.</p> <p> TYPE: <code>type[StateT] | None</code> DEFAULT: <code>None</code> </p> <code>deps_type</code> <p>The injected dependencies type, or <code>None</code> for no deps.</p> <p> TYPE: <code>type[DepsT] | None</code> DEFAULT: <code>None</code> </p> <code>name</code> <p>Human-readable name carried onto the built graph for diagrams and logs.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'graph'</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def __init__(\n    self,\n    *,\n    input_type: type[InputT] | None = None,\n    output_type: type[OutputT] | None = None,\n    state_type: type[StateT] | None = None,\n    deps_type: type[DepsT] | None = None,\n    name: str = \"graph\",\n) -&gt; None:\n    \"\"\"Start an empty builder.\n\n    The four ``*_type`` arguments are optional and bind the builder's type\n    parameters by name; ``state_type`` and ``deps_type`` default to ``None``.\n    They are recorded for introspection and are not required at run time.\n\n    Args:\n        input_type: The type the built graph accepts.\n        output_type: The type the built graph returns.\n        state_type: The run-scoped state type, or ``None`` for no state.\n        deps_type: The injected dependencies type, or ``None`` for no deps.\n        name: Human-readable name carried onto the built graph for\n            diagrams and logs.\n    \"\"\"\n    self._name = name\n    self._input_type = input_type\n    self._output_type = output_type\n    self._state_type = state_type\n    self._deps_type = deps_type\n    self._steps: dict[str, AnyStep] = {}\n    self._joins: dict[str, AnyJoin] = {}\n    self._decisions: dict[str, AnyDecision] = {}\n    self._pauses: dict[str, AnyPause] = {}\n    self._subgraphs: dict[str, GraphSpec] = {}\n    self._edges: list[Edge] = []\n    self._branches: list[Branch] = []\n    self.start_node: StartNode[InputT] = StartNode()\n    self.end_node: EndNode[OutputT] = EndNode()\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.step_node","title":"step_node","text":"<pre><code>step_node(\n    call: Callable[\n        [StepContext[StepInputT, StateT, DepsT]],\n        Awaitable[StepOutputT],\n    ],\n    *,\n    node_id: str | None = None,\n    retries: int = 0,\n    retry_backoff: float = 0.0,\n    timeout: float | None = None,\n) -&gt; Step[StepInputT, StepOutputT, StateT, DepsT]\n</code></pre><pre><code>step_node(\n    call: None = None,\n    *,\n    node_id: str | None = None,\n    retries: int = 0,\n    retry_backoff: float = 0.0,\n    timeout: float | None = None,\n) -&gt; Callable[\n    [\n        Callable[\n            [StepContext[StepInputT, StateT, DepsT]],\n            Awaitable[StepOutputT],\n        ]\n    ],\n    Step[StepInputT, StepOutputT, StateT, DepsT],\n]\n</code></pre> <pre><code>step_node(\n    call: Callable[\n        [StepContext[StepInputT, StateT, DepsT]],\n        Awaitable[StepOutputT],\n    ]\n    | None = None,\n    *,\n    node_id: str | None = None,\n    retries: int = 0,\n    retry_backoff: float = 0.0,\n    timeout: float | None = None,\n) -&gt; (\n    Step[StepInputT, StepOutputT, StateT, DepsT]\n    | Callable[\n        [\n            Callable[\n                [StepContext[StepInputT, StateT, DepsT]],\n                Awaitable[StepOutputT],\n            ]\n        ],\n        Step[StepInputT, StepOutputT, StateT, DepsT],\n    ]\n)\n</code></pre> <p>Register an async function as a node and return it for wiring.</p> <p>Apply it as a bare decorator, <code>@g.step_node</code> (the node's id is the function's name); as a decorator with options, <code>@g.step_node(node_id=\"verify\", retries=3)</code>; or call it directly, <code>g.step_node(fn, node_id=\"verify\")</code>. The input and output types are read from the function's annotations for build-time edge validation.</p> PARAMETER DESCRIPTION <code>call</code> <p>The async function to register, or <code>None</code> to return a decorator that registers the function it is applied to.</p> <p> TYPE: <code>Callable[[StepContext[StepInputT, StateT, DepsT]], Awaitable[StepOutputT]] | None</code> DEFAULT: <code>None</code> </p> <code>node_id</code> <p>Identifier for the node; defaults to the function's name.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>retries</code> <p>Extra attempts after the first if the call fails.</p> <p> TYPE: <code>int</code> DEFAULT: <code>0</code> </p> <code>retry_backoff</code> <p>Base seconds before a retry; doubles each attempt.</p> <p> TYPE: <code>float</code> DEFAULT: <code>0.0</code> </p> <code>timeout</code> <p>Seconds a single attempt may run, or <code>None</code> for no limit.</p> <p> TYPE: <code>float | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def step_node[StepInputT, StepOutputT](\n    self,\n    call: Callable[[StepContext[StepInputT, StateT, DepsT]], Awaitable[StepOutputT]] | None = None,\n    *,\n    node_id: str | None = None,\n    retries: int = 0,\n    retry_backoff: float = 0.0,\n    timeout: float | None = None,\n) -&gt; (\n    Step[StepInputT, StepOutputT, StateT, DepsT]\n    | Callable[\n        [Callable[[StepContext[StepInputT, StateT, DepsT]], Awaitable[StepOutputT]]],\n        Step[StepInputT, StepOutputT, StateT, DepsT],\n    ]\n):\n    \"\"\"Register an async function as a node and return it for wiring.\n\n    Apply it as a bare decorator, ``@g.step_node`` (the node's id is the\n    function's name); as a decorator with options,\n    ``@g.step_node(node_id=\"verify\", retries=3)``; or call it directly,\n    ``g.step_node(fn, node_id=\"verify\")``. The input and output types are\n    read from the function's annotations for build-time edge validation.\n\n    Args:\n        call: The async function to register, or ``None`` to return a\n            decorator that registers the function it is applied to.\n        node_id: Identifier for the node; defaults to the function's name.\n        retries: Extra attempts after the first if the call fails.\n        retry_backoff: Base seconds before a retry; doubles each attempt.\n        timeout: Seconds a single attempt may run, or ``None`` for no limit.\n\n    Returns:\n        The registered [`Step`][Step] when ``call`` is given, otherwise a\n        decorator that registers its function and returns the step.\n\n    Raises:\n        GraphBuildError: If a step with the same id is already registered,\n            or an option is out of range.\n    \"\"\"\n    if retries &lt; 0 or retry_backoff &lt; 0:\n        raise GraphBuildError(\"retries and retry_backoff must be non-negative\")\n    if timeout is not None and timeout &lt;= 0:\n        raise GraphBuildError(\"timeout must be positive when set\")\n\n    def register(\n        fn: Callable[[StepContext[StepInputT, StateT, DepsT]], Awaitable[StepOutputT]],\n    ) -&gt; Step[StepInputT, StepOutputT, StateT, DepsT]:\n        input_type, output_type = self._io_types(fn)\n        node = Step(\n            id=node_id or fn.__name__,\n            call=fn,\n            input_type=input_type,\n            output_type=output_type,\n            retries=retries,\n            retry_backoff=retry_backoff,\n            timeout=timeout,\n        )\n        if self._id_taken(node.id):\n            raise GraphBuildError(f\"duplicate node id {node.id!r}\")\n        self._steps[node.id] = node\n        return node\n\n    if call is None:\n        return register\n    return register(call)\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.subgraph_node","title":"subgraph_node","text":"<pre><code>subgraph_node(\n    graph: Graph[SubInputT, SubOutputT, StateT, DepsT],\n    *,\n    node_id: str,\n) -&gt; Step[SubInputT, SubOutputT, StateT, DepsT]\n</code></pre> <p>Wrap a built graph as a node that runs it and forwards its output.</p> <p>The inner graph runs with the outer run's state and dependencies, so its state and dependency types must match this builder's. Wire the returned node like any other; its input and output types are the inner graph's.</p> PARAMETER DESCRIPTION <code>graph</code> <p>A built <code>Graph</code> to embed as a node.</p> <p> TYPE: <code>Graph[SubInputT, SubOutputT, StateT, DepsT]</code> </p> <code>node_id</code> <p>Identifier for the node; must be unique within the graph.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def subgraph_node[SubInputT, SubOutputT](\n    self,\n    graph: Graph[SubInputT, SubOutputT, StateT, DepsT],\n    *,\n    node_id: str,\n) -&gt; Step[SubInputT, SubOutputT, StateT, DepsT]:\n    \"\"\"Wrap a built graph as a node that runs it and forwards its output.\n\n    The inner graph runs with the outer run's state and dependencies, so its\n    state and dependency types must match this builder's. Wire the returned\n    node like any other; its input and output types are the inner graph's.\n\n    Args:\n        graph: A built [`Graph`][Graph] to embed as a node.\n        node_id: Identifier for the node; must be unique within the graph.\n\n    Returns:\n        A [`Step`][Step] that runs the inner graph.\n\n    Raises:\n        GraphBuildError: If the chosen node id is already in use.\n    \"\"\"\n\n    async def run_subgraph(ctx: StepContext[SubInputT, StateT, DepsT]) -&gt; SubOutputT:\n        \"\"\"Run the embedded graph with the outer run's state and dependencies.\"\"\"\n        return await graph.arun(ctx.inputs, state=ctx.state, deps=ctx.deps)\n\n    node: Step[SubInputT, SubOutputT, StateT, DepsT] = Step(\n        id=node_id,\n        call=run_subgraph,\n        input_type=None,\n        output_type=None,\n    )\n    if self._id_taken(node.id):\n        raise GraphBuildError(f\"duplicate node id {node.id!r}\")\n    self._steps[node.id] = node\n    self._subgraphs[node_id] = graph.spec\n    return node\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.collect_node","title":"collect_node","text":"<pre><code>collect_node(\n    item_type: type[ItemT], *, node_id: str\n) -&gt; Join[ItemT, list[ItemT]]\n</code></pre> <p>Create a fan-in node that gathers each branch's output into an ordered list.</p> <p>Wire it as the destination of a fanned-out subpath (the <code>map</code> target's successor) and as the source of the edge carrying the collected list. The list preserves the source collection's order regardless of the order branches finish in.</p> PARAMETER DESCRIPTION <code>item_type</code> <p>The type of each branch's value, recorded for build-time edge validation.</p> <p> TYPE: <code>type[ItemT]</code> </p> <code>node_id</code> <p>Identifier for the join node; must be unique within the graph.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def collect_node[ItemT](self, item_type: type[ItemT], *, node_id: str) -&gt; Join[ItemT, list[ItemT]]:\n    \"\"\"Create a fan-in node that gathers each branch's output into an ordered list.\n\n    Wire it as the destination of a fanned-out subpath (the\n    [`map`][EdgePathBuilder.map] target's successor) and as the source of\n    the edge carrying the collected list. The list preserves the source\n    collection's order regardless of the order branches finish in.\n\n    Args:\n        item_type: The type of each branch's value, recorded for build-time\n            edge validation.\n        node_id: Identifier for the join node; must be unique within the graph.\n\n    Returns:\n        The registered [`Join`][Join] node, collecting into a list.\n\n    Raises:\n        GraphBuildError: If the chosen node id is already in use.\n    \"\"\"\n    return self._add_join(\n        item_type=item_type,\n        reducer=self._normalize_reducer(reduce_list_append),\n        initial_factory=list,\n        ordered=True,\n        verb=\"collect\",\n        node_id=node_id,\n    )\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.reduce_node","title":"reduce_node","text":"<pre><code>reduce_node(\n    reducer: Reducer[AccT, ItemT]\n    | ContextReducer[StateT, DepsT, AccT, ItemT],\n    initial_factory: Callable[[], AccT],\n    *,\n    item_type: type[ItemT],\n    node_id: str,\n) -&gt; Join[ItemT, AccT]\n</code></pre> <p>Create a fan-in node that folds each branch's output with a reducer.</p> <p>The reducer takes the running accumulator and one branch's value and returns the next accumulator, or takes a <code>ReducerContext</code> first to read run state and stop early. Branches are folded in the order they finish, so a reducer should not depend on order; use <code>collect_node</code> when order matters.</p> PARAMETER DESCRIPTION <code>reducer</code> <p>The fold applied to each branch's value.</p> <p> TYPE: <code>Reducer[AccT, ItemT] | ContextReducer[StateT, DepsT, AccT, ItemT]</code> </p> <code>initial_factory</code> <p>Builds the accumulator each time the join fires.</p> <p> TYPE: <code>Callable[[], AccT]</code> </p> <code>item_type</code> <p>The type of each branch's value, recorded for build-time edge validation.</p> <p> TYPE: <code>type[ItemT]</code> </p> <code>node_id</code> <p>Identifier for the join node; must be unique within the graph.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def reduce_node[ItemT, AccT](\n    self,\n    reducer: Reducer[AccT, ItemT] | ContextReducer[StateT, DepsT, AccT, ItemT],\n    initial_factory: Callable[[], AccT],\n    *,\n    item_type: type[ItemT],\n    node_id: str,\n) -&gt; Join[ItemT, AccT]:\n    \"\"\"Create a fan-in node that folds each branch's output with a reducer.\n\n    The reducer takes the running accumulator and one branch's value and\n    returns the next accumulator, or takes a\n    [`ReducerContext`][ReducerContext] first to read run state and stop\n    early. Branches are folded in the order they finish, so a reducer should\n    not depend on order; use [`collect_node`][GraphBuilder.collect_node] when\n    order matters.\n\n    Args:\n        reducer: The fold applied to each branch's value.\n        initial_factory: Builds the accumulator each time the join fires.\n        item_type: The type of each branch's value, recorded for build-time\n            edge validation.\n        node_id: Identifier for the join node; must be unique within the graph.\n\n    Returns:\n        The registered [`Join`][Join] node.\n\n    Raises:\n        GraphBuildError: If the chosen node id is already in use, or the\n            reducer takes neither two nor three parameters.\n    \"\"\"\n    return self._add_join(\n        item_type=item_type,\n        reducer=self._normalize_reducer(reducer),\n        initial_factory=initial_factory,\n        ordered=False,\n        verb=\"reduce\",\n        node_id=node_id,\n    )\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.decision_node","title":"decision_node","text":"<pre><code>decision_node(\n    input_type: type[DecisionInputT], *, node_id: str\n) -&gt; Decision[DecisionInputT, StateT, DepsT]\n</code></pre> <p>Create a routing node that forwards its input to the first matching branch.</p> <p>Wire it as the destination of the edge carrying the value to route, then register its branches (from <code>Decision.when</code> and friends) with <code>add</code>. A value matching no branch and reaching no default branch fails the run.</p> PARAMETER DESCRIPTION <code>input_type</code> <p>The type of value the decision routes, matched against the output of the edge feeding it.</p> <p> TYPE: <code>type[DecisionInputT]</code> </p> <code>node_id</code> <p>Identifier for the decision node; must be unique within the graph.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def decision_node[DecisionInputT](\n    self,\n    input_type: type[DecisionInputT],\n    *,\n    node_id: str,\n) -&gt; Decision[DecisionInputT, StateT, DepsT]:\n    \"\"\"Create a routing node that forwards its input to the first matching branch.\n\n    Wire it as the destination of the edge carrying the value to route, then\n    register its branches (from [`Decision.when`][Decision.when] and friends)\n    with [`add`][GraphBuilder.add]. A value matching no branch and reaching\n    no default branch fails the run.\n\n    Args:\n        input_type: The type of value the decision routes, matched against\n            the output of the edge feeding it.\n        node_id: Identifier for the decision node; must be unique within the graph.\n\n    Returns:\n        The registered [`Decision`][Decision] node.\n\n    Raises:\n        GraphBuildError: If the chosen node id is already in use.\n    \"\"\"\n    if self._id_taken(node_id):\n        raise GraphBuildError(f\"duplicate node id {node_id!r}\")\n    decision: Decision[DecisionInputT, StateT, DepsT] = Decision(node_id=node_id, input_type=input_type)\n    self._decisions[node_id] = decision\n    return decision\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.pause_node","title":"pause_node","text":"<pre><code>pause_node(\n    context_type: type[ContextT],\n    answer_type: type[AnswerT],\n    *,\n    node_id: str,\n    prompt: str | None = None,\n) -&gt; Pause[ContextT, AnswerT]\n</code></pre> <p>Create a node that suspends the run to await an answer.</p> <p>When the run reaches it, it snapshots and stops, raising <code>GraphPaused</code> with the value that arrived and <code>prompt</code>. Resume with <code>Graph.resume_with</code>; the answer becomes the node's output and flows to its successor. Pausing requires a store and run id on the run so the suspended state can be reloaded.</p> PARAMETER DESCRIPTION <code>context_type</code> <p>The type flowing into the pause, recorded for build-time edge validation.</p> <p> TYPE: <code>type[ContextT]</code> </p> <code>answer_type</code> <p>The type supplied on resume, recorded for build-time edge validation.</p> <p> TYPE: <code>type[AnswerT]</code> </p> <code>node_id</code> <p>Identifier for the pause node; must be unique within the graph.</p> <p> TYPE: <code>str</code> </p> <code>prompt</code> <p>A description of what is being asked, surfaced on pause.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def pause_node[ContextT, AnswerT](\n    self,\n    context_type: type[ContextT],\n    answer_type: type[AnswerT],\n    *,\n    node_id: str,\n    prompt: str | None = None,\n) -&gt; Pause[ContextT, AnswerT]:\n    \"\"\"Create a node that suspends the run to await an answer.\n\n    When the run reaches it, it snapshots and stops, raising\n    [`GraphPaused`][openfactcheck.graph.errors.GraphPaused] with the value\n    that arrived and ``prompt``. Resume with\n    [`Graph.resume_with`][Graph.resume_with]; the answer becomes the node's\n    output and flows to its successor. Pausing requires a store and run id\n    on the run so the suspended state can be reloaded.\n\n    Args:\n        context_type: The type flowing into the pause, recorded for\n            build-time edge validation.\n        answer_type: The type supplied on resume, recorded for build-time\n            edge validation.\n        node_id: Identifier for the pause node; must be unique within the graph.\n        prompt: A description of what is being asked, surfaced on pause.\n\n    Returns:\n        The registered [`Pause`][Pause] node.\n\n    Raises:\n        GraphBuildError: If the chosen node id is already in use.\n    \"\"\"\n    if self._id_taken(node_id):\n        raise GraphBuildError(f\"duplicate node id {node_id!r}\")\n    pause: Pause[ContextT, AnswerT] = Pause(\n        id=node_id,\n        context_type=context_type,\n        answer_type=answer_type,\n        prompt=prompt,\n    )\n    self._pauses[node_id] = pause\n    return pause\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.edge_from","title":"edge_from","text":"<pre><code>edge_from(\n    source: SourceNode[EdgeOutputT, StateT, DepsT],\n) -&gt; EdgePathBuilder[EdgeOutputT, StateT, DepsT]\n</code></pre> <p>Begin an edge leaving <code>source</code>.</p> PARAMETER DESCRIPTION <code>source</code> <p>The step or start node the edge leaves.</p> <p> TYPE: <code>SourceNode[EdgeOutputT, StateT, DepsT]</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def edge_from[EdgeOutputT](\n    self,\n    source: SourceNode[EdgeOutputT, StateT, DepsT],\n) -&gt; EdgePathBuilder[EdgeOutputT, StateT, DepsT]:\n    \"\"\"Begin an edge leaving ``source``.\n\n    Args:\n        source: The step or start node the edge leaves.\n\n    Returns:\n        A builder whose [`to`][EdgePathBuilder.to] names the destination,\n        carrying ``source``'s output type so an incompatible destination is\n        a type error.\n    \"\"\"\n    return EdgePathBuilder(source.id)\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.add","title":"add","text":"<pre><code>add(*items: Edge | Branch | _InlineJoinEdge) -&gt; None\n</code></pre> <p>Register edges and decision branches into the graph being built.</p> PARAMETER DESCRIPTION <code>items</code> <p>Edges from <code>edge_from</code>, inline-join edges from <code>collect</code> or <code>reduce</code>, and branches from a <code>Decision</code>'s branch methods, in any mix.</p> <p> TYPE: <code>Edge | Branch | _InlineJoinEdge</code> DEFAULT: <code>()</code> </p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def add(self, *items: Edge | Branch | _InlineJoinEdge) -&gt; None:\n    \"\"\"Register edges and decision branches into the graph being built.\n\n    Args:\n        items: Edges from [`edge_from`][GraphBuilder.edge_from], inline-join\n            edges from [`collect`][EdgePathBuilder.collect] or\n            [`reduce`][EdgePathBuilder.reduce], and branches from a\n            [`Decision`][Decision]'s branch methods, in any mix.\n    \"\"\"\n    for item in items:\n        if isinstance(item, Branch):\n            self._branches.append(item)\n            self._edges.append(Edge(source_id=item.source_id, dest_id=item.dest_id))\n        elif isinstance(item, _InlineJoinEdge):\n            self._add_inline_join(item)\n        else:\n            self._edges.append(item)\n</code></pre>"},{"location":"reference/graph/builder/#openfactcheck.graph.builder.GraphBuilder.build","title":"build","text":"<pre><code>build() -&gt; Graph[InputT, OutputT, StateT, DepsT]\n</code></pre> <p>Validate the assembled graph and return a runnable <code>Graph</code>.</p> Source code in <code>src/openfactcheck/graph/builder.py</code> <pre><code>def build(self) -&gt; Graph[InputT, OutputT, StateT, DepsT]:\n    \"\"\"Validate the assembled graph and return a runnable [`Graph`][Graph].\n\n    Returns:\n        The validated, runnable graph.\n\n    Raises:\n        GraphBuildError: If an edge references an unknown node or a step id\n            is duplicated.\n        GraphValidationError: If the graph has no entry or exit, has\n            unreachable nodes or dead-ends, or connects incompatible types.\n    \"\"\"\n    edges_by_source = self._index_edges()\n    self._validate_endpoints(edges_by_source)\n    self._validate_reachability(edges_by_source)\n    self._validate_edge_types()\n    fork_join = self._resolve_fork_joins(edges_by_source)\n    spec = GraphSpec(\n        steps=self._steps,\n        joins=self._joins,\n        decisions=self._group_branches(),\n        pauses=self._pauses,\n        edges_by_source=edges_by_source,\n        fork_join=fork_join,\n        start_id=START_ID,\n        end_id=END_ID,\n        state_type=self._state_type,\n        input_type=self._input_type,\n        output_type=self._output_type,\n        subgraphs=self._subgraphs,\n    )\n    return Graph(spec, name=self._name)\n</code></pre>"},{"location":"reference/graph/decision/","title":"Decisions","text":"<p>Conditional branching: route one value to one of several downstream paths.</p> <p>A <code>Decision</code> node inspects the value routed into it and forwards it to the first branch whose condition matches, or to a default branch. Conditions match on a predicate, the value's type, or equality. Branches are tried in the order they are added; the first match wins, and a value that matches no branch and has no default fails the run.</p> <p>Wire a decision as the destination of the edge carrying the value to route, then add its branches with <code>when</code>, <code>when_type</code>, <code>when_equals</code>, and <code>otherwise</code>.</p>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Matcher","title":"Matcher","text":"<pre><code>Matcher = Callable[[object], bool]\n</code></pre> <p>Tests whether a value should take a given branch.</p>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.AnyDecision","title":"AnyDecision","text":"<pre><code>AnyDecision = Decision[Any, Any, Any]\n</code></pre> <p>A decision with its type parameters erased, for the executor's wiring layer.</p>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Branch","title":"Branch  <code>dataclass</code>","text":"<pre><code>Branch(\n    source_id: str,\n    dest_id: str,\n    condition: Matcher | None,\n    max_iterations: int | None = None,\n)\n</code></pre> <p>One ordered branch of a decision: a condition and where it routes.</p> <p>Produced by a <code>Decision</code>'s branch methods and registered with <code>GraphBuilder.add</code>. A <code>None</code> condition marks the default branch.</p>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Branch.source_id","title":"source_id  <code>instance-attribute</code>","text":"<pre><code>source_id: str\n</code></pre> <p>Identifier of the decision the branch leaves.</p>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Branch.dest_id","title":"dest_id  <code>instance-attribute</code>","text":"<pre><code>dest_id: str\n</code></pre> <p>Identifier of the node the branch routes to.</p>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Branch.condition","title":"condition  <code>instance-attribute</code>","text":"<pre><code>condition: Matcher | None\n</code></pre> <p>The test that selects this branch, or <code>None</code> for the default branch.</p>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Branch.max_iterations","title":"max_iterations  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_iterations: int | None = None\n</code></pre> <p>For a branch that loops back to an earlier node, the most times it may be taken before the run fails.</p>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Decision","title":"Decision","text":"<pre><code>Decision(*, node_id: str, input_type: object | None)\n</code></pre> <p>A routing node that forwards its input to the first matching branch.</p> <p>Created by <code>GraphBuilder.decision_node</code>. Its type parameter is the type of the value it routes, matched against the output of the edge feeding it.</p> <p>Create a decision node that routes values of one type.</p> Source code in <code>src/openfactcheck/graph/decision.py</code> <pre><code>def __init__(self, *, node_id: str, input_type: object | None) -&gt; None:\n    \"\"\"Create a decision node that routes values of one type.\"\"\"\n    self.id = node_id\n    self.input_type = input_type\n</code></pre>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Decision.when","title":"when","text":"<pre><code>when(\n    predicate: Callable[[InputValueT], bool],\n    dest: DestNode[Any, StateT, DepsT],\n    *,\n    max_iterations: int | None = None,\n) -&gt; Branch\n</code></pre> <p>Route to <code>dest</code> when <code>predicate</code> returns true for the value.</p> PARAMETER DESCRIPTION <code>predicate</code> <p>Test applied to the routed value.</p> <p> TYPE: <code>Callable[[InputValueT], bool]</code> </p> <code>dest</code> <p>The node this branch routes to.</p> <p> TYPE: <code>DestNode[Any, StateT, DepsT]</code> </p> <code>max_iterations</code> <p>When this branch loops back to an earlier node, the most times it may be taken before the run fails.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/decision.py</code> <pre><code>def when[InputValueT](\n    self: Decision[InputValueT, StateT, DepsT],\n    predicate: Callable[[InputValueT], bool],\n    dest: DestNode[Any, StateT, DepsT],\n    *,\n    max_iterations: int | None = None,\n) -&gt; Branch:\n    \"\"\"Route to ``dest`` when ``predicate`` returns true for the value.\n\n    Args:\n        predicate: Test applied to the routed value.\n        dest: The node this branch routes to.\n        max_iterations: When this branch loops back to an earlier node, the\n            most times it may be taken before the run fails.\n\n    Returns:\n        The branch to register with [`GraphBuilder.add`][GraphBuilder.add].\n    \"\"\"\n    return Branch(self.id, dest.id, cast(\"Matcher\", predicate), max_iterations)\n</code></pre>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Decision.when_type","title":"when_type","text":"<pre><code>when_type(\n    cls: type,\n    dest: DestNode[Any, StateT, DepsT],\n    *,\n    max_iterations: int | None = None,\n) -&gt; Branch\n</code></pre> <p>Route to <code>dest</code> when the value is an instance of <code>cls</code>.</p> PARAMETER DESCRIPTION <code>cls</code> <p>Type the value is tested against with <code>isinstance</code>.</p> <p> TYPE: <code>type</code> </p> <code>dest</code> <p>The node this branch routes to.</p> <p> TYPE: <code>DestNode[Any, StateT, DepsT]</code> </p> <code>max_iterations</code> <p>When this branch loops back to an earlier node, the most times it may be taken before the run fails.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/decision.py</code> <pre><code>def when_type(\n    self,\n    cls: type,\n    dest: DestNode[Any, StateT, DepsT],\n    *,\n    max_iterations: int | None = None,\n) -&gt; Branch:\n    \"\"\"Route to ``dest`` when the value is an instance of ``cls``.\n\n    Args:\n        cls: Type the value is tested against with ``isinstance``.\n        dest: The node this branch routes to.\n        max_iterations: When this branch loops back to an earlier node, the\n            most times it may be taken before the run fails.\n\n    Returns:\n        The branch to register with [`GraphBuilder.add`][GraphBuilder.add].\n    \"\"\"\n    return Branch(self.id, dest.id, lambda value: isinstance(value, cls), max_iterations)\n</code></pre>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Decision.when_equals","title":"when_equals","text":"<pre><code>when_equals(\n    expected: object,\n    dest: DestNode[Any, StateT, DepsT],\n    *,\n    max_iterations: int | None = None,\n) -&gt; Branch\n</code></pre> <p>Route to <code>dest</code> when the value equals <code>expected</code>.</p> PARAMETER DESCRIPTION <code>expected</code> <p>Value compared against the routed value with <code>==</code>.</p> <p> TYPE: <code>object</code> </p> <code>dest</code> <p>The node this branch routes to.</p> <p> TYPE: <code>DestNode[Any, StateT, DepsT]</code> </p> <code>max_iterations</code> <p>When this branch loops back to an earlier node, the most times it may be taken before the run fails.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/decision.py</code> <pre><code>def when_equals(\n    self,\n    expected: object,\n    dest: DestNode[Any, StateT, DepsT],\n    *,\n    max_iterations: int | None = None,\n) -&gt; Branch:\n    \"\"\"Route to ``dest`` when the value equals ``expected``.\n\n    Args:\n        expected: Value compared against the routed value with ``==``.\n        dest: The node this branch routes to.\n        max_iterations: When this branch loops back to an earlier node, the\n            most times it may be taken before the run fails.\n\n    Returns:\n        The branch to register with [`GraphBuilder.add`][GraphBuilder.add].\n    \"\"\"\n    return Branch(self.id, dest.id, lambda value: value == expected, max_iterations)\n</code></pre>"},{"location":"reference/graph/decision/#openfactcheck.graph.decision.Decision.otherwise","title":"otherwise","text":"<pre><code>otherwise(\n    dest: DestNode[Any, StateT, DepsT],\n    *,\n    max_iterations: int | None = None,\n) -&gt; Branch\n</code></pre> <p>Route to <code>dest</code> when no other branch matched.</p> PARAMETER DESCRIPTION <code>dest</code> <p>The node values reaching no other branch route to.</p> <p> TYPE: <code>DestNode[Any, StateT, DepsT]</code> </p> <code>max_iterations</code> <p>When this branch loops back to an earlier node, the most times it may be taken before the run fails.</p> <p> TYPE: <code>int | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/decision.py</code> <pre><code>def otherwise(self, dest: DestNode[Any, StateT, DepsT], *, max_iterations: int | None = None) -&gt; Branch:\n    \"\"\"Route to ``dest`` when no other branch matched.\n\n    Args:\n        dest: The node values reaching no other branch route to.\n        max_iterations: When this branch loops back to an earlier node, the\n            most times it may be taken before the run fails.\n\n    Returns:\n        The default branch to register with [`GraphBuilder.add`][GraphBuilder.add].\n    \"\"\"\n    return Branch(self.id, dest.id, None, max_iterations)\n</code></pre>"},{"location":"reference/graph/errors/","title":"Errors","text":"<p>Error hierarchy for the graph layer.</p> <p>Every graph layer error derives from <code>GraphError</code>. Failures split by stage: assembling a graph (<code>GraphBuildError</code>), validating its structure before a run (<code>GraphValidationError</code>), executing it (<code>GraphRuntimeError</code>), saving or loading a run snapshot (<code>GraphPersistenceError</code>), and rendering its diagram to an image (<code>GraphRenderError</code>).</p> <p>Catch the specific subclass for the stage you can recover from, or catch <code>GraphError</code> to handle every graph-layer failure in one place.</p>"},{"location":"reference/graph/errors/#openfactcheck.graph.errors.GraphError","title":"GraphError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for every graph layer error.</p> <p>Catch this to handle any failure from building, validating, or running a <code>Graph</code> without branching on the specific cause.</p>"},{"location":"reference/graph/errors/#openfactcheck.graph.errors.GraphBuildError","title":"GraphBuildError","text":"<p>               Bases: <code>GraphError</code></p> <p>A graph could not be assembled.</p> <p>Raised while wiring nodes and edges: a duplicate node identifier, an edge that references an unknown node, or a step registered more than once.</p>"},{"location":"reference/graph/errors/#openfactcheck.graph.errors.GraphValidationError","title":"GraphValidationError","text":"<p>               Bases: <code>GraphError</code></p> <p>A graph's structure failed validation before running.</p> <p>Raised when finalizing a graph with no path from entry to exit, unreachable nodes, or an edge whose source output type cannot feed its destination input type.</p>"},{"location":"reference/graph/errors/#openfactcheck.graph.errors.GraphRuntimeError","title":"GraphRuntimeError","text":"<p>               Bases: <code>GraphError</code></p> <p>A graph failed while running.</p> <p>Raised when a node raises, a loop exceeds its iteration bound, or a decision reaches no matching branch.</p>"},{"location":"reference/graph/errors/#openfactcheck.graph.errors.GraphPersistenceError","title":"GraphPersistenceError","text":"<p>               Bases: <code>GraphError</code></p> <p>A run's snapshot could not be saved or loaded.</p> <p>Raised when a store is given a run id it cannot map to a safe location, or when a stored snapshot cannot be read back into a run.</p>"},{"location":"reference/graph/errors/#openfactcheck.graph.errors.GraphRenderError","title":"GraphRenderError","text":"<p>               Bases: <code>GraphError</code></p> <p>A graph diagram could not be rendered to an image.</p> <p>Raised when the rendering service cannot be reached or returns an error, or the diagram is too large for the request. The Mermaid source is always available from <code>Graph.to_mermaid</code> regardless.</p>"},{"location":"reference/graph/errors/#openfactcheck.graph.errors.GraphPaused","title":"GraphPaused","text":"<pre><code>GraphPaused(\n    message: str,\n    *,\n    node_id: str,\n    context: object,\n    prompt: str | None,\n    run_id: str | None,\n)\n</code></pre> <p>               Bases: <code>GraphError</code></p> <p>A run reached a pause node and is waiting for external input.</p> <p>Not a failure: the run snapshotted its state and stopped at a pause node. Inspect <code>context</code> and <code>prompt</code> to learn what is being asked, then continue with <code>Graph.resume_with</code>, supplying the answer.</p> <p>Record where the run paused and what it is asking for.</p> Source code in <code>src/openfactcheck/graph/errors.py</code> <pre><code>def __init__(self, message: str, *, node_id: str, context: object, prompt: str | None, run_id: str | None) -&gt; None:\n    \"\"\"Record where the run paused and what it is asking for.\"\"\"\n    self.node_id = node_id\n    self.context = context\n    self.prompt = prompt\n    self.run_id = run_id\n    super().__init__(message)\n</code></pre>"},{"location":"reference/graph/events/","title":"Events","text":"<p>Progress events emitted while a graph runs.</p> <p>A run can be observed two ways, both yielding the same <code>GraphEvent</code> values: <code>Graph.astream</code> yields them live as an async iterator, and the <code>on_event</code> hook on <code>RunOptions</code> is called with each one during <code>Graph.arun</code>. A node start and finish (or failure) is emitted per task, so a fanned-out collection reports one finish per item, and a single run-finished event closes a successful run.</p> <p>A run can also surface data a node emits from inside its own task (for example LLM token fragments) as <code>NodeEmitted</code> events, but only when it opts in via <code>stream_node_data</code> on <code>RunOptions</code>; the default stream stays node-level.</p> <p>Match on the event class to handle each kind:</p> <pre><code>match event:\n    case NodeFinished(node_id=name, duration=seconds):\n        ...\n    case RunFinished(output=result):\n        ...\n</code></pre> <p>To stream a node's LLM tokens through the run, the node forwards them to its <code>emit</code> hook and the consumer opts in to receive them:</p> <pre><code>async def answer(ctx):\n    parts = []\n    async for chunk in ctx.deps.client.astream(messages):\n        if isinstance(chunk, TextDelta):\n            ctx.emit(chunk)  # surfaced as NodeEmitted when the run opts in\n            parts.append(chunk.content)\n    return \"\".join(parts)\n\n\noptions = RunOptions(stream_node_data=True)\nasync for event in graph.astream(text, state=None, deps=deps, options=options):\n    match event:\n        case NodeEmitted(node_id=node, data=TextDelta(content=token)):\n            print(f\"[{node}] {token}\", end=\"\")\n</code></pre>"},{"location":"reference/graph/events/#openfactcheck.graph.events.GraphEvent","title":"GraphEvent","text":"<pre><code>GraphEvent = (\n    NodeStarted\n    | NodeFinished\n    | NodeFailed\n    | NodeEmitted\n    | RunFinished\n)\n</code></pre> <p>Any progress event a run emits, distinguished by class or by its <code>type</code> tag.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.GraphObserver","title":"GraphObserver","text":"<pre><code>GraphObserver = Callable[[GraphEvent], None]\n</code></pre> <p>A callback invoked with each <code>GraphEvent</code> during a run.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeStarted","title":"NodeStarted  <code>dataclass</code>","text":"<pre><code>NodeStarted(\n    node_id: str,\n    fork_stack: ForkStack,\n    type: Literal[\"node_started\"] = \"node_started\",\n)\n</code></pre> <p>A node's task has begun running.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeStarted.node_id","title":"node_id  <code>instance-attribute</code>","text":"<pre><code>node_id: str\n</code></pre> <p>Identifier of the node that started.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeStarted.fork_stack","title":"fork_stack  <code>instance-attribute</code>","text":"<pre><code>fork_stack: ForkStack\n</code></pre> <p>The fork branch the task belongs to.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeStarted.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: Literal['node_started'] = 'node_started'\n</code></pre> <p>Event tag identifying a node start.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFinished","title":"NodeFinished  <code>dataclass</code>","text":"<pre><code>NodeFinished(\n    node_id: str,\n    output: object,\n    duration: float,\n    fork_stack: ForkStack,\n    type: Literal[\"node_finished\"] = \"node_finished\",\n)\n</code></pre> <p>A node's task finished successfully.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFinished.node_id","title":"node_id  <code>instance-attribute</code>","text":"<pre><code>node_id: str\n</code></pre> <p>Identifier of the node that finished.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFinished.output","title":"output  <code>instance-attribute</code>","text":"<pre><code>output: object\n</code></pre> <p>The value the node returned.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFinished.duration","title":"duration  <code>instance-attribute</code>","text":"<pre><code>duration: float\n</code></pre> <p>Seconds the node's task took, including any retries.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFinished.fork_stack","title":"fork_stack  <code>instance-attribute</code>","text":"<pre><code>fork_stack: ForkStack\n</code></pre> <p>The fork branch the task belonged to.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFinished.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: Literal['node_finished'] = 'node_finished'\n</code></pre> <p>Event tag identifying a node finish.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFailed","title":"NodeFailed  <code>dataclass</code>","text":"<pre><code>NodeFailed(\n    node_id: str,\n    error: Exception,\n    duration: float,\n    fork_stack: ForkStack,\n    type: Literal[\"node_failed\"] = \"node_failed\",\n)\n</code></pre> <p>A node's task raised after exhausting its retries.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFailed.node_id","title":"node_id  <code>instance-attribute</code>","text":"<pre><code>node_id: str\n</code></pre> <p>Identifier of the node that failed.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFailed.error","title":"error  <code>instance-attribute</code>","text":"<pre><code>error: Exception\n</code></pre> <p>The error the node raised.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFailed.duration","title":"duration  <code>instance-attribute</code>","text":"<pre><code>duration: float\n</code></pre> <p>Seconds the node's task ran before failing, including any retries.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFailed.fork_stack","title":"fork_stack  <code>instance-attribute</code>","text":"<pre><code>fork_stack: ForkStack\n</code></pre> <p>The fork branch the task belonged to.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeFailed.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: Literal['node_failed'] = 'node_failed'\n</code></pre> <p>Event tag identifying a node failure.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeEmitted","title":"NodeEmitted  <code>dataclass</code>","text":"<pre><code>NodeEmitted(\n    node_id: str,\n    data: object,\n    fork_stack: ForkStack,\n    type: Literal[\"node_emitted\"] = \"node_emitted\",\n)\n</code></pre> <p>A datum a node emitted from inside its task while running.</p> <p>Surfaced only when a run opts in via <code>stream_node_data</code> on <code>RunOptions</code>; the default stream carries node lifecycle events only. A node pushes the datum through <code>StepContext.emit</code>, so its meaning is whatever that node chose, for example a chat token fragment. Emissions are produced inside the node's own task, so those from concurrent fan-out branches interleave; <code>fork_stack</code> identifies the branch each belongs to.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeEmitted.node_id","title":"node_id  <code>instance-attribute</code>","text":"<pre><code>node_id: str\n</code></pre> <p>Identifier of the node that emitted the datum.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeEmitted.data","title":"data  <code>instance-attribute</code>","text":"<pre><code>data: object\n</code></pre> <p>The emitted value, of whatever type the node passed to its <code>emit</code> hook.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeEmitted.fork_stack","title":"fork_stack  <code>instance-attribute</code>","text":"<pre><code>fork_stack: ForkStack\n</code></pre> <p>The fork branch the emitting task belongs to.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.NodeEmitted.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: Literal['node_emitted'] = 'node_emitted'\n</code></pre> <p>Event tag identifying a node-emitted datum.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.RunFinished","title":"RunFinished  <code>dataclass</code>","text":"<pre><code>RunFinished(\n    output: object,\n    type: Literal[\"run_finished\"] = \"run_finished\",\n)\n</code></pre> <p>The run completed and produced its terminal output.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.RunFinished.output","title":"output  <code>instance-attribute</code>","text":"<pre><code>output: object\n</code></pre> <p>The value routed into the end node.</p>"},{"location":"reference/graph/events/#openfactcheck.graph.events.RunFinished.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: Literal['run_finished'] = 'run_finished'\n</code></pre> <p>Event tag identifying the end of a run.</p>"},{"location":"reference/graph/forks/","title":"Forks","text":"<p>Fork-stack identity for fanned-out tasks.</p> <p>When a run fans a value out to many parallel branches, each branch needs an identity so its result can later be combined with its siblings at a join. That identity is a <code>ForkStack</code>: a tuple of <code>ForkStackItem</code> frames, one per enclosing fork, innermost fork last. A task at the root of the graph carries the empty stack; every fan-out pushes a new frame.</p> <p>Each frame pairs the fork's stable identifier with the identifier of the specific firing that produced the branch, so repeated and nested fan-outs stay distinct without collisions.</p>"},{"location":"reference/graph/forks/#openfactcheck.graph.forks.ForkStack","title":"ForkStack","text":"<pre><code>ForkStack = tuple[ForkStackItem, ...]\n</code></pre> <p>A task's path through nested forks, innermost fork last; empty at the root.</p>"},{"location":"reference/graph/forks/#openfactcheck.graph.forks.ForkStackItem","title":"ForkStackItem  <code>dataclass</code>","text":"<pre><code>ForkStackItem(\n    fork_id: str, fork_run_id: str, branch_index: int\n)\n</code></pre> <p>One frame of a task's fork stack, identifying a single fanned-out branch.</p>"},{"location":"reference/graph/forks/#openfactcheck.graph.forks.ForkStackItem.fork_id","title":"fork_id  <code>instance-attribute</code>","text":"<pre><code>fork_id: str\n</code></pre> <p>Identifier of the fork that created this branch, stable across runs.</p>"},{"location":"reference/graph/forks/#openfactcheck.graph.forks.ForkStackItem.fork_run_id","title":"fork_run_id  <code>instance-attribute</code>","text":"<pre><code>fork_run_id: str\n</code></pre> <p>Identifier of the specific firing of that fork within one run.</p>"},{"location":"reference/graph/forks/#openfactcheck.graph.forks.ForkStackItem.branch_index","title":"branch_index  <code>instance-attribute</code>","text":"<pre><code>branch_index: int\n</code></pre> <p>Position of this branch within its fork; the item index for a mapped collection.</p>"},{"location":"reference/graph/graph/","title":"Graph and execution","text":"<p>The built graph and its concurrent executor.</p> <p>A <code>Graph</code> is the runnable artifact produced by <code>GraphBuilder.build</code>. A run drains a work queue of tasks, each carrying a node, its input value, and a fork stack identifying the branch it belongs to. A node runs and its outgoing edges enqueue successors: a plain edge hands the whole output to one successor; a map edge fans an iterable out to one branch per item, each running concurrently up to a bounded limit; a join collects a fork's branch outputs into a list and forwards it once every branch has arrived. The value routed into the end node is the run's result.</p> <p>The async <code>arun</code> is the native entry point; the sync <code>run</code> wraps it for scripts and notebooks.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.DEFAULT_CONCURRENCY","title":"DEFAULT_CONCURRENCY  <code>module-attribute</code>","text":"<pre><code>DEFAULT_CONCURRENCY = 8\n</code></pre> <p>Default cap on the number of step invocations running at once in a single run.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.ErrorPolicy","title":"ErrorPolicy","text":"<pre><code>ErrorPolicy = Literal['fail_fast', 'isolate']\n</code></pre> <p>How a run reacts to a step error: abort the whole run, or drop the failed branch and continue.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.RunOptions","title":"RunOptions  <code>dataclass</code>","text":"<pre><code>RunOptions(\n    concurrency: int = DEFAULT_CONCURRENCY,\n    timeout: float | None = None,\n    on_error: ErrorPolicy = \"fail_fast\",\n    on_event: GraphObserver | None = None,\n    stream_node_data: bool = False,\n    store: StateStore | None = None,\n    run_id: str | None = None,\n)\n</code></pre> <p>Tuning knobs for a single graph run, separate from its input and state.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.RunOptions.concurrency","title":"concurrency  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>concurrency: int = DEFAULT_CONCURRENCY\n</code></pre> <p>Maximum number of step invocations running at once.</p> <p>Must be at least one.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.RunOptions.timeout","title":"timeout  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>timeout: float | None = None\n</code></pre> <p>Seconds the whole run may take before it is cancelled, or <code>None</code> for no limit.</p> <p>Must be positive when set.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.RunOptions.on_error","title":"on_error  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>on_error: ErrorPolicy = 'fail_fast'\n</code></pre> <p>How a step error is handled: abort the run, or drop the failed branch and continue.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.RunOptions.on_event","title":"on_event  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>on_event: GraphObserver | None = None\n</code></pre> <p>A callback invoked with each progress event during a run, or <code>None</code> for none.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.RunOptions.stream_node_data","title":"stream_node_data  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stream_node_data: bool = False\n</code></pre> <p>Whether to surface data a node emits via its <code>emit</code> hook.</p> <p>When <code>False</code> (the default) the stream carries node lifecycle events only and a node's <code>emit</code> calls are discarded. When <code>True</code> each emitted datum becomes a <code>NodeEmitted</code> event tagged with the emitting node and its fork branch.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.RunOptions.store","title":"store  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>store: StateStore | None = None\n</code></pre> <p>A store to snapshot the run to after each task, or <code>None</code> to not persist.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.RunOptions.run_id","title":"run_id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>run_id: str | None = None\n</code></pre> <p>The id snapshots are saved under; required when <code>store</code> is set.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.GraphSpec","title":"GraphSpec  <code>dataclass</code>","text":"<pre><code>GraphSpec(\n    steps: dict[str, AnyStep],\n    joins: dict[str, AnyJoin],\n    decisions: dict[str, list[Branch]],\n    pauses: dict[str, AnyPause],\n    edges_by_source: dict[str, list[Edge]],\n    fork_join: dict[str, str],\n    start_id: str,\n    end_id: str,\n    state_type: object | None,\n    input_type: object | None,\n    output_type: object | None,\n    subgraphs: dict[str, GraphSpec],\n)\n</code></pre> <p>The immutable, validated definition a run executes against.</p> <p>Assembled by <code>GraphBuilder.build</code> and handed to a <code>Graph</code>; not constructed directly.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.GraphSpec.subgraphs","title":"subgraphs  <code>instance-attribute</code>","text":"<pre><code>subgraphs: dict[str, GraphSpec]\n</code></pre> <p>Inner specs of subgraph nodes, keyed by node id, used to render them expanded.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.StepResult","title":"StepResult  <code>dataclass</code>","text":"<pre><code>StepResult(\n    node_id: str,\n    output: object,\n    error: Exception | None,\n    fork_stack: ForkStack,\n)\n</code></pre> <p>One finished task surfaced by a step-by-step run.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.StepResult.node_id","title":"node_id  <code>instance-attribute</code>","text":"<pre><code>node_id: str\n</code></pre> <p>Identifier of the node that ran.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.StepResult.output","title":"output  <code>instance-attribute</code>","text":"<pre><code>output: object\n</code></pre> <p>The node's output, or <code>None</code> when it raised.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.StepResult.error","title":"error  <code>instance-attribute</code>","text":"<pre><code>error: Exception | None\n</code></pre> <p>The error the node raised, or <code>None</code> on success.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.StepResult.fork_stack","title":"fork_stack  <code>instance-attribute</code>","text":"<pre><code>fork_stack: ForkStack\n</code></pre> <p>The fork branch the task belonged to.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.GraphStepper","title":"GraphStepper","text":"<pre><code>GraphStepper(\n    run: _GraphRun[StateT, DepsT, OutputT], inputs: object\n)\n</code></pre> <p>A handle that advances a run one task at a time.</p> <p>Obtained from <code>Graph.stepper</code> and used as an async context manager. Call <code>advance</code> to run the next task and get a <code>StepResult</code> describing it, looping until <code>advance</code> returns <code>None</code>. A failed task can be resumed with <code>recover</code>; once the run is complete, read <code>output</code>.</p> Example <pre><code>async with graph.stepper(inputs, state=None, deps=None) as run:\n    while (step := await run.advance()) is not None:\n        if step.error is not None:\n            run.recover(step, fallback)\nresult = run.output\n</code></pre> <p>Wrap an executor and the input that seeds it; construct via <code>Graph.stepper</code>.</p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def __init__(self, run: _GraphRun[StateT, DepsT, OutputT], inputs: object) -&gt; None:\n    \"\"\"Wrap an executor and the input that seeds it; construct via [`Graph.stepper`][Graph.stepper].\"\"\"\n    self._run = run\n    self._inputs = inputs\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.GraphStepper.output","title":"output  <code>property</code>","text":"<pre><code>output: OutputT\n</code></pre> <p>The value routed into the end node, valid once the run is complete.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.GraphStepper.advance","title":"advance  <code>async</code>","text":"<pre><code>advance() -&gt; StepResult | None\n</code></pre> <p>Run the next task, dispatch its successors on success, and report it.</p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>async def advance(self) -&gt; StepResult | None:\n    \"\"\"Run the next task, dispatch its successors on success, and report it.\n\n    Returns:\n        The finished task's [`StepResult`][StepResult], or ``None`` once no\n        tasks remain. A result with an ``error`` has not been routed onward:\n        call [`recover`][GraphStepper.recover] to resume it with a fallback,\n        or [`drop`][GraphStepper.drop] to drop its branch.\n    \"\"\"\n    result = await self._run.pump()\n    if result is None:\n        return None\n    if result.error is None:\n        self._run.route_output(result.node_id, result.output, result.fork_stack)\n    return StepResult(\n        node_id=result.node_id,\n        output=result.output,\n        error=result.error,\n        fork_stack=result.fork_stack,\n    )\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.GraphStepper.recover","title":"recover","text":"<pre><code>recover(step: StepResult, output: object) -&gt; None\n</code></pre> <p>Resume a failed task by routing <code>output</code> as if its node had returned it.</p> PARAMETER DESCRIPTION <code>step</code> <p>The failed <code>StepResult</code> from <code>advance</code>.</p> <p> TYPE: <code>StepResult</code> </p> <code>output</code> <p>The value to route onward in place of the missing output.</p> <p> TYPE: <code>object</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def recover(self, step: StepResult, output: object) -&gt; None:\n    \"\"\"Resume a failed task by routing ``output`` as if its node had returned it.\n\n    Args:\n        step: The failed [`StepResult`][StepResult] from\n            [`advance`][GraphStepper.advance].\n        output: The value to route onward in place of the missing output.\n    \"\"\"\n    self._run.route_output(step.node_id, output, step.fork_stack)\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.GraphStepper.drop","title":"drop","text":"<pre><code>drop(step: StepResult) -&gt; None\n</code></pre> <p>Drop a failed step's branch so its downstream join can fire without it.</p> <p>The counterpart to <code>recover</code>: where <code>recover</code> supplies a replacement value, <code>drop</code> abandons the branch and lets any join awaiting it complete with the branches that did arrive.</p> PARAMETER DESCRIPTION <code>step</code> <p>The failed <code>StepResult</code> from <code>advance</code>.</p> <p> TYPE: <code>StepResult</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def drop(self, step: StepResult) -&gt; None:\n    \"\"\"Drop a failed step's branch so its downstream join can fire without it.\n\n    The counterpart to [`recover`][GraphStepper.recover]: where ``recover``\n    supplies a replacement value, ``drop`` abandons the branch and lets any\n    join awaiting it complete with the branches that did arrive.\n\n    Args:\n        step: The failed [`StepResult`][StepResult] from\n            [`advance`][GraphStepper.advance].\n    \"\"\"\n    self._run.drop_failed(step.fork_stack)\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph","title":"Graph","text":"<pre><code>Graph(spec: GraphSpec, *, name: str)\n</code></pre> <p>               Bases: <code>Generic[InputT, OutputT, StateT, DepsT]</code></p> <p>An executable graph of typed nodes.</p> <p>Assembled by <code>GraphBuilder.build</code> rather than constructed directly. Run it with <code>run</code> or its async peer <code>arun</code>, or drive it one task at a time with <code>stepper</code>. The type parameters, in order, are:</p> <ol> <li><code>InputT</code>: the value the graph accepts.</li> <li><code>OutputT</code>: the value the graph returns.</li> <li><code>StateT</code>: run-scoped mutable state shared across every node. Optional;    defaults to <code>None</code> (no shared state).</li> <li><code>DepsT</code>: read-only dependencies injected into every node (clients,    configuration). Optional; defaults to <code>None</code> (no deps).</li> </ol> <p>Pass the matching <code>state</code> and <code>deps</code> when running; they flow unchanged into every <code>StepContext</code>.</p> Example <pre><code># str in, a dict out, no shared state, a Deps bag of clients.\ngraph = builder.build()  # Graph[str, dict[str, int], None, Deps]\nresult = graph.run(\"hello\", state=None, deps=Deps(...))\n</code></pre> <p>Record the validated definition of a built graph.</p> PARAMETER DESCRIPTION <code>spec</code> <p>The validated nodes, edges, and routing from the builder.</p> <p> TYPE: <code>GraphSpec</code> </p> <code>name</code> <p>Human-readable name for diagrams and logs.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def __init__(self, spec: GraphSpec, *, name: str) -&gt; None:\n    \"\"\"Record the validated definition of a built graph.\n\n    Args:\n        spec: The validated nodes, edges, and routing from the builder.\n        name: Human-readable name for diagrams and logs.\n    \"\"\"\n    self._spec = spec\n    self.name = name\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.spec","title":"spec  <code>property</code>","text":"<pre><code>spec: GraphSpec\n</code></pre> <p>The validated definition this graph runs.</p>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.to_mermaid","title":"to_mermaid","text":"<pre><code>to_mermaid(\n    *,\n    direction: Direction = \"TD\",\n    title: str | None = None,\n    highlight: Iterable[str] | None = None,\n    show_types: bool = False,\n    expand_subgraphs: bool = False,\n) -&gt; str\n</code></pre> <p>Render this graph as Mermaid flowchart source.</p> PARAMETER DESCRIPTION <code>direction</code> <p>Layout direction of the flowchart.</p> <p> TYPE: <code>Direction</code> DEFAULT: <code>'TD'</code> </p> <code>title</code> <p>An optional title shown above the diagram.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>highlight</code> <p>Node ids to draw with a highlight style.</p> <p> TYPE: <code>Iterable[str] | None</code> DEFAULT: <code>None</code> </p> <code>show_types</code> <p>Label each edge with the type of data it carries, read from the source node's declared output type.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>expand_subgraphs</code> <p>Draw each subgraph node as a nested cluster showing its inner nodes, rather than as a single opaque node.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def to_mermaid(\n    self,\n    *,\n    direction: Direction = \"TD\",\n    title: str | None = None,\n    highlight: Iterable[str] | None = None,\n    show_types: bool = False,\n    expand_subgraphs: bool = False,\n) -&gt; str:\n    \"\"\"Render this graph as Mermaid flowchart source.\n\n    Args:\n        direction: Layout direction of the flowchart.\n        title: An optional title shown above the diagram.\n        highlight: Node ids to draw with a highlight style.\n        show_types: Label each edge with the type of data it carries, read\n            from the source node's declared output type.\n        expand_subgraphs: Draw each subgraph node as a nested cluster showing\n            its inner nodes, rather than as a single opaque node.\n\n    Returns:\n        Mermaid flowchart source as a string.\n    \"\"\"\n    return to_mermaid(\n        self._spec,\n        direction=direction,\n        title=title,\n        highlight=highlight,\n        show_types=show_types,\n        expand_subgraphs=expand_subgraphs,\n    )\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.to_mermaid_image","title":"to_mermaid_image","text":"<pre><code>to_mermaid_image(\n    *,\n    base_url: str = \"https://mermaid.ink\",\n    image_type: ImageType = \"png\",\n    timeout: float = 30.0,\n) -&gt; bytes\n</code></pre> <p>Render this graph as an image through a Mermaid server.</p> PARAMETER DESCRIPTION <code>base_url</code> <p>Base URL of the Mermaid rendering server; point it at a self-hosted server to render offline or to handle a large diagram.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'https://mermaid.ink'</code> </p> <code>image_type</code> <p>Image format to request.</p> <p> TYPE: <code>ImageType</code> DEFAULT: <code>'png'</code> </p> <code>timeout</code> <p>Seconds to wait for the server before giving up.</p> <p> TYPE: <code>float</code> DEFAULT: <code>30.0</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def to_mermaid_image(\n    self, *, base_url: str = \"https://mermaid.ink\", image_type: ImageType = \"png\", timeout: float = 30.0\n) -&gt; bytes:\n    \"\"\"Render this graph as an image through a Mermaid server.\n\n    Args:\n        base_url: Base URL of the Mermaid rendering server; point it at a\n            self-hosted server to render offline or to handle a large diagram.\n        image_type: Image format to request.\n        timeout: Seconds to wait for the server before giving up.\n\n    Returns:\n        The rendered image's raw bytes.\n\n    Raises:\n        GraphRenderError: If the server cannot be reached or returns an error.\n    \"\"\"\n    return to_mermaid_image(self.to_mermaid(), base_url=base_url, image_type=image_type, timeout=timeout)\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.to_mermaid_view","title":"to_mermaid_view","text":"<pre><code>to_mermaid_view(\n    *,\n    base_url: str = \"https://mermaid.ink\",\n    timeout: float = 30.0,\n) -&gt; _MermaidView\n</code></pre> <p>Render this graph as a PNG that displays inline in a notebook.</p> <p>Returns an object a notebook renders as the diagram image; elsewhere, reach for <code>to_mermaid_image</code> to get the bytes directly.</p> PARAMETER DESCRIPTION <code>base_url</code> <p>Base URL of the Mermaid rendering server; point it at a self-hosted server to render offline or to handle a large diagram.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'https://mermaid.ink'</code> </p> <code>timeout</code> <p>Seconds to wait for the server before giving up.</p> <p> TYPE: <code>float</code> DEFAULT: <code>30.0</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def to_mermaid_view(self, *, base_url: str = \"https://mermaid.ink\", timeout: float = 30.0) -&gt; _MermaidView:\n    \"\"\"Render this graph as a PNG that displays inline in a notebook.\n\n    Returns an object a notebook renders as the diagram image; elsewhere,\n    reach for [`to_mermaid_image`][Graph.to_mermaid_image] to get the bytes directly.\n\n    Args:\n        base_url: Base URL of the Mermaid rendering server; point it at a\n            self-hosted server to render offline or to handle a large diagram.\n        timeout: Seconds to wait for the server before giving up.\n\n    Returns:\n        A display wrapper a notebook shows as the rendered diagram.\n\n    Raises:\n        GraphRenderError: If the server cannot be reached or returns an error.\n    \"\"\"\n    return _MermaidView(self.to_mermaid_image(base_url=base_url, timeout=timeout))\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.arun","title":"arun  <code>async</code>","text":"<pre><code>arun(\n    inputs: InputT,\n    *,\n    state: StateT,\n    deps: DepsT,\n    options: RunOptions | None = None,\n) -&gt; OutputT\n</code></pre> <p>Run the graph to completion and return its terminal output.</p> PARAMETER DESCRIPTION <code>inputs</code> <p>The value handed to the first node.</p> <p> TYPE: <code>InputT</code> </p> <code>state</code> <p>Run-scoped state shared across nodes.</p> <p> TYPE: <code>StateT</code> </p> <code>deps</code> <p>Run-scoped dependencies injected into nodes.</p> <p> TYPE: <code>DepsT</code> </p> <code>options</code> <p>Concurrency, timeout, and error-handling knobs for the run; defaults to <code>RunOptions</code>'s own defaults.</p> <p> TYPE: <code>RunOptions | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>async def arun(self, inputs: InputT, *, state: StateT, deps: DepsT, options: RunOptions | None = None) -&gt; OutputT:\n    \"\"\"Run the graph to completion and return its terminal output.\n\n    Args:\n        inputs: The value handed to the first node.\n        state: Run-scoped state shared across nodes.\n        deps: Run-scoped dependencies injected into nodes.\n        options: Concurrency, timeout, and error-handling knobs for the run;\n            defaults to [`RunOptions`][RunOptions]'s own defaults.\n\n    Returns:\n        The value routed into the end node.\n\n    Raises:\n        GraphRuntimeError: If the run finishes without reaching the end node.\n        TimeoutError: If the run's timeout elapses.\n        ValueError: If the configured concurrency is less than one.\n    \"\"\"\n    resolved = self._validated(options)\n    run: _GraphRun[StateT, DepsT, OutputT] = _GraphRun(self._spec, state=state, deps=deps, options=resolved)\n    return await run.execute(inputs)\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.run","title":"run","text":"<pre><code>run(\n    inputs: InputT,\n    *,\n    state: StateT,\n    deps: DepsT,\n    options: RunOptions | None = None,\n) -&gt; OutputT\n</code></pre> <p>Run the graph to completion synchronously.</p> <p>A blocking wrapper over <code>arun</code> for scripts and notebooks. Call <code>arun</code> directly from inside a running event loop.</p> PARAMETER DESCRIPTION <code>inputs</code> <p>The value handed to the first node.</p> <p> TYPE: <code>InputT</code> </p> <code>state</code> <p>Run-scoped state shared across nodes.</p> <p> TYPE: <code>StateT</code> </p> <code>deps</code> <p>Run-scoped dependencies injected into nodes.</p> <p> TYPE: <code>DepsT</code> </p> <code>options</code> <p>Concurrency, timeout, and error-handling knobs for the run; defaults to <code>RunOptions</code>'s own defaults.</p> <p> TYPE: <code>RunOptions | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def run(self, inputs: InputT, *, state: StateT, deps: DepsT, options: RunOptions | None = None) -&gt; OutputT:\n    \"\"\"Run the graph to completion synchronously.\n\n    A blocking wrapper over [`arun`][Graph.arun] for scripts and notebooks.\n    Call ``arun`` directly from inside a running event loop.\n\n    Args:\n        inputs: The value handed to the first node.\n        state: Run-scoped state shared across nodes.\n        deps: Run-scoped dependencies injected into nodes.\n        options: Concurrency, timeout, and error-handling knobs for the run;\n            defaults to [`RunOptions`][RunOptions]'s own defaults.\n\n    Returns:\n        The value routed into the end node.\n\n    Raises:\n        GraphRuntimeError: If the run finishes without reaching the end node.\n        TimeoutError: If the run's timeout elapses.\n        ValueError: If the configured concurrency is less than one.\n    \"\"\"\n    return asyncio.run(self.arun(inputs, state=state, deps=deps, options=options))\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.astream","title":"astream  <code>async</code>","text":"<pre><code>astream(\n    inputs: InputT,\n    *,\n    state: StateT,\n    deps: DepsT,\n    options: RunOptions | None = None,\n) -&gt; AsyncIterator[GraphEvent]\n</code></pre> <p>Run the graph and yield progress events as they happen.</p> <p>Yields a <code>NodeStarted</code> and a <code>NodeFinished</code> (or <code>NodeFailed</code>) per task, then a final <code>RunFinished</code>. When <code>stream_node_data</code> is set, a node's <code>emit</code> calls additionally appear as <code>NodeEmitted</code> events between its start and finish. Any <code>on_event</code> set in <code>options</code> is ignored; the stream is the observer.</p> PARAMETER DESCRIPTION <code>inputs</code> <p>The value handed to the first node.</p> <p> TYPE: <code>InputT</code> </p> <code>state</code> <p>Run-scoped state shared across nodes.</p> <p> TYPE: <code>StateT</code> </p> <code>deps</code> <p>Run-scoped dependencies injected into nodes.</p> <p> TYPE: <code>DepsT</code> </p> <code>options</code> <p>Concurrency, timeout, and error-handling knobs for the run.</p> <p> TYPE: <code>RunOptions | None</code> DEFAULT: <code>None</code> </p> YIELDS DESCRIPTION <code>AsyncIterator[GraphEvent]</code> <p>Each <code>GraphEvent</code> in turn.</p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>async def astream(\n    self,\n    inputs: InputT,\n    *,\n    state: StateT,\n    deps: DepsT,\n    options: RunOptions | None = None,\n) -&gt; AsyncIterator[GraphEvent]:\n    \"\"\"Run the graph and yield progress events as they happen.\n\n    Yields a [`NodeStarted`][openfactcheck.graph.events.NodeStarted] and a\n    [`NodeFinished`][openfactcheck.graph.events.NodeFinished] (or\n    [`NodeFailed`][openfactcheck.graph.events.NodeFailed]) per task, then a\n    final [`RunFinished`][openfactcheck.graph.events.RunFinished]. When\n    ``stream_node_data`` is set, a node's ``emit`` calls additionally appear as\n    [`NodeEmitted`][openfactcheck.graph.events.NodeEmitted] events between its\n    start and finish. Any ``on_event`` set in ``options`` is ignored; the\n    stream is the observer.\n\n    Args:\n        inputs: The value handed to the first node.\n        state: Run-scoped state shared across nodes.\n        deps: Run-scoped dependencies injected into nodes.\n        options: Concurrency, timeout, and error-handling knobs for the run.\n\n    Yields:\n        Each [`GraphEvent`][openfactcheck.graph.events.GraphEvent] in turn.\n\n    Raises:\n        GraphRuntimeError: If the run finishes without reaching the end node.\n        TimeoutError: If the run's timeout elapses.\n        ValueError: If the configured concurrency is less than one.\n    \"\"\"\n    resolved = self._validated(options)\n    events: asyncio.Queue[GraphEvent | object] = asyncio.Queue()\n    streaming = replace(resolved, on_event=events.put_nowait)\n    run: _GraphRun[StateT, DepsT, OutputT] = _GraphRun(self._spec, state=state, deps=deps, options=streaming)\n    drive = asyncio.create_task(self._drain_run(run, inputs, events))\n    try:\n        while True:\n            event = await events.get()\n            if event is _STREAM_END:\n                break\n            yield cast(\"GraphEvent\", event)\n        await drive\n    finally:\n        if not drive.done():\n            drive.cancel()\n            with suppress(asyncio.CancelledError):\n                await drive\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.stepper","title":"stepper","text":"<pre><code>stepper(\n    inputs: InputT,\n    *,\n    state: StateT,\n    deps: DepsT,\n    options: RunOptions | None = None,\n) -&gt; GraphStepper[OutputT, StateT, DepsT]\n</code></pre> <p>Drive the graph one task at a time for inspection or recovery.</p> <p>Returns an async-context-manager handle; see <code>GraphStepper</code> for the loop.</p> PARAMETER DESCRIPTION <code>inputs</code> <p>The value handed to the first node.</p> <p> TYPE: <code>InputT</code> </p> <code>state</code> <p>Run-scoped state shared across nodes.</p> <p> TYPE: <code>StateT</code> </p> <code>deps</code> <p>Run-scoped dependencies injected into nodes.</p> <p> TYPE: <code>DepsT</code> </p> <code>options</code> <p>Concurrency, timeout, and error-handling knobs; note the whole-run timeout and <code>on_error</code> policy do not apply while stepping, since the caller drives and handles each task.</p> <p> TYPE: <code>RunOptions | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def stepper(\n    self,\n    inputs: InputT,\n    *,\n    state: StateT,\n    deps: DepsT,\n    options: RunOptions | None = None,\n) -&gt; GraphStepper[OutputT, StateT, DepsT]:\n    \"\"\"Drive the graph one task at a time for inspection or recovery.\n\n    Returns an async-context-manager handle; see\n    [`GraphStepper`][GraphStepper] for the loop.\n\n    Args:\n        inputs: The value handed to the first node.\n        state: Run-scoped state shared across nodes.\n        deps: Run-scoped dependencies injected into nodes.\n        options: Concurrency, timeout, and error-handling knobs; note the\n            whole-run timeout and ``on_error`` policy do not apply while\n            stepping, since the caller drives and handles each task.\n\n    Returns:\n        A [`GraphStepper`][GraphStepper] over a fresh run.\n\n    Raises:\n        ValueError: If the configured concurrency is less than one.\n    \"\"\"\n    resolved = self._validated(options)\n    run: _GraphRun[StateT, DepsT, OutputT] = _GraphRun(self._spec, state=state, deps=deps, options=resolved)\n    return GraphStepper(run, inputs)\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.aresume","title":"aresume  <code>async</code>","text":"<pre><code>aresume(\n    run_id: str,\n    *,\n    store: StateStore,\n    deps: DepsT,\n    options: RunOptions | None = None,\n) -&gt; OutputT\n</code></pre> <p>Resume a snapshotted run from its latest snapshot and finish it.</p> <p>Loads the snapshot, restores the run's state, and re-runs the tasks that were still pending. Pending tasks run again, so steps should be safe to re-run. The run's state and each pending node's input are restored to the types the graph declares, so resumed steps receive the same typed values a fresh run would. Dependencies are not persisted and are supplied here; the run continues to snapshot under the same id and store.</p> PARAMETER DESCRIPTION <code>run_id</code> <p>The id the run was snapshotted under.</p> <p> TYPE: <code>str</code> </p> <code>store</code> <p>The store holding the run's snapshots.</p> <p> TYPE: <code>StateStore</code> </p> <code>deps</code> <p>Run-scoped dependencies injected into nodes.</p> <p> TYPE: <code>DepsT</code> </p> <code>options</code> <p>Concurrency, timeout, and error-handling knobs for the resumed run.</p> <p> TYPE: <code>RunOptions | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>async def aresume(\n    self, run_id: str, *, store: StateStore, deps: DepsT, options: RunOptions | None = None\n) -&gt; OutputT:\n    \"\"\"Resume a snapshotted run from its latest snapshot and finish it.\n\n    Loads the snapshot, restores the run's state, and re-runs the tasks that\n    were still pending. Pending tasks run again, so steps should be safe to\n    re-run. The run's state and each pending node's input are restored to the\n    types the graph declares, so resumed steps receive the same typed values\n    a fresh run would. Dependencies are not persisted and are supplied here;\n    the run continues to snapshot under the same id and store.\n\n    Args:\n        run_id: The id the run was snapshotted under.\n        store: The store holding the run's snapshots.\n        deps: Run-scoped dependencies injected into nodes.\n        options: Concurrency, timeout, and error-handling knobs for the\n            resumed run.\n\n    Returns:\n        The value routed into the end node.\n\n    Raises:\n        GraphRuntimeError: If no snapshot exists for ``run_id`` or the run\n            finishes without reaching the end node.\n        ValueError: If the configured concurrency is less than one.\n    \"\"\"\n    snapshot = await store.load(run_id)\n    if snapshot is None:\n        raise GraphRuntimeError(f\"no snapshot found for run {run_id!r}\")\n    resolved = replace(self._validated(options), store=store, run_id=run_id)\n    run: _GraphRun[StateT, DepsT, OutputT] = _GraphRun(\n        self._spec,\n        state=cast(\"StateT\", snapshot.state),\n        deps=deps,\n        options=resolved,\n    )\n    run.load(snapshot)\n    return await run.resume()\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.resume","title":"resume","text":"<pre><code>resume(\n    run_id: str,\n    *,\n    store: StateStore,\n    deps: DepsT,\n    options: RunOptions | None = None,\n) -&gt; OutputT\n</code></pre> <p>Resume a snapshotted run synchronously.</p> <p>A blocking wrapper over <code>aresume</code> for scripts and notebooks.</p> PARAMETER DESCRIPTION <code>run_id</code> <p>The id the run was snapshotted under.</p> <p> TYPE: <code>str</code> </p> <code>store</code> <p>The store holding the run's snapshots.</p> <p> TYPE: <code>StateStore</code> </p> <code>deps</code> <p>Run-scoped dependencies injected into nodes.</p> <p> TYPE: <code>DepsT</code> </p> <code>options</code> <p>Concurrency, timeout, and error-handling knobs.</p> <p> TYPE: <code>RunOptions | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def resume(self, run_id: str, *, store: StateStore, deps: DepsT, options: RunOptions | None = None) -&gt; OutputT:\n    \"\"\"Resume a snapshotted run synchronously.\n\n    A blocking wrapper over [`aresume`][Graph.aresume] for scripts and notebooks.\n\n    Args:\n        run_id: The id the run was snapshotted under.\n        store: The store holding the run's snapshots.\n        deps: Run-scoped dependencies injected into nodes.\n        options: Concurrency, timeout, and error-handling knobs.\n\n    Returns:\n        The value routed into the end node.\n\n    Raises:\n        GraphRuntimeError: If no snapshot exists for ``run_id`` or the run\n            finishes without reaching the end node.\n        ValueError: If the configured concurrency is less than one.\n    \"\"\"\n    return asyncio.run(self.aresume(run_id, store=store, deps=deps, options=options))\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.aresume_with","title":"aresume_with  <code>async</code>","text":"<pre><code>aresume_with(\n    run_id: str,\n    *,\n    store: StateStore,\n    deps: DepsT,\n    value: object,\n    options: RunOptions | None = None,\n) -&gt; OutputT\n</code></pre> <p>Resume a paused run, injecting <code>value</code> as the pause node's output.</p> PARAMETER DESCRIPTION <code>run_id</code> <p>The id the paused run was snapshotted under.</p> <p> TYPE: <code>str</code> </p> <code>store</code> <p>The store holding the run's snapshots.</p> <p> TYPE: <code>StateStore</code> </p> <code>deps</code> <p>Run-scoped dependencies injected into nodes.</p> <p> TYPE: <code>DepsT</code> </p> <code>value</code> <p>The answer to inject; becomes the pause node's output.</p> <p> TYPE: <code>object</code> </p> <code>options</code> <p>Concurrency, timeout, and error-handling knobs.</p> <p> TYPE: <code>RunOptions | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>async def aresume_with(\n    self,\n    run_id: str,\n    *,\n    store: StateStore,\n    deps: DepsT,\n    value: object,\n    options: RunOptions | None = None,\n) -&gt; OutputT:\n    \"\"\"Resume a paused run, injecting ``value`` as the pause node's output.\n\n    Args:\n        run_id: The id the paused run was snapshotted under.\n        store: The store holding the run's snapshots.\n        deps: Run-scoped dependencies injected into nodes.\n        value: The answer to inject; becomes the pause node's output.\n        options: Concurrency, timeout, and error-handling knobs.\n\n    Returns:\n        The value routed into the end node.\n\n    Raises:\n        GraphRuntimeError: If no snapshot exists for ``run_id``, the snapshot\n            is not paused, or the run finishes without reaching the end node.\n        GraphPaused: If the run reaches another pause node.\n        ValueError: If the configured concurrency is less than one.\n    \"\"\"\n    snapshot = await store.load(run_id)\n    if snapshot is None:\n        raise GraphRuntimeError(f\"no snapshot found for run {run_id!r}\")\n    if snapshot.paused is None:\n        raise GraphRuntimeError(f\"run {run_id!r} is not paused\")\n    resolved = replace(self._validated(options), store=store, run_id=run_id)\n    run: _GraphRun[StateT, DepsT, OutputT] = _GraphRun(\n        self._spec,\n        state=cast(\"StateT\", snapshot.state),\n        deps=deps,\n        options=resolved,\n    )\n    run.load(snapshot)\n    return await run.resume_with(snapshot.paused, value)\n</code></pre>"},{"location":"reference/graph/graph/#openfactcheck.graph.graph.Graph.resume_with","title":"resume_with","text":"<pre><code>resume_with(\n    run_id: str,\n    *,\n    store: StateStore,\n    deps: DepsT,\n    value: object,\n    options: RunOptions | None = None,\n) -&gt; OutputT\n</code></pre> <p>Resume a paused run synchronously, injecting <code>value</code> as the pause node's output.</p> <p>A blocking wrapper over <code>aresume_with</code>.</p> PARAMETER DESCRIPTION <code>run_id</code> <p>The id the paused run was snapshotted under.</p> <p> TYPE: <code>str</code> </p> <code>store</code> <p>The store holding the run's snapshots.</p> <p> TYPE: <code>StateStore</code> </p> <code>deps</code> <p>Run-scoped dependencies injected into nodes.</p> <p> TYPE: <code>DepsT</code> </p> <code>value</code> <p>The answer to inject; becomes the pause node's output.</p> <p> TYPE: <code>object</code> </p> <code>options</code> <p>Concurrency, timeout, and error-handling knobs.</p> <p> TYPE: <code>RunOptions | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/graph/graph.py</code> <pre><code>def resume_with(\n    self,\n    run_id: str,\n    *,\n    store: StateStore,\n    deps: DepsT,\n    value: object,\n    options: RunOptions | None = None,\n) -&gt; OutputT:\n    \"\"\"Resume a paused run synchronously, injecting ``value`` as the pause node's output.\n\n    A blocking wrapper over [`aresume_with`][Graph.aresume_with].\n\n    Args:\n        run_id: The id the paused run was snapshotted under.\n        store: The store holding the run's snapshots.\n        deps: Run-scoped dependencies injected into nodes.\n        value: The answer to inject; becomes the pause node's output.\n        options: Concurrency, timeout, and error-handling knobs.\n\n    Returns:\n        The value routed into the end node.\n\n    Raises:\n        GraphRuntimeError: If no snapshot exists for ``run_id``, the snapshot\n            is not paused, or the run finishes without reaching the end node.\n        GraphPaused: If the run reaches another pause node.\n        ValueError: If the configured concurrency is less than one.\n    \"\"\"\n    return asyncio.run(self.aresume_with(run_id, store=store, deps=deps, value=value, options=options))\n</code></pre>"},{"location":"reference/graph/join/","title":"Joins and reducers","text":"<p>Fan-in joins and the reducers that combine a fork's branch outputs.</p> <p>A <code>Join</code> gathers the outputs of a fanned-out subpath and folds them into a single value with a reducer. A reducer takes the running accumulator and one branch's value and returns the next accumulator; built-in reducers cover the common shapes (collect into a list, merge dicts, sum, keep the first). A reducer may instead take a <code>ReducerContext</code> first, giving it the run's state and dependencies and the ability to stop early once it has enough.</p> <p>Joins are created through <code>GraphBuilder.collect_node</code> (ordered list collection) and <code>GraphBuilder.reduce_node</code> (any reducer); this module holds the node type and the reducer library they draw on.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Reducer","title":"Reducer","text":"<pre><code>Reducer = Callable[[AccT, ItemT], AccT]\n</code></pre> <p>A fold over branch values: the running accumulator and one value give the next accumulator.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.ContextReducer","title":"ContextReducer","text":"<pre><code>ContextReducer = Callable[\n    [ReducerContext[StateT, DepsT], AccT, ItemT], AccT\n]\n</code></pre> <p>A <code>Reducer</code> that also receives the run's <code>ReducerContext</code>.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.NormalizedReducer","title":"NormalizedReducer","text":"<pre><code>NormalizedReducer = Callable[\n    [ReducerContext[Any, Any], Any, Any], Any\n]\n</code></pre> <p>A reducer reduced to its context-taking form, ready for the executor to call uniformly.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.AnyJoin","title":"AnyJoin","text":"<pre><code>AnyJoin = Join[Any, Any]\n</code></pre> <p>A join with its type parameters erased, for the executor's wiring layer.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.ReducerContext","title":"ReducerContext","text":"<pre><code>ReducerContext(*, state: StateT, deps: DepsT)\n</code></pre> <p>The run context handed to a context-taking reducer.</p> <p>Carries the run-scoped <code>state</code> and injected <code>deps</code>, and lets a reducer signal that it has seen enough and the remaining branches can be dropped.</p> <p>Build a context exposing the run's state and dependencies.</p> Source code in <code>src/openfactcheck/graph/join.py</code> <pre><code>def __init__(self, *, state: StateT, deps: DepsT) -&gt; None:\n    \"\"\"Build a context exposing the run's state and dependencies.\"\"\"\n    self.state = state\n    self.deps = deps\n    self._stop = False\n</code></pre>"},{"location":"reference/graph/join/#openfactcheck.graph.join.ReducerContext.stopped","title":"stopped  <code>property</code>","text":"<pre><code>stopped: bool\n</code></pre> <p>Whether a reducer asked to stop this fan-in early.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.ReducerContext.cancel_sibling_tasks","title":"cancel_sibling_tasks","text":"<pre><code>cancel_sibling_tasks() -&gt; None\n</code></pre> <p>Stop this fan-in early: emit the accumulator now and drop branches still in flight.</p> <p>Use it for first-acceptable-wins reducers. The branches already running finish but their values are discarded; their work is cancelled where the executor can.</p> Source code in <code>src/openfactcheck/graph/join.py</code> <pre><code>def cancel_sibling_tasks(self) -&gt; None:\n    \"\"\"Stop this fan-in early: emit the accumulator now and drop branches still in flight.\n\n    Use it for first-acceptable-wins reducers. The branches already running\n    finish but their values are discarded; their work is cancelled where the\n    executor can.\n    \"\"\"\n    self._stop = True\n</code></pre>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Join","title":"Join  <code>dataclass</code>","text":"<pre><code>Join(\n    id: str,\n    verb: str,\n    item_type: object | None,\n    reducer: NormalizedReducer,\n    initial_factory: Callable[[], Any],\n    ordered: bool,\n    inline: bool = False,\n)\n</code></pre> <p>A fan-in node that folds a fork's branch outputs into one value.</p> <p>Created by <code>GraphBuilder.collect_node</code> or <code>GraphBuilder.reduce_node</code>. Wire it as the destination of a fanned-out subpath and as the source of the edge carrying the folded result, which flows on once every branch of the fork has arrived (or a reducer stops early).</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Join.id","title":"id  <code>instance-attribute</code>","text":"<pre><code>id: str\n</code></pre> <p>Stable identifier of this join node.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Join.verb","title":"verb  <code>instance-attribute</code>","text":"<pre><code>verb: str\n</code></pre> <p>Short name of the fold operation, shown when the graph is rendered.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Join.item_type","title":"item_type  <code>instance-attribute</code>","text":"<pre><code>item_type: object | None\n</code></pre> <p>The per-branch input type, recorded for build-time edge validation.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Join.reducer","title":"reducer  <code>instance-attribute</code>","text":"<pre><code>reducer: NormalizedReducer\n</code></pre> <p>The fold applied to each branch's value, in context-taking form.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Join.initial_factory","title":"initial_factory  <code>instance-attribute</code>","text":"<pre><code>initial_factory: Callable[[], Any]\n</code></pre> <p>Builds the accumulator each time the join fires.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Join.ordered","title":"ordered  <code>instance-attribute</code>","text":"<pre><code>ordered: bool\n</code></pre> <p>Whether to fold branches in source order; otherwise in arrival order.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.Join.inline","title":"inline  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>inline: bool = False\n</code></pre> <p>Whether the join was declared inline on an edge and renders without its own node.</p>"},{"location":"reference/graph/join/#openfactcheck.graph.join.reduce_list_append","title":"reduce_list_append","text":"<pre><code>reduce_list_append(acc: list[T], item: T) -&gt; list[T]\n</code></pre> <p>Append each branch's value to a list.</p> Source code in <code>src/openfactcheck/graph/join.py</code> <pre><code>def reduce_list_append[T](acc: list[T], item: T) -&gt; list[T]:\n    \"\"\"Append each branch's value to a list.\"\"\"\n    acc.append(item)\n    return acc\n</code></pre>"},{"location":"reference/graph/join/#openfactcheck.graph.join.reduce_list_extend","title":"reduce_list_extend","text":"<pre><code>reduce_list_extend(\n    acc: list[T], item: Iterable[T]\n) -&gt; list[T]\n</code></pre> <p>Extend a list with each branch's iterable of values.</p> Source code in <code>src/openfactcheck/graph/join.py</code> <pre><code>def reduce_list_extend[T](acc: list[T], item: Iterable[T]) -&gt; list[T]:\n    \"\"\"Extend a list with each branch's iterable of values.\"\"\"\n    acc.extend(item)\n    return acc\n</code></pre>"},{"location":"reference/graph/join/#openfactcheck.graph.join.reduce_dict_update","title":"reduce_dict_update","text":"<pre><code>reduce_dict_update(\n    acc: dict[K, V], item: Mapping[K, V]\n) -&gt; dict[K, V]\n</code></pre> <p>Merge each branch's mapping into one dict, later branches winning on key clashes.</p> Source code in <code>src/openfactcheck/graph/join.py</code> <pre><code>def reduce_dict_update[K, V](acc: dict[K, V], item: Mapping[K, V]) -&gt; dict[K, V]:\n    \"\"\"Merge each branch's mapping into one dict, later branches winning on key clashes.\"\"\"\n    acc.update(item)\n    return acc\n</code></pre>"},{"location":"reference/graph/join/#openfactcheck.graph.join.reduce_sum","title":"reduce_sum","text":"<pre><code>reduce_sum(acc: float, item: float) -&gt; float\n</code></pre> <p>Add each branch's number to a running total.</p> Source code in <code>src/openfactcheck/graph/join.py</code> <pre><code>def reduce_sum(acc: float, item: float) -&gt; float:\n    \"\"\"Add each branch's number to a running total.\"\"\"\n    return acc + item\n</code></pre>"},{"location":"reference/graph/join/#openfactcheck.graph.join.reduce_null","title":"reduce_null","text":"<pre><code>reduce_null(acc: AccT, item: object) -&gt; AccT\n</code></pre> <p>Discard every branch's value, leaving the initial accumulator unchanged.</p> Source code in <code>src/openfactcheck/graph/join.py</code> <pre><code>def reduce_null[AccT](acc: AccT, item: object) -&gt; AccT:  # noqa: ARG001 - discards the value by design.\n    \"\"\"Discard every branch's value, leaving the initial accumulator unchanged.\"\"\"\n    return acc\n</code></pre>"},{"location":"reference/graph/join/#openfactcheck.graph.join.reduce_first","title":"reduce_first","text":"<pre><code>reduce_first(\n    ctx: ReducerContext[Any, Any],\n    acc: ItemT | None,\n    item: ItemT,\n) -&gt; ItemT\n</code></pre> <p>Keep the first branch's value and stop, dropping the rest.</p> Source code in <code>src/openfactcheck/graph/join.py</code> <pre><code>def reduce_first[ItemT](ctx: ReducerContext[Any, Any], acc: ItemT | None, item: ItemT) -&gt; ItemT:  # noqa: ARG001 - acc is the unused running value.\n    \"\"\"Keep the first branch's value and stop, dropping the rest.\"\"\"\n    ctx.cancel_sibling_tasks()\n    return item\n</code></pre>"},{"location":"reference/graph/mermaid/","title":"Mermaid","text":"<p>Render a graph definition as a Mermaid flowchart, and that flowchart as an image.</p> <p><code>to_mermaid</code> turns a built graph's structure into Mermaid flowchart source: each node kind gets a distinct shape (a step is a rectangle, a join a hexagon, a decision a rhombus, a pause a parallelogram, and the start and end are filled circles). Fan-out (map) edges are drawn thick, decision branches dotted, and an edge entering a join is labeled with that join's fold operation (collect or reduce). A join declared inline on the edge itself has no node of its own and renders as that label on a single edge to its successor. So the diagram shows where the graph forks, routes, and rejoins; nodes are emitted in breadth-first order from the start so the output is stable. With <code>show_types</code>, each edge is also labeled with the data it carries, read from the source node's declared output type. With <code>expand_subgraphs</code>, a subgraph node is drawn as a nested cluster of its inner nodes, with edges wired to the inner nodes its flow enters and leaves, rather than a single box.</p> <p><code>to_mermaid_image</code> takes that source and renders it to image bytes through a Mermaid server (mermaid.ink by default); the default server encodes the diagram into the request URL, so a very large graph may need a self-hosted server supplied as <code>base_url</code>.</p>"},{"location":"reference/graph/mermaid/#openfactcheck.graph.mermaid.Direction","title":"Direction","text":"<pre><code>Direction = Literal['TD', 'LR', 'RL', 'BT']\n</code></pre> <p>A Mermaid flowchart layout direction: top-down, left-right, right-left, or bottom-top.</p>"},{"location":"reference/graph/mermaid/#openfactcheck.graph.mermaid.ImageType","title":"ImageType","text":"<pre><code>ImageType = Literal['png', 'svg']\n</code></pre> <p>The image format a Mermaid server renders: a PNG raster or an SVG vector.</p>"},{"location":"reference/graph/mermaid/#openfactcheck.graph.mermaid.to_mermaid","title":"to_mermaid","text":"<pre><code>to_mermaid(\n    spec: GraphSpec,\n    *,\n    direction: Direction = \"TD\",\n    title: str | None = None,\n    highlight: Iterable[str] | None = None,\n    show_types: bool = False,\n    expand_subgraphs: bool = False,\n) -&gt; str\n</code></pre> <p>Render a graph definition as Mermaid flowchart source.</p> PARAMETER DESCRIPTION <code>spec</code> <p>The built graph's definition to render.</p> <p> TYPE: <code>GraphSpec</code> </p> <code>direction</code> <p>Layout direction of the flowchart.</p> <p> TYPE: <code>Direction</code> DEFAULT: <code>'TD'</code> </p> <code>title</code> <p>An optional title shown above the diagram.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>highlight</code> <p>Node ids to draw with a highlight style.</p> <p> TYPE: <code>Iterable[str] | None</code> DEFAULT: <code>None</code> </p> <code>show_types</code> <p>Label each edge with the type of data it carries, read from the source node's declared output type (the graph input type for the start edge); a fan-in collect is labeled with the gathered list type.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>expand_subgraphs</code> <p>Draw each subgraph node as a nested cluster of its inner nodes, wiring edges to the inner nodes its flow enters and leaves, rather than a single opaque node.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> Source code in <code>src/openfactcheck/graph/mermaid.py</code> <pre><code>def to_mermaid(  # noqa: PLR0913 - public renderer; each argument is a distinct display option\n    spec: GraphSpec,\n    *,\n    direction: Direction = \"TD\",\n    title: str | None = None,\n    highlight: Iterable[str] | None = None,\n    show_types: bool = False,\n    expand_subgraphs: bool = False,\n) -&gt; str:\n    \"\"\"Render a graph definition as Mermaid flowchart source.\n\n    Args:\n        spec: The built graph's definition to render.\n        direction: Layout direction of the flowchart.\n        title: An optional title shown above the diagram.\n        highlight: Node ids to draw with a highlight style.\n        show_types: Label each edge with the type of data it carries, read from\n            the source node's declared output type (the graph input type for the\n            start edge); a fan-in collect is labeled with the gathered list type.\n        expand_subgraphs: Draw each subgraph node as a nested cluster of its inner\n            nodes, wiring edges to the inner nodes its flow enters and leaves,\n            rather than a single opaque node.\n\n    Returns:\n        Mermaid flowchart source as a string.\n    \"\"\"\n    doc = _Doc(expand=expand_subgraphs, show_types=show_types, highlighted=frozenset(highlight or ()))\n    if title is not None:\n        doc.lines += [\"---\", f\"title: {title}\", \"---\"]\n    doc.lines.append(f\"flowchart {direction}\")\n    _emit(doc, spec, prefix=\"\", root=True)\n    doc.lines.append(\"    classDef boundary fill:#005355,stroke:#005355,color:#ffffff;\")\n    doc.lines.append(f\"    class {','.join(doc.boundary)} boundary;\")\n    if doc.marked:\n        doc.lines.append(\"    classDef highlight fill:#fdff32,stroke:#333;\")\n        doc.lines.extend(f\"    class {rendered_id} highlight;\" for rendered_id in doc.marked)\n    return \"\\n\".join(doc.lines)\n</code></pre>"},{"location":"reference/graph/mermaid/#openfactcheck.graph.mermaid.to_mermaid_image","title":"to_mermaid_image","text":"<pre><code>to_mermaid_image(\n    source: str,\n    *,\n    base_url: str = \"https://mermaid.ink\",\n    image_type: ImageType = \"png\",\n    timeout: float = 30.0,\n) -&gt; bytes\n</code></pre> <p>Render Mermaid source to image bytes through a Mermaid server.</p> <p>The diagram is encoded into the request URL, so a very large graph can exceed the server's URL length limit; point <code>base_url</code> at a self-hosted server when that happens.</p> PARAMETER DESCRIPTION <code>source</code> <p>Mermaid diagram source, as returned by <code>to_mermaid</code>.</p> <p> TYPE: <code>str</code> </p> <code>base_url</code> <p>Base URL of the Mermaid rendering server.</p> <p> TYPE: <code>str</code> DEFAULT: <code>'https://mermaid.ink'</code> </p> <code>image_type</code> <p>Image format to request.</p> <p> TYPE: <code>ImageType</code> DEFAULT: <code>'png'</code> </p> <code>timeout</code> <p>Seconds to wait for the server before giving up.</p> <p> TYPE: <code>float</code> DEFAULT: <code>30.0</code> </p> Source code in <code>src/openfactcheck/graph/mermaid.py</code> <pre><code>def to_mermaid_image(\n    source: str,\n    *,\n    base_url: str = \"https://mermaid.ink\",\n    image_type: ImageType = \"png\",\n    timeout: float = 30.0,\n) -&gt; bytes:\n    \"\"\"Render Mermaid source to image bytes through a Mermaid server.\n\n    The diagram is encoded into the request URL, so a very large graph can\n    exceed the server's URL length limit; point ``base_url`` at a self-hosted\n    server when that happens.\n\n    Args:\n        source: Mermaid diagram source, as returned by [`to_mermaid`][to_mermaid].\n        base_url: Base URL of the Mermaid rendering server.\n        image_type: Image format to request.\n        timeout: Seconds to wait for the server before giving up.\n\n    Returns:\n        The rendered image's raw bytes.\n\n    Raises:\n        GraphRenderError: If ``base_url`` is not an http or https URL, or the\n            server cannot be reached or returns an error, including when the\n            diagram exceeds the request's limits.\n    \"\"\"\n    if urllib.parse.urlsplit(base_url).scheme not in {\"http\", \"https\"}:\n        raise GraphRenderError(f\"base_url must be an http or https URL, got {base_url!r}\")\n    encoded = base64.urlsafe_b64encode(source.encode()).decode(\"ascii\")\n    url = f\"{base_url}/{_IMAGE_PATHS[image_type]}/{encoded}\"\n    try:\n        response = httpx.get(url, timeout=timeout)\n        response.raise_for_status()\n    except httpx.HTTPError as exc:\n        raise GraphRenderError(f\"could not render diagram via {base_url}: {exc}\") from exc\n    return response.content\n</code></pre>"},{"location":"reference/graph/pause/","title":"Pause","text":"<p>A node that suspends a run to await external or human input.</p> <p>When a run reaches a <code>Pause</code> node, it snapshots its state and stops, raising <code>GraphPaused</code> with the value that arrived (the context for whoever answers) and an optional prompt. The run continues with <code>Graph.resume_with</code>, whose supplied value becomes the pause node's output and flows to its successor.</p>"},{"location":"reference/graph/pause/#openfactcheck.graph.pause.AnyPause","title":"AnyPause","text":"<pre><code>AnyPause = Pause[Any, Any]\n</code></pre> <p>A pause with its type parameters erased, for the executor's wiring layer.</p>"},{"location":"reference/graph/pause/#openfactcheck.graph.pause.Pause","title":"Pause  <code>dataclass</code>","text":"<pre><code>Pause(\n    id: str,\n    context_type: object | None,\n    answer_type: object | None,\n    prompt: str | None,\n)\n</code></pre> <p>A node that suspends a run until an answer is supplied on resume.</p> <p>Created by <code>GraphBuilder.pause_node</code>. Its first type parameter is the context type that flows in (what the pauser sees); its second is the answer type supplied on resume, which becomes the node's output.</p>"},{"location":"reference/graph/pause/#openfactcheck.graph.pause.Pause.id","title":"id  <code>instance-attribute</code>","text":"<pre><code>id: str\n</code></pre> <p>Stable identifier of this pause node.</p>"},{"location":"reference/graph/pause/#openfactcheck.graph.pause.Pause.context_type","title":"context_type  <code>instance-attribute</code>","text":"<pre><code>context_type: object | None\n</code></pre> <p>The type flowing in, recorded for build-time edge validation.</p>"},{"location":"reference/graph/pause/#openfactcheck.graph.pause.Pause.answer_type","title":"answer_type  <code>instance-attribute</code>","text":"<pre><code>answer_type: object | None\n</code></pre> <p>The type supplied on resume, recorded for build-time edge validation.</p>"},{"location":"reference/graph/pause/#openfactcheck.graph.pause.Pause.prompt","title":"prompt  <code>instance-attribute</code>","text":"<pre><code>prompt: str | None\n</code></pre> <p>A human-readable description of what is being asked, surfaced on pause.</p>"},{"location":"reference/graph/persistence/","title":"Persistence","text":"<p>The snapshot model and the pluggable store interface for run persistence.</p> <p>A <code>RunSnapshot</code> is a plain-data picture of a run at a task boundary: the tasks still to run, the join accumulators in progress, the loop counters, and the shared state, plus the terminal output once it exists. A <code>StateStore</code> saves and loads these snapshots.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunStatus","title":"RunStatus","text":"<p>               Bases: <code>StrEnum</code></p> <p>Where a run is in its lifecycle.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunStatus.CREATED","title":"CREATED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>CREATED = 'created'\n</code></pre> <p>Set up but not yet started.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunStatus.RUNNING","title":"RUNNING  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RUNNING = 'running'\n</code></pre> <p>Executing tasks.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunStatus.PAUSED","title":"PAUSED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PAUSED = 'paused'\n</code></pre> <p>Suspended awaiting external input.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunStatus.SUCCEEDED","title":"SUCCEEDED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>SUCCEEDED = 'succeeded'\n</code></pre> <p>Finished and produced its terminal output.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunStatus.FAILED","title":"FAILED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FAILED = 'failed'\n</code></pre> <p>Aborted by an error.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.TaskSnapshot","title":"TaskSnapshot  <code>dataclass</code>","text":"<pre><code>TaskSnapshot(\n    node_id: str, value: object, fork_stack: ForkStack\n)\n</code></pre> <p>A task that still needs to run when a run is resumed.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.TaskSnapshot.node_id","title":"node_id  <code>instance-attribute</code>","text":"<pre><code>node_id: str\n</code></pre> <p>Identifier of the node to run.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.TaskSnapshot.value","title":"value  <code>instance-attribute</code>","text":"<pre><code>value: object\n</code></pre> <p>The input value the node receives.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.TaskSnapshot.fork_stack","title":"fork_stack  <code>instance-attribute</code>","text":"<pre><code>fork_stack: ForkStack\n</code></pre> <p>The fork branch the task belongs to.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.JoinSnapshot","title":"JoinSnapshot  <code>dataclass</code>","text":"<pre><code>JoinSnapshot(\n    downstream_stack: ForkStack,\n    acc: object,\n    count: int,\n    items: list[tuple[int, object]],\n)\n</code></pre> <p>One join's accumulation in progress at snapshot time.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.JoinSnapshot.downstream_stack","title":"downstream_stack  <code>instance-attribute</code>","text":"<pre><code>downstream_stack: ForkStack\n</code></pre> <p>The fork stack the join emits with once it fires.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.JoinSnapshot.acc","title":"acc  <code>instance-attribute</code>","text":"<pre><code>acc: object\n</code></pre> <p>The accumulator so far, for an unordered reducer.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.JoinSnapshot.count","title":"count  <code>instance-attribute</code>","text":"<pre><code>count: int\n</code></pre> <p>How many branches have arrived, for an unordered reducer.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.JoinSnapshot.items","title":"items  <code>instance-attribute</code>","text":"<pre><code>items: list[tuple[int, object]]\n</code></pre> <p>The collected <code>(branch index, value)</code> pairs, for an ordered collect.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.PausePoint","title":"PausePoint  <code>dataclass</code>","text":"<pre><code>PausePoint(\n    node_id: str,\n    context: object,\n    fork_stack: ForkStack,\n    prompt: str | None,\n)\n</code></pre> <p>Where a run suspended at a pause node, and what it is asking for.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.PausePoint.node_id","title":"node_id  <code>instance-attribute</code>","text":"<pre><code>node_id: str\n</code></pre> <p>Identifier of the pause node the run stopped at.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.PausePoint.context","title":"context  <code>instance-attribute</code>","text":"<pre><code>context: object\n</code></pre> <p>The value that arrived at the pause node, shown to whoever answers.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.PausePoint.fork_stack","title":"fork_stack  <code>instance-attribute</code>","text":"<pre><code>fork_stack: ForkStack\n</code></pre> <p>The fork branch the paused value belongs to.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.PausePoint.prompt","title":"prompt  <code>instance-attribute</code>","text":"<pre><code>prompt: str | None\n</code></pre> <p>A human-readable description of what is being asked, if the node set one.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot","title":"RunSnapshot  <code>dataclass</code>","text":"<pre><code>RunSnapshot(\n    run_id: str,\n    status: RunStatus,\n    pending: tuple[TaskSnapshot, ...],\n    reducers: dict[tuple[str, str], JoinSnapshot],\n    expected: dict[str, int],\n    loops: dict[tuple[str, str, ForkStack], int],\n    finalized: frozenset[tuple[str, str]],\n    fork_seq: int,\n    has_final: bool,\n    final: object,\n    state: object,\n    paused: PausePoint | None = None,\n)\n</code></pre> <p>A run's resumable state captured at a task boundary.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.run_id","title":"run_id  <code>instance-attribute</code>","text":"<pre><code>run_id: str\n</code></pre> <p>Identifier the snapshot is stored and resumed under.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.status","title":"status  <code>instance-attribute</code>","text":"<pre><code>status: RunStatus\n</code></pre> <p>The run's lifecycle status when the snapshot was taken.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.pending","title":"pending  <code>instance-attribute</code>","text":"<pre><code>pending: tuple[TaskSnapshot, ...]\n</code></pre> <p>Tasks spawned but not yet processed; re-run on resume.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.reducers","title":"reducers  <code>instance-attribute</code>","text":"<pre><code>reducers: dict[tuple[str, str], JoinSnapshot]\n</code></pre> <p>Join accumulations in progress, keyed by <code>(join id, fork run id)</code>.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.expected","title":"expected  <code>instance-attribute</code>","text":"<pre><code>expected: dict[str, int]\n</code></pre> <p>Branch counts per fork run, keyed by fork run id.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.loops","title":"loops  <code>instance-attribute</code>","text":"<pre><code>loops: dict[tuple[str, str, ForkStack], int]\n</code></pre> <p>Loop-branch traversal counts, keyed by <code>(source, destination, fork stack)</code>.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.finalized","title":"finalized  <code>instance-attribute</code>","text":"<pre><code>finalized: frozenset[tuple[str, str]]\n</code></pre> <p>Joins already fired, keyed by <code>(join id, fork run id)</code>.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.fork_seq","title":"fork_seq  <code>instance-attribute</code>","text":"<pre><code>fork_seq: int\n</code></pre> <p>The fork-run id counter, so resumed forks keep minting distinct ids.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.has_final","title":"has_final  <code>instance-attribute</code>","text":"<pre><code>has_final: bool\n</code></pre> <p>Whether the end node was reached and <code>final</code> holds the output.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.final","title":"final  <code>instance-attribute</code>","text":"<pre><code>final: object\n</code></pre> <p>The terminal output when <code>has_final</code> is true, otherwise <code>None</code>.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.state","title":"state  <code>instance-attribute</code>","text":"<pre><code>state: object\n</code></pre> <p>The run-scoped state shared across nodes.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.RunSnapshot.paused","title":"paused  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>paused: PausePoint | None = None\n</code></pre> <p>Where the run is suspended when its status is paused, otherwise <code>None</code>.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.StateStore","title":"StateStore","text":"<p>               Bases: <code>Protocol</code></p> <p>Saves and loads run snapshots so a run can be resumed.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.StateStore.save","title":"save  <code>async</code>","text":"<pre><code>save(snapshot: RunSnapshot) -&gt; None\n</code></pre> <p>Append <code>snapshot</code> to its run's history.</p> Source code in <code>src/openfactcheck/graph/persistence/protocols.py</code> <pre><code>async def save(self, snapshot: RunSnapshot) -&gt; None:\n    \"\"\"Append ``snapshot`` to its run's history.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.StateStore.load","title":"load  <code>async</code>","text":"<pre><code>load(run_id: str) -&gt; RunSnapshot | None\n</code></pre> <p>Return the latest snapshot for <code>run_id</code>, or <code>None</code> if there is none.</p> Source code in <code>src/openfactcheck/graph/persistence/protocols.py</code> <pre><code>async def load(self, run_id: str) -&gt; RunSnapshot | None:\n    \"\"\"Return the latest snapshot for ``run_id``, or ``None`` if there is none.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.protocols.StateStore.history","title":"history  <code>async</code>","text":"<pre><code>history(run_id: str) -&gt; list[RunSnapshot]\n</code></pre> <p>Return every snapshot saved for <code>run_id</code>, oldest first.</p> Source code in <code>src/openfactcheck/graph/persistence/protocols.py</code> <pre><code>async def history(self, run_id: str) -&gt; list[RunSnapshot]:\n    \"\"\"Return every snapshot saved for ``run_id``, oldest first.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/graph/persistence/#in-memory-store","title":"In-memory store","text":"<p>An in-process store that keeps each run's snapshot history in memory.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.in_memory.InMemoryStateStore","title":"InMemoryStateStore","text":"<pre><code>InMemoryStateStore()\n</code></pre> <p>Keeps a per-run snapshot history in memory.</p> <p>The default store: it deep-copies each snapshot on the way in and out, so later mutation of a run's state cannot corrupt saved history. Suitable for tests and for resuming within a single process; it does not survive restart.</p> <p>Start with no saved runs.</p> Source code in <code>src/openfactcheck/graph/persistence/in_memory.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Start with no saved runs.\"\"\"\n    self._runs: dict[str, list[RunSnapshot]] = {}\n</code></pre>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.in_memory.InMemoryStateStore.save","title":"save  <code>async</code>","text":"<pre><code>save(snapshot: RunSnapshot) -&gt; None\n</code></pre> <p>Append a deep copy of <code>snapshot</code> to its run's history.</p> Source code in <code>src/openfactcheck/graph/persistence/in_memory.py</code> <pre><code>async def save(self, snapshot: RunSnapshot) -&gt; None:\n    \"\"\"Append a deep copy of ``snapshot`` to its run's history.\"\"\"\n    self._runs.setdefault(snapshot.run_id, []).append(copy.deepcopy(snapshot))\n</code></pre>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.in_memory.InMemoryStateStore.load","title":"load  <code>async</code>","text":"<pre><code>load(run_id: str) -&gt; RunSnapshot | None\n</code></pre> <p>Return a deep copy of the latest snapshot for <code>run_id</code>, or <code>None</code>.</p> Source code in <code>src/openfactcheck/graph/persistence/in_memory.py</code> <pre><code>async def load(self, run_id: str) -&gt; RunSnapshot | None:\n    \"\"\"Return a deep copy of the latest snapshot for ``run_id``, or ``None``.\"\"\"\n    history = self._runs.get(run_id)\n    if not history:\n        return None\n    return copy.deepcopy(history[-1])\n</code></pre>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.in_memory.InMemoryStateStore.history","title":"history  <code>async</code>","text":"<pre><code>history(run_id: str) -&gt; list[RunSnapshot]\n</code></pre> <p>Return deep copies of every snapshot for <code>run_id</code>, oldest first.</p> Source code in <code>src/openfactcheck/graph/persistence/in_memory.py</code> <pre><code>async def history(self, run_id: str) -&gt; list[RunSnapshot]:\n    \"\"\"Return deep copies of every snapshot for ``run_id``, oldest first.\"\"\"\n    return [copy.deepcopy(snapshot) for snapshot in self._runs.get(run_id, [])]\n</code></pre>"},{"location":"reference/graph/persistence/#file-store","title":"File store","text":"<p>A file-backed store that persists each run's snapshot history to disk.</p>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.file.FileStateStore","title":"FileStateStore","text":"<pre><code>FileStateStore(directory: Path)\n</code></pre> <p>Persists run snapshots to disk so a run survives across processes.</p> <p>Each run's history is a single JSON file in the store directory. A save writes to a temporary file and atomically replaces the target, so a crash mid-write cannot corrupt the history.</p> <p>A run's values are stored as JSON, so any value it carries in its state must be JSON-serializable. A single run should have one writer at a time.</p> <p>Persist runs under <code>directory</code>, creating it if needed.</p> Source code in <code>src/openfactcheck/graph/persistence/file.py</code> <pre><code>def __init__(self, directory: Path) -&gt; None:\n    \"\"\"Persist runs under ``directory``, creating it if needed.\"\"\"\n    self._directory = directory\n    self._directory.mkdir(parents=True, exist_ok=True)\n</code></pre>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.file.FileStateStore.save","title":"save  <code>async</code>","text":"<pre><code>save(snapshot: RunSnapshot) -&gt; None\n</code></pre> <p>Append <code>snapshot</code> to its run's history file, replacing it atomically.</p> Source code in <code>src/openfactcheck/graph/persistence/file.py</code> <pre><code>async def save(self, snapshot: RunSnapshot) -&gt; None:\n    \"\"\"Append ``snapshot`` to its run's history file, replacing it atomically.\"\"\"\n    history = await self.history(snapshot.run_id)\n    history.append(snapshot)\n    await asyncio.to_thread(self._write, snapshot.run_id, history)\n</code></pre>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.file.FileStateStore.load","title":"load  <code>async</code>","text":"<pre><code>load(run_id: str) -&gt; RunSnapshot | None\n</code></pre> <p>Return the latest snapshot for <code>run_id</code>, or <code>None</code> if there is none.</p> Source code in <code>src/openfactcheck/graph/persistence/file.py</code> <pre><code>async def load(self, run_id: str) -&gt; RunSnapshot | None:\n    \"\"\"Return the latest snapshot for ``run_id``, or ``None`` if there is none.\"\"\"\n    history = await self.history(run_id)\n    return history[-1] if history else None\n</code></pre>"},{"location":"reference/graph/persistence/#openfactcheck.graph.persistence.file.FileStateStore.history","title":"history  <code>async</code>","text":"<pre><code>history(run_id: str) -&gt; list[RunSnapshot]\n</code></pre> <p>Return every snapshot saved for <code>run_id</code>, oldest first.</p> Source code in <code>src/openfactcheck/graph/persistence/file.py</code> <pre><code>async def history(self, run_id: str) -&gt; list[RunSnapshot]:\n    \"\"\"Return every snapshot saved for ``run_id``, oldest first.\"\"\"\n    return await asyncio.to_thread(self._read, run_id)\n</code></pre>"},{"location":"reference/graph/step/","title":"Steps","text":"<p>Typed nodes, edges, and the per-invocation context for the graph layer.</p> <p>A graph is built from nodes connected by edges. The work-bearing node is a <code>Step</code>: an async unit with one typed input and one typed output. <code>StartNode</code> and <code>EndNode</code> mark where a run begins and ends. Edges name a source and a destination by id.</p> <p>A step's own function is fully statically typed (its <code>StepContext.inputs</code> and its return value). The wiring between nodes is validated when the graph is built rather than by the type checker, so the executor treats node inputs and outputs opaquely; see <code>GraphBuilder.build</code> for the build-time checks.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.START_ID","title":"START_ID  <code>module-attribute</code>","text":"<pre><code>START_ID = '__start__'\n</code></pre> <p>Fixed identifier of every graph's start node.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.END_ID","title":"END_ID  <code>module-attribute</code>","text":"<pre><code>END_ID = '__end__'\n</code></pre> <p>Fixed identifier of every graph's end node.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.AnyStep","title":"AnyStep","text":"<pre><code>AnyStep = Step[Any, Any, Any, Any]\n</code></pre> <p>A step with its type parameters erased, for the executor's wiring layer.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.SourceNode","title":"SourceNode","text":"<pre><code>SourceNode = (\n    Step[Any, OutputT, StateT, DepsT]\n    | StartNode[OutputT]\n    | Join[Any, OutputT]\n    | Pause[Any, OutputT]\n)\n</code></pre> <p>A node an edge may leave, projected to the output type it emits.</p> <p>Lets <code>GraphBuilder.edge_from</code> capture a source node's output type regardless of what input the node accepts.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.DestNode","title":"DestNode","text":"<pre><code>DestNode = (\n    Step[InputT, Any, StateT, DepsT]\n    | EndNode[InputT]\n    | Join[InputT, Any]\n    | Decision[InputT, StateT, DepsT]\n    | Pause[InputT, Any]\n)\n</code></pre> <p>A node an edge may enter, projected to the input type it accepts.</p> <p>Lets <code>EdgePathBuilder.to</code> require a destination whose input type the source node's output can feed, regardless of what the destination emits.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.StepContext","title":"StepContext  <code>dataclass</code>","text":"<pre><code>StepContext(\n    inputs: InputT,\n    state: StateT,\n    deps: DepsT,\n    emit: Callable[[object], None] = _discard,\n    streaming: bool = False,\n)\n</code></pre> <p>               Bases: <code>Generic[InputT, StateT, DepsT]</code></p> <p>Context passed to a step function on each invocation.</p> <p>Carries the node's typed <code>inputs</code> alongside the run-scoped <code>state</code> and the injected <code>deps</code>, both shared across every node in a single run.</p> <p>The type parameters, in order, are:</p> <ol> <li><code>InputT</code>: the type of <code>inputs</code>, the value flowing into this node.</li> <li><code>StateT</code>: the type of <code>state</code>, the run-scoped state shared across    nodes. Optional; defaults to <code>None</code> when the graph keeps no state.</li> <li><code>DepsT</code>: the type of <code>deps</code>, the read-only dependencies injected into    nodes (clients, configuration). Optional; defaults to <code>None</code>.</li> </ol> <p>A step annotates its single parameter with this context; the function's return type is the node's output. The output type is not a parameter here because it lives on the step's return annotation. State and deps may be omitted, so a step with neither annotates <code>StepContext[str]</code>.</p> Example <pre><code># this node receives a str, with a Deps bag and no shared state.\nasync def extract(ctx: StepContext[str, None, Deps]) -&gt; list[Claim]:\n    client = ctx.deps.chat_client\n    ...\n</code></pre>"},{"location":"reference/graph/step/#openfactcheck.graph.step.StepContext.inputs","title":"inputs  <code>instance-attribute</code>","text":"<pre><code>inputs: InputT\n</code></pre> <p>The value flowing into this node along its incoming edge.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.StepContext.state","title":"state  <code>instance-attribute</code>","text":"<pre><code>state: StateT\n</code></pre> <p>Run-scoped state shared across nodes for cross-cutting context.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.StepContext.deps","title":"deps  <code>instance-attribute</code>","text":"<pre><code>deps: DepsT\n</code></pre> <p>Run-scoped dependencies (clients, configuration) injected into nodes.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.StepContext.emit","title":"emit  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>emit: Callable[[object], None] = _discard\n</code></pre> <p>Emit a progress datum from inside this node while it runs.</p> <p>Call it with any value to surface that value as a <code>NodeEmitted</code> event tagged with this node, for instance to forward chat token fragments as they arrive. The datum is forwarded only when the run sets <code>stream_node_data</code> on <code>RunOptions</code>; otherwise it is discarded, so a node can emit unconditionally at no cost when nothing observes it.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.StepContext.streaming","title":"streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>streaming: bool = False\n</code></pre> <p>Whether emitted data is observed on this run.</p> <p>True only when the run sets <code>stream_node_data</code> on <code>RunOptions</code>. A node that can stream partial results (for instance through a component's <code>on_partial</code> hook) should do so only when this is set, and take its cheaper one-shot path otherwise.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Step","title":"Step  <code>dataclass</code>","text":"<pre><code>Step(\n    id: str,\n    call: Callable[\n        [StepContext[InputT, StateT, DepsT]],\n        Awaitable[OutputT],\n    ],\n    input_type: object | None,\n    output_type: object | None,\n    retries: int = 0,\n    retry_backoff: float = 0.0,\n    timeout: float | None = None,\n)\n</code></pre> <p>A typed async node: one input value in, one output value out.</p> <p>Created by <code>GraphBuilder.step_node</code> (usually as the <code>@g.step_node</code> decorator), then referenced when wiring edges. The recorded <code>input_type</code> and <code>output_type</code> are read from the wrapped function's annotations and used to validate edge compatibility at build time.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Step.id","title":"id  <code>instance-attribute</code>","text":"<pre><code>id: str\n</code></pre> <p>Stable identifier, supplied to <code>step_node</code> or taken from the function's name.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Step.call","title":"call  <code>instance-attribute</code>","text":"<pre><code>call: Callable[\n    [StepContext[InputT, StateT, DepsT]], Awaitable[OutputT]\n]\n</code></pre> <p>The async function this node runs.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Step.input_type","title":"input_type  <code>instance-attribute</code>","text":"<pre><code>input_type: object | None\n</code></pre> <p>The declared input type, or <code>None</code> when it could not be read.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Step.output_type","title":"output_type  <code>instance-attribute</code>","text":"<pre><code>output_type: object | None\n</code></pre> <p>The declared output type, or <code>None</code> when it could not be read.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Step.retries","title":"retries  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>retries: int = 0\n</code></pre> <p>How many extra attempts to make after the first if the call fails.</p> <p>Must be non-negative.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Step.retry_backoff","title":"retry_backoff  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>retry_backoff: float = 0.0\n</code></pre> <p>Base seconds to wait before a retry; the wait doubles each further attempt.</p> <p>Must be non-negative.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Step.timeout","title":"timeout  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>timeout: float | None = None\n</code></pre> <p>Seconds a single attempt may run before it is cancelled, or <code>None</code> for no limit.</p> <p>Must be positive when set.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.StartNode","title":"StartNode  <code>dataclass</code>","text":"<pre><code>StartNode(id: str = START_ID)\n</code></pre> <p>The graph's entry node; the graph's input value flows out of it.</p> <p>Its type parameter is the graph input type, matched against the first step's input type when wiring the entry edge.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.StartNode.id","title":"id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>id: str = START_ID\n</code></pre> <p>Fixed start-node identifier.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.EndNode","title":"EndNode  <code>dataclass</code>","text":"<pre><code>EndNode(id: str = END_ID)\n</code></pre> <p>The graph's terminal node; the value routed into it is the graph output.</p> <p>Its type parameter is the graph output type, matched against the last step's output type when wiring the exit edge.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.EndNode.id","title":"id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>id: str = END_ID\n</code></pre> <p>Fixed end-node identifier.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.EdgeKind","title":"EdgeKind","text":"<p>               Bases: <code>StrEnum</code></p> <p>How an edge delivers its source node's output to its destination.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.EdgeKind.PLAIN","title":"PLAIN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PLAIN = 'plain'\n</code></pre> <p>Deliver the whole output as one value to the destination.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.EdgeKind.MAP","title":"MAP  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>MAP = 'map'\n</code></pre> <p>Fan an iterable output out to one parallel branch per item.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Edge","title":"Edge  <code>dataclass</code>","text":"<pre><code>Edge(\n    source_id: str,\n    dest_id: str,\n    kind: EdgeKind = EdgeKind.PLAIN,\n)\n</code></pre> <p>A directed connection from one node to another, named by id.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Edge.source_id","title":"source_id  <code>instance-attribute</code>","text":"<pre><code>source_id: str\n</code></pre> <p>Identifier of the node the edge leaves.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Edge.dest_id","title":"dest_id  <code>instance-attribute</code>","text":"<pre><code>dest_id: str\n</code></pre> <p>Identifier of the node the edge enters.</p>"},{"location":"reference/graph/step/#openfactcheck.graph.step.Edge.kind","title":"kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>kind: EdgeKind = PLAIN\n</code></pre> <p>Whether the edge delivers the whole output or fans an iterable per item.</p>"},{"location":"reference/integrations/","title":"Integrations","text":"<p>Typed clients for external services that components depend on. Integrations are domain-agnostic: they return service-shaped data, and a component maps that into the fact-checking types. They implement no category contract.</p> <ul> <li>Serper: typed async client for the Serper.dev Google Search API, covering search and webpage scraping.</li> </ul>"},{"location":"reference/integrations/serper/","title":"Serper","text":"<p>Typed async client for the Serper.dev Google Search API.</p> <p>Build a <code>SerperClient</code>, then call <code>search</code> or <code>scrape</code>; results come back as typed models. Import from <code>openfactcheck.integrations.serper</code>.</p> Example <pre><code>from openfactcheck.integrations.serper import SerperClient\n\nclient = SerperClient()\nresult = await client.search(\"who founded openai\")\nprint(result.organic[0].link)\n</code></pre>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.DEFAULT_SCRAPE_BASE_URL","title":"DEFAULT_SCRAPE_BASE_URL  <code>module-attribute</code>","text":"<pre><code>DEFAULT_SCRAPE_BASE_URL = 'https://scrape.serper.dev'\n</code></pre> <p>Default base URL for the Serper webpage scrape endpoint.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.DEFAULT_SEARCH_BASE_URL","title":"DEFAULT_SEARCH_BASE_URL  <code>module-attribute</code>","text":"<pre><code>DEFAULT_SEARCH_BASE_URL = 'https://google.serper.dev'\n</code></pre> <p>Default base URL for the Serper search endpoints.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.DEFAULT_TIMEOUT","title":"DEFAULT_TIMEOUT  <code>module-attribute</code>","text":"<pre><code>DEFAULT_TIMEOUT = 30.0\n</code></pre> <p>Default per-request timeout in seconds.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperTimeRange","title":"SerperTimeRange","text":"<pre><code>SerperTimeRange = Literal[\n    \"qdr:h\", \"qdr:d\", \"qdr:w\", \"qdr:m\", \"qdr:y\"\n]\n</code></pre> <p>Recency window for results: past hour, day, week, month, or year.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperClient","title":"SerperClient","text":"<pre><code>SerperClient(\n    api_key: str | None = None,\n    *,\n    base_url: str = DEFAULT_SEARCH_BASE_URL,\n    scrape_base_url: str = DEFAULT_SCRAPE_BASE_URL,\n    timeout: float = DEFAULT_TIMEOUT,\n    gl: str | None = None,\n    hl: str | None = None,\n)\n</code></pre> <p>Async client for the Serper.dev Google Search API.</p> <p>Exposes the search and webpage-scrape endpoints as typed methods. The API key is read from the <code>api_key</code> argument or the <code>SERPER_API_KEY</code> environment variable. New endpoints are cheap to add: route them through the shared request path.</p> Example <pre><code>client = SerperClient()\nresult = await client.search(\"who founded openai\")\nfor organic in result.organic:\n    print(organic.title, organic.link)\n</code></pre> <p>Build a Serper client.</p> PARAMETER DESCRIPTION <code>api_key</code> <p>Serper API key. Falls back to the <code>SERPER_API_KEY</code> environment variable when omitted.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>base_url</code> <p>Base URL for the search endpoints.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_SEARCH_BASE_URL</code> </p> <code>scrape_base_url</code> <p>Base URL for the webpage scrape endpoint.</p> <p> TYPE: <code>str</code> DEFAULT: <code>DEFAULT_SCRAPE_BASE_URL</code> </p> <code>timeout</code> <p>Per-request timeout in seconds.</p> <p> TYPE: <code>float</code> DEFAULT: <code>DEFAULT_TIMEOUT</code> </p> <code>gl</code> <p>Default country code applied to bare-string searches.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>hl</code> <p>Default language code applied to bare-string searches.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/integrations/serper/client.py</code> <pre><code>def __init__(\n    self,\n    api_key: str | None = None,\n    *,\n    base_url: str = DEFAULT_SEARCH_BASE_URL,\n    scrape_base_url: str = DEFAULT_SCRAPE_BASE_URL,\n    timeout: float = DEFAULT_TIMEOUT,\n    gl: str | None = None,\n    hl: str | None = None,\n) -&gt; None:\n    \"\"\"Build a Serper client.\n\n    Args:\n        api_key: Serper API key. Falls back to the ``SERPER_API_KEY``\n            environment variable when omitted.\n        base_url: Base URL for the search endpoints.\n        scrape_base_url: Base URL for the webpage scrape endpoint.\n        timeout: Per-request timeout in seconds.\n        gl: Default country code applied to bare-string searches.\n        hl: Default language code applied to bare-string searches.\n\n    Raises:\n        SerperConfigError: No API key was given or found in the environment.\n    \"\"\"\n    key = api_key if api_key is not None else os.environ.get(\"SERPER_API_KEY\")\n    if not key:\n        raise SerperConfigError(\"no Serper API key; pass api_key= or set the SERPER_API_KEY environment variable.\")\n    self._api_key = key\n    self._base_url = base_url.rstrip(\"/\")\n    self._scrape_base_url = scrape_base_url.rstrip(\"/\")\n    self._timeout = timeout\n    self._gl = gl\n    self._hl = hl\n</code></pre>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperClient.search","title":"search  <code>async</code>","text":"<pre><code>search(query: str | SearchParams) -&gt; SearchResponse\n</code></pre> <p>Run a single web search.</p> PARAMETER DESCRIPTION <code>query</code> <p>A query string (using the client's default <code>gl</code> and <code>hl</code>) or a fully specified <code>SearchParams</code>.</p> <p> TYPE: <code>str | SearchParams</code> </p> Source code in <code>src/openfactcheck/integrations/serper/client.py</code> <pre><code>async def search(self, query: str | SearchParams) -&gt; SearchResponse:\n    \"\"\"Run a single web search.\n\n    Args:\n        query: A query string (using the client's default ``gl`` and\n            ``hl``) or a fully specified [`SearchParams`][SearchParams].\n\n    Returns:\n        The parsed search response.\n\n    Raises:\n        SerperRequestError: The request failed or returned an error status.\n    \"\"\"\n    data = await self._post(f\"{self._base_url}/search\", self._params_for(query).to_payload())\n    return SearchResponse.model_validate(data)\n</code></pre>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperClient.search_batch","title":"search_batch  <code>async</code>","text":"<pre><code>search_batch(\n    queries: Sequence[str | SearchParams],\n) -&gt; list[SearchResponse]\n</code></pre> <p>Run several web searches in a single request.</p> PARAMETER DESCRIPTION <code>queries</code> <p>Query strings or <code>SearchParams</code> to search for together.</p> <p> TYPE: <code>Sequence[str | SearchParams]</code> </p> Source code in <code>src/openfactcheck/integrations/serper/client.py</code> <pre><code>async def search_batch(self, queries: Sequence[str | SearchParams]) -&gt; list[SearchResponse]:\n    \"\"\"Run several web searches in a single request.\n\n    Args:\n        queries: Query strings or [`SearchParams`][SearchParams] to search\n            for together.\n\n    Returns:\n        One response per query, in the order given.\n\n    Raises:\n        SerperRequestError: The request failed, returned an error status, or\n            did not return a list.\n    \"\"\"\n    payload = [self._params_for(query).to_payload() for query in queries]\n    data = await self._post(f\"{self._base_url}/search\", payload)\n    if not isinstance(data, list):\n        raise SerperRequestError(\"expected a list response for a batch search.\")\n    return [SearchResponse.model_validate(item) for item in cast(\"list[object]\", data)]\n</code></pre>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperClient.scrape","title":"scrape  <code>async</code>","text":"<pre><code>scrape(\n    url: str, *, include_markdown: bool = False\n) -&gt; ScrapeResponse\n</code></pre> <p>Extract the contents of a web page.</p> PARAMETER DESCRIPTION <code>url</code> <p>The page to scrape.</p> <p> TYPE: <code>str</code> </p> <code>include_markdown</code> <p>Whether to also return the page as Markdown.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> Source code in <code>src/openfactcheck/integrations/serper/client.py</code> <pre><code>async def scrape(self, url: str, *, include_markdown: bool = False) -&gt; ScrapeResponse:\n    \"\"\"Extract the contents of a web page.\n\n    Args:\n        url: The page to scrape.\n        include_markdown: Whether to also return the page as Markdown.\n\n    Returns:\n        The page's extracted text, metadata, and optional Markdown.\n\n    Raises:\n        SerperRequestError: The request failed or returned an error status.\n    \"\"\"\n    data = await self._post(self._scrape_base_url, {\"url\": url, \"includeMarkdown\": include_markdown})\n    return ScrapeResponse.model_validate(data)\n</code></pre>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperSpec","title":"SerperSpec","text":"<p>               Bases: <code>BaseModel</code></p> <p>Settings for the Serper web-search service.</p> <p>An unset field takes the client's default. The API key falls back to the one supplied at build time, then to the <code>SERPER_API_KEY</code> environment variable.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperSpec.api_key","title":"api_key  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>api_key: SecretStr | None = None\n</code></pre> <p>Serper API key, overriding the fallback supplied at build time.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperSpec.gl","title":"gl  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gl: str | None = None\n</code></pre> <p>Country code applied to searches (for example <code>\"us\"</code>).</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperSpec.hl","title":"hl  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>hl: str | None = None\n</code></pre> <p>Language code applied to searches (for example <code>\"en\"</code>).</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperSpec.timeout","title":"timeout  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>timeout: float | None = Field(default=None, gt=0)\n</code></pre> <p>Per-request timeout in seconds.</p> <p>Must be positive.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperSpec.to_client","title":"to_client","text":"<pre><code>to_client(\n    *, fallback_api_key: SecretStr | None = None\n) -&gt; SerperClient\n</code></pre> <p>Build a Serper client from this spec.</p> PARAMETER DESCRIPTION <code>fallback_api_key</code> <p>API key to use when this spec sets none; an empty value defers to the <code>SERPER_API_KEY</code> environment variable.</p> <p> TYPE: <code>SecretStr | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/integrations/serper/config.py</code> <pre><code>def to_client(self, *, fallback_api_key: SecretStr | None = None) -&gt; SerperClient:\n    \"\"\"Build a Serper client from this spec.\n\n    Args:\n        fallback_api_key: API key to use when this spec sets none; an empty value defers to the\n            ``SERPER_API_KEY`` environment variable.\n\n    Returns:\n        A Serper client for the resolved key, locale, and timeout.\n    \"\"\"\n    key = self.api_key if self.api_key is not None else fallback_api_key\n    api_key = key.get_secret_value() if key is not None else None\n    return SerperClient(\n        api_key=api_key or None,\n        gl=self.gl,\n        hl=self.hl,\n        timeout=self.timeout if self.timeout is not None else DEFAULT_TIMEOUT,\n    )\n</code></pre>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperConfigError","title":"SerperConfigError","text":"<p>               Bases: <code>SerperError</code></p> <p>The client is missing required configuration, such as the API key.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperError","title":"SerperError","text":"<p>               Bases: <code>Exception</code></p> <p>Base error for every Serper.dev integration failure.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SerperRequestError","title":"SerperRequestError","text":"<p>               Bases: <code>SerperError</code></p> <p>A request to the Serper.dev API failed or returned an error status.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams","title":"SearchParams","text":"<p>               Bases: <code>BaseModel</code></p> <p>Parameters for a Serper.dev search request.</p> <p>Only <code>q</code> is required. Unset fields are dropped from the request body so the service applies its own defaults.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.q","title":"q  <code>instance-attribute</code>","text":"<pre><code>q: str\n</code></pre> <p>The search query.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.gl","title":"gl  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gl: str | None = None\n</code></pre> <p>Country code for results, such as <code>us</code>.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.hl","title":"hl  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>hl: str | None = None\n</code></pre> <p>Language code for results, such as <code>en</code>.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.location","title":"location  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>location: str | None = None\n</code></pre> <p>Location to search from, such as <code>SoHo, New York, United States</code>.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.num","title":"num  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num: int | None = None\n</code></pre> <p>Number of results to return.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.page","title":"page  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>page: int | None = None\n</code></pre> <p>Result page number, starting at 1.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.tbs","title":"tbs  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tbs: SerperTimeRange | None = None\n</code></pre> <p>Restrict results to a recent time window.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.autocorrect","title":"autocorrect  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>autocorrect: bool | None = None\n</code></pre> <p>Whether the service may autocorrect the query spelling.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParams.to_payload","title":"to_payload","text":"<pre><code>to_payload() -&gt; dict[str, object]\n</code></pre> <p>Render the request body, omitting unset fields.</p> Source code in <code>src/openfactcheck/integrations/serper/params.py</code> <pre><code>def to_payload(self) -&gt; dict[str, object]:\n    \"\"\"Render the request body, omitting unset fields.\n\n    Returns:\n        A JSON-serialisable mapping of the parameters that were set.\n    \"\"\"\n    return self.model_dump(exclude_none=True)\n</code></pre>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.AnswerBox","title":"AnswerBox","text":"<p>               Bases: <code>BaseModel</code></p> <p>A direct answer or featured snippet for the query.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.AnswerBox.answer","title":"answer  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>answer: str | None = None\n</code></pre> <p>The direct answer, when the service extracts one.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.AnswerBox.snippet","title":"snippet  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>snippet: str | None = None\n</code></pre> <p>The featured snippet text.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.AnswerBox.snippet_highlighted","title":"snippet_highlighted  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>snippet_highlighted: list[str] | None = None\n</code></pre> <p>Highlighted phrases within the snippet.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.AnswerBox.title","title":"title  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>title: str | None = None\n</code></pre> <p>Title of the source page.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.AnswerBox.link","title":"link  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>link: str | None = None\n</code></pre> <p>URL of the source page.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.KnowledgeGraph","title":"KnowledgeGraph","text":"<p>               Bases: <code>BaseModel</code></p> <p>The structured entity panel shown alongside results.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.KnowledgeGraph.title","title":"title  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>title: str | None = None\n</code></pre> <p>The entity's name.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.KnowledgeGraph.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: str | None = None\n</code></pre> <p>The entity's category, such as <code>Company</code>.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.KnowledgeGraph.website","title":"website  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>website: str | None = None\n</code></pre> <p>The entity's official website.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.KnowledgeGraph.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str | None = None\n</code></pre> <p>A short description of the entity.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.KnowledgeGraph.attributes","title":"attributes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attributes: dict[str, str] = Field(\n    default_factory=dict[str, str]\n)\n</code></pre> <p>Key facts about the entity, such as its founder or founding date.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.OrganicResult","title":"OrganicResult","text":"<p>               Bases: <code>BaseModel</code></p> <p>A single organic (non-paid) search result.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.OrganicResult.title","title":"title  <code>instance-attribute</code>","text":"<pre><code>title: str\n</code></pre> <p>The result's title.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.OrganicResult.link","title":"link  <code>instance-attribute</code>","text":"<pre><code>link: str\n</code></pre> <p>The result's URL.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.OrganicResult.snippet","title":"snippet  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>snippet: str | None = None\n</code></pre> <p>A short extract from the page.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.OrganicResult.position","title":"position  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>position: int | None = None\n</code></pre> <p>The result's rank on the page, starting at 1.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.OrganicResult.date","title":"date  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>date: str | None = None\n</code></pre> <p>The page's date, when the service reports one.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.OrganicResult.sitelinks","title":"sitelinks  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>sitelinks: list[Sitelink] = Field(\n    default_factory=list[Sitelink]\n)\n</code></pre> <p>Secondary links shown beneath the result.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.OrganicResult.attributes","title":"attributes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attributes: dict[str, str] = Field(\n    default_factory=dict[str, str]\n)\n</code></pre> <p>Extra key-value details shown with the result.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.PeopleAlsoAsk","title":"PeopleAlsoAsk","text":"<p>               Bases: <code>BaseModel</code></p> <p>A related question shown in the \"People also ask\" panel.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.PeopleAlsoAsk.question","title":"question  <code>instance-attribute</code>","text":"<pre><code>question: str\n</code></pre> <p>The related question.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.PeopleAlsoAsk.snippet","title":"snippet  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>snippet: str | None = None\n</code></pre> <p>A short answer extract.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.PeopleAlsoAsk.title","title":"title  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>title: str | None = None\n</code></pre> <p>Title of the source page.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.PeopleAlsoAsk.link","title":"link  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>link: str | None = None\n</code></pre> <p>URL of the source page.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.RelatedSearch","title":"RelatedSearch","text":"<p>               Bases: <code>BaseModel</code></p> <p>A related query suggestion.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.RelatedSearch.query","title":"query  <code>instance-attribute</code>","text":"<pre><code>query: str\n</code></pre> <p>The suggested query.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.ScrapeResponse","title":"ScrapeResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>A response from the Serper.dev webpage scrape endpoint.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.ScrapeResponse.text","title":"text  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>text: str | None = None\n</code></pre> <p>The page's extracted plain text.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.ScrapeResponse.markdown","title":"markdown  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>markdown: str | None = None\n</code></pre> <p>The page rendered as Markdown, when requested.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.ScrapeResponse.metadata","title":"metadata  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>metadata: dict[str, str] = Field(\n    default_factory=dict[str, str]\n)\n</code></pre> <p>Metadata extracted from the page head.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.ScrapeResponse.jsonld","title":"jsonld  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>jsonld: dict[str, object] | None = None\n</code></pre> <p>JSON-LD structured data found on the page, when present.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.ScrapeResponse.credits","title":"credits  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>credits: int | None = None\n</code></pre> <p>API credits the scrape consumed.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters","title":"SearchParameters","text":"<p>               Bases: <code>BaseModel</code></p> <p>The search parameters echoed back by the service.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters.q","title":"q  <code>instance-attribute</code>","text":"<pre><code>q: str\n</code></pre> <p>The query that was searched.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: str | None = None\n</code></pre> <p>The search type, such as <code>search</code>.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters.gl","title":"gl  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gl: str | None = None\n</code></pre> <p>Country code applied to the search.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters.hl","title":"hl  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>hl: str | None = None\n</code></pre> <p>Language code applied to the search.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters.location","title":"location  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>location: str | None = None\n</code></pre> <p>Location applied to the search.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters.num","title":"num  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num: int | None = None\n</code></pre> <p>Number of results requested.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters.page","title":"page  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>page: int | None = None\n</code></pre> <p>Result page number.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchParameters.autocorrect","title":"autocorrect  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>autocorrect: bool | None = None\n</code></pre> <p>Whether the query spelling was autocorrected.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchResponse","title":"SearchResponse","text":"<p>               Bases: <code>BaseModel</code></p> <p>A response from the Serper.dev <code>/search</code> endpoint.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchResponse.search_parameters","title":"search_parameters  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>search_parameters: SearchParameters | None = None\n</code></pre> <p>The search parameters echoed by the service.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchResponse.organic","title":"organic  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>organic: list[OrganicResult] = Field(\n    default_factory=list[OrganicResult]\n)\n</code></pre> <p>The organic search results, in rank order.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchResponse.knowledge_graph","title":"knowledge_graph  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>knowledge_graph: KnowledgeGraph | None = None\n</code></pre> <p>The entity panel, when one is shown for the query.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchResponse.answer_box","title":"answer_box  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>answer_box: AnswerBox | None = None\n</code></pre> <p>The direct answer or featured snippet, when present.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchResponse.people_also_ask","title":"people_also_ask  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>people_also_ask: list[PeopleAlsoAsk] = Field(\n    default_factory=list[PeopleAlsoAsk]\n)\n</code></pre> <p>Related questions shown for the query.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.SearchResponse.related_searches","title":"related_searches  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>related_searches: list[RelatedSearch] = Field(\n    default_factory=list[RelatedSearch]\n)\n</code></pre> <p>Related query suggestions.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.Sitelink","title":"Sitelink","text":"<p>               Bases: <code>BaseModel</code></p> <p>A secondary link shown beneath an organic result.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.Sitelink.title","title":"title  <code>instance-attribute</code>","text":"<pre><code>title: str\n</code></pre> <p>The sitelink's display title.</p>"},{"location":"reference/integrations/serper/#openfactcheck.integrations.serper.Sitelink.link","title":"link  <code>instance-attribute</code>","text":"<pre><code>link: str\n</code></pre> <p>The sitelink's URL.</p>"},{"location":"reference/messages/","title":"Messages","text":"<p>Message types for the chat layer.</p> <p>Build a conversation as a list of messages, each carrying a <code>role</code> that identifies the sender, and pass it to a chat client to get a completion or stream. Backend implementations convert messages to and from the underlying provider format at the boundary.</p> Example <pre><code>from openfactcheck.messages import (\n    AssistantMessage,\n    SystemMessage,\n    ToolCall,\n    ToolMessage,\n    UserMessage,\n)\n\nconversation = [\n    SystemMessage(content=\"You are a fact-checker.\"),\n    UserMessage(content=\"Is the sky blue?\"),\n    AssistantMessage(\n        content=\"\",\n        tool_calls=[ToolCall(id=\"call_1\", name=\"search\", arguments='{\"q\": \"sky color\"}')],\n    ),\n    ToolMessage(content=\"Rayleigh scattering makes the sky blue.\", tool_call_id=\"call_1\"),\n    AssistantMessage(content=\"Yes, due to Rayleigh scattering.\"),\n]\n</code></pre>"},{"location":"reference/messages/#core-message-types","title":"Core message types","text":""},{"location":"reference/messages/#openfactcheck.messages.SystemMessage","title":"SystemMessage","text":"<p>               Bases: <code>BaseModel</code></p> <p>Instruction to the model, typically placed at the top of a conversation.</p> Example <pre><code>SystemMessage(content=\"You are a fact-checker. Answer in one sentence.\")\n</code></pre>"},{"location":"reference/messages/#openfactcheck.messages.SystemMessage.role","title":"role  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>role: Literal['system'] = 'system'\n</code></pre> <p>Role tag identifying this as a system instruction.</p>"},{"location":"reference/messages/#openfactcheck.messages.SystemMessage.content","title":"content  <code>instance-attribute</code>","text":"<pre><code>content: str\n</code></pre> <p>Text of the system instruction.</p>"},{"location":"reference/messages/#openfactcheck.messages.UserMessage","title":"UserMessage","text":"<p>               Bases: <code>BaseModel</code></p> <p>Input from the end user.</p> Example <pre><code>UserMessage(content=\"Is the sky really blue?\")\n</code></pre>"},{"location":"reference/messages/#openfactcheck.messages.UserMessage.role","title":"role  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>role: Literal['user'] = 'user'\n</code></pre> <p>Role tag identifying this as user input.</p>"},{"location":"reference/messages/#openfactcheck.messages.UserMessage.content","title":"content  <code>instance-attribute</code>","text":"<pre><code>content: str\n</code></pre> <p>Text of the user's message.</p>"},{"location":"reference/messages/#openfactcheck.messages.AssistantMessage","title":"AssistantMessage","text":"<p>               Bases: <code>BaseModel</code></p> <p>Reply from the model.</p> <p>Carries the natural-language response plus any tool calls the model wants to invoke before continuing.</p> Example <pre><code>AssistantMessage(content=\"Yes, due to Rayleigh scattering.\")\n</code></pre>"},{"location":"reference/messages/#openfactcheck.messages.AssistantMessage.role","title":"role  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>role: Literal['assistant'] = 'assistant'\n</code></pre> <p>Role tag identifying this as a model reply.</p>"},{"location":"reference/messages/#openfactcheck.messages.AssistantMessage.content","title":"content  <code>instance-attribute</code>","text":"<pre><code>content: str\n</code></pre> <p>Text of the model's reply. May be empty when <code>tool_calls</code> is populated.</p>"},{"location":"reference/messages/#openfactcheck.messages.AssistantMessage.tool_calls","title":"tool_calls  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tool_calls: list[ToolCall] | None = None\n</code></pre> <p>Tools the model is requesting. <code>None</code> when the model replied with text only.</p>"},{"location":"reference/messages/#openfactcheck.messages.ToolMessage","title":"ToolMessage","text":"<p>               Bases: <code>BaseModel</code></p> <p>Result of a tool invocation, fed back to the model.</p> Example <pre><code>ToolMessage(content=\"Rayleigh scattering makes the sky blue.\", tool_call_id=\"call_1\")\n</code></pre>"},{"location":"reference/messages/#openfactcheck.messages.ToolMessage.role","title":"role  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>role: Literal['tool'] = 'tool'\n</code></pre> <p>Role tag identifying this as a tool result.</p>"},{"location":"reference/messages/#openfactcheck.messages.ToolMessage.content","title":"content  <code>instance-attribute</code>","text":"<pre><code>content: str\n</code></pre> <p>Text of the tool's output. Typically a JSON string or a natural-language summary.</p>"},{"location":"reference/messages/#openfactcheck.messages.ToolMessage.tool_call_id","title":"tool_call_id  <code>instance-attribute</code>","text":"<pre><code>tool_call_id: str\n</code></pre> <p>Identifier of the <code>ToolCall</code> this result is answering.</p>"},{"location":"reference/messages/#tool-calls","title":"Tool calls","text":""},{"location":"reference/messages/#openfactcheck.messages.ToolCall","title":"ToolCall","text":"<p>               Bases: <code>BaseModel</code></p> <p>A tool invocation requested by the model.</p> <p>Emitted as part of an <code>AssistantMessage</code> when the model decides to call a tool. Match the resulting <code>ToolMessage</code> back to this call by <code>id</code>.</p> Example <pre><code>call = ToolCall(id=\"call_1\", name=\"search\", arguments='{\"q\": \"sky color\"}')\n</code></pre>"},{"location":"reference/messages/#openfactcheck.messages.ToolCall.id","title":"id  <code>instance-attribute</code>","text":"<pre><code>id: str\n</code></pre> <p>Unique identifier for this call, referenced by the matching <code>ToolMessage</code>.</p>"},{"location":"reference/messages/#openfactcheck.messages.ToolCall.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>Name of the tool the model is asking to invoke.</p>"},{"location":"reference/messages/#openfactcheck.messages.ToolCall.arguments","title":"arguments  <code>instance-attribute</code>","text":"<pre><code>arguments: str\n</code></pre> <p>JSON-encoded arguments for the tool.</p> <p>Kept as a string so arbitrary JSON round-trips across providers without type loss.</p>"},{"location":"reference/messages/#union-type","title":"Union type","text":""},{"location":"reference/messages/#openfactcheck.messages.Message","title":"Message  <code>module-attribute</code>","text":"<pre><code>Message = Annotated[\n    SystemMessage\n    | UserMessage\n    | AssistantMessage\n    | ToolMessage,\n    Discriminator(\"role\"),\n]\n</code></pre> <p>Union of every message type that can appear in a conversation.</p> <p>Callers build a <code>list[Message]</code> and pass it to a chat client; branch on <code>role</code> to distinguish the concrete type.</p>"},{"location":"reference/prompts/","title":"Prompts","text":"<p>Public API for the prompts layer.</p> <p>Build a <code>PromptTemplate</code> from chat messages with <code>{{variables}}</code> (or load one from a file with <code>PromptTemplate.from_file</code>), then fill it: to a chat message list with <code>to_messages</code>, to a <code>Prompt</code> value with <code>to_prompt</code>, or to a single string with <code>to_string</code>.</p> <p>Import everything from <code>openfactcheck.prompts</code> directly; submodule paths are not part of the public API.</p> Example <pre><code>from openfactcheck.prompts import PromptTemplate\n\nverifier = PromptTemplate.from_messages(\n    [\n        (\"system\", \"You are a fact-checker.\"),\n        (\"user\", \"Claim: {{claim}}\"),\n    ],\n    name=\"verifier\",\n)\nmessages = verifier.to_messages(claim=\"The sky is green.\")\n</code></pre>"},{"location":"reference/prompts/#pages","title":"Pages","text":"<ul> <li><code>PromptTemplate</code> class: the fillable template of chat messages whose content carries <code>{{variables}}</code>.</li> <li><code>Prompt</code> class: the filled value, ready to hand to the chat client.</li> <li>Variables: the role and variable-contract declarations a template is built from.</li> <li>Codecs: the file formats a template loads from and serializes to.</li> <li>Errors: exception hierarchy raised while building, loading, or filling a template.</li> </ul>"},{"location":"reference/prompts/errors/","title":"Errors","text":"<p>Exception hierarchy for the prompts layer.</p> <p>Every prompts-layer exception derives from <code>PromptError</code>. Subclasses also inherit from the closest stdlib exception so code that catches by stdlib type (<code>FileNotFoundError</code>, <code>ValueError</code>, <code>KeyError</code>) still works.</p> <p>There are two <code>ValueError</code> subclasses:</p> <ul> <li><code>PromptFormatError</code>: a codec-specific parse   failure. Malformed YAML, broken tag structure, missing required H1,   codec-local policy violation (for example, the markdown codec rejecting   repeated roles). Only codecs raise this.</li> <li><code>PromptValidationError</code>: a domain-invariant   failure. Undeclared placeholder reference, invalid identifier. Raised   from both codec-decoded and code-built templates because the rule is a   property of the domain, not of any single format.</li> </ul> <p>Users who want \"something wrong with this prompt\" catch <code>PromptError</code>. Users who want to branch on \"bad file format\" vs \"bad constructor input\" catch the specific subclass.</p> Example <pre><code>from openfactcheck.prompts import (\n    PromptError,\n    PromptFormatError,\n    PromptNotFoundError,\n    PromptTemplate,\n    PromptValidationError,\n)\n\ntry:\n    template = PromptTemplate.from_file(\"prompts/verifier.md\")\nexcept PromptNotFoundError:\n    ...  # no file at that path\nexcept PromptFormatError:\n    ...  # bad YAML or tag structure in a markdown file\nexcept PromptValidationError:\n    ...  # placeholder references an undeclared variable\nexcept PromptError:\n    ...  # anything else from the prompts layer\n</code></pre>"},{"location":"reference/prompts/errors/#base","title":"Base","text":""},{"location":"reference/prompts/errors/#openfactcheck.prompts.errors.PromptError","title":"PromptError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for every prompts-layer failure.</p> <p>Catch this to handle any failure from the prompts layer without branching on the specific cause.</p>"},{"location":"reference/prompts/errors/#specific-failures","title":"Specific failures","text":""},{"location":"reference/prompts/errors/#openfactcheck.prompts.errors.PromptNotFoundError","title":"PromptNotFoundError","text":"<pre><code>PromptNotFoundError(\n    prompt_name: str, attempted_paths: tuple[Path, ...]\n)\n</code></pre> <p>               Bases: <code>PromptError</code>, <code>FileNotFoundError</code></p> <p>No prompt file was found at the requested path.</p> <p>Raised by <code>PromptTemplate.from_file</code> when the path does not exist. <code>attempted_paths</code> records the path(s) probed.</p> <p>Record the prompt name and the paths that were probed.</p> Source code in <code>src/openfactcheck/prompts/errors.py</code> <pre><code>def __init__(self, prompt_name: str, attempted_paths: tuple[Path, ...]) -&gt; None:\n    \"\"\"Record the prompt name and the paths that were probed.\"\"\"\n    self.prompt_name = prompt_name\n    self.attempted_paths = attempted_paths\n    joined = \", \".join(str(p) for p in attempted_paths) or \"(none)\"\n    super().__init__(f\"prompt {prompt_name!r} not found; attempted: {joined}\")\n</code></pre>"},{"location":"reference/prompts/errors/#openfactcheck.prompts.errors.PromptFormatError","title":"PromptFormatError","text":"<pre><code>PromptFormatError(\n    *,\n    path: Path | None,\n    line: int | None,\n    reason: str,\n    expected: str | None = None,\n    got: str | None = None,\n)\n</code></pre> <p>               Bases: <code>PromptError</code>, <code>ValueError</code></p> <p>A codec could not parse the source text into a <code>PromptTemplate</code>.</p> <p>Covers YAML frontmatter errors, malformed tag structure, H1 convention failures, and codec-local policy violations (for example, the markdown codec forbidding repeated role blocks). Format errors are always tied to a specific codec; the domain is not aware of any of them.</p> <p>Message shape::</p> <pre><code>&lt;path or '(python)'&gt;:&lt;line&gt;: &lt;reason&gt;\n  expected: &lt;expected&gt;\n  got:      &lt;got&gt;\n</code></pre> <p>Record the source location and reason for the parse failure.</p> Source code in <code>src/openfactcheck/prompts/errors.py</code> <pre><code>def __init__(\n    self,\n    *,\n    path: Path | None,\n    line: int | None,\n    reason: str,\n    expected: str | None = None,\n    got: str | None = None,\n) -&gt; None:\n    \"\"\"Record the source location and reason for the parse failure.\"\"\"\n    self.path = path\n    self.line = line\n    self.reason = reason\n    self.expected = expected\n    self.got = got\n    super().__init__(_format_message(path, line, reason, expected, got))\n</code></pre>"},{"location":"reference/prompts/errors/#openfactcheck.prompts.errors.PromptValidationError","title":"PromptValidationError","text":"<pre><code>PromptValidationError(\n    *,\n    path: Path | None,\n    line: int | None,\n    reason: str,\n    expected: str | None = None,\n    got: str | None = None,\n)\n</code></pre> <p>               Bases: <code>PromptError</code>, <code>ValueError</code></p> <p>A <code>PromptTemplate</code> violates a domain invariant.</p> <p>Raised from both <code>PromptCodec.decode</code> and <code>PromptTemplate.from_messages</code> when the template would contain a placeholder that references no declared variable, an invalid identifier, or any other rule that holds regardless of how the template was authored.</p> <p>Record the location and reason for the invariant violation.</p> Source code in <code>src/openfactcheck/prompts/errors.py</code> <pre><code>def __init__(\n    self,\n    *,\n    path: Path | None,\n    line: int | None,\n    reason: str,\n    expected: str | None = None,\n    got: str | None = None,\n) -&gt; None:\n    \"\"\"Record the location and reason for the invariant violation.\"\"\"\n    self.path = path\n    self.line = line\n    self.reason = reason\n    self.expected = expected\n    self.got = got\n    super().__init__(_format_message(path, line, reason, expected, got))\n</code></pre>"},{"location":"reference/prompts/errors/#openfactcheck.prompts.errors.PromptVariableError","title":"PromptVariableError","text":"<pre><code>PromptVariableError(\n    prompt_name: str,\n    *,\n    missing: tuple[str, ...] = (),\n    unexpected: tuple[str, ...] = (),\n)\n</code></pre> <p>               Bases: <code>PromptError</code>, <code>KeyError</code></p> <p>Supplied values did not match the template's variable contract.</p> <p>Raised by <code>PromptTemplate.to_prompt</code> when a required variable is missing, or when an unexpected variable name is supplied.</p> <p>Record which required variables were missing or unexpectedly supplied.</p> Source code in <code>src/openfactcheck/prompts/errors.py</code> <pre><code>def __init__(\n    self,\n    prompt_name: str,\n    *,\n    missing: tuple[str, ...] = (),\n    unexpected: tuple[str, ...] = (),\n) -&gt; None:\n    \"\"\"Record which required variables were missing or unexpectedly supplied.\"\"\"\n    self.prompt_name = prompt_name\n    self.missing = missing\n    self.unexpected = unexpected\n    details: list[str] = []\n    if missing:\n        details.append(f\"missing={list(missing)}\")\n    if unexpected:\n        details.append(f\"unexpected={list(unexpected)}\")\n    super().__init__(\n        f\"prompt {prompt_name!r}: {', '.join(details) or 'no variable mismatch'}\",\n    )\n</code></pre>"},{"location":"reference/prompts/prompt/","title":"<code>Prompt</code> class","text":"<p>The filled prompt value produced by <code>PromptTemplate.to_prompt</code>.</p> <p>A <code>Prompt</code> holds the chat messages a template produced after its variables were substituted, plus the values that were used. Convert it to a chat message list with <code>to_messages</code> or to a single role-labeled string with <code>to_string</code>.</p> Example <pre><code>prompt = template.to_prompt(claim=\"X\", evidence=\"Y\")\nresponse = client.completion(prompt.to_messages())\n</code></pre>"},{"location":"reference/prompts/prompt/#openfactcheck.prompts.prompt.Prompt","title":"Prompt  <code>dataclass</code>","text":"<pre><code>Prompt(\n    name: str,\n    messages: tuple[Message, ...],\n    variables_used: Mapping[str, object] = (\n        lambda: MappingProxyType({})\n    )(),\n)\n</code></pre> <p>A template filled with values, ready to send to a model.</p> <p>Wraps the chat messages produced by <code>PromptTemplate.to_prompt</code>, in authoring order, alongside the values that were substituted.</p>"},{"location":"reference/prompts/prompt/#openfactcheck.prompts.prompt.Prompt.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>Identifier of the source <code>PromptTemplate</code>.</p>"},{"location":"reference/prompts/prompt/#openfactcheck.prompts.prompt.Prompt.messages","title":"messages  <code>instance-attribute</code>","text":"<pre><code>messages: tuple[Message, ...]\n</code></pre> <p>The filled chat messages, in authoring order.</p>"},{"location":"reference/prompts/prompt/#openfactcheck.prompts.prompt.Prompt.variables_used","title":"variables_used  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>variables_used: Mapping[str, object] = field(\n    default_factory=lambda: MappingProxyType({})\n)\n</code></pre> <p>Values substituted when the template was filled (supplied kwargs plus any optional defaults).</p> <p>An immutable view over a private copy; mutating the caller's original kwargs does not affect it.</p>"},{"location":"reference/prompts/prompt/#openfactcheck.prompts.prompt.Prompt.to_messages","title":"to_messages","text":"<pre><code>to_messages() -&gt; list[Message]\n</code></pre> <p>Return the filled messages as a list, ready for the chat client.</p> Source code in <code>src/openfactcheck/prompts/prompt.py</code> <pre><code>def to_messages(self) -&gt; list[Message]:\n    \"\"\"Return the filled messages as a list, ready for the chat client.\"\"\"\n    return list(self.messages)\n</code></pre>"},{"location":"reference/prompts/prompt/#openfactcheck.prompts.prompt.Prompt.to_string","title":"to_string","text":"<pre><code>to_string() -&gt; str\n</code></pre> <p>Return the messages as one role-labeled string.</p> <p>Each message renders as <code>\"&lt;role&gt;: &lt;content&gt;\"</code>, joined by blank lines.</p> Source code in <code>src/openfactcheck/prompts/prompt.py</code> <pre><code>def to_string(self) -&gt; str:\n    \"\"\"Return the messages as one role-labeled string.\n\n    Each message renders as ``\"&lt;role&gt;: &lt;content&gt;\"``, joined by blank\n    lines.\n    \"\"\"\n    return \"\\n\\n\".join(f\"{message.role}: {message.content}\" for message in self.messages)\n</code></pre>"},{"location":"reference/prompts/template/","title":"<code>PromptTemplate</code> class","text":"<p>Prompt templates: chat messages whose content carries <code>{{variables}}</code>.</p> <p>A template is an immutable record: a name, a declared variable contract, and an ordered sequence of chat messages whose content carries <code>{{placeholder}}</code> references. Fill it against values to get a <code>Prompt</code> (<code>to_prompt</code>), a chat message list (<code>to_messages</code>), or a single string (<code>to_string</code>).</p> <p>Build one from messages or <code>(role, text)</code> tuples in code, or load one from a file (the extension selects the codec).</p> Example <pre><code>from openfactcheck.prompts import PromptTemplate\n\nverifier = PromptTemplate.from_messages(\n    [\n        (\"system\", \"You are a fact-checker.\"),\n        (\"user\", \"Claim: {{claim}}\"),\n    ],\n    name=\"verifier\",\n)\nmessages = verifier.to_messages(claim=\"The sky is green.\")\n</code></pre>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate","title":"PromptTemplate  <code>dataclass</code>","text":"<pre><code>PromptTemplate(\n    name: str,\n    description: str | None = None,\n    variables: Mapping[str, VariableSpec] = dict[\n        str, VariableSpec\n    ](),\n    messages: tuple[Message, ...] = (),\n    metadata: Mapping[str, object] = dict[str, object](),\n)\n</code></pre> <p>An immutable, fillable prompt.</p> <p>Holds a stable <code>name</code>, a declared variable contract, the chat messages that make up the template (content carries <code>{{variables}}</code>), and an untyped <code>metadata</code> bucket for provenance. Equality compares <code>(name, description, variables, messages)</code>; <code>metadata</code> is ignored, so two templates that differ only in provenance compare equal.</p>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>Stable identifier used to refer to this template, and used in equality.</p> <p>A programmatic identifier, not a display label; must be a non-empty valid Python identifier. <code>description</code> is the human-readable field.</p>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str | None = None\n</code></pre> <p>Optional human-readable description.</p>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.variables","title":"variables  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>variables: Mapping[str, VariableSpec] = field(\n    default_factory=dict[str, VariableSpec]\n)\n</code></pre> <p>Declared variable contract keyed by variable name.</p> <p>The contract is authoritative: a placeholder referencing no entry here is rejected at construction. A declared variable that appears in no message is allowed.</p>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.messages","title":"messages  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>messages: tuple[Message, ...] = ()\n</code></pre> <p>The template's chat messages in authoring order, content carrying <code>{{variables}}</code>.</p> <p>Repeated roles are permitted (for example user/assistant pairs for few-shot examples).</p>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.metadata","title":"metadata  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>metadata: Mapping[str, object] = field(\n    default_factory=dict[str, object]\n)\n</code></pre> <p>Non-semantic bucket for provenance and miscellany.</p> <p>Excluded from equality; callers should not branch logic on it.</p>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.to_prompt","title":"to_prompt","text":"<pre><code>to_prompt(**values: object) -&gt; Prompt\n</code></pre> <p>Substitute <code>values</code> into every message and return a <code>Prompt</code>.</p> PARAMETER DESCRIPTION <code>**values</code> <p>Values for the declared variables. Every required variable must be supplied; an omitted optional variable falls back to its <code>default</code>. Unexpected names are rejected.</p> <p> TYPE: <code>object</code> DEFAULT: <code>{}</code> </p> Source code in <code>src/openfactcheck/prompts/template.py</code> <pre><code>def to_prompt(self, **values: object) -&gt; Prompt:\n    \"\"\"Substitute ``values`` into every message and return a [`Prompt`][Prompt].\n\n    Args:\n        **values: Values for the declared variables. Every required\n            variable must be supplied; an omitted optional variable falls\n            back to its [`default`][VariableSpec.default]. Unexpected\n            names are rejected.\n\n    Returns:\n        A [`Prompt`][Prompt] wrapping the filled messages.\n\n    Raises:\n        PromptVariableError: A required variable is missing, or an\n            unexpected variable was supplied.\n    \"\"\"\n    required = {name for name, spec in self.variables.items() if spec.required}\n    declared = set(self.variables)\n    supplied = set(values)\n\n    missing = tuple(sorted(required - supplied))\n    unexpected = tuple(sorted(supplied - declared))\n    if missing or unexpected:\n        raise PromptVariableError(self.name, missing=missing, unexpected=unexpected)\n\n    effective: dict[str, object] = dict(values)\n    for name, spec in self.variables.items():\n        if name not in effective and not spec.required:\n            effective[name] = spec.default\n\n    filled = tuple(\n        message.model_copy(update={\"content\": substitute(message.content, effective)}) for message in self.messages\n    )\n    return Prompt(name=self.name, messages=filled, variables_used=effective)\n</code></pre>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.to_messages","title":"to_messages","text":"<pre><code>to_messages(**values: object) -&gt; list[Message]\n</code></pre> <p>Fill the template and return the chat messages.</p> <p>Shortcut for <code>self.to_prompt(**values).to_messages()</code>.</p> Source code in <code>src/openfactcheck/prompts/template.py</code> <pre><code>def to_messages(self, **values: object) -&gt; list[Message]:\n    \"\"\"Fill the template and return the chat messages.\n\n    Shortcut for ``self.to_prompt(**values).to_messages()``.\n    \"\"\"\n    return self.to_prompt(**values).to_messages()\n</code></pre>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.to_string","title":"to_string","text":"<pre><code>to_string(**values: object) -&gt; str\n</code></pre> <p>Fill the template and return one role-labeled string.</p> <p>Shortcut for <code>self.to_prompt(**values).to_string()</code>.</p> Source code in <code>src/openfactcheck/prompts/template.py</code> <pre><code>def to_string(self, **values: object) -&gt; str:\n    \"\"\"Fill the template and return one role-labeled string.\n\n    Shortcut for ``self.to_prompt(**values).to_string()``.\n    \"\"\"\n    return self.to_prompt(**values).to_string()\n</code></pre>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.from_messages","title":"from_messages  <code>classmethod</code>","text":"<pre><code>from_messages(\n    messages: Sequence[Message | tuple[Role, str]],\n    *,\n    name: str,\n    description: str | None = None,\n    variables: Mapping[str, VariableSpec] | None = None,\n    metadata: Mapping[str, object] | None = None,\n) -&gt; PromptTemplate\n</code></pre> <p>Build a template from chat messages or <code>(role, text)</code> tuples.</p> <p><code>messages</code> accepts chat message objects (whose content may carry <code>{{variables}}</code>) or <code>(role, text)</code> tuples as a shorthand.</p> <p>If <code>variables</code> is omitted, the contract is inferred by scanning each message for <code>{{placeholder}}</code> references; each unique name becomes a required string variable. When <code>variables</code> is supplied, the contract is authoritative: a referenced-but-undeclared placeholder raises <code>PromptValidationError</code>; a declared-but-unused variable is allowed.</p> PARAMETER DESCRIPTION <code>messages</code> <p>Template messages in authoring order.</p> <p> TYPE: <code>Sequence[Message | tuple[Role, str]]</code> </p> <code>name</code> <p>Stable identifier.</p> <p> TYPE: <code>str</code> </p> <code>description</code> <p>Optional human-readable description.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>variables</code> <p>Optional explicit variable contract; inferred when omitted.</p> <p> TYPE: <code>Mapping[str, VariableSpec] | None</code> DEFAULT: <code>None</code> </p> <code>metadata</code> <p>Optional provenance bucket (for example, set by a codec).</p> <p> TYPE: <code>Mapping[str, object] | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/prompts/template.py</code> <pre><code>@classmethod\ndef from_messages(\n    cls,\n    messages: Sequence[Message | tuple[Role, str]],\n    *,\n    name: str,\n    description: str | None = None,\n    variables: Mapping[str, VariableSpec] | None = None,\n    metadata: Mapping[str, object] | None = None,\n) -&gt; PromptTemplate:\n    \"\"\"Build a template from chat messages or ``(role, text)`` tuples.\n\n    ``messages`` accepts chat message objects (whose content may carry\n    ``{{variables}}``) or ``(role, text)`` tuples as a shorthand.\n\n    If ``variables`` is omitted, the contract is inferred by scanning each\n    message for ``{{placeholder}}`` references; each unique name becomes a\n    required string variable. When ``variables`` is supplied, the contract\n    is authoritative: a referenced-but-undeclared placeholder raises\n    [`PromptValidationError`][PromptValidationError]; a declared-but-unused\n    variable is allowed.\n\n    Args:\n        messages: Template messages in authoring order.\n        name: Stable identifier.\n        description: Optional human-readable description.\n        variables: Optional explicit variable contract; inferred when omitted.\n        metadata: Optional provenance bucket (for example, set by a codec).\n\n    Returns:\n        A validated template.\n\n    Raises:\n        PromptValidationError: Any domain invariant is violated.\n    \"\"\"\n    normalized = tuple(_to_message(item) for item in messages)\n    if variables is None:\n        inferred: dict[str, VariableSpec] = {}\n        for message in normalized:\n            for placeholder in find_placeholders(message.content):\n                inferred.setdefault(placeholder, VariableSpec.string(placeholder))\n        variables = inferred\n    return cls(\n        name=name,\n        description=description,\n        variables=variables,\n        messages=normalized,\n        metadata=metadata if metadata is not None else {},\n    )\n</code></pre>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.from_template","title":"from_template  <code>classmethod</code>","text":"<pre><code>from_template(\n    template: str, *, role: Role = \"user\", name: str\n) -&gt; PromptTemplate\n</code></pre> <p>Build a single-message template.</p> PARAMETER DESCRIPTION <code>template</code> <p>Message content, which may carry <code>{{variables}}</code>.</p> <p> TYPE: <code>str</code> </p> <code>role</code> <p>Role for the single message. Defaults to <code>\"user\"</code>.</p> <p> TYPE: <code>Role</code> DEFAULT: <code>'user'</code> </p> <code>name</code> <p>Stable identifier.</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/openfactcheck/prompts/template.py</code> <pre><code>@classmethod\ndef from_template(cls, template: str, *, role: Role = \"user\", name: str) -&gt; PromptTemplate:\n    \"\"\"Build a single-message template.\n\n    Args:\n        template: Message content, which may carry ``{{variables}}``.\n        role: Role for the single message. Defaults to ``\"user\"``.\n        name: Stable identifier.\n    \"\"\"\n    return cls.from_messages([(role, template)], name=name)\n</code></pre>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.from_markdown","title":"from_markdown  <code>classmethod</code>","text":"<pre><code>from_markdown(\n    text: str, *, name_hint: str | None = None\n) -&gt; PromptTemplate\n</code></pre> <p>Decode <code>text</code> as a markdown prompt.</p> PARAMETER DESCRIPTION <code>text</code> <p>Markdown source with YAML frontmatter and role-tagged body blocks.</p> <p> TYPE: <code>str</code> </p> <code>name_hint</code> <p>Name to fall back to or verify against the frontmatter.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/prompts/template.py</code> <pre><code>@classmethod\ndef from_markdown(cls, text: str, *, name_hint: str | None = None) -&gt; PromptTemplate:\n    \"\"\"Decode ``text`` as a markdown prompt.\n\n    Args:\n        text: Markdown source with YAML frontmatter and role-tagged body\n            blocks.\n        name_hint: Name to fall back to or verify against the frontmatter.\n\n    Returns:\n        The decoded template.\n\n    Raises:\n        PromptFormatError: The text is malformed for the markdown codec.\n        PromptValidationError: The decoded template violates a domain\n            invariant.\n    \"\"\"\n    from openfactcheck.prompts.codecs.markdown import MarkdownPromptCodec  # noqa: PLC0415 - avoid circular import.\n\n    return MarkdownPromptCodec().decode(text, name_hint=name_hint)\n</code></pre>"},{"location":"reference/prompts/template/#openfactcheck.prompts.template.PromptTemplate.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(path: str | Path) -&gt; PromptTemplate\n</code></pre> <p>Load a template from a file, routing on the file extension.</p> <p>The extension selects the codec (<code>.md</code> and <code>.markdown</code> route to the markdown codec); the filename stem is passed as the name hint.</p> PARAMETER DESCRIPTION <code>path</code> <p>Path to a prompt file.</p> <p> TYPE: <code>str | Path</code> </p> Source code in <code>src/openfactcheck/prompts/template.py</code> <pre><code>@classmethod\ndef from_file(cls, path: str | Path) -&gt; PromptTemplate:\n    \"\"\"Load a template from a file, routing on the file extension.\n\n    The extension selects the codec (``.md`` and ``.markdown`` route to the\n    markdown codec); the filename stem is passed as the name hint.\n\n    Args:\n        path: Path to a prompt file.\n\n    Returns:\n        The decoded template.\n\n    Raises:\n        PromptNotFoundError: No file exists at ``path``.\n        PromptFormatError: No codec is registered for the extension, or the\n            source is malformed for the matched codec.\n        PromptValidationError: The decoded template violates a domain\n            invariant.\n    \"\"\"\n    from openfactcheck.prompts.codecs import codec_for_path  # noqa: PLC0415 - avoid circular import.\n\n    file_path = Path(path)\n    if not file_path.is_file():\n        raise PromptNotFoundError(file_path.stem, (file_path,))\n    text = file_path.read_text(encoding=\"utf-8\")\n    return codec_for_path(file_path).decode(text, name_hint=file_path.stem)\n</code></pre>"},{"location":"reference/prompts/variables/","title":"Variables","text":"<p>Role and variable declarations for prompt templates.</p> <p><code>Role</code> is the role a template message can take. <code>VariableSpec</code> declares one input variable's contract (name, type, required, default). A template stores its variables as a mapping keyed by name; the spec carries its own name so <code>VariableSpec.string</code> reads naturally.</p> Example <pre><code>from openfactcheck.prompts import VariableSpec\n\nclaim = VariableSpec.string(\"claim\")  # required\ntone = VariableSpec.string(\"tone\", required=False, default=\"neutral\")  # optional\n</code></pre>"},{"location":"reference/prompts/variables/#variable-contract","title":"Variable contract","text":""},{"location":"reference/prompts/variables/#openfactcheck.prompts.variables.VariableSpec","title":"VariableSpec  <code>dataclass</code>","text":"<pre><code>VariableSpec(\n    name: str,\n    type: Literal[\"string\"] = \"string\",\n    required: bool = True,\n    default: str = \"\",\n)\n</code></pre> <p>Declaration of one templated variable.</p> <p>Frozen dataclass; values are immutable after construction. Users typically build these with <code>VariableSpec.string</code> rather than the primary constructor. Future type factories (<code>VariableSpec.integer</code>, <code>VariableSpec.boolean</code>) will land alongside their corresponding <code>type</code> values; the current v1 surface is deliberately string-only.</p>"},{"location":"reference/prompts/variables/#openfactcheck.prompts.variables.VariableSpec.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>Variable name. Must be a valid Python identifier, matching the <code>{{name}}</code> placeholder syntax. The template keys its variables by name, so duplicates cannot occur.</p>"},{"location":"reference/prompts/variables/#openfactcheck.prompts.variables.VariableSpec.type","title":"type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>type: Literal['string'] = 'string'\n</code></pre> <p>Value type. Only <code>\"string\"</code> is supported today.</p>"},{"location":"reference/prompts/variables/#openfactcheck.prompts.variables.VariableSpec.required","title":"required  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>required: bool = True\n</code></pre> <p>Whether the variable must appear in the values passed to <code>PromptTemplate.to_prompt</code>. An optional variable may be omitted; when it is, its <code>default</code> is substituted instead of raising <code>PromptVariableError</code>.</p>"},{"location":"reference/prompts/variables/#openfactcheck.prompts.variables.VariableSpec.default","title":"default  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default: str = ''\n</code></pre> <p>Value substituted when an optional variable is omitted while filling.</p> <p>Applies only to optional variables; a required variable must be supplied, so its default is never used.</p>"},{"location":"reference/prompts/variables/#openfactcheck.prompts.variables.VariableSpec.string","title":"string  <code>classmethod</code>","text":"<pre><code>string(\n    name: str, *, required: bool = True, default: str = \"\"\n) -&gt; VariableSpec\n</code></pre> <p>Declare a string-valued variable.</p> PARAMETER DESCRIPTION <code>name</code> <p>Variable name; must be a valid Python identifier.</p> <p> TYPE: <code>str</code> </p> <code>required</code> <p>Whether the values passed when filling must include this name. Defaults to <code>True</code>.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>default</code> <p>Value substituted when an optional variable is omitted. Ignored for required variables.</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p> Source code in <code>src/openfactcheck/prompts/variables.py</code> <pre><code>@classmethod\ndef string(cls, name: str, *, required: bool = True, default: str = \"\") -&gt; VariableSpec:\n    \"\"\"Declare a string-valued variable.\n\n    Args:\n        name: Variable name; must be a valid Python identifier.\n        required: Whether the values passed when filling must include this\n            name. Defaults to ``True``.\n        default: Value substituted when an optional variable is omitted.\n            Ignored for required variables.\n\n    Returns:\n        A ``VariableSpec`` with ``type=\"string\"``.\n    \"\"\"\n    return cls(name=name, type=\"string\", required=required, default=default)\n</code></pre>"},{"location":"reference/prompts/variables/#role","title":"Role","text":""},{"location":"reference/prompts/variables/#openfactcheck.prompts.variables.Role","title":"Role  <code>module-attribute</code>","text":"<pre><code>Role = Literal['system', 'user', 'assistant']\n</code></pre> <p>Role a <code>PromptTemplate</code> message can take.</p> <p>These are the roles a template authors directly, and the roles the <code>(role, text)</code> tuple shorthand accepts. The chat layer's <code>Message</code> union covers additional roles (such as tool results) that belong to a conversation rather than a template.</p>"},{"location":"reference/prompts/codecs/","title":"Codecs","text":"<p>Serialization formats for prompts.</p> <p>The <code>PromptCodec</code> Protocol is the extension seam. <code>MarkdownPromptCodec</code> is the one concrete codec that ships in v1; future codecs (YAML, JSON) register their file extensions in <code>codec_for_path</code> and become loadable through <code>PromptTemplate.from_file</code> with no further change.</p>"},{"location":"reference/prompts/codecs/#lookup","title":"Lookup","text":""},{"location":"reference/prompts/codecs/#openfactcheck.prompts.codecs.codec_for_path","title":"codec_for_path","text":"<pre><code>codec_for_path(path: Path) -&gt; PromptCodec\n</code></pre> <p>Return the codec registered for <code>path</code>'s file extension.</p> PARAMETER DESCRIPTION <code>path</code> <p>File path whose suffix selects the codec.</p> <p> TYPE: <code>Path</code> </p> Source code in <code>src/openfactcheck/prompts/codecs/__init__.py</code> <pre><code>def codec_for_path(path: Path) -&gt; PromptCodec:\n    \"\"\"Return the codec registered for ``path``'s file extension.\n\n    Args:\n        path: File path whose suffix selects the codec.\n\n    Returns:\n        The codec registered for the suffix.\n\n    Raises:\n        PromptFormatError: No codec is registered for the suffix.\n    \"\"\"\n    codec = _CODEC_BY_SUFFIX.get(path.suffix.lower())\n    if codec is None:\n        supported = \", \".join(sorted(_CODEC_BY_SUFFIX))\n        raise PromptFormatError(\n            path=path,\n            line=None,\n            reason=\"no codec registered for this file extension\",\n            expected=f\"one of: {supported}\",\n            got=path.suffix or \"(none)\",\n        )\n    return codec\n</code></pre>"},{"location":"reference/prompts/codecs/#pages","title":"Pages","text":"<ul> <li><code>PromptCodec</code> protocol: the interface every codec satisfies.</li> <li><code>MarkdownPromptCodec</code> class: the codec for YAML-frontmatter markdown prompts.</li> </ul>"},{"location":"reference/prompts/codecs/markdown/","title":"<code>MarkdownPromptCodec</code> class","text":"<p>Markdown codec: YAML frontmatter + role-tagged body blocks.</p> <p>Accepts and emits prompts in a YAML-frontmatter, role-tagged markdown format. The rules below are codec-local, not domain invariants.</p> <p>Authoring rules:</p> <ol> <li>File starts with a YAML frontmatter block delimited by <code>---</code> lines.</li> <li>Body contains zero or more role blocks delimited by <code>&lt;system&gt;</code>,    <code>&lt;user&gt;</code>, <code>&lt;assistant&gt;</code> (and their closers) on their own lines at    column 0. Content outside role blocks is documentation and ignored.</li> <li>At most one block of each role; at least one role block total. The domain    permits repeated roles; this codec does not.</li> <li>Inside each block, the first non-blank line must exactly match the    canonical H1 (<code># System Prompt</code> / <code># User Prompt</code> /    <code># Assistant Prompt</code>). The H1 is stripped from the message content; it    labels the block for human readers, and the model never sees it.</li> </ol>"},{"location":"reference/prompts/codecs/markdown/#openfactcheck.prompts.codecs.markdown.MarkdownPromptCodec","title":"MarkdownPromptCodec","text":"<p>Default prompt codec: YAML frontmatter + role-tagged markdown body.</p> <p>Instances are stateless; construct one per call or share freely.</p>"},{"location":"reference/prompts/codecs/markdown/#openfactcheck.prompts.codecs.markdown.MarkdownPromptCodec.decode","title":"decode","text":"<pre><code>decode(\n    text: str, *, name_hint: str | None = None\n) -&gt; PromptTemplate\n</code></pre> <p>Decode markdown source into a <code>PromptTemplate</code>.</p> PARAMETER DESCRIPTION <code>text</code> <p>Full file contents.</p> <p> TYPE: <code>str</code> </p> <code>name_hint</code> <p>Filename stem (or equivalent) for error messages and for cross-checking against the frontmatter <code>name</code>. If supplied and the frontmatter declares a name, the two must match.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/prompts/codecs/markdown/codec.py</code> <pre><code>def decode(self, text: str, *, name_hint: str | None = None) -&gt; PromptTemplate:\n    \"\"\"Decode markdown source into a [`PromptTemplate`][PromptTemplate].\n\n    Args:\n        text: Full file contents.\n        name_hint: Filename stem (or equivalent) for error messages and\n            for cross-checking against the frontmatter ``name``. If\n            supplied and the frontmatter declares a name, the two must\n            match.\n\n    Returns:\n        The decoded template.\n\n    Raises:\n        PromptFormatError: Source cannot be parsed by this codec.\n        PromptValidationError: The decoded template violates a domain\n            invariant.\n    \"\"\"\n    path_for_errors = Path(name_hint) if name_hint else None\n    frontmatter = FrontMatterParser(path=path_for_errors)\n    frontmatter_lines, body_lines, body_start_line = frontmatter.split(text)\n    name, version, description, variables = frontmatter.parse(\"\\n\".join(frontmatter_lines))\n\n    if name_hint is not None and name != name_hint:\n        raise PromptFormatError(\n            path=path_for_errors,\n            line=None,\n            reason=\"frontmatter 'name' does not match name_hint\",\n            expected=name_hint,\n            got=name,\n        )\n\n    blocks = BodyParser(path=path_for_errors).parse(body_lines, body_start_line=body_start_line)\n\n    metadata: dict[str, object] = {\"source\": \"markdown\"}\n    if version is not None:\n        metadata[\"version\"] = version\n\n    return PromptTemplate.from_messages(\n        blocks,\n        name=name,\n        description=description,\n        variables=variables,\n        metadata=metadata,\n    )\n</code></pre>"},{"location":"reference/prompts/codecs/markdown/#openfactcheck.prompts.codecs.markdown.MarkdownPromptCodec.encode","title":"encode","text":"<pre><code>encode(template: PromptTemplate) -&gt; str\n</code></pre> <p>Encode a <code>PromptTemplate</code> into this codec's markdown format.</p> <p>Round-trips through <code>decode</code>; the exact byte layout is not part of the contract.</p> PARAMETER DESCRIPTION <code>template</code> <p>Template to serialize.</p> <p> TYPE: <code>PromptTemplate</code> </p> Source code in <code>src/openfactcheck/prompts/codecs/markdown/codec.py</code> <pre><code>def encode(self, template: PromptTemplate) -&gt; str:\n    \"\"\"Encode a [`PromptTemplate`][PromptTemplate] into this codec's markdown format.\n\n    Round-trips through [`decode`][MarkdownPromptCodec.decode]; the exact\n    byte layout is not part of the contract.\n\n    Args:\n        template: Template to serialize.\n\n    Returns:\n        Markdown text.\n\n    Raises:\n        PromptFormatError: The template repeats a role, or uses a role this\n            codec does not emit.\n    \"\"\"\n    seen_roles: set[str] = set()\n    for message in template.messages:\n        if message.role not in ROLES:\n            raise PromptFormatError(\n                path=None,\n                line=None,\n                reason=\"markdown codec only emits system/user/assistant messages\",\n                expected=f\"one of {list(ROLES)}\",\n                got=f\"{message.role!r} message\",\n            )\n        if message.role in seen_roles:\n            raise PromptFormatError(\n                path=None,\n                line=None,\n                reason=\"markdown codec cannot encode repeated role blocks\",\n                expected=\"at most one block of each role\",\n                got=f\"second {message.role!r} block\",\n            )\n        seen_roles.add(message.role)\n    return emit_markdown(template)\n</code></pre>"},{"location":"reference/prompts/codecs/protocol/","title":"<code>PromptCodec</code> protocol","text":"<p>The codec Protocol, the extension seam for prompt serialization formats.</p> <p>Implementations translate between a <code>PromptTemplate</code> and a text representation. Markdown is the one shipped codec; other formats (YAML, JSON) are added by implementing this Protocol, with no other change to the layer.</p>"},{"location":"reference/prompts/codecs/protocol/#openfactcheck.prompts.codecs.protocol.PromptCodec","title":"PromptCodec","text":"<p>               Bases: <code>Protocol</code></p> <p>Translate between a serialized representation and a <code>PromptTemplate</code>.</p> <p><code>encode</code> is best-effort: its output round-trips through <code>decode</code>, but the exact byte layout (spacing, key ordering) is not part of the contract. Keep the authored source as the source of truth rather than relying on encode/decode to reproduce bytes.</p>"},{"location":"reference/prompts/codecs/protocol/#openfactcheck.prompts.codecs.protocol.PromptCodec.decode","title":"decode","text":"<pre><code>decode(\n    text: str, *, name_hint: str | None = None\n) -&gt; PromptTemplate\n</code></pre> <p>Decode <code>text</code> into a <code>PromptTemplate</code>.</p> PARAMETER DESCRIPTION <code>text</code> <p>Serialized representation.</p> <p> TYPE: <code>str</code> </p> <code>name_hint</code> <p>Name to fall back to, or to verify against the source's own name field. File loading passes the filename stem so the codec can check it agrees with the declared name.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> Source code in <code>src/openfactcheck/prompts/codecs/protocol.py</code> <pre><code>def decode(self, text: str, *, name_hint: str | None = None) -&gt; PromptTemplate:\n    \"\"\"Decode ``text`` into a [`PromptTemplate`][PromptTemplate].\n\n    Args:\n        text: Serialized representation.\n        name_hint: Name to fall back to, or to verify against the\n            source's own name field. File loading passes the filename\n            stem so the codec can check it agrees with the declared name.\n\n    Returns:\n        The decoded template.\n\n    Raises:\n        PromptFormatError: The source could not be parsed by this codec.\n        PromptValidationError: The decoded template violates a domain\n            invariant.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/prompts/codecs/protocol/#openfactcheck.prompts.codecs.protocol.PromptCodec.encode","title":"encode","text":"<pre><code>encode(template: PromptTemplate) -&gt; str\n</code></pre> <p>Encode <code>template</code> into this codec's text format.</p> PARAMETER DESCRIPTION <code>template</code> <p>Template to serialize.</p> <p> TYPE: <code>PromptTemplate</code> </p> Source code in <code>src/openfactcheck/prompts/codecs/protocol.py</code> <pre><code>def encode(self, template: PromptTemplate) -&gt; str:\n    \"\"\"Encode ``template`` into this codec's text format.\n\n    Args:\n        template: Template to serialize.\n\n    Returns:\n        Serialized text.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/rest/","title":"REST API","text":""},{"location":"reference/rest/#openfactcheck-api-200a9","title":"OpenFactCheck API 2.0.0a9","text":"<p>HTTP reference for the OpenFactCheck server.</p> <ul> <li>Interactive sandbox: live Swagger UI at <code>/docs</code>, ReDoc at <code>/redoc</code>.</li> <li>Auth: every non-health endpoint expects an <code>Authorization: Bearer &lt;token&gt;</code> header.</li> <li>Errors: failures return a structured JSON body with <code>detail</code>, <code>code</code>, and <code>status</code> fields.</li> </ul>"},{"location":"reference/rest/#endpoints","title":"Endpoints","text":""},{"location":"reference/rest/#get","title":"GET /","text":"<p>Root</p> Description <p>Root endpoint returning service identity.</p> <p>Responses</p> 200 OK application/json Schema of the response body <pre><code>{\n    \"additionalProperties\": {\n        \"type\": \"string\"\n    },\n    \"type\": \"object\",\n    \"title\": \"Response Root  Get\"\n}\n</code></pre>"},{"location":"reference/rest/#get-health","title":"GET /health","text":"<p>Health</p> Description <p>Health check endpoint.</p> <p>Responses</p> 200 OK application/json Schema of the response body <pre><code>{\n    \"additionalProperties\": {\n        \"type\": \"string\"\n    },\n    \"type\": \"object\",\n    \"title\": \"Response Health Health Get\"\n}\n</code></pre>"},{"location":"reference/rest/#projects","title":"projects","text":""},{"location":"reference/rest/#get-apiv1projects","title":"GET /api/v1/projects/","text":"<p>List Projects</p> Description <p>List all projects for the current user.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>limit</code> query integer 50 No <code>offset</code> query integer 0 No <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>[\n    {\n        \"id\": \"string\",\n        \"name\": \"string\",\n        \"description\": \"string\",\n        \"created_at\": \"2022-04-13T15:42:05.901Z\",\n        \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n    }\n]\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"type\": \"array\",\n    \"items\": {\n        \"$ref\": \"#/components/schemas/ProjectResponse\"\n    },\n    \"title\": \"Response List Projects Api V1 Projects  Get\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#post-apiv1projects","title":"POST /api/v1/projects/","text":"<p>Create Project</p> Description <p>Create a new project.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <p>Request body</p> application/json <p><pre><code>{\n    \"name\": \"string\",\n    \"description\": \"string\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"name\": {\n            \"type\": \"string\",\n            \"maxLength\": 255,\n            \"minLength\": 1,\n            \"title\": \"Name\",\n            \"description\": \"Human-readable project name, 1 to 255 characters.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"maxLength\": 10000,\n            \"title\": \"Description\",\n            \"description\": \"Freeform project description, up to 10000 characters.\",\n            \"default\": \"\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"name\"\n    ],\n    \"title\": \"CreateProjectRequest\",\n    \"description\": \"Input payload for creating a project.\"\n}\n</code></pre> <p>Responses</p> 201 Created422 Unprocessable Entity application/json <p><pre><code>{\n    \"id\": \"string\",\n    \"name\": \"string\",\n    \"description\": \"string\",\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"id\": {\n            \"type\": \"string\",\n            \"title\": \"Id\",\n            \"description\": \"Opaque unique identifier for the project.\"\n        },\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Human-readable project name.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"title\": \"Description\",\n            \"description\": \"Freeform project description.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the project was created.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent modification.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"id\",\n        \"name\",\n        \"description\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"ProjectResponse\",\n    \"description\": \"Single project returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#get-apiv1projectsproject_id","title":"GET /api/v1/projects/{project_id}","text":"<p>Get Project</p> Description <p>Get a single project by ID.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>{\n    \"id\": \"string\",\n    \"name\": \"string\",\n    \"description\": \"string\",\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"id\": {\n            \"type\": \"string\",\n            \"title\": \"Id\",\n            \"description\": \"Opaque unique identifier for the project.\"\n        },\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Human-readable project name.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"title\": \"Description\",\n            \"description\": \"Freeform project description.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the project was created.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent modification.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"id\",\n        \"name\",\n        \"description\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"ProjectResponse\",\n    \"description\": \"Single project returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#patch-apiv1projectsproject_id","title":"PATCH /api/v1/projects/{project_id}","text":"<p>Update Project</p> Description <p>Update a project.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <p>Request body</p> application/json <p><pre><code>{\n    \"name\": null,\n    \"description\": null\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"name\": {\n            \"anyOf\": [\n                {\n                    \"type\": \"string\",\n                    \"maxLength\": 255,\n                    \"minLength\": 1\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"title\": \"Name\",\n            \"description\": \"New project name, 1 to 255 characters. ``None`` leaves the existing name unchanged.\"\n        },\n        \"description\": {\n            \"anyOf\": [\n                {\n                    \"type\": \"string\",\n                    \"maxLength\": 10000\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"title\": \"Description\",\n            \"description\": \"New description, up to 10000 characters. ``None`` leaves the existing description unchanged.\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"UpdateProjectRequest\",\n    \"description\": \"Input payload for partially updating a project.\"\n}\n</code></pre> <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>{\n    \"id\": \"string\",\n    \"name\": \"string\",\n    \"description\": \"string\",\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"id\": {\n            \"type\": \"string\",\n            \"title\": \"Id\",\n            \"description\": \"Opaque unique identifier for the project.\"\n        },\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Human-readable project name.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"title\": \"Description\",\n            \"description\": \"Freeform project description.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the project was created.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent modification.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"id\",\n        \"name\",\n        \"description\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"ProjectResponse\",\n    \"description\": \"Single project returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#delete-apiv1projectsproject_id","title":"DELETE /api/v1/projects/{project_id}","text":"<p>Delete Project</p> Description <p>Delete a project.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <p>Responses</p> 204 No Content422 Unprocessable Entity application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#workspaces","title":"workspaces","text":""},{"location":"reference/rest/#get-apiv1projectsproject_idworkspaces","title":"GET /api/v1/projects/{project_id}/workspaces/","text":"<p>List Workspaces</p> Description <p>List all workspaces in a project.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>[\n    {\n        \"id\": \"string\",\n        \"project_id\": \"string\",\n        \"name\": \"string\",\n        \"description\": \"string\",\n        \"locked\": true,\n        \"sort_order\": 0,\n        \"settings\": {\n            \"verboseMode\": true\n        },\n        \"content\": null,\n        \"created_at\": \"2022-04-13T15:42:05.901Z\",\n        \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n    }\n]\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"type\": \"array\",\n    \"items\": {\n        \"$ref\": \"#/components/schemas/WorkspaceResponse\"\n    },\n    \"title\": \"Response List Workspaces Api V1 Projects  Project Id  Workspaces  Get\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#post-apiv1projectsproject_idworkspaces","title":"POST /api/v1/projects/{project_id}/workspaces/","text":"<p>Create Workspace</p> Description <p>Create a new workspace in a project.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <p>Request body</p> application/json <p><pre><code>{\n    \"name\": \"string\",\n    \"description\": \"string\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"name\": {\n            \"type\": \"string\",\n            \"maxLength\": 255,\n            \"minLength\": 1,\n            \"title\": \"Name\",\n            \"description\": \"Human-readable workspace name, 1 to 255 characters.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"maxLength\": 10000,\n            \"title\": \"Description\",\n            \"description\": \"Freeform workspace description, up to 10000 characters.\",\n            \"default\": \"\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"name\"\n    ],\n    \"title\": \"CreateWorkspaceRequest\",\n    \"description\": \"Input payload for creating a workspace.\"\n}\n</code></pre> <p>Responses</p> 201 Created422 Unprocessable Entity application/json <p><pre><code>{\n    \"id\": \"string\",\n    \"project_id\": \"string\",\n    \"name\": \"string\",\n    \"description\": \"string\",\n    \"locked\": true,\n    \"sort_order\": 0,\n    \"settings\": {\n        \"verboseMode\": true\n    },\n    \"content\": null,\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"id\": {\n            \"type\": \"string\",\n            \"title\": \"Id\",\n            \"description\": \"Opaque unique identifier for the workspace.\"\n        },\n        \"project_id\": {\n            \"type\": \"string\",\n            \"title\": \"Project Id\",\n            \"description\": \"Identifier of the project the workspace belongs to.\"\n        },\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Human-readable workspace name.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"title\": \"Description\",\n            \"description\": \"Freeform workspace description.\"\n        },\n        \"locked\": {\n            \"type\": \"boolean\",\n            \"title\": \"Locked\",\n            \"description\": \"When ``True``, the workspace is read-only.\"\n        },\n        \"sort_order\": {\n            \"type\": \"integer\",\n            \"title\": \"Sort Order\",\n            \"description\": \"Ordering hint for display; lower values appear first.\"\n        },\n        \"settings\": {\n            \"$ref\": \"#/components/schemas/WorkspaceSettings\",\n            \"description\": \"Workspace-level configuration.\"\n        },\n        \"content\": {\n            \"anyOf\": [\n                {\n                    \"$ref\": \"#/components/schemas/JSONObject-Output\"\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"description\": \"Pipeline configuration as a JSON object. ``None`` for an empty workspace.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the workspace was created.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent modification.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"id\",\n        \"project_id\",\n        \"name\",\n        \"description\",\n        \"locked\",\n        \"sort_order\",\n        \"settings\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"WorkspaceResponse\",\n    \"description\": \"Single workspace returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#get-apiv1projectsproject_idworkspacesworkspace_id","title":"GET /api/v1/projects/{project_id}/workspaces/{workspace_id}","text":"<p>Get Workspace</p> Description <p>Get a single workspace by ID.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <code>workspace_id</code> path string No <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>{\n    \"id\": \"string\",\n    \"project_id\": \"string\",\n    \"name\": \"string\",\n    \"description\": \"string\",\n    \"locked\": true,\n    \"sort_order\": 0,\n    \"settings\": {\n        \"verboseMode\": true\n    },\n    \"content\": null,\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"id\": {\n            \"type\": \"string\",\n            \"title\": \"Id\",\n            \"description\": \"Opaque unique identifier for the workspace.\"\n        },\n        \"project_id\": {\n            \"type\": \"string\",\n            \"title\": \"Project Id\",\n            \"description\": \"Identifier of the project the workspace belongs to.\"\n        },\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Human-readable workspace name.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"title\": \"Description\",\n            \"description\": \"Freeform workspace description.\"\n        },\n        \"locked\": {\n            \"type\": \"boolean\",\n            \"title\": \"Locked\",\n            \"description\": \"When ``True``, the workspace is read-only.\"\n        },\n        \"sort_order\": {\n            \"type\": \"integer\",\n            \"title\": \"Sort Order\",\n            \"description\": \"Ordering hint for display; lower values appear first.\"\n        },\n        \"settings\": {\n            \"$ref\": \"#/components/schemas/WorkspaceSettings\",\n            \"description\": \"Workspace-level configuration.\"\n        },\n        \"content\": {\n            \"anyOf\": [\n                {\n                    \"$ref\": \"#/components/schemas/JSONObject-Output\"\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"description\": \"Pipeline configuration as a JSON object. ``None`` for an empty workspace.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the workspace was created.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent modification.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"id\",\n        \"project_id\",\n        \"name\",\n        \"description\",\n        \"locked\",\n        \"sort_order\",\n        \"settings\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"WorkspaceResponse\",\n    \"description\": \"Single workspace returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#patch-apiv1projectsproject_idworkspacesworkspace_id","title":"PATCH /api/v1/projects/{project_id}/workspaces/{workspace_id}","text":"<p>Update Workspace</p> Description <p>Update a workspace.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <code>workspace_id</code> path string No <p>Request body</p> application/json <p><pre><code>{\n    \"name\": null,\n    \"description\": null,\n    \"locked\": null,\n    \"settings\": null,\n    \"content\": null\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"name\": {\n            \"anyOf\": [\n                {\n                    \"type\": \"string\",\n                    \"maxLength\": 255,\n                    \"minLength\": 1\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"title\": \"Name\",\n            \"description\": \"New workspace name, 1 to 255 characters. ``None`` leaves the existing name unchanged.\"\n        },\n        \"description\": {\n            \"anyOf\": [\n                {\n                    \"type\": \"string\",\n                    \"maxLength\": 10000\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"title\": \"Description\",\n            \"description\": \"New description, up to 10000 characters. ``None`` leaves the existing description unchanged.\"\n        },\n        \"locked\": {\n            \"anyOf\": [\n                {\n                    \"type\": \"boolean\"\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"title\": \"Locked\",\n            \"description\": \"New lock state. ``None`` leaves the existing value unchanged.\"\n        },\n        \"settings\": {\n            \"anyOf\": [\n                {\n                    \"$ref\": \"#/components/schemas/WorkspaceSettings\"\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"description\": \"Replacement settings. ``None`` leaves the existing settings unchanged.\"\n        },\n        \"content\": {\n            \"anyOf\": [\n                {\n                    \"$ref\": \"#/components/schemas/JSONObject-Input\"\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"description\": \"Replacement pipeline configuration. ``None`` leaves the existing content unchanged.\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"UpdateWorkspaceRequest\",\n    \"description\": \"Input payload for partially updating a workspace.\"\n}\n</code></pre> <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>{\n    \"id\": \"string\",\n    \"project_id\": \"string\",\n    \"name\": \"string\",\n    \"description\": \"string\",\n    \"locked\": true,\n    \"sort_order\": 0,\n    \"settings\": {\n        \"verboseMode\": true\n    },\n    \"content\": null,\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"id\": {\n            \"type\": \"string\",\n            \"title\": \"Id\",\n            \"description\": \"Opaque unique identifier for the workspace.\"\n        },\n        \"project_id\": {\n            \"type\": \"string\",\n            \"title\": \"Project Id\",\n            \"description\": \"Identifier of the project the workspace belongs to.\"\n        },\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Human-readable workspace name.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"title\": \"Description\",\n            \"description\": \"Freeform workspace description.\"\n        },\n        \"locked\": {\n            \"type\": \"boolean\",\n            \"title\": \"Locked\",\n            \"description\": \"When ``True``, the workspace is read-only.\"\n        },\n        \"sort_order\": {\n            \"type\": \"integer\",\n            \"title\": \"Sort Order\",\n            \"description\": \"Ordering hint for display; lower values appear first.\"\n        },\n        \"settings\": {\n            \"$ref\": \"#/components/schemas/WorkspaceSettings\",\n            \"description\": \"Workspace-level configuration.\"\n        },\n        \"content\": {\n            \"anyOf\": [\n                {\n                    \"$ref\": \"#/components/schemas/JSONObject-Output\"\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"description\": \"Pipeline configuration as a JSON object. ``None`` for an empty workspace.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the workspace was created.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent modification.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"id\",\n        \"project_id\",\n        \"name\",\n        \"description\",\n        \"locked\",\n        \"sort_order\",\n        \"settings\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"WorkspaceResponse\",\n    \"description\": \"Single workspace returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#delete-apiv1projectsproject_idworkspacesworkspace_id","title":"DELETE /api/v1/projects/{project_id}/workspaces/{workspace_id}","text":"<p>Delete Workspace</p> Description <p>Delete a workspace.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <code>workspace_id</code> path string No <p>Responses</p> 204 No Content422 Unprocessable Entity application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#post-apiv1projectsproject_idworkspacesworkspace_idduplicate","title":"POST /api/v1/projects/{project_id}/workspaces/{workspace_id}/duplicate","text":"<p>Duplicate Workspace</p> Description <p>Duplicate a workspace.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <code>workspace_id</code> path string No <p>Responses</p> 201 Created422 Unprocessable Entity application/json <p><pre><code>{\n    \"id\": \"string\",\n    \"project_id\": \"string\",\n    \"name\": \"string\",\n    \"description\": \"string\",\n    \"locked\": true,\n    \"sort_order\": 0,\n    \"settings\": {\n        \"verboseMode\": true\n    },\n    \"content\": null,\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"id\": {\n            \"type\": \"string\",\n            \"title\": \"Id\",\n            \"description\": \"Opaque unique identifier for the workspace.\"\n        },\n        \"project_id\": {\n            \"type\": \"string\",\n            \"title\": \"Project Id\",\n            \"description\": \"Identifier of the project the workspace belongs to.\"\n        },\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Human-readable workspace name.\"\n        },\n        \"description\": {\n            \"type\": \"string\",\n            \"title\": \"Description\",\n            \"description\": \"Freeform workspace description.\"\n        },\n        \"locked\": {\n            \"type\": \"boolean\",\n            \"title\": \"Locked\",\n            \"description\": \"When ``True``, the workspace is read-only.\"\n        },\n        \"sort_order\": {\n            \"type\": \"integer\",\n            \"title\": \"Sort Order\",\n            \"description\": \"Ordering hint for display; lower values appear first.\"\n        },\n        \"settings\": {\n            \"$ref\": \"#/components/schemas/WorkspaceSettings\",\n            \"description\": \"Workspace-level configuration.\"\n        },\n        \"content\": {\n            \"anyOf\": [\n                {\n                    \"$ref\": \"#/components/schemas/JSONObject-Output\"\n                },\n                {\n                    \"type\": \"null\"\n                }\n            ],\n            \"description\": \"Pipeline configuration as a JSON object. ``None`` for an empty workspace.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the workspace was created.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent modification.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"id\",\n        \"project_id\",\n        \"name\",\n        \"description\",\n        \"locked\",\n        \"sort_order\",\n        \"settings\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"WorkspaceResponse\",\n    \"description\": \"Single workspace returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#put-apiv1projectsproject_idworkspacesreorder","title":"PUT /api/v1/projects/{project_id}/workspaces/reorder","text":"<p>Reorder Workspaces</p> Description <p>Reorder workspaces in a project. ordered_ids must be a complete permutation.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <p>Request body</p> application/json <p><pre><code>{\n    \"ordered_ids\": [\n        \"string\"\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"ordered_ids\": {\n            \"items\": {\n                \"type\": \"string\",\n                \"maxLength\": 20\n            },\n            \"type\": \"array\",\n            \"maxItems\": 5,\n            \"minItems\": 1,\n            \"title\": \"Ordered Ids\",\n            \"description\": \"Workspace IDs in the desired display order. Must be a complete permutation of the project's workspaces.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"ordered_ids\"\n    ],\n    \"title\": \"ReorderWorkspacesRequest\",\n    \"description\": \"Input payload for reordering workspaces within a project.\"\n}\n</code></pre> <p>Responses</p> 204 No Content422 Unprocessable Entity application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#post-apiv1projectsproject_idworkspacesworkspace_idrun","title":"POST /api/v1/projects/{project_id}/workspaces/{workspace_id}/run","text":"<p>Run Pipeline</p> Description <p>Run a pipeline. Sets workspace run to running and starts execution.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <code>workspace_id</code> path string No <p>Request body</p> application/json <p><pre><code>{\n    \"pipeline\": {}\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"pipeline\": {\n            \"additionalProperties\": true,\n            \"type\": \"object\",\n            \"title\": \"Pipeline\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"pipeline\"\n    ],\n    \"title\": \"RunRequest\",\n    \"description\": \"Request body for running a pipeline.\"\n}\n</code></pre> <p>Responses</p> 202 Accepted422 Unprocessable Entity application/json <p><pre><code>{\n    \"status\": \"string\",\n    \"output\": \"string\",\n    \"error\": \"string\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"status\": {\n            \"type\": \"string\",\n            \"title\": \"Status\"\n        },\n        \"output\": {\n            \"type\": \"string\",\n            \"title\": \"Output\",\n            \"default\": \"\"\n        },\n        \"error\": {\n            \"type\": \"string\",\n            \"title\": \"Error\",\n            \"default\": \"\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"status\"\n    ],\n    \"title\": \"RunResponse\",\n    \"description\": \"Run state returned to the frontend.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#get-apiv1projectsproject_idworkspacesworkspace_idrun","title":"GET /api/v1/projects/{project_id}/workspaces/{workspace_id}/run","text":"<p>Get Run</p> Description <p>Get the latest run state for a workspace.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <code>workspace_id</code> path string No <p>Responses</p> 200 OK422 Unprocessable Entity application/json Schema of the response body <pre><code>{\n    \"anyOf\": [\n        {\n            \"$ref\": \"#/components/schemas/RunResponse\"\n        },\n        {\n            \"type\": \"null\"\n        }\n    ],\n    \"title\": \"Response Get Run Api V1 Projects  Project Id  Workspaces  Workspace Id  Run Get\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#secrets","title":"secrets","text":""},{"location":"reference/rest/#get-apiv1secrets","title":"GET /api/v1/secrets/","text":"<p>List Secrets</p> Description <p>List the user's global secrets, without their values.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>[\n    {\n        \"name\": \"string\",\n        \"hint\": \"string\",\n        \"created_at\": \"2022-04-13T15:42:05.901Z\",\n        \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n    }\n]\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"type\": \"array\",\n    \"items\": {\n        \"$ref\": \"#/components/schemas/SecretResponse\"\n    },\n    \"title\": \"Response List Secrets Api V1 Secrets  Get\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#put-apiv1secretsname","title":"PUT /api/v1/secrets/{name}","text":"<p>Set Secret</p> Description <p>Set or replace a global secret's value. The value is encrypted and never returned.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>name</code> path string No <p>Request body</p> application/json <p><pre><code>{\n    \"value\": \"string\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"value\": {\n            \"type\": \"string\",\n            \"maxLength\": 4096,\n            \"minLength\": 1,\n            \"title\": \"Value\",\n            \"description\": \"The raw secret value. Stored encrypted and never returned.\\n\\n1 to 4096 characters.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"value\"\n    ],\n    \"title\": \"SetSecretRequest\",\n    \"description\": \"Input payload for setting or replacing a secret's value.\"\n}\n</code></pre> <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>{\n    \"name\": \"string\",\n    \"hint\": \"string\",\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Identifier for the secret, for example ``\\\"openai\\\"``.\"\n        },\n        \"hint\": {\n            \"type\": \"string\",\n            \"title\": \"Hint\",\n            \"description\": \"Trailing characters of the value, shown so the user can recognize the stored key.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the secret was first set.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent change.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"name\",\n        \"hint\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"SecretResponse\",\n    \"description\": \"A stored secret returned in API responses, without its value.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#delete-apiv1secretsname","title":"DELETE /api/v1/secrets/{name}","text":"<p>Delete Secret</p> Description <p>Delete a global secret.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>name</code> path string No <p>Responses</p> 204 No Content422 Unprocessable Entity application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#get-apiv1secretsprojectsproject_id","title":"GET /api/v1/secrets/projects/{project_id}","text":"<p>List Project Secrets</p> Description <p>List the project's secret overrides, without their values.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>project_id</code> path string No <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>[\n    {\n        \"name\": \"string\",\n        \"hint\": \"string\",\n        \"created_at\": \"2022-04-13T15:42:05.901Z\",\n        \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n    }\n]\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"type\": \"array\",\n    \"items\": {\n        \"$ref\": \"#/components/schemas/SecretResponse\"\n    },\n    \"title\": \"Response List Project Secrets Api V1 Secrets Projects  Project Id  Get\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#put-apiv1secretsprojectsproject_idname","title":"PUT /api/v1/secrets/projects/{project_id}/{name}","text":"<p>Set Project Secret</p> Description <p>Set or replace a project override for a secret. The value is encrypted and never returned.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>name</code> path string No <code>project_id</code> path string No <p>Request body</p> application/json <p><pre><code>{\n    \"value\": \"string\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"value\": {\n            \"type\": \"string\",\n            \"maxLength\": 4096,\n            \"minLength\": 1,\n            \"title\": \"Value\",\n            \"description\": \"The raw secret value. Stored encrypted and never returned.\\n\\n1 to 4096 characters.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"value\"\n    ],\n    \"title\": \"SetSecretRequest\",\n    \"description\": \"Input payload for setting or replacing a secret's value.\"\n}\n</code></pre> <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>{\n    \"name\": \"string\",\n    \"hint\": \"string\",\n    \"created_at\": \"2022-04-13T15:42:05.901Z\",\n    \"updated_at\": \"2022-04-13T15:42:05.901Z\"\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"name\": {\n            \"type\": \"string\",\n            \"title\": \"Name\",\n            \"description\": \"Identifier for the secret, for example ``\\\"openai\\\"``.\"\n        },\n        \"hint\": {\n            \"type\": \"string\",\n            \"title\": \"Hint\",\n            \"description\": \"Trailing characters of the value, shown so the user can recognize the stored key.\"\n        },\n        \"created_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Created At\",\n            \"description\": \"Timestamp the secret was first set.\"\n        },\n        \"updated_at\": {\n            \"type\": \"string\",\n            \"format\": \"date-time\",\n            \"title\": \"Updated At\",\n            \"description\": \"Timestamp of the most recent change.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"name\",\n        \"hint\",\n        \"created_at\",\n        \"updated_at\"\n    ],\n    \"title\": \"SecretResponse\",\n    \"description\": \"A stored secret returned in API responses, without its value.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#delete-apiv1secretsprojectsproject_idname","title":"DELETE /api/v1/secrets/projects/{project_id}/{name}","text":"<p>Delete Project Secret</p> Description <p>Delete a project override for a secret.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <code>name</code> path string No <code>project_id</code> path string No <p>Responses</p> 204 No Content422 Unprocessable Entity application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#preferences","title":"preferences","text":""},{"location":"reference/rest/#get-apiv1preferences","title":"GET /api/v1/preferences/","text":"<p>Get Preferences</p> Description <p>Return the current user's preferences.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>{\n    \"tour_completed\": true\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"tour_completed\": {\n            \"type\": \"boolean\",\n            \"title\": \"Tour Completed\",\n            \"description\": \"Whether the user has finished the welcome tour.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"tour_completed\"\n    ],\n    \"title\": \"PreferencesResponse\",\n    \"description\": \"A user's preferences returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#put-apiv1preferences","title":"PUT /api/v1/preferences/","text":"<p>Update Preferences</p> Description <p>Replace the current user's preferences.</p> <p>Input parameters</p> Parameter In Type Default Nullable Description <code>authorization</code> header No <p>Request body</p> application/json <p><pre><code>{\n    \"tour_completed\": true\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the request body <pre><code>{\n    \"properties\": {\n        \"tour_completed\": {\n            \"type\": \"boolean\",\n            \"title\": \"Tour Completed\",\n            \"description\": \"Whether the user has finished the welcome tour.\",\n            \"default\": false\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"UpdatePreferencesRequest\",\n    \"description\": \"Input payload for replacing a user's preferences.\"\n}\n</code></pre> <p>Responses</p> 200 OK422 Unprocessable Entity application/json <p><pre><code>{\n    \"tour_completed\": true\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"tour_completed\": {\n            \"type\": \"boolean\",\n            \"title\": \"Tour Completed\",\n            \"description\": \"Whether the user has finished the welcome tour.\"\n        }\n    },\n    \"type\": \"object\",\n    \"required\": [\n        \"tour_completed\"\n    ],\n    \"title\": \"PreferencesResponse\",\n    \"description\": \"A user's preferences returned in API responses.\"\n}\n</code></pre> application/json <p><pre><code>{\n    \"detail\": [\n        {\n            \"loc\": [\n                null\n            ],\n            \"msg\": \"string\",\n            \"type\": \"string\",\n            \"input\": null,\n            \"ctx\": {}\n        }\n    ]\n}\n</code></pre> \u26a0\ufe0f This example has been generated automatically from the schema and it is not accurate. Refer to the schema for more information.</p> Schema of the response body <pre><code>{\n    \"properties\": {\n        \"detail\": {\n            \"items\": {\n                \"$ref\": \"#/components/schemas/ValidationError\"\n            },\n            \"type\": \"array\",\n            \"title\": \"Detail\"\n        }\n    },\n    \"type\": \"object\",\n    \"title\": \"HTTPValidationError\"\n}\n</code></pre>"},{"location":"reference/rest/#schemas","title":"Schemas","text":""},{"location":"reference/rest/#createprojectrequest","title":"CreateProjectRequest","text":"Name Type Description <code>description</code> string Freeform project description, up to 10000 characters. <code>name</code> string Human-readable project name, 1 to 255 characters."},{"location":"reference/rest/#createworkspacerequest","title":"CreateWorkspaceRequest","text":"Name Type Description <code>description</code> string Freeform workspace description, up to 10000 characters. <code>name</code> string Human-readable workspace name, 1 to 255 characters."},{"location":"reference/rest/#httpvalidationerror","title":"HTTPValidationError","text":"Name Type Description <code>detail</code> Array&lt;ValidationError&gt;"},{"location":"reference/rest/#jsonobject-input","title":"JSONObject-Input","text":""},{"location":"reference/rest/#jsonobject-output","title":"JSONObject-Output","text":""},{"location":"reference/rest/#jsonscalar","title":"JSONScalar","text":"<p>Type: </p>"},{"location":"reference/rest/#jsonvalue-input","title":"JSONValue-Input","text":"<p>Type: </p>"},{"location":"reference/rest/#jsonvalue-output","title":"JSONValue-Output","text":"<p>Type: </p>"},{"location":"reference/rest/#preferencesresponse","title":"PreferencesResponse","text":"Name Type Description <code>tour_completed</code> boolean Whether the user has finished the welcome tour."},{"location":"reference/rest/#projectresponse","title":"ProjectResponse","text":"Name Type Description <code>created_at</code> string(date-time) Timestamp the project was created. <code>description</code> string Freeform project description. <code>id</code> string Opaque unique identifier for the project. <code>name</code> string Human-readable project name. <code>updated_at</code> string(date-time) Timestamp of the most recent modification."},{"location":"reference/rest/#reorderworkspacesrequest","title":"ReorderWorkspacesRequest","text":"Name Type Description <code>ordered_ids</code> Array&lt;string&gt; Workspace IDs in the desired display order. Must be a complete permutation of the project's workspaces."},{"location":"reference/rest/#runrequest","title":"RunRequest","text":"Name Type Description <code>pipeline</code>"},{"location":"reference/rest/#runresponse","title":"RunResponse","text":"Name Type Description <code>error</code> string <code>output</code> string <code>status</code> string"},{"location":"reference/rest/#secretresponse","title":"SecretResponse","text":"Name Type Description <code>created_at</code> string(date-time) Timestamp the secret was first set. <code>hint</code> string Trailing characters of the value, shown so the user can recognize the stored key. <code>name</code> string Identifier for the secret, for example ``\"openai\"``. <code>updated_at</code> string(date-time) Timestamp of the most recent change."},{"location":"reference/rest/#setsecretrequest","title":"SetSecretRequest","text":"Name Type Description <code>value</code> string The raw secret value. Stored encrypted and never returned.  1 to 4096 characters."},{"location":"reference/rest/#updatepreferencesrequest","title":"UpdatePreferencesRequest","text":"Name Type Description <code>tour_completed</code> boolean Whether the user has finished the welcome tour."},{"location":"reference/rest/#updateprojectrequest","title":"UpdateProjectRequest","text":"Name Type Description <code>description</code> New description, up to 10000 characters. ``None`` leaves the existing description unchanged. <code>name</code> New project name, 1 to 255 characters. ``None`` leaves the existing name unchanged."},{"location":"reference/rest/#updateworkspacerequest","title":"UpdateWorkspaceRequest","text":"Name Type Description <code>content</code> Replacement pipeline configuration. ``None`` leaves the existing content unchanged. <code>description</code> New description, up to 10000 characters. ``None`` leaves the existing description unchanged. <code>locked</code> New lock state. ``None`` leaves the existing value unchanged. <code>name</code> New workspace name, 1 to 255 characters. ``None`` leaves the existing name unchanged. <code>settings</code> Replacement settings. ``None`` leaves the existing settings unchanged."},{"location":"reference/rest/#validationerror","title":"ValidationError","text":"Name Type Description <code>ctx</code> <code>input</code> <code>loc</code> Array&lt;&gt; <code>msg</code> string <code>type</code> string"},{"location":"reference/rest/#workspaceresponse","title":"WorkspaceResponse","text":"Name Type Description <code>content</code> Pipeline configuration as a JSON object. ``None`` for an empty workspace. <code>created_at</code> string(date-time) Timestamp the workspace was created. <code>description</code> string Freeform workspace description. <code>id</code> string Opaque unique identifier for the workspace. <code>locked</code> boolean When ``True``, the workspace is read-only. <code>name</code> string Human-readable workspace name. <code>project_id</code> string Identifier of the project the workspace belongs to. <code>settings</code> WorkspaceSettings Workspace-level configuration. <code>sort_order</code> integer Ordering hint for display; lower values appear first. <code>updated_at</code> string(date-time) Timestamp of the most recent modification."},{"location":"reference/rest/#workspacesettings","title":"WorkspaceSettings","text":"Name Type Description <code>verboseMode</code> boolean Emit verbose execution logs during pipeline runs."}]}